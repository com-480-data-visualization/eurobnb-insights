import {
  require_jsx_runtime
} from "./chunk-S725DACQ.js";
import {
  ascending_default,
  basisClosed_default,
  basisOpen_default,
  basis_default,
  bundle_default,
  cardinalClosed_default,
  cardinalOpen_default,
  cardinal_default,
  catmullRomClosed_default,
  catmullRomOpen_default,
  catmullRom_default,
  cubehelix,
  cubehelixLong,
  descending_default,
  diverging_default,
  expand_default,
  format,
  friday,
  hour_default,
  insideOut_default,
  line_default,
  linear,
  linearClosed_default,
  linear_default,
  millisecond_default,
  minute_default,
  monday,
  monotoneX,
  monotoneY,
  month_default,
  natural_default,
  newInterval,
  none_default,
  none_default2,
  ordinal,
  point,
  require_SetCache,
  require_Stack,
  require_arrayIncludes,
  require_arrayIncludesWith,
  require_arrayMap,
  require_assignValue,
  require_baseAssignValue,
  require_baseFor,
  require_baseGet,
  require_baseGetTag,
  require_baseRest,
  require_baseUnary,
  require_baseUniq,
  require_cacheHas,
  require_castPath,
  require_cloneBuffer,
  require_cloneTypedArray,
  require_copyArray,
  require_copyObject,
  require_eq,
  require_flatRest,
  require_get,
  require_hasIn,
  require_initCloneObject,
  require_isArguments,
  require_isArray,
  require_isArrayLike,
  require_isBuffer,
  require_isEqual,
  require_isFunction,
  require_isIndex,
  require_isIterateeCall,
  require_isObject,
  require_isObjectLike,
  require_isPlainObject,
  require_isString,
  require_isTypedArray,
  require_keysIn,
  require_last,
  require_nodeUtil,
  require_prop_types,
  require_sortBy,
  require_toKey,
  require_uniqBy,
  reverse_default,
  rgb,
  rgbBasis,
  saturday,
  second_default,
  silhouette_default,
  stepAfter,
  stepBefore,
  step_default,
  string_default,
  sunday,
  thursday,
  timeFormat,
  tuesday,
  utcFriday,
  utcHour_default,
  utcMinute_default,
  utcMonday,
  utcMonth_default,
  utcSaturday,
  utcSunday,
  utcThursday,
  utcTuesday,
  utcWednesday,
  utcYear_default,
  wednesday,
  wiggle_default,
  year_default
} from "./chunk-UOFBSVJ3.js";
import {
  require_react_dom
} from "./chunk-KDCVS43I.js";
import {
  require_react
} from "./chunk-RLJ2RCJQ.js";
import {
  __commonJS,
  __toESM
} from "./chunk-DC5AMYBS.js";

// node_modules/lodash/_assignMergeValue.js
var require_assignMergeValue = __commonJS({
  "node_modules/lodash/_assignMergeValue.js"(exports, module) {
    var baseAssignValue = require_baseAssignValue();
    var eq = require_eq();
    function assignMergeValue(object, key, value) {
      if (value !== void 0 && !eq(object[key], value) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    module.exports = assignMergeValue;
  }
});

// node_modules/lodash/isArrayLikeObject.js
var require_isArrayLikeObject = __commonJS({
  "node_modules/lodash/isArrayLikeObject.js"(exports, module) {
    var isArrayLike = require_isArrayLike();
    var isObjectLike = require_isObjectLike();
    function isArrayLikeObject(value) {
      return isObjectLike(value) && isArrayLike(value);
    }
    module.exports = isArrayLikeObject;
  }
});

// node_modules/lodash/_safeGet.js
var require_safeGet = __commonJS({
  "node_modules/lodash/_safeGet.js"(exports, module) {
    function safeGet(object, key) {
      if (key === "constructor" && typeof object[key] === "function") {
        return;
      }
      if (key == "__proto__") {
        return;
      }
      return object[key];
    }
    module.exports = safeGet;
  }
});

// node_modules/lodash/toPlainObject.js
var require_toPlainObject = __commonJS({
  "node_modules/lodash/toPlainObject.js"(exports, module) {
    var copyObject = require_copyObject();
    var keysIn = require_keysIn();
    function toPlainObject(value) {
      return copyObject(value, keysIn(value));
    }
    module.exports = toPlainObject;
  }
});

// node_modules/lodash/_baseMergeDeep.js
var require_baseMergeDeep = __commonJS({
  "node_modules/lodash/_baseMergeDeep.js"(exports, module) {
    var assignMergeValue = require_assignMergeValue();
    var cloneBuffer = require_cloneBuffer();
    var cloneTypedArray = require_cloneTypedArray();
    var copyArray = require_copyArray();
    var initCloneObject = require_initCloneObject();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isArrayLikeObject = require_isArrayLikeObject();
    var isBuffer = require_isBuffer();
    var isFunction = require_isFunction();
    var isObject = require_isObject();
    var isPlainObject = require_isPlainObject();
    var isTypedArray = require_isTypedArray();
    var safeGet = require_safeGet();
    var toPlainObject = require_toPlainObject();
    function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
      var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
      if (stacked) {
        assignMergeValue(object, key, stacked);
        return;
      }
      var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : void 0;
      var isCommon = newValue === void 0;
      if (isCommon) {
        var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
        newValue = srcValue;
        if (isArr || isBuff || isTyped) {
          if (isArray(objValue)) {
            newValue = objValue;
          } else if (isArrayLikeObject(objValue)) {
            newValue = copyArray(objValue);
          } else if (isBuff) {
            isCommon = false;
            newValue = cloneBuffer(srcValue, true);
          } else if (isTyped) {
            isCommon = false;
            newValue = cloneTypedArray(srcValue, true);
          } else {
            newValue = [];
          }
        } else if (isPlainObject(srcValue) || isArguments(srcValue)) {
          newValue = objValue;
          if (isArguments(objValue)) {
            newValue = toPlainObject(objValue);
          } else if (!isObject(objValue) || isFunction(objValue)) {
            newValue = initCloneObject(srcValue);
          }
        } else {
          isCommon = false;
        }
      }
      if (isCommon) {
        stack.set(srcValue, newValue);
        mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
        stack["delete"](srcValue);
      }
      assignMergeValue(object, key, newValue);
    }
    module.exports = baseMergeDeep;
  }
});

// node_modules/lodash/_baseMerge.js
var require_baseMerge = __commonJS({
  "node_modules/lodash/_baseMerge.js"(exports, module) {
    var Stack = require_Stack();
    var assignMergeValue = require_assignMergeValue();
    var baseFor = require_baseFor();
    var baseMergeDeep = require_baseMergeDeep();
    var isObject = require_isObject();
    var keysIn = require_keysIn();
    var safeGet = require_safeGet();
    function baseMerge(object, source, srcIndex, customizer, stack) {
      if (object === source) {
        return;
      }
      baseFor(source, function(srcValue, key) {
        stack || (stack = new Stack());
        if (isObject(srcValue)) {
          baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
        } else {
          var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : void 0;
          if (newValue === void 0) {
            newValue = srcValue;
          }
          assignMergeValue(object, key, newValue);
        }
      }, keysIn);
    }
    module.exports = baseMerge;
  }
});

// node_modules/lodash/_createAssigner.js
var require_createAssigner = __commonJS({
  "node_modules/lodash/_createAssigner.js"(exports, module) {
    var baseRest = require_baseRest();
    var isIterateeCall = require_isIterateeCall();
    function createAssigner(assigner) {
      return baseRest(function(object, sources) {
        var index = -1, length = sources.length, customizer = length > 1 ? sources[length - 1] : void 0, guard = length > 2 ? sources[2] : void 0;
        customizer = assigner.length > 3 && typeof customizer == "function" ? (length--, customizer) : void 0;
        if (guard && isIterateeCall(sources[0], sources[1], guard)) {
          customizer = length < 3 ? void 0 : customizer;
          length = 1;
        }
        object = Object(object);
        while (++index < length) {
          var source = sources[index];
          if (source) {
            assigner(object, source, index, customizer);
          }
        }
        return object;
      });
    }
    module.exports = createAssigner;
  }
});

// node_modules/lodash/merge.js
var require_merge = __commonJS({
  "node_modules/lodash/merge.js"(exports, module) {
    var baseMerge = require_baseMerge();
    var createAssigner = require_createAssigner();
    var merge = createAssigner(function(object, source, srcIndex) {
      baseMerge(object, source, srcIndex);
    });
    module.exports = merge;
  }
});

// node_modules/lodash/_baseSet.js
var require_baseSet = __commonJS({
  "node_modules/lodash/_baseSet.js"(exports, module) {
    var assignValue = require_assignValue();
    var castPath = require_castPath();
    var isIndex = require_isIndex();
    var isObject = require_isObject();
    var toKey = require_toKey();
    function baseSet(object, path, value, customizer) {
      if (!isObject(object)) {
        return object;
      }
      path = castPath(path, object);
      var index = -1, length = path.length, lastIndex = length - 1, nested = object;
      while (nested != null && ++index < length) {
        var key = toKey(path[index]), newValue = value;
        if (key === "__proto__" || key === "constructor" || key === "prototype") {
          return object;
        }
        if (index != lastIndex) {
          var objValue = nested[key];
          newValue = customizer ? customizer(objValue, key, nested) : void 0;
          if (newValue === void 0) {
            newValue = isObject(objValue) ? objValue : isIndex(path[index + 1]) ? [] : {};
          }
        }
        assignValue(nested, key, newValue);
        nested = nested[key];
      }
      return object;
    }
    module.exports = baseSet;
  }
});

// node_modules/lodash/set.js
var require_set = __commonJS({
  "node_modules/lodash/set.js"(exports, module) {
    var baseSet = require_baseSet();
    function set(object, path, value) {
      return object == null ? object : baseSet(object, path, value);
    }
    module.exports = set;
  }
});

// node_modules/lodash/_baseDifference.js
var require_baseDifference = __commonJS({
  "node_modules/lodash/_baseDifference.js"(exports, module) {
    var SetCache = require_SetCache();
    var arrayIncludes = require_arrayIncludes();
    var arrayIncludesWith = require_arrayIncludesWith();
    var arrayMap = require_arrayMap();
    var baseUnary = require_baseUnary();
    var cacheHas = require_cacheHas();
    var LARGE_ARRAY_SIZE = 200;
    function baseDifference(array, values, iteratee, comparator) {
      var index = -1, includes = arrayIncludes, isCommon = true, length = array.length, result = [], valuesLength = values.length;
      if (!length) {
        return result;
      }
      if (iteratee) {
        values = arrayMap(values, baseUnary(iteratee));
      }
      if (comparator) {
        includes = arrayIncludesWith;
        isCommon = false;
      } else if (values.length >= LARGE_ARRAY_SIZE) {
        includes = cacheHas;
        isCommon = false;
        values = new SetCache(values);
      }
      outer:
        while (++index < length) {
          var value = array[index], computed = iteratee == null ? value : iteratee(value);
          value = comparator || value !== 0 ? value : 0;
          if (isCommon && computed === computed) {
            var valuesIndex = valuesLength;
            while (valuesIndex--) {
              if (values[valuesIndex] === computed) {
                continue outer;
              }
            }
            result.push(value);
          } else if (!includes(values, computed, comparator)) {
            result.push(value);
          }
        }
      return result;
    }
    module.exports = baseDifference;
  }
});

// node_modules/lodash/without.js
var require_without = __commonJS({
  "node_modules/lodash/without.js"(exports, module) {
    var baseDifference = require_baseDifference();
    var baseRest = require_baseRest();
    var isArrayLikeObject = require_isArrayLikeObject();
    var without = baseRest(function(array, values) {
      return isArrayLikeObject(array) ? baseDifference(array, values) : [];
    });
    module.exports = without;
  }
});

// node_modules/lodash/_basePickBy.js
var require_basePickBy = __commonJS({
  "node_modules/lodash/_basePickBy.js"(exports, module) {
    var baseGet = require_baseGet();
    var baseSet = require_baseSet();
    var castPath = require_castPath();
    function basePickBy(object, paths, predicate) {
      var index = -1, length = paths.length, result = {};
      while (++index < length) {
        var path = paths[index], value = baseGet(object, path);
        if (predicate(value, path)) {
          baseSet(result, castPath(path, object), value);
        }
      }
      return result;
    }
    module.exports = basePickBy;
  }
});

// node_modules/lodash/_basePick.js
var require_basePick = __commonJS({
  "node_modules/lodash/_basePick.js"(exports, module) {
    var basePickBy = require_basePickBy();
    var hasIn = require_hasIn();
    function basePick(object, paths) {
      return basePickBy(object, paths, function(value, path) {
        return hasIn(object, path);
      });
    }
    module.exports = basePick;
  }
});

// node_modules/lodash/pick.js
var require_pick = __commonJS({
  "node_modules/lodash/pick.js"(exports, module) {
    var basePick = require_basePick();
    var flatRest = require_flatRest();
    var pick = flatRest(function(object, paths) {
      return object == null ? {} : basePick(object, paths);
    });
    module.exports = pick;
  }
});

// node_modules/lodash/uniq.js
var require_uniq = __commonJS({
  "node_modules/lodash/uniq.js"(exports, module) {
    var baseUniq = require_baseUniq();
    function uniq(array) {
      return array && array.length ? baseUniq(array) : [];
    }
    module.exports = uniq;
  }
});

// node_modules/lodash/_baseIsDate.js
var require_baseIsDate = __commonJS({
  "node_modules/lodash/_baseIsDate.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var dateTag = "[object Date]";
    function baseIsDate(value) {
      return isObjectLike(value) && baseGetTag(value) == dateTag;
    }
    module.exports = baseIsDate;
  }
});

// node_modules/lodash/isDate.js
var require_isDate = __commonJS({
  "node_modules/lodash/isDate.js"(exports, module) {
    var baseIsDate = require_baseIsDate();
    var baseUnary = require_baseUnary();
    var nodeUtil = require_nodeUtil();
    var nodeIsDate = nodeUtil && nodeUtil.isDate;
    var isDate = nodeIsDate ? baseUnary(nodeIsDate) : baseIsDate;
    module.exports = isDate;
  }
});

// node_modules/@nivo/tooltip/dist/nivo-tooltip.es.js
var import_react16 = __toESM(require_react());

// node_modules/@react-spring/rafz/dist/react-spring_rafz.modern.mjs
var updateQueue = makeQueue();
var raf = (fn2) => schedule(fn2, updateQueue);
var writeQueue = makeQueue();
raf.write = (fn2) => schedule(fn2, writeQueue);
var onStartQueue = makeQueue();
raf.onStart = (fn2) => schedule(fn2, onStartQueue);
var onFrameQueue = makeQueue();
raf.onFrame = (fn2) => schedule(fn2, onFrameQueue);
var onFinishQueue = makeQueue();
raf.onFinish = (fn2) => schedule(fn2, onFinishQueue);
var timeouts = [];
raf.setTimeout = (handler, ms) => {
  const time2 = raf.now() + ms;
  const cancel = () => {
    const i5 = timeouts.findIndex((t6) => t6.cancel == cancel);
    if (~i5)
      timeouts.splice(i5, 1);
    pendingCount -= ~i5 ? 1 : 0;
  };
  const timeout = { time: time2, handler, cancel };
  timeouts.splice(findTimeout(time2), 0, timeout);
  pendingCount += 1;
  start();
  return timeout;
};
var findTimeout = (time2) => ~(~timeouts.findIndex((t6) => t6.time > time2) || ~timeouts.length);
raf.cancel = (fn2) => {
  onStartQueue.delete(fn2);
  onFrameQueue.delete(fn2);
  onFinishQueue.delete(fn2);
  updateQueue.delete(fn2);
  writeQueue.delete(fn2);
};
raf.sync = (fn2) => {
  sync = true;
  raf.batchedUpdates(fn2);
  sync = false;
};
raf.throttle = (fn2) => {
  let lastArgs;
  function queuedFn() {
    try {
      fn2(...lastArgs);
    } finally {
      lastArgs = null;
    }
  }
  function throttled(...args) {
    lastArgs = args;
    raf.onStart(queuedFn);
  }
  throttled.handler = fn2;
  throttled.cancel = () => {
    onStartQueue.delete(queuedFn);
    lastArgs = null;
  };
  return throttled;
};
var nativeRaf = typeof window != "undefined" ? window.requestAnimationFrame : (
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  () => {
  }
);
raf.use = (impl) => nativeRaf = impl;
raf.now = typeof performance != "undefined" ? () => performance.now() : Date.now;
raf.batchedUpdates = (fn2) => fn2();
raf.catch = console.error;
raf.frameLoop = "always";
raf.advance = () => {
  if (raf.frameLoop !== "demand") {
    console.warn(
      "Cannot call the manual advancement of rafz whilst frameLoop is not set as demand"
    );
  } else {
    update();
  }
};
var ts = -1;
var pendingCount = 0;
var sync = false;
function schedule(fn2, queue) {
  if (sync) {
    queue.delete(fn2);
    fn2(0);
  } else {
    queue.add(fn2);
    start();
  }
}
function start() {
  if (ts < 0) {
    ts = 0;
    if (raf.frameLoop !== "demand") {
      nativeRaf(loop);
    }
  }
}
function stop() {
  ts = -1;
}
function loop() {
  if (~ts) {
    nativeRaf(loop);
    raf.batchedUpdates(update);
  }
}
function update() {
  const prevTs = ts;
  ts = raf.now();
  const count = findTimeout(ts);
  if (count) {
    eachSafely(timeouts.splice(0, count), (t6) => t6.handler());
    pendingCount -= count;
  }
  if (!pendingCount) {
    stop();
    return;
  }
  onStartQueue.flush();
  updateQueue.flush(prevTs ? Math.min(64, ts - prevTs) : 16.667);
  onFrameQueue.flush();
  writeQueue.flush();
  onFinishQueue.flush();
}
function makeQueue() {
  let next = /* @__PURE__ */ new Set();
  let current = next;
  return {
    add(fn2) {
      pendingCount += current == next && !next.has(fn2) ? 1 : 0;
      next.add(fn2);
    },
    delete(fn2) {
      pendingCount -= current == next && next.has(fn2) ? 1 : 0;
      return next.delete(fn2);
    },
    flush(arg) {
      if (current.size) {
        next = /* @__PURE__ */ new Set();
        pendingCount -= current.size;
        eachSafely(current, (fn2) => fn2(arg) && next.add(fn2));
        pendingCount += next.size;
        current = next;
      }
    }
  };
}
function eachSafely(values, each2) {
  values.forEach((value) => {
    try {
      each2(value);
    } catch (e6) {
      raf.catch(e6);
    }
  });
}

// node_modules/@react-spring/shared/dist/react-spring_shared.modern.mjs
var import_react = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_react3 = __toESM(require_react(), 1);
var import_react4 = __toESM(require_react(), 1);
var import_react5 = __toESM(require_react(), 1);
var import_react6 = __toESM(require_react(), 1);
var import_react7 = __toESM(require_react(), 1);
var import_react8 = __toESM(require_react(), 1);
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var globals_exports = {};
__export(globals_exports, {
  assign: () => assign,
  colors: () => colors,
  createStringInterpolator: () => createStringInterpolator,
  skipAnimation: () => skipAnimation,
  to: () => to,
  willAdvance: () => willAdvance
});
function noop() {
}
var defineHidden = (obj, key, value) => Object.defineProperty(obj, key, { value, writable: true, configurable: true });
var is = {
  arr: Array.isArray,
  obj: (a4) => !!a4 && a4.constructor.name === "Object",
  fun: (a4) => typeof a4 === "function",
  str: (a4) => typeof a4 === "string",
  num: (a4) => typeof a4 === "number",
  und: (a4) => a4 === void 0
};
function isEqual(a4, b4) {
  if (is.arr(a4)) {
    if (!is.arr(b4) || a4.length !== b4.length)
      return false;
    for (let i5 = 0; i5 < a4.length; i5++) {
      if (a4[i5] !== b4[i5])
        return false;
    }
    return true;
  }
  return a4 === b4;
}
var each = (obj, fn2) => obj.forEach(fn2);
function eachProp(obj, fn2, ctx2) {
  if (is.arr(obj)) {
    for (let i5 = 0; i5 < obj.length; i5++) {
      fn2.call(ctx2, obj[i5], `${i5}`);
    }
    return;
  }
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      fn2.call(ctx2, obj[key], key);
    }
  }
}
var toArray = (a4) => is.und(a4) ? [] : is.arr(a4) ? a4 : [a4];
function flush(queue, iterator) {
  if (queue.size) {
    const items = Array.from(queue);
    queue.clear();
    each(items, iterator);
  }
}
var flushCalls = (queue, ...args) => flush(queue, (fn2) => fn2(...args));
var isSSR = () => typeof window === "undefined" || !window.navigator || /ServerSideRendering|^Deno\//.test(window.navigator.userAgent);
var createStringInterpolator;
var to;
var colors = null;
var skipAnimation = false;
var willAdvance = noop;
var assign = (globals) => {
  if (globals.to)
    to = globals.to;
  if (globals.now)
    raf.now = globals.now;
  if (globals.colors !== void 0)
    colors = globals.colors;
  if (globals.skipAnimation != null)
    skipAnimation = globals.skipAnimation;
  if (globals.createStringInterpolator)
    createStringInterpolator = globals.createStringInterpolator;
  if (globals.requestAnimationFrame)
    raf.use(globals.requestAnimationFrame);
  if (globals.batchedUpdates)
    raf.batchedUpdates = globals.batchedUpdates;
  if (globals.willAdvance)
    willAdvance = globals.willAdvance;
  if (globals.frameLoop)
    raf.frameLoop = globals.frameLoop;
};
var startQueue = /* @__PURE__ */ new Set();
var currentFrame = [];
var prevFrame = [];
var priority = 0;
var frameLoop = {
  get idle() {
    return !startQueue.size && !currentFrame.length;
  },
  /** Advance the given animation on every frame until idle. */
  start(animation) {
    if (priority > animation.priority) {
      startQueue.add(animation);
      raf.onStart(flushStartQueue);
    } else {
      startSafely(animation);
      raf(advance);
    }
  },
  /** Advance all animations by the given time. */
  advance,
  /** Call this when an animation's priority changes. */
  sort(animation) {
    if (priority) {
      raf.onFrame(() => frameLoop.sort(animation));
    } else {
      const prevIndex = currentFrame.indexOf(animation);
      if (~prevIndex) {
        currentFrame.splice(prevIndex, 1);
        startUnsafely(animation);
      }
    }
  },
  /**
   * Clear all animations. For testing purposes.
   *
   * ☠️ Never call this from within the frameloop.
   */
  clear() {
    currentFrame = [];
    startQueue.clear();
  }
};
function flushStartQueue() {
  startQueue.forEach(startSafely);
  startQueue.clear();
  raf(advance);
}
function startSafely(animation) {
  if (!currentFrame.includes(animation))
    startUnsafely(animation);
}
function startUnsafely(animation) {
  currentFrame.splice(
    findIndex(currentFrame, (other) => other.priority > animation.priority),
    0,
    animation
  );
}
function advance(dt2) {
  const nextFrame = prevFrame;
  for (let i5 = 0; i5 < currentFrame.length; i5++) {
    const animation = currentFrame[i5];
    priority = animation.priority;
    if (!animation.idle) {
      willAdvance(animation);
      animation.advance(dt2);
      if (!animation.idle) {
        nextFrame.push(animation);
      }
    }
  }
  priority = 0;
  prevFrame = currentFrame;
  prevFrame.length = 0;
  currentFrame = nextFrame;
  return currentFrame.length > 0;
}
function findIndex(arr, test) {
  const index = arr.findIndex(test);
  return index < 0 ? arr.length : index;
}
var clamp = (min, max, v5) => Math.min(Math.max(v5, min), max);
var colors2 = {
  transparent: 0,
  aliceblue: 4042850303,
  antiquewhite: 4209760255,
  aqua: 16777215,
  aquamarine: 2147472639,
  azure: 4043309055,
  beige: 4126530815,
  bisque: 4293182719,
  black: 255,
  blanchedalmond: 4293643775,
  blue: 65535,
  blueviolet: 2318131967,
  brown: 2771004159,
  burlywood: 3736635391,
  burntsienna: 3934150143,
  cadetblue: 1604231423,
  chartreuse: 2147418367,
  chocolate: 3530104575,
  coral: 4286533887,
  cornflowerblue: 1687547391,
  cornsilk: 4294499583,
  crimson: 3692313855,
  cyan: 16777215,
  darkblue: 35839,
  darkcyan: 9145343,
  darkgoldenrod: 3095792639,
  darkgray: 2846468607,
  darkgreen: 6553855,
  darkgrey: 2846468607,
  darkkhaki: 3182914559,
  darkmagenta: 2332068863,
  darkolivegreen: 1433087999,
  darkorange: 4287365375,
  darkorchid: 2570243327,
  darkred: 2332033279,
  darksalmon: 3918953215,
  darkseagreen: 2411499519,
  darkslateblue: 1211993087,
  darkslategray: 793726975,
  darkslategrey: 793726975,
  darkturquoise: 13554175,
  darkviolet: 2483082239,
  deeppink: 4279538687,
  deepskyblue: 12582911,
  dimgray: 1768516095,
  dimgrey: 1768516095,
  dodgerblue: 512819199,
  firebrick: 2988581631,
  floralwhite: 4294635775,
  forestgreen: 579543807,
  fuchsia: 4278255615,
  gainsboro: 3705462015,
  ghostwhite: 4177068031,
  gold: 4292280575,
  goldenrod: 3668254975,
  gray: 2155905279,
  green: 8388863,
  greenyellow: 2919182335,
  grey: 2155905279,
  honeydew: 4043305215,
  hotpink: 4285117695,
  indianred: 3445382399,
  indigo: 1258324735,
  ivory: 4294963455,
  khaki: 4041641215,
  lavender: 3873897215,
  lavenderblush: 4293981695,
  lawngreen: 2096890111,
  lemonchiffon: 4294626815,
  lightblue: 2916673279,
  lightcoral: 4034953471,
  lightcyan: 3774873599,
  lightgoldenrodyellow: 4210742015,
  lightgray: 3553874943,
  lightgreen: 2431553791,
  lightgrey: 3553874943,
  lightpink: 4290167295,
  lightsalmon: 4288707327,
  lightseagreen: 548580095,
  lightskyblue: 2278488831,
  lightslategray: 2005441023,
  lightslategrey: 2005441023,
  lightsteelblue: 2965692159,
  lightyellow: 4294959359,
  lime: 16711935,
  limegreen: 852308735,
  linen: 4210091775,
  magenta: 4278255615,
  maroon: 2147483903,
  mediumaquamarine: 1724754687,
  mediumblue: 52735,
  mediumorchid: 3126187007,
  mediumpurple: 2473647103,
  mediumseagreen: 1018393087,
  mediumslateblue: 2070474495,
  mediumspringgreen: 16423679,
  mediumturquoise: 1221709055,
  mediumvioletred: 3340076543,
  midnightblue: 421097727,
  mintcream: 4127193855,
  mistyrose: 4293190143,
  moccasin: 4293178879,
  navajowhite: 4292783615,
  navy: 33023,
  oldlace: 4260751103,
  olive: 2155872511,
  olivedrab: 1804477439,
  orange: 4289003775,
  orangered: 4282712319,
  orchid: 3664828159,
  palegoldenrod: 4008225535,
  palegreen: 2566625535,
  paleturquoise: 2951671551,
  palevioletred: 3681588223,
  papayawhip: 4293907967,
  peachpuff: 4292524543,
  peru: 3448061951,
  pink: 4290825215,
  plum: 3718307327,
  powderblue: 2967529215,
  purple: 2147516671,
  rebeccapurple: 1714657791,
  red: 4278190335,
  rosybrown: 3163525119,
  royalblue: 1097458175,
  saddlebrown: 2336560127,
  salmon: 4202722047,
  sandybrown: 4104413439,
  seagreen: 780883967,
  seashell: 4294307583,
  sienna: 2689740287,
  silver: 3233857791,
  skyblue: 2278484991,
  slateblue: 1784335871,
  slategray: 1887473919,
  slategrey: 1887473919,
  snow: 4294638335,
  springgreen: 16744447,
  steelblue: 1182971135,
  tan: 3535047935,
  teal: 8421631,
  thistle: 3636451583,
  tomato: 4284696575,
  turquoise: 1088475391,
  violet: 4001558271,
  wheat: 4125012991,
  white: 4294967295,
  whitesmoke: 4126537215,
  yellow: 4294902015,
  yellowgreen: 2597139199
};
var NUMBER = "[-+]?\\d*\\.?\\d+";
var PERCENTAGE = NUMBER + "%";
function call(...parts) {
  return "\\(\\s*(" + parts.join(")\\s*,\\s*(") + ")\\s*\\)";
}
var rgb2 = new RegExp("rgb" + call(NUMBER, NUMBER, NUMBER));
var rgba = new RegExp("rgba" + call(NUMBER, NUMBER, NUMBER, NUMBER));
var hsl = new RegExp("hsl" + call(NUMBER, PERCENTAGE, PERCENTAGE));
var hsla = new RegExp(
  "hsla" + call(NUMBER, PERCENTAGE, PERCENTAGE, NUMBER)
);
var hex3 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex4 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex6 = /^#([0-9a-fA-F]{6})$/;
var hex8 = /^#([0-9a-fA-F]{8})$/;
function normalizeColor(color) {
  let match;
  if (typeof color === "number") {
    return color >>> 0 === color && color >= 0 && color <= 4294967295 ? color : null;
  }
  if (match = hex6.exec(color))
    return parseInt(match[1] + "ff", 16) >>> 0;
  if (colors && colors[color] !== void 0) {
    return colors[color];
  }
  if (match = rgb2.exec(color)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    255) >>> // a
    0;
  }
  if (match = rgba.exec(color)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    parse1(match[4])) >>> // a
    0;
  }
  if (match = hex3.exec(color)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      "ff",
      // a
      16
    ) >>> 0;
  }
  if (match = hex8.exec(color))
    return parseInt(match[1], 16) >>> 0;
  if (match = hex4.exec(color)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      match[4] + match[4],
      // a
      16
    ) >>> 0;
  }
  if (match = hsl.exec(color)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | 255) >>> // a
    0;
  }
  if (match = hsla.exec(color)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | parse1(match[4])) >>> // a
    0;
  }
  return null;
}
function hue2rgb(p4, q3, t6) {
  if (t6 < 0)
    t6 += 1;
  if (t6 > 1)
    t6 -= 1;
  if (t6 < 1 / 6)
    return p4 + (q3 - p4) * 6 * t6;
  if (t6 < 1 / 2)
    return q3;
  if (t6 < 2 / 3)
    return p4 + (q3 - p4) * (2 / 3 - t6) * 6;
  return p4;
}
function hslToRgb(h2, s3, l4) {
  const q3 = l4 < 0.5 ? l4 * (1 + s3) : l4 + s3 - l4 * s3;
  const p4 = 2 * l4 - q3;
  const r6 = hue2rgb(p4, q3, h2 + 1 / 3);
  const g4 = hue2rgb(p4, q3, h2);
  const b4 = hue2rgb(p4, q3, h2 - 1 / 3);
  return Math.round(r6 * 255) << 24 | Math.round(g4 * 255) << 16 | Math.round(b4 * 255) << 8;
}
function parse255(str) {
  const int = parseInt(str, 10);
  if (int < 0)
    return 0;
  if (int > 255)
    return 255;
  return int;
}
function parse360(str) {
  const int = parseFloat(str);
  return (int % 360 + 360) % 360 / 360;
}
function parse1(str) {
  const num = parseFloat(str);
  if (num < 0)
    return 0;
  if (num > 1)
    return 255;
  return Math.round(num * 255);
}
function parsePercentage(str) {
  const int = parseFloat(str);
  if (int < 0)
    return 0;
  if (int > 100)
    return 1;
  return int / 100;
}
function colorToRgba(input) {
  let int32Color = normalizeColor(input);
  if (int32Color === null)
    return input;
  int32Color = int32Color || 0;
  const r6 = (int32Color & 4278190080) >>> 24;
  const g4 = (int32Color & 16711680) >>> 16;
  const b4 = (int32Color & 65280) >>> 8;
  const a4 = (int32Color & 255) / 255;
  return `rgba(${r6}, ${g4}, ${b4}, ${a4})`;
}
var createInterpolator = (range, output, extrapolate) => {
  if (is.fun(range)) {
    return range;
  }
  if (is.arr(range)) {
    return createInterpolator({
      range,
      output,
      extrapolate
    });
  }
  if (is.str(range.output[0])) {
    return createStringInterpolator(range);
  }
  const config2 = range;
  const outputRange = config2.output;
  const inputRange = config2.range || [0, 1];
  const extrapolateLeft = config2.extrapolateLeft || config2.extrapolate || "extend";
  const extrapolateRight = config2.extrapolateRight || config2.extrapolate || "extend";
  const easing = config2.easing || ((t6) => t6);
  return (input) => {
    const range2 = findRange(input, inputRange);
    return interpolate(
      input,
      inputRange[range2],
      inputRange[range2 + 1],
      outputRange[range2],
      outputRange[range2 + 1],
      easing,
      extrapolateLeft,
      extrapolateRight,
      config2.map
    );
  };
};
function interpolate(input, inputMin, inputMax, outputMin, outputMax, easing, extrapolateLeft, extrapolateRight, map) {
  let result = map ? map(input) : input;
  if (result < inputMin) {
    if (extrapolateLeft === "identity")
      return result;
    else if (extrapolateLeft === "clamp")
      result = inputMin;
  }
  if (result > inputMax) {
    if (extrapolateRight === "identity")
      return result;
    else if (extrapolateRight === "clamp")
      result = inputMax;
  }
  if (outputMin === outputMax)
    return outputMin;
  if (inputMin === inputMax)
    return input <= inputMin ? outputMin : outputMax;
  if (inputMin === -Infinity)
    result = -result;
  else if (inputMax === Infinity)
    result = result - inputMin;
  else
    result = (result - inputMin) / (inputMax - inputMin);
  result = easing(result);
  if (outputMin === -Infinity)
    result = -result;
  else if (outputMax === Infinity)
    result = result + outputMin;
  else
    result = result * (outputMax - outputMin) + outputMin;
  return result;
}
function findRange(input, inputRange) {
  for (var i5 = 1; i5 < inputRange.length - 1; ++i5)
    if (inputRange[i5] >= input)
      break;
  return i5 - 1;
}
var steps = (steps2, direction = "end") => (progress2) => {
  progress2 = direction === "end" ? Math.min(progress2, 0.999) : Math.max(progress2, 1e-3);
  const expanded = progress2 * steps2;
  const rounded = direction === "end" ? Math.floor(expanded) : Math.ceil(expanded);
  return clamp(0, 1, rounded / steps2);
};
var c1 = 1.70158;
var c2 = c1 * 1.525;
var c3 = c1 + 1;
var c4 = 2 * Math.PI / 3;
var c5 = 2 * Math.PI / 4.5;
var bounceOut = (x3) => {
  const n1 = 7.5625;
  const d1 = 2.75;
  if (x3 < 1 / d1) {
    return n1 * x3 * x3;
  } else if (x3 < 2 / d1) {
    return n1 * (x3 -= 1.5 / d1) * x3 + 0.75;
  } else if (x3 < 2.5 / d1) {
    return n1 * (x3 -= 2.25 / d1) * x3 + 0.9375;
  } else {
    return n1 * (x3 -= 2.625 / d1) * x3 + 0.984375;
  }
};
var easings = {
  linear: (x3) => x3,
  easeInQuad: (x3) => x3 * x3,
  easeOutQuad: (x3) => 1 - (1 - x3) * (1 - x3),
  easeInOutQuad: (x3) => x3 < 0.5 ? 2 * x3 * x3 : 1 - Math.pow(-2 * x3 + 2, 2) / 2,
  easeInCubic: (x3) => x3 * x3 * x3,
  easeOutCubic: (x3) => 1 - Math.pow(1 - x3, 3),
  easeInOutCubic: (x3) => x3 < 0.5 ? 4 * x3 * x3 * x3 : 1 - Math.pow(-2 * x3 + 2, 3) / 2,
  easeInQuart: (x3) => x3 * x3 * x3 * x3,
  easeOutQuart: (x3) => 1 - Math.pow(1 - x3, 4),
  easeInOutQuart: (x3) => x3 < 0.5 ? 8 * x3 * x3 * x3 * x3 : 1 - Math.pow(-2 * x3 + 2, 4) / 2,
  easeInQuint: (x3) => x3 * x3 * x3 * x3 * x3,
  easeOutQuint: (x3) => 1 - Math.pow(1 - x3, 5),
  easeInOutQuint: (x3) => x3 < 0.5 ? 16 * x3 * x3 * x3 * x3 * x3 : 1 - Math.pow(-2 * x3 + 2, 5) / 2,
  easeInSine: (x3) => 1 - Math.cos(x3 * Math.PI / 2),
  easeOutSine: (x3) => Math.sin(x3 * Math.PI / 2),
  easeInOutSine: (x3) => -(Math.cos(Math.PI * x3) - 1) / 2,
  easeInExpo: (x3) => x3 === 0 ? 0 : Math.pow(2, 10 * x3 - 10),
  easeOutExpo: (x3) => x3 === 1 ? 1 : 1 - Math.pow(2, -10 * x3),
  easeInOutExpo: (x3) => x3 === 0 ? 0 : x3 === 1 ? 1 : x3 < 0.5 ? Math.pow(2, 20 * x3 - 10) / 2 : (2 - Math.pow(2, -20 * x3 + 10)) / 2,
  easeInCirc: (x3) => 1 - Math.sqrt(1 - Math.pow(x3, 2)),
  easeOutCirc: (x3) => Math.sqrt(1 - Math.pow(x3 - 1, 2)),
  easeInOutCirc: (x3) => x3 < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x3, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x3 + 2, 2)) + 1) / 2,
  easeInBack: (x3) => c3 * x3 * x3 * x3 - c1 * x3 * x3,
  easeOutBack: (x3) => 1 + c3 * Math.pow(x3 - 1, 3) + c1 * Math.pow(x3 - 1, 2),
  easeInOutBack: (x3) => x3 < 0.5 ? Math.pow(2 * x3, 2) * ((c2 + 1) * 2 * x3 - c2) / 2 : (Math.pow(2 * x3 - 2, 2) * ((c2 + 1) * (x3 * 2 - 2) + c2) + 2) / 2,
  easeInElastic: (x3) => x3 === 0 ? 0 : x3 === 1 ? 1 : -Math.pow(2, 10 * x3 - 10) * Math.sin((x3 * 10 - 10.75) * c4),
  easeOutElastic: (x3) => x3 === 0 ? 0 : x3 === 1 ? 1 : Math.pow(2, -10 * x3) * Math.sin((x3 * 10 - 0.75) * c4) + 1,
  easeInOutElastic: (x3) => x3 === 0 ? 0 : x3 === 1 ? 1 : x3 < 0.5 ? -(Math.pow(2, 20 * x3 - 10) * Math.sin((20 * x3 - 11.125) * c5)) / 2 : Math.pow(2, -20 * x3 + 10) * Math.sin((20 * x3 - 11.125) * c5) / 2 + 1,
  easeInBounce: (x3) => 1 - bounceOut(1 - x3),
  easeOutBounce: bounceOut,
  easeInOutBounce: (x3) => x3 < 0.5 ? (1 - bounceOut(1 - 2 * x3)) / 2 : (1 + bounceOut(2 * x3 - 1)) / 2,
  steps
};
var $get = Symbol.for("FluidValue.get");
var $observers = Symbol.for("FluidValue.observers");
var hasFluidValue = (arg) => Boolean(arg && arg[$get]);
var getFluidValue = (arg) => arg && arg[$get] ? arg[$get]() : arg;
var getFluidObservers = (target) => target[$observers] || null;
function callFluidObserver(observer2, event) {
  if (observer2.eventObserved) {
    observer2.eventObserved(event);
  } else {
    observer2(event);
  }
}
function callFluidObservers(target, event) {
  const observers = target[$observers];
  if (observers) {
    observers.forEach((observer2) => {
      callFluidObserver(observer2, event);
    });
  }
}
var FluidValue = class {
  constructor(get) {
    if (!get && !(get = this.get)) {
      throw Error("Unknown getter");
    }
    setFluidGetter(this, get);
  }
};
var setFluidGetter = (target, get) => setHidden(target, $get, get);
function addFluidObserver(target, observer2) {
  if (target[$get]) {
    let observers = target[$observers];
    if (!observers) {
      setHidden(target, $observers, observers = /* @__PURE__ */ new Set());
    }
    if (!observers.has(observer2)) {
      observers.add(observer2);
      if (target.observerAdded) {
        target.observerAdded(observers.size, observer2);
      }
    }
  }
  return observer2;
}
function removeFluidObserver(target, observer2) {
  const observers = target[$observers];
  if (observers && observers.has(observer2)) {
    const count = observers.size - 1;
    if (count) {
      observers.delete(observer2);
    } else {
      target[$observers] = null;
    }
    if (target.observerRemoved) {
      target.observerRemoved(count, observer2);
    }
  }
}
var setHidden = (target, key, value) => Object.defineProperty(target, key, {
  value,
  writable: true,
  configurable: true
});
var numberRegex = /[+\-]?(?:0|[1-9]\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?/g;
var colorRegex = /(#(?:[0-9a-f]{2}){2,4}|(#[0-9a-f]{3})|(rgb|hsl)a?\((-?\d+%?[,\s]+){2,3}\s*[\d\.]+%?\))/gi;
var unitRegex = new RegExp(`(${numberRegex.source})(%|[a-z]+)`, "i");
var rgbaRegex = /rgba\(([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+)\)/gi;
var cssVariableRegex = /var\((--[a-zA-Z0-9-_]+),? ?([a-zA-Z0-9 ()%#.,-]+)?\)/;
var variableToRgba = (input) => {
  const [token, fallback] = parseCSSVariable(input);
  if (!token || isSSR()) {
    return input;
  }
  const value = window.getComputedStyle(document.documentElement).getPropertyValue(token);
  if (value) {
    return value.trim();
  } else if (fallback && fallback.startsWith("--")) {
    const value2 = window.getComputedStyle(document.documentElement).getPropertyValue(fallback);
    if (value2) {
      return value2;
    } else {
      return input;
    }
  } else if (fallback && cssVariableRegex.test(fallback)) {
    return variableToRgba(fallback);
  } else if (fallback) {
    return fallback;
  }
  return input;
};
var parseCSSVariable = (current) => {
  const match = cssVariableRegex.exec(current);
  if (!match)
    return [,];
  const [, token, fallback] = match;
  return [token, fallback];
};
var namedColorRegex;
var rgbaRound = (_2, p1, p22, p32, p4) => `rgba(${Math.round(p1)}, ${Math.round(p22)}, ${Math.round(p32)}, ${p4})`;
var createStringInterpolator2 = (config2) => {
  if (!namedColorRegex)
    namedColorRegex = colors ? (
      // match color names, ignore partial matches
      new RegExp(`(${Object.keys(colors).join("|")})(?!\\w)`, "g")
    ) : (
      // never match
      /^\b$/
    );
  const output = config2.output.map((value) => {
    return getFluidValue(value).replace(cssVariableRegex, variableToRgba).replace(colorRegex, colorToRgba).replace(namedColorRegex, colorToRgba);
  });
  const keyframes = output.map((value) => value.match(numberRegex).map(Number));
  const outputRanges = keyframes[0].map(
    (_2, i5) => keyframes.map((values) => {
      if (!(i5 in values)) {
        throw Error('The arity of each "output" value must be equal');
      }
      return values[i5];
    })
  );
  const interpolators = outputRanges.map(
    (output2) => createInterpolator({ ...config2, output: output2 })
  );
  return (input) => {
    var _a;
    const missingUnit = !unitRegex.test(output[0]) && ((_a = output.find((value) => unitRegex.test(value))) == null ? void 0 : _a.replace(numberRegex, ""));
    let i5 = 0;
    return output[0].replace(
      numberRegex,
      () => `${interpolators[i5++](input)}${missingUnit || ""}`
    ).replace(rgbaRegex, rgbaRound);
  };
};
var prefix = "react-spring: ";
var once = (fn2) => {
  const func = fn2;
  let called = false;
  if (typeof func != "function") {
    throw new TypeError(`${prefix}once requires a function parameter`);
  }
  return (...args) => {
    if (!called) {
      func(...args);
      called = true;
    }
  };
};
var warnInterpolate = once(console.warn);
function deprecateInterpolate() {
  warnInterpolate(
    `${prefix}The "interpolate" function is deprecated in v9 (use "to" instead)`
  );
}
var warnDirectCall = once(console.warn);
function deprecateDirectCall() {
  warnDirectCall(
    `${prefix}Directly calling start instead of using the api object is deprecated in v9 (use ".start" instead), this will be removed in later 0.X.0 versions`
  );
}
function isAnimatedString(value) {
  return is.str(value) && (value[0] == "#" || /\d/.test(value) || // Do not identify a CSS variable as an AnimatedString if its SSR
  !isSSR() && cssVariableRegex.test(value) || value in (colors || {}));
}
var useIsomorphicLayoutEffect = isSSR() ? import_react4.useEffect : import_react4.useLayoutEffect;
var useIsMounted = () => {
  const isMounted = (0, import_react3.useRef)(false);
  useIsomorphicLayoutEffect(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
function useForceUpdate() {
  const update3 = (0, import_react2.useState)()[1];
  const isMounted = useIsMounted();
  return () => {
    if (isMounted.current) {
      update3(Math.random());
    }
  };
}
function useMemoOne(getResult, inputs) {
  const [initial] = (0, import_react5.useState)(
    () => ({
      inputs,
      result: getResult()
    })
  );
  const committed = (0, import_react5.useRef)();
  const prevCache = committed.current;
  let cache = prevCache;
  if (cache) {
    const useCache = Boolean(
      inputs && cache.inputs && areInputsEqual(inputs, cache.inputs)
    );
    if (!useCache) {
      cache = {
        inputs,
        result: getResult()
      };
    }
  } else {
    cache = initial;
  }
  (0, import_react5.useEffect)(() => {
    committed.current = cache;
    if (prevCache == initial) {
      initial.inputs = initial.result = void 0;
    }
  }, [cache]);
  return cache.result;
}
function areInputsEqual(next, prev) {
  if (next.length !== prev.length) {
    return false;
  }
  for (let i5 = 0; i5 < next.length; i5++) {
    if (next[i5] !== prev[i5]) {
      return false;
    }
  }
  return true;
}
var useOnce = (effect) => (0, import_react6.useEffect)(effect, emptyDeps);
var emptyDeps = [];
function usePrev(value) {
  const prevRef = (0, import_react7.useRef)();
  (0, import_react7.useEffect)(() => {
    prevRef.current = value;
  });
  return prevRef.current;
}

// node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var import_react10 = __toESM(require_react(), 1);

// node_modules/@react-spring/animated/dist/react-spring_animated.modern.mjs
var React = __toESM(require_react(), 1);
var import_react9 = __toESM(require_react(), 1);
var $node = Symbol.for("Animated:node");
var isAnimated = (value) => !!value && value[$node] === value;
var getAnimated = (owner) => owner && owner[$node];
var setAnimated = (owner, node) => defineHidden(owner, $node, node);
var getPayload = (owner) => owner && owner[$node] && owner[$node].getPayload();
var Animated = class {
  constructor() {
    setAnimated(this, this);
  }
  /** Get every `AnimatedValue` used by this node. */
  getPayload() {
    return this.payload || [];
  }
};
var AnimatedValue = class extends Animated {
  constructor(_value) {
    super();
    this._value = _value;
    this.done = true;
    this.durationProgress = 0;
    if (is.num(this._value)) {
      this.lastPosition = this._value;
    }
  }
  /** @internal */
  static create(value) {
    return new AnimatedValue(value);
  }
  getPayload() {
    return [this];
  }
  getValue() {
    return this._value;
  }
  setValue(value, step) {
    if (is.num(value)) {
      this.lastPosition = value;
      if (step) {
        value = Math.round(value / step) * step;
        if (this.done) {
          this.lastPosition = value;
        }
      }
    }
    if (this._value === value) {
      return false;
    }
    this._value = value;
    return true;
  }
  reset() {
    const { done } = this;
    this.done = false;
    if (is.num(this._value)) {
      this.elapsedTime = 0;
      this.durationProgress = 0;
      this.lastPosition = this._value;
      if (done)
        this.lastVelocity = null;
      this.v0 = null;
    }
  }
};
var AnimatedString = class extends AnimatedValue {
  constructor(value) {
    super(0);
    this._string = null;
    this._toString = createInterpolator({
      output: [value, value]
    });
  }
  /** @internal */
  static create(value) {
    return new AnimatedString(value);
  }
  getValue() {
    const value = this._string;
    return value == null ? this._string = this._toString(this._value) : value;
  }
  setValue(value) {
    if (is.str(value)) {
      if (value == this._string) {
        return false;
      }
      this._string = value;
      this._value = 1;
    } else if (super.setValue(value)) {
      this._string = null;
    } else {
      return false;
    }
    return true;
  }
  reset(goal) {
    if (goal) {
      this._toString = createInterpolator({
        output: [this.getValue(), goal]
      });
    }
    this._value = 0;
    super.reset();
  }
};
var TreeContext = { dependencies: null };
var AnimatedObject = class extends Animated {
  constructor(source) {
    super();
    this.source = source;
    this.setValue(source);
  }
  getValue(animated2) {
    const values = {};
    eachProp(this.source, (source, key) => {
      if (isAnimated(source)) {
        values[key] = source.getValue(animated2);
      } else if (hasFluidValue(source)) {
        values[key] = getFluidValue(source);
      } else if (!animated2) {
        values[key] = source;
      }
    });
    return values;
  }
  /** Replace the raw object data */
  setValue(source) {
    this.source = source;
    this.payload = this._makePayload(source);
  }
  reset() {
    if (this.payload) {
      each(this.payload, (node) => node.reset());
    }
  }
  /** Create a payload set. */
  _makePayload(source) {
    if (source) {
      const payload = /* @__PURE__ */ new Set();
      eachProp(source, this._addToPayload, payload);
      return Array.from(payload);
    }
  }
  /** Add to a payload set. */
  _addToPayload(source) {
    if (TreeContext.dependencies && hasFluidValue(source)) {
      TreeContext.dependencies.add(source);
    }
    const payload = getPayload(source);
    if (payload) {
      each(payload, (node) => this.add(node));
    }
  }
};
var AnimatedArray = class extends AnimatedObject {
  constructor(source) {
    super(source);
  }
  /** @internal */
  static create(source) {
    return new AnimatedArray(source);
  }
  getValue() {
    return this.source.map((node) => node.getValue());
  }
  setValue(source) {
    const payload = this.getPayload();
    if (source.length == payload.length) {
      return payload.map((node, i5) => node.setValue(source[i5])).some(Boolean);
    }
    super.setValue(source.map(makeAnimated));
    return true;
  }
};
function makeAnimated(value) {
  const nodeType = isAnimatedString(value) ? AnimatedString : AnimatedValue;
  return nodeType.create(value);
}
function getAnimatedType(value) {
  const parentNode = getAnimated(value);
  return parentNode ? parentNode.constructor : is.arr(value) ? AnimatedArray : isAnimatedString(value) ? AnimatedString : AnimatedValue;
}
var withAnimated = (Component, host2) => {
  const hasInstance = (
    // Function components must use "forwardRef" to avoid being
    // re-rendered on every animation frame.
    !is.fun(Component) || Component.prototype && Component.prototype.isReactComponent
  );
  return (0, import_react9.forwardRef)((givenProps, givenRef) => {
    const instanceRef = (0, import_react9.useRef)(null);
    const ref = hasInstance && // eslint-disable-next-line react-hooks/rules-of-hooks
    (0, import_react9.useCallback)(
      (value) => {
        instanceRef.current = updateRef(givenRef, value);
      },
      [givenRef]
    );
    const [props, deps] = getAnimatedState(givenProps, host2);
    const forceUpdate = useForceUpdate();
    const callback = () => {
      const instance = instanceRef.current;
      if (hasInstance && !instance) {
        return;
      }
      const didUpdate = instance ? host2.applyAnimatedValues(instance, props.getValue(true)) : false;
      if (didUpdate === false) {
        forceUpdate();
      }
    };
    const observer = new PropsObserver(callback, deps);
    const observerRef = (0, import_react9.useRef)();
    useIsomorphicLayoutEffect(() => {
      observerRef.current = observer;
      each(deps, (dep) => addFluidObserver(dep, observer));
      return () => {
        if (observerRef.current) {
          each(
            observerRef.current.deps,
            (dep) => removeFluidObserver(dep, observerRef.current)
          );
          raf.cancel(observerRef.current.update);
        }
      };
    });
    (0, import_react9.useEffect)(callback, []);
    useOnce(() => () => {
      const observer2 = observerRef.current;
      each(observer2.deps, (dep) => removeFluidObserver(dep, observer2));
    });
    const usedProps = host2.getComponentProps(props.getValue());
    return React.createElement(Component, { ...usedProps, ref });
  });
};
var PropsObserver = class {
  constructor(update3, deps) {
    this.update = update3;
    this.deps = deps;
  }
  eventObserved(event) {
    if (event.type == "change") {
      raf.write(this.update);
    }
  }
};
function getAnimatedState(props, host2) {
  const dependencies = /* @__PURE__ */ new Set();
  TreeContext.dependencies = dependencies;
  if (props.style)
    props = {
      ...props,
      style: host2.createAnimatedStyle(props.style)
    };
  props = new AnimatedObject(props);
  TreeContext.dependencies = null;
  return [props, dependencies];
}
function updateRef(ref, value) {
  if (ref) {
    if (is.fun(ref))
      ref(value);
    else
      ref.current = value;
  }
  return value;
}
var cacheKey = Symbol.for("AnimatedComponent");
var createHost = (components, {
  applyAnimatedValues: applyAnimatedValues2 = () => false,
  createAnimatedStyle = (style) => new AnimatedObject(style),
  getComponentProps = (props) => props
} = {}) => {
  const hostConfig = {
    applyAnimatedValues: applyAnimatedValues2,
    createAnimatedStyle,
    getComponentProps
  };
  const animated2 = (Component) => {
    const displayName = getDisplayName(Component) || "Anonymous";
    if (is.str(Component)) {
      Component = animated2[Component] || (animated2[Component] = withAnimated(Component, hostConfig));
    } else {
      Component = Component[cacheKey] || (Component[cacheKey] = withAnimated(Component, hostConfig));
    }
    Component.displayName = `Animated(${displayName})`;
    return Component;
  };
  eachProp(components, (Component, key) => {
    if (is.arr(components)) {
      key = getDisplayName(Component);
    }
    animated2[key] = animated2(Component);
  });
  return {
    animated: animated2
  };
};
var getDisplayName = (arg) => is.str(arg) ? arg : arg && is.str(arg.displayName) ? arg.displayName : is.fun(arg) && arg.name || null;

// node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var React2 = __toESM(require_react(), 1);
var import_react11 = __toESM(require_react(), 1);
var import_react12 = __toESM(require_react(), 1);
var React22 = __toESM(require_react(), 1);
var import_react13 = __toESM(require_react(), 1);
var import_react14 = __toESM(require_react(), 1);
function callProp(value, ...args) {
  return is.fun(value) ? value(...args) : value;
}
var matchProp = (value, key) => value === true || !!(key && value && (is.fun(value) ? value(key) : toArray(value).includes(key)));
var resolveProp = (prop, key) => is.obj(prop) ? key && prop[key] : prop;
var getDefaultProp = (props, key) => props.default === true ? props[key] : props.default ? props.default[key] : void 0;
var noopTransform = (value) => value;
var getDefaultProps = (props, transform = noopTransform) => {
  let keys = DEFAULT_PROPS;
  if (props.default && props.default !== true) {
    props = props.default;
    keys = Object.keys(props);
  }
  const defaults2 = {};
  for (const key of keys) {
    const value = transform(props[key], key);
    if (!is.und(value)) {
      defaults2[key] = value;
    }
  }
  return defaults2;
};
var DEFAULT_PROPS = [
  "config",
  "onProps",
  "onStart",
  "onChange",
  "onPause",
  "onResume",
  "onRest"
];
var RESERVED_PROPS = {
  config: 1,
  from: 1,
  to: 1,
  ref: 1,
  loop: 1,
  reset: 1,
  pause: 1,
  cancel: 1,
  reverse: 1,
  immediate: 1,
  default: 1,
  delay: 1,
  onProps: 1,
  onStart: 1,
  onChange: 1,
  onPause: 1,
  onResume: 1,
  onRest: 1,
  onResolve: 1,
  // Transition props
  items: 1,
  trail: 1,
  sort: 1,
  expires: 1,
  initial: 1,
  enter: 1,
  update: 1,
  leave: 1,
  children: 1,
  onDestroyed: 1,
  // Internal props
  keys: 1,
  callId: 1,
  parentId: 1
};
function getForwardProps(props) {
  const forward = {};
  let count = 0;
  eachProp(props, (value, prop) => {
    if (!RESERVED_PROPS[prop]) {
      forward[prop] = value;
      count++;
    }
  });
  if (count) {
    return forward;
  }
}
function inferTo(props) {
  const to22 = getForwardProps(props);
  if (to22) {
    const out = { to: to22 };
    eachProp(props, (val, key) => key in to22 || (out[key] = val));
    return out;
  }
  return { ...props };
}
function computeGoal(value) {
  value = getFluidValue(value);
  return is.arr(value) ? value.map(computeGoal) : isAnimatedString(value) ? globals_exports.createStringInterpolator({
    range: [0, 1],
    output: [value, value]
  })(1) : value;
}
function hasProps(props) {
  for (const _2 in props)
    return true;
  return false;
}
function isAsyncTo(to22) {
  return is.fun(to22) || is.arr(to22) && is.obj(to22[0]);
}
function detachRefs(ctrl, ref) {
  var _a;
  (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
  ref == null ? void 0 : ref.delete(ctrl);
}
function replaceRef(ctrl, ref) {
  var _a;
  if (ref && ctrl.ref !== ref) {
    (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
    ref.add(ctrl);
    ctrl.ref = ref;
  }
}
var config = {
  default: { tension: 170, friction: 26 },
  gentle: { tension: 120, friction: 14 },
  wobbly: { tension: 180, friction: 12 },
  stiff: { tension: 210, friction: 20 },
  slow: { tension: 280, friction: 60 },
  molasses: { tension: 280, friction: 120 }
};
var defaults = {
  ...config.default,
  mass: 1,
  damping: 1,
  easing: easings.linear,
  clamp: false
};
var AnimationConfig = class {
  constructor() {
    this.velocity = 0;
    Object.assign(this, defaults);
  }
};
function mergeConfig(config2, newConfig, defaultConfig) {
  if (defaultConfig) {
    defaultConfig = { ...defaultConfig };
    sanitizeConfig(defaultConfig, newConfig);
    newConfig = { ...defaultConfig, ...newConfig };
  }
  sanitizeConfig(config2, newConfig);
  Object.assign(config2, newConfig);
  for (const key in defaults) {
    if (config2[key] == null) {
      config2[key] = defaults[key];
    }
  }
  let { frequency, damping } = config2;
  const { mass } = config2;
  if (!is.und(frequency)) {
    if (frequency < 0.01)
      frequency = 0.01;
    if (damping < 0)
      damping = 0;
    config2.tension = Math.pow(2 * Math.PI / frequency, 2) * mass;
    config2.friction = 4 * Math.PI * damping * mass / frequency;
  }
  return config2;
}
function sanitizeConfig(config2, props) {
  if (!is.und(props.decay)) {
    config2.duration = void 0;
  } else {
    const isTensionConfig = !is.und(props.tension) || !is.und(props.friction);
    if (isTensionConfig || !is.und(props.frequency) || !is.und(props.damping) || !is.und(props.mass)) {
      config2.duration = void 0;
      config2.decay = void 0;
    }
    if (isTensionConfig) {
      config2.frequency = void 0;
    }
  }
}
var emptyArray = [];
var Animation = class {
  constructor() {
    this.changed = false;
    this.values = emptyArray;
    this.toValues = null;
    this.fromValues = emptyArray;
    this.config = new AnimationConfig();
    this.immediate = false;
  }
};
function scheduleProps(callId, { key, props, defaultProps, state, actions }) {
  return new Promise((resolve, reject) => {
    let delay;
    let timeout;
    let cancel = matchProp(props.cancel ?? (defaultProps == null ? void 0 : defaultProps.cancel), key);
    if (cancel) {
      onStart();
    } else {
      if (!is.und(props.pause)) {
        state.paused = matchProp(props.pause, key);
      }
      let pause = defaultProps == null ? void 0 : defaultProps.pause;
      if (pause !== true) {
        pause = state.paused || matchProp(pause, key);
      }
      delay = callProp(props.delay || 0, key);
      if (pause) {
        state.resumeQueue.add(onResume);
        actions.pause();
      } else {
        actions.resume();
        onResume();
      }
    }
    function onPause() {
      state.resumeQueue.add(onResume);
      state.timeouts.delete(timeout);
      timeout.cancel();
      delay = timeout.time - raf.now();
    }
    function onResume() {
      if (delay > 0 && !globals_exports.skipAnimation) {
        state.delayed = true;
        timeout = raf.setTimeout(onStart, delay);
        state.pauseQueue.add(onPause);
        state.timeouts.add(timeout);
      } else {
        onStart();
      }
    }
    function onStart() {
      if (state.delayed) {
        state.delayed = false;
      }
      state.pauseQueue.delete(onPause);
      state.timeouts.delete(timeout);
      if (callId <= (state.cancelId || 0)) {
        cancel = true;
      }
      try {
        actions.start({ ...props, callId, cancel }, resolve);
      } catch (err) {
        reject(err);
      }
    }
  });
}
var getCombinedResult = (target, results) => results.length == 1 ? results[0] : results.some((result) => result.cancelled) ? getCancelledResult(target.get()) : results.every((result) => result.noop) ? getNoopResult(target.get()) : getFinishedResult(
  target.get(),
  results.every((result) => result.finished)
);
var getNoopResult = (value) => ({
  value,
  noop: true,
  finished: true,
  cancelled: false
});
var getFinishedResult = (value, finished, cancelled = false) => ({
  value,
  finished,
  cancelled
});
var getCancelledResult = (value) => ({
  value,
  cancelled: true,
  finished: false
});
function runAsync(to22, props, state, target) {
  const { callId, parentId, onRest } = props;
  const { asyncTo: prevTo, promise: prevPromise } = state;
  if (!parentId && to22 === prevTo && !props.reset) {
    return prevPromise;
  }
  return state.promise = (async () => {
    state.asyncId = callId;
    state.asyncTo = to22;
    const defaultProps = getDefaultProps(
      props,
      (value, key) => (
        // The `onRest` prop is only called when the `runAsync` promise is resolved.
        key === "onRest" ? void 0 : value
      )
    );
    let preventBail;
    let bail;
    const bailPromise = new Promise(
      (resolve, reject) => (preventBail = resolve, bail = reject)
    );
    const bailIfEnded = (bailSignal) => {
      const bailResult = (
        // The `cancel` prop or `stop` method was used.
        callId <= (state.cancelId || 0) && getCancelledResult(target) || // The async `to` prop was replaced.
        callId !== state.asyncId && getFinishedResult(target, false)
      );
      if (bailResult) {
        bailSignal.result = bailResult;
        bail(bailSignal);
        throw bailSignal;
      }
    };
    const animate = (arg1, arg2) => {
      const bailSignal = new BailSignal();
      const skipAnimationSignal = new SkipAnimationSignal();
      return (async () => {
        if (globals_exports.skipAnimation) {
          stopAsync(state);
          skipAnimationSignal.result = getFinishedResult(target, false);
          bail(skipAnimationSignal);
          throw skipAnimationSignal;
        }
        bailIfEnded(bailSignal);
        const props2 = is.obj(arg1) ? { ...arg1 } : { ...arg2, to: arg1 };
        props2.parentId = callId;
        eachProp(defaultProps, (value, key) => {
          if (is.und(props2[key])) {
            props2[key] = value;
          }
        });
        const result2 = await target.start(props2);
        bailIfEnded(bailSignal);
        if (state.paused) {
          await new Promise((resume) => {
            state.resumeQueue.add(resume);
          });
        }
        return result2;
      })();
    };
    let result;
    if (globals_exports.skipAnimation) {
      stopAsync(state);
      return getFinishedResult(target, false);
    }
    try {
      let animating;
      if (is.arr(to22)) {
        animating = (async (queue) => {
          for (const props2 of queue) {
            await animate(props2);
          }
        })(to22);
      } else {
        animating = Promise.resolve(to22(animate, target.stop.bind(target)));
      }
      await Promise.all([animating.then(preventBail), bailPromise]);
      result = getFinishedResult(target.get(), true, false);
    } catch (err) {
      if (err instanceof BailSignal) {
        result = err.result;
      } else if (err instanceof SkipAnimationSignal) {
        result = err.result;
      } else {
        throw err;
      }
    } finally {
      if (callId == state.asyncId) {
        state.asyncId = parentId;
        state.asyncTo = parentId ? prevTo : void 0;
        state.promise = parentId ? prevPromise : void 0;
      }
    }
    if (is.fun(onRest)) {
      raf.batchedUpdates(() => {
        onRest(result, target, target.item);
      });
    }
    return result;
  })();
}
function stopAsync(state, cancelId) {
  flush(state.timeouts, (t6) => t6.cancel());
  state.pauseQueue.clear();
  state.resumeQueue.clear();
  state.asyncId = state.asyncTo = state.promise = void 0;
  if (cancelId)
    state.cancelId = cancelId;
}
var BailSignal = class extends Error {
  constructor() {
    super(
      "An async animation has been interrupted. You see this error because you forgot to use `await` or `.catch(...)` on its returned promise."
    );
  }
};
var SkipAnimationSignal = class extends Error {
  constructor() {
    super("SkipAnimationSignal");
  }
};
var isFrameValue = (value) => value instanceof FrameValue;
var nextId = 1;
var FrameValue = class extends FluidValue {
  constructor() {
    super(...arguments);
    this.id = nextId++;
    this._priority = 0;
  }
  get priority() {
    return this._priority;
  }
  set priority(priority2) {
    if (this._priority != priority2) {
      this._priority = priority2;
      this._onPriorityChange(priority2);
    }
  }
  /** Get the current value */
  get() {
    const node = getAnimated(this);
    return node && node.getValue();
  }
  /** Create a spring that maps our value to another value */
  to(...args) {
    return globals_exports.to(this, args);
  }
  /** @deprecated Use the `to` method instead. */
  interpolate(...args) {
    deprecateInterpolate();
    return globals_exports.to(this, args);
  }
  toJSON() {
    return this.get();
  }
  observerAdded(count) {
    if (count == 1)
      this._attach();
  }
  observerRemoved(count) {
    if (count == 0)
      this._detach();
  }
  /** Called when the first child is added. */
  _attach() {
  }
  /** Called when the last child is removed. */
  _detach() {
  }
  /** Tell our children about our new value */
  _onChange(value, idle = false) {
    callFluidObservers(this, {
      type: "change",
      parent: this,
      value,
      idle
    });
  }
  /** Tell our children about our new priority */
  _onPriorityChange(priority2) {
    if (!this.idle) {
      frameLoop.sort(this);
    }
    callFluidObservers(this, {
      type: "priority",
      parent: this,
      priority: priority2
    });
  }
};
var $P = Symbol.for("SpringPhase");
var HAS_ANIMATED = 1;
var IS_ANIMATING = 2;
var IS_PAUSED = 4;
var hasAnimated = (target) => (target[$P] & HAS_ANIMATED) > 0;
var isAnimating = (target) => (target[$P] & IS_ANIMATING) > 0;
var isPaused = (target) => (target[$P] & IS_PAUSED) > 0;
var setActiveBit = (target, active) => active ? target[$P] |= IS_ANIMATING | HAS_ANIMATED : target[$P] &= ~IS_ANIMATING;
var setPausedBit = (target, paused) => paused ? target[$P] |= IS_PAUSED : target[$P] &= ~IS_PAUSED;
var SpringValue = class extends FrameValue {
  constructor(arg1, arg2) {
    super();
    this.animation = new Animation();
    this.defaultProps = {};
    this._state = {
      paused: false,
      delayed: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._pendingCalls = /* @__PURE__ */ new Set();
    this._lastCallId = 0;
    this._lastToId = 0;
    this._memoizedDuration = 0;
    if (!is.und(arg1) || !is.und(arg2)) {
      const props = is.obj(arg1) ? { ...arg1 } : { ...arg2, from: arg1 };
      if (is.und(props.default)) {
        props.default = true;
      }
      this.start(props);
    }
  }
  /** Equals true when not advancing on each frame. */
  get idle() {
    return !(isAnimating(this) || this._state.asyncTo) || isPaused(this);
  }
  get goal() {
    return getFluidValue(this.animation.to);
  }
  get velocity() {
    const node = getAnimated(this);
    return node instanceof AnimatedValue ? node.lastVelocity || 0 : node.getPayload().map((node2) => node2.lastVelocity || 0);
  }
  /**
   * When true, this value has been animated at least once.
   */
  get hasAnimated() {
    return hasAnimated(this);
  }
  /**
   * When true, this value has an unfinished animation,
   * which is either active or paused.
   */
  get isAnimating() {
    return isAnimating(this);
  }
  /**
   * When true, all current and future animations are paused.
   */
  get isPaused() {
    return isPaused(this);
  }
  /**
   *
   *
   */
  get isDelayed() {
    return this._state.delayed;
  }
  /** Advance the current animation by a number of milliseconds */
  advance(dt2) {
    let idle = true;
    let changed = false;
    const anim = this.animation;
    let { toValues } = anim;
    const { config: config2 } = anim;
    const payload = getPayload(anim.to);
    if (!payload && hasFluidValue(anim.to)) {
      toValues = toArray(getFluidValue(anim.to));
    }
    anim.values.forEach((node2, i5) => {
      if (node2.done)
        return;
      const to22 = (
        // Animated strings always go from 0 to 1.
        node2.constructor == AnimatedString ? 1 : payload ? payload[i5].lastPosition : toValues[i5]
      );
      let finished = anim.immediate;
      let position = to22;
      if (!finished) {
        position = node2.lastPosition;
        if (config2.tension <= 0) {
          node2.done = true;
          return;
        }
        let elapsed = node2.elapsedTime += dt2;
        const from = anim.fromValues[i5];
        const v0 = node2.v0 != null ? node2.v0 : node2.v0 = is.arr(config2.velocity) ? config2.velocity[i5] : config2.velocity;
        let velocity;
        const precision = config2.precision || (from == to22 ? 5e-3 : Math.min(1, Math.abs(to22 - from) * 1e-3));
        if (!is.und(config2.duration)) {
          let p4 = 1;
          if (config2.duration > 0) {
            if (this._memoizedDuration !== config2.duration) {
              this._memoizedDuration = config2.duration;
              if (node2.durationProgress > 0) {
                node2.elapsedTime = config2.duration * node2.durationProgress;
                elapsed = node2.elapsedTime += dt2;
              }
            }
            p4 = (config2.progress || 0) + elapsed / this._memoizedDuration;
            p4 = p4 > 1 ? 1 : p4 < 0 ? 0 : p4;
            node2.durationProgress = p4;
          }
          position = from + config2.easing(p4) * (to22 - from);
          velocity = (position - node2.lastPosition) / dt2;
          finished = p4 == 1;
        } else if (config2.decay) {
          const decay = config2.decay === true ? 0.998 : config2.decay;
          const e6 = Math.exp(-(1 - decay) * elapsed);
          position = from + v0 / (1 - decay) * (1 - e6);
          finished = Math.abs(node2.lastPosition - position) <= precision;
          velocity = v0 * e6;
        } else {
          velocity = node2.lastVelocity == null ? v0 : node2.lastVelocity;
          const restVelocity = config2.restVelocity || precision / 10;
          const bounceFactor = config2.clamp ? 0 : config2.bounce;
          const canBounce = !is.und(bounceFactor);
          const isGrowing = from == to22 ? node2.v0 > 0 : from < to22;
          let isMoving;
          let isBouncing = false;
          const step = 1;
          const numSteps = Math.ceil(dt2 / step);
          for (let n6 = 0; n6 < numSteps; ++n6) {
            isMoving = Math.abs(velocity) > restVelocity;
            if (!isMoving) {
              finished = Math.abs(to22 - position) <= precision;
              if (finished) {
                break;
              }
            }
            if (canBounce) {
              isBouncing = position == to22 || position > to22 == isGrowing;
              if (isBouncing) {
                velocity = -velocity * bounceFactor;
                position = to22;
              }
            }
            const springForce = -config2.tension * 1e-6 * (position - to22);
            const dampingForce = -config2.friction * 1e-3 * velocity;
            const acceleration = (springForce + dampingForce) / config2.mass;
            velocity = velocity + acceleration * step;
            position = position + velocity * step;
          }
        }
        node2.lastVelocity = velocity;
        if (Number.isNaN(position)) {
          console.warn(`Got NaN while animating:`, this);
          finished = true;
        }
      }
      if (payload && !payload[i5].done) {
        finished = false;
      }
      if (finished) {
        node2.done = true;
      } else {
        idle = false;
      }
      if (node2.setValue(position, config2.round)) {
        changed = true;
      }
    });
    const node = getAnimated(this);
    const currVal = node.getValue();
    if (idle) {
      const finalVal = getFluidValue(anim.to);
      if ((currVal !== finalVal || changed) && !config2.decay) {
        node.setValue(finalVal);
        this._onChange(finalVal);
      } else if (changed && config2.decay) {
        this._onChange(currVal);
      }
      this._stop();
    } else if (changed) {
      this._onChange(currVal);
    }
  }
  /** Set the current value, while stopping the current animation */
  set(value) {
    raf.batchedUpdates(() => {
      this._stop();
      this._focus(value);
      this._set(value);
    });
    return this;
  }
  /**
   * Freeze the active animation in time, as well as any updates merged
   * before `resume` is called.
   */
  pause() {
    this._update({ pause: true });
  }
  /** Resume the animation if paused. */
  resume() {
    this._update({ pause: false });
  }
  /** Skip to the end of the current animation. */
  finish() {
    if (isAnimating(this)) {
      const { to: to22, config: config2 } = this.animation;
      raf.batchedUpdates(() => {
        this._onStart();
        if (!config2.decay) {
          this._set(to22, false);
        }
        this._stop();
      });
    }
    return this;
  }
  /** Push props into the pending queue. */
  update(props) {
    const queue = this.queue || (this.queue = []);
    queue.push(props);
    return this;
  }
  start(to22, arg2) {
    let queue;
    if (!is.und(to22)) {
      queue = [is.obj(to22) ? to22 : { ...arg2, to: to22 }];
    } else {
      queue = this.queue || [];
      this.queue = [];
    }
    return Promise.all(
      queue.map((props) => {
        const up = this._update(props);
        return up;
      })
    ).then((results) => getCombinedResult(this, results));
  }
  /**
   * Stop the current animation, and cancel any delayed updates.
   *
   * Pass `true` to call `onRest` with `cancelled: true`.
   */
  stop(cancel) {
    const { to: to22 } = this.animation;
    this._focus(this.get());
    stopAsync(this._state, cancel && this._lastCallId);
    raf.batchedUpdates(() => this._stop(to22, cancel));
    return this;
  }
  /** Restart the animation. */
  reset() {
    this._update({ reset: true });
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._start();
    } else if (event.type == "priority") {
      this.priority = event.priority + 1;
    }
  }
  /**
   * Parse the `to` and `from` range from the given `props` object.
   *
   * This also ensures the initial value is available to animated components
   * during the render phase.
   */
  _prepareNode(props) {
    const key = this.key || "";
    let { to: to22, from } = props;
    to22 = is.obj(to22) ? to22[key] : to22;
    if (to22 == null || isAsyncTo(to22)) {
      to22 = void 0;
    }
    from = is.obj(from) ? from[key] : from;
    if (from == null) {
      from = void 0;
    }
    const range = { to: to22, from };
    if (!hasAnimated(this)) {
      if (props.reverse)
        [to22, from] = [from, to22];
      from = getFluidValue(from);
      if (!is.und(from)) {
        this._set(from);
      } else if (!getAnimated(this)) {
        this._set(to22);
      }
    }
    return range;
  }
  /** Every update is processed by this method before merging. */
  _update({ ...props }, isLoop) {
    const { key, defaultProps } = this;
    if (props.default)
      Object.assign(
        defaultProps,
        getDefaultProps(
          props,
          (value, prop) => /^on/.test(prop) ? resolveProp(value, key) : value
        )
      );
    mergeActiveFn(this, props, "onProps");
    sendEvent(this, "onProps", props, this);
    const range = this._prepareNode(props);
    if (Object.isFrozen(this)) {
      throw Error(
        "Cannot animate a `SpringValue` object that is frozen. Did you forget to pass your component to `animated(...)` before animating its props?"
      );
    }
    const state = this._state;
    return scheduleProps(++this._lastCallId, {
      key,
      props,
      defaultProps,
      state,
      actions: {
        pause: () => {
          if (!isPaused(this)) {
            setPausedBit(this, true);
            flushCalls(state.pauseQueue);
            sendEvent(
              this,
              "onPause",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        resume: () => {
          if (isPaused(this)) {
            setPausedBit(this, false);
            if (isAnimating(this)) {
              this._resume();
            }
            flushCalls(state.resumeQueue);
            sendEvent(
              this,
              "onResume",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        start: this._merge.bind(this, range)
      }
    }).then((result) => {
      if (props.loop && result.finished && !(isLoop && result.noop)) {
        const nextProps = createLoopUpdate(props);
        if (nextProps) {
          return this._update(nextProps, true);
        }
      }
      return result;
    });
  }
  /** Merge props into the current animation */
  _merge(range, props, resolve) {
    if (props.cancel) {
      this.stop(true);
      return resolve(getCancelledResult(this));
    }
    const hasToProp = !is.und(range.to);
    const hasFromProp = !is.und(range.from);
    if (hasToProp || hasFromProp) {
      if (props.callId > this._lastToId) {
        this._lastToId = props.callId;
      } else {
        return resolve(getCancelledResult(this));
      }
    }
    const { key, defaultProps, animation: anim } = this;
    const { to: prevTo, from: prevFrom } = anim;
    let { to: to22 = prevTo, from = prevFrom } = range;
    if (hasFromProp && !hasToProp && (!props.default || is.und(to22))) {
      to22 = from;
    }
    if (props.reverse)
      [to22, from] = [from, to22];
    const hasFromChanged = !isEqual(from, prevFrom);
    if (hasFromChanged) {
      anim.from = from;
    }
    from = getFluidValue(from);
    const hasToChanged = !isEqual(to22, prevTo);
    if (hasToChanged) {
      this._focus(to22);
    }
    const hasAsyncTo = isAsyncTo(props.to);
    const { config: config2 } = anim;
    const { decay, velocity } = config2;
    if (hasToProp || hasFromProp) {
      config2.velocity = 0;
    }
    if (props.config && !hasAsyncTo) {
      mergeConfig(
        config2,
        callProp(props.config, key),
        // Avoid calling the same "config" prop twice.
        props.config !== defaultProps.config ? callProp(defaultProps.config, key) : void 0
      );
    }
    let node = getAnimated(this);
    if (!node || is.und(to22)) {
      return resolve(getFinishedResult(this, true));
    }
    const reset = (
      // When `reset` is undefined, the `from` prop implies `reset: true`,
      // except for declarative updates. When `reset` is defined, there
      // must exist a value to animate from.
      is.und(props.reset) ? hasFromProp && !props.default : !is.und(from) && matchProp(props.reset, key)
    );
    const value = reset ? from : this.get();
    const goal = computeGoal(to22);
    const isAnimatable = is.num(goal) || is.arr(goal) || isAnimatedString(goal);
    const immediate = !hasAsyncTo && (!isAnimatable || matchProp(defaultProps.immediate || props.immediate, key));
    if (hasToChanged) {
      const nodeType = getAnimatedType(to22);
      if (nodeType !== node.constructor) {
        if (immediate) {
          node = this._set(goal);
        } else
          throw Error(
            `Cannot animate between ${node.constructor.name} and ${nodeType.name}, as the "to" prop suggests`
          );
      }
    }
    const goalType = node.constructor;
    let started = hasFluidValue(to22);
    let finished = false;
    if (!started) {
      const hasValueChanged = reset || !hasAnimated(this) && hasFromChanged;
      if (hasToChanged || hasValueChanged) {
        finished = isEqual(computeGoal(value), goal);
        started = !finished;
      }
      if (!isEqual(anim.immediate, immediate) && !immediate || !isEqual(config2.decay, decay) || !isEqual(config2.velocity, velocity)) {
        started = true;
      }
    }
    if (finished && isAnimating(this)) {
      if (anim.changed && !reset) {
        started = true;
      } else if (!started) {
        this._stop(prevTo);
      }
    }
    if (!hasAsyncTo) {
      if (started || hasFluidValue(prevTo)) {
        anim.values = node.getPayload();
        anim.toValues = hasFluidValue(to22) ? null : goalType == AnimatedString ? [1] : toArray(goal);
      }
      if (anim.immediate != immediate) {
        anim.immediate = immediate;
        if (!immediate && !reset) {
          this._set(prevTo);
        }
      }
      if (started) {
        const { onRest } = anim;
        each(ACTIVE_EVENTS, (type) => mergeActiveFn(this, props, type));
        const result = getFinishedResult(this, checkFinished(this, prevTo));
        flushCalls(this._pendingCalls, result);
        this._pendingCalls.add(resolve);
        if (anim.changed)
          raf.batchedUpdates(() => {
            var _a;
            anim.changed = !reset;
            onRest == null ? void 0 : onRest(result, this);
            if (reset) {
              callProp(defaultProps.onRest, result);
            } else {
              (_a = anim.onStart) == null ? void 0 : _a.call(anim, result, this);
            }
          });
      }
    }
    if (reset) {
      this._set(value);
    }
    if (hasAsyncTo) {
      resolve(runAsync(props.to, props, this._state, this));
    } else if (started) {
      this._start();
    } else if (isAnimating(this) && !hasToChanged) {
      this._pendingCalls.add(resolve);
    } else {
      resolve(getNoopResult(value));
    }
  }
  /** Update the `animation.to` value, which might be a `FluidValue` */
  _focus(value) {
    const anim = this.animation;
    if (value !== anim.to) {
      if (getFluidObservers(this)) {
        this._detach();
      }
      anim.to = value;
      if (getFluidObservers(this)) {
        this._attach();
      }
    }
  }
  _attach() {
    let priority2 = 0;
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      addFluidObserver(to22, this);
      if (isFrameValue(to22)) {
        priority2 = to22.priority + 1;
      }
    }
    this.priority = priority2;
  }
  _detach() {
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      removeFluidObserver(to22, this);
    }
  }
  /**
   * Update the current value from outside the frameloop,
   * and return the `Animated` node.
   */
  _set(arg, idle = true) {
    const value = getFluidValue(arg);
    if (!is.und(value)) {
      const oldNode = getAnimated(this);
      if (!oldNode || !isEqual(value, oldNode.getValue())) {
        const nodeType = getAnimatedType(value);
        if (!oldNode || oldNode.constructor != nodeType) {
          setAnimated(this, nodeType.create(value));
        } else {
          oldNode.setValue(value);
        }
        if (oldNode) {
          raf.batchedUpdates(() => {
            this._onChange(value, idle);
          });
        }
      }
    }
    return getAnimated(this);
  }
  _onStart() {
    const anim = this.animation;
    if (!anim.changed) {
      anim.changed = true;
      sendEvent(
        this,
        "onStart",
        getFinishedResult(this, checkFinished(this, anim.to)),
        this
      );
    }
  }
  _onChange(value, idle) {
    if (!idle) {
      this._onStart();
      callProp(this.animation.onChange, value, this);
    }
    callProp(this.defaultProps.onChange, value, this);
    super._onChange(value, idle);
  }
  // This method resets the animation state (even if already animating) to
  // ensure the latest from/to range is used, and it also ensures this spring
  // is added to the frameloop.
  _start() {
    const anim = this.animation;
    getAnimated(this).reset(getFluidValue(anim.to));
    if (!anim.immediate) {
      anim.fromValues = anim.values.map((node) => node.lastPosition);
    }
    if (!isAnimating(this)) {
      setActiveBit(this, true);
      if (!isPaused(this)) {
        this._resume();
      }
    }
  }
  _resume() {
    if (globals_exports.skipAnimation) {
      this.finish();
    } else {
      frameLoop.start(this);
    }
  }
  /**
   * Exit the frameloop and notify `onRest` listeners.
   *
   * Always wrap `_stop` calls with `batchedUpdates`.
   */
  _stop(goal, cancel) {
    if (isAnimating(this)) {
      setActiveBit(this, false);
      const anim = this.animation;
      each(anim.values, (node) => {
        node.done = true;
      });
      if (anim.toValues) {
        anim.onChange = anim.onPause = anim.onResume = void 0;
      }
      callFluidObservers(this, {
        type: "idle",
        parent: this
      });
      const result = cancel ? getCancelledResult(this.get()) : getFinishedResult(this.get(), checkFinished(this, goal ?? anim.to));
      flushCalls(this._pendingCalls, result);
      if (anim.changed) {
        anim.changed = false;
        sendEvent(this, "onRest", result, this);
      }
    }
  }
};
function checkFinished(target, to22) {
  const goal = computeGoal(to22);
  const value = computeGoal(target.get());
  return isEqual(value, goal);
}
function createLoopUpdate(props, loop2 = props.loop, to22 = props.to) {
  const loopRet = callProp(loop2);
  if (loopRet) {
    const overrides = loopRet !== true && inferTo(loopRet);
    const reverse = (overrides || props).reverse;
    const reset = !overrides || overrides.reset;
    return createUpdate({
      ...props,
      loop: loop2,
      // Avoid updating default props when looping.
      default: false,
      // Never loop the `pause` prop.
      pause: void 0,
      // For the "reverse" prop to loop as expected, the "to" prop
      // must be undefined. The "reverse" prop is ignored when the
      // "to" prop is an array or function.
      to: !reverse || isAsyncTo(to22) ? to22 : void 0,
      // Ignore the "from" prop except on reset.
      from: reset ? props.from : void 0,
      reset,
      // The "loop" prop can return a "useSpring" props object to
      // override any of the original props.
      ...overrides
    });
  }
}
function createUpdate(props) {
  const { to: to22, from } = props = inferTo(props);
  const keys = /* @__PURE__ */ new Set();
  if (is.obj(to22))
    findDefined(to22, keys);
  if (is.obj(from))
    findDefined(from, keys);
  props.keys = keys.size ? Array.from(keys) : null;
  return props;
}
function declareUpdate(props) {
  const update22 = createUpdate(props);
  if (is.und(update22.default)) {
    update22.default = getDefaultProps(update22);
  }
  return update22;
}
function findDefined(values, keys) {
  eachProp(values, (value, key) => value != null && keys.add(key));
}
var ACTIVE_EVENTS = [
  "onStart",
  "onRest",
  "onChange",
  "onPause",
  "onResume"
];
function mergeActiveFn(target, props, type) {
  target.animation[type] = props[type] !== getDefaultProp(props, type) ? resolveProp(props[type], target.key) : void 0;
}
function sendEvent(target, type, ...args) {
  var _a, _b, _c, _d;
  (_b = (_a = target.animation)[type]) == null ? void 0 : _b.call(_a, ...args);
  (_d = (_c = target.defaultProps)[type]) == null ? void 0 : _d.call(_c, ...args);
}
var BATCHED_EVENTS = ["onStart", "onChange", "onRest"];
var nextId2 = 1;
var Controller = class {
  constructor(props, flush3) {
    this.id = nextId2++;
    this.springs = {};
    this.queue = [];
    this._lastAsyncId = 0;
    this._active = /* @__PURE__ */ new Set();
    this._changed = /* @__PURE__ */ new Set();
    this._started = false;
    this._state = {
      paused: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._events = {
      onStart: /* @__PURE__ */ new Map(),
      onChange: /* @__PURE__ */ new Map(),
      onRest: /* @__PURE__ */ new Map()
    };
    this._onFrame = this._onFrame.bind(this);
    if (flush3) {
      this._flush = flush3;
    }
    if (props) {
      this.start({ default: true, ...props });
    }
  }
  /**
   * Equals `true` when no spring values are in the frameloop, and
   * no async animation is currently active.
   */
  get idle() {
    return !this._state.asyncTo && Object.values(this.springs).every((spring) => {
      return spring.idle && !spring.isDelayed && !spring.isPaused;
    });
  }
  get item() {
    return this._item;
  }
  set item(item) {
    this._item = item;
  }
  /** Get the current values of our springs */
  get() {
    const values = {};
    this.each((spring, key) => values[key] = spring.get());
    return values;
  }
  /** Set the current values without animating. */
  set(values) {
    for (const key in values) {
      const value = values[key];
      if (!is.und(value)) {
        this.springs[key].set(value);
      }
    }
  }
  /** Push an update onto the queue of each value. */
  update(props) {
    if (props) {
      this.queue.push(createUpdate(props));
    }
    return this;
  }
  /**
   * Start the queued animations for every spring, and resolve the returned
   * promise once all queued animations have finished or been cancelled.
   *
   * When you pass a queue (instead of nothing), that queue is used instead of
   * the queued animations added with the `update` method, which are left alone.
   */
  start(props) {
    let { queue } = this;
    if (props) {
      queue = toArray(props).map(createUpdate);
    } else {
      this.queue = [];
    }
    if (this._flush) {
      return this._flush(this, queue);
    }
    prepareKeys(this, queue);
    return flushUpdateQueue(this, queue);
  }
  /** @internal */
  stop(arg, keys) {
    if (arg !== !!arg) {
      keys = arg;
    }
    if (keys) {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].stop(!!arg));
    } else {
      stopAsync(this._state, this._lastAsyncId);
      this.each((spring) => spring.stop(!!arg));
    }
    return this;
  }
  /** Freeze the active animation in time */
  pause(keys) {
    if (is.und(keys)) {
      this.start({ pause: true });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].pause());
    }
    return this;
  }
  /** Resume the animation if paused. */
  resume(keys) {
    if (is.und(keys)) {
      this.start({ pause: false });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].resume());
    }
    return this;
  }
  /** Call a function once per spring value */
  each(iterator) {
    eachProp(this.springs, iterator);
  }
  /** @internal Called at the end of every animation frame */
  _onFrame() {
    const { onStart, onChange, onRest } = this._events;
    const active = this._active.size > 0;
    const changed = this._changed.size > 0;
    if (active && !this._started || changed && !this._started) {
      this._started = true;
      flush(onStart, ([onStart2, result]) => {
        result.value = this.get();
        onStart2(result, this, this._item);
      });
    }
    const idle = !active && this._started;
    const values = changed || idle && onRest.size ? this.get() : null;
    if (changed && onChange.size) {
      flush(onChange, ([onChange2, result]) => {
        result.value = values;
        onChange2(result, this, this._item);
      });
    }
    if (idle) {
      this._started = false;
      flush(onRest, ([onRest2, result]) => {
        result.value = values;
        onRest2(result, this, this._item);
      });
    }
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._changed.add(event.parent);
      if (!event.idle) {
        this._active.add(event.parent);
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else
      return;
    raf.onFrame(this._onFrame);
  }
};
function flushUpdateQueue(ctrl, queue) {
  return Promise.all(queue.map((props) => flushUpdate(ctrl, props))).then(
    (results) => getCombinedResult(ctrl, results)
  );
}
async function flushUpdate(ctrl, props, isLoop) {
  const { keys, to: to22, from, loop: loop2, onRest, onResolve } = props;
  const defaults2 = is.obj(props.default) && props.default;
  if (loop2) {
    props.loop = false;
  }
  if (to22 === false)
    props.to = null;
  if (from === false)
    props.from = null;
  const asyncTo = is.arr(to22) || is.fun(to22) ? to22 : void 0;
  if (asyncTo) {
    props.to = void 0;
    props.onRest = void 0;
    if (defaults2) {
      defaults2.onRest = void 0;
    }
  } else {
    each(BATCHED_EVENTS, (key) => {
      const handler = props[key];
      if (is.fun(handler)) {
        const queue = ctrl["_events"][key];
        props[key] = ({ finished, cancelled }) => {
          const result2 = queue.get(handler);
          if (result2) {
            if (!finished)
              result2.finished = false;
            if (cancelled)
              result2.cancelled = true;
          } else {
            queue.set(handler, {
              value: null,
              finished: finished || false,
              cancelled: cancelled || false
            });
          }
        };
        if (defaults2) {
          defaults2[key] = props[key];
        }
      }
    });
  }
  const state = ctrl["_state"];
  if (props.pause === !state.paused) {
    state.paused = props.pause;
    flushCalls(props.pause ? state.pauseQueue : state.resumeQueue);
  } else if (state.paused) {
    props.pause = true;
  }
  const promises = (keys || Object.keys(ctrl.springs)).map(
    (key) => ctrl.springs[key].start(props)
  );
  const cancel = props.cancel === true || getDefaultProp(props, "cancel") === true;
  if (asyncTo || cancel && state.asyncId) {
    promises.push(
      scheduleProps(++ctrl["_lastAsyncId"], {
        props,
        state,
        actions: {
          pause: noop,
          resume: noop,
          start(props2, resolve) {
            if (cancel) {
              stopAsync(state, ctrl["_lastAsyncId"]);
              resolve(getCancelledResult(ctrl));
            } else {
              props2.onRest = onRest;
              resolve(
                runAsync(
                  asyncTo,
                  props2,
                  state,
                  ctrl
                )
              );
            }
          }
        }
      })
    );
  }
  if (state.paused) {
    await new Promise((resume) => {
      state.resumeQueue.add(resume);
    });
  }
  const result = getCombinedResult(ctrl, await Promise.all(promises));
  if (loop2 && result.finished && !(isLoop && result.noop)) {
    const nextProps = createLoopUpdate(props, loop2, to22);
    if (nextProps) {
      prepareKeys(ctrl, [nextProps]);
      return flushUpdate(ctrl, nextProps, true);
    }
  }
  if (onResolve) {
    raf.batchedUpdates(() => onResolve(result, ctrl, ctrl.item));
  }
  return result;
}
function getSprings(ctrl, props) {
  const springs = { ...ctrl.springs };
  if (props) {
    each(toArray(props), (props2) => {
      if (is.und(props2.keys)) {
        props2 = createUpdate(props2);
      }
      if (!is.obj(props2.to)) {
        props2 = { ...props2, to: void 0 };
      }
      prepareSprings(springs, props2, (key) => {
        return createSpring(key);
      });
    });
  }
  setSprings(ctrl, springs);
  return springs;
}
function setSprings(ctrl, springs) {
  eachProp(springs, (spring, key) => {
    if (!ctrl.springs[key]) {
      ctrl.springs[key] = spring;
      addFluidObserver(spring, ctrl);
    }
  });
}
function createSpring(key, observer) {
  const spring = new SpringValue();
  spring.key = key;
  if (observer) {
    addFluidObserver(spring, observer);
  }
  return spring;
}
function prepareSprings(springs, props, create) {
  if (props.keys) {
    each(props.keys, (key) => {
      const spring = springs[key] || (springs[key] = create(key));
      spring["_prepareNode"](props);
    });
  }
}
function prepareKeys(ctrl, queue) {
  each(queue, (props) => {
    prepareSprings(ctrl.springs, props, (key) => {
      return createSpring(key, ctrl);
    });
  });
}
var SpringContext = ({
  children,
  ...props
}) => {
  const inherited = (0, import_react11.useContext)(ctx);
  const pause = props.pause || !!inherited.pause, immediate = props.immediate || !!inherited.immediate;
  props = useMemoOne(() => ({ pause, immediate }), [pause, immediate]);
  const { Provider } = ctx;
  return React2.createElement(Provider, { value: props }, children);
};
var ctx = makeContext(SpringContext, {});
SpringContext.Provider = ctx.Provider;
SpringContext.Consumer = ctx.Consumer;
function makeContext(target, init) {
  Object.assign(target, React2.createContext(init));
  target.Provider._context = target;
  target.Consumer._context = target;
  return target;
}
var SpringRef = () => {
  const current = [];
  const SpringRef2 = function(props) {
    deprecateDirectCall();
    const results = [];
    each(current, (ctrl, i5) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = _getProps(props, ctrl, i5);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef2.current = current;
  SpringRef2.add = function(ctrl) {
    if (!current.includes(ctrl)) {
      current.push(ctrl);
    }
  };
  SpringRef2.delete = function(ctrl) {
    const i5 = current.indexOf(ctrl);
    if (~i5)
      current.splice(i5, 1);
  };
  SpringRef2.pause = function() {
    each(current, (ctrl) => ctrl.pause(...arguments));
    return this;
  };
  SpringRef2.resume = function() {
    each(current, (ctrl) => ctrl.resume(...arguments));
    return this;
  };
  SpringRef2.set = function(values) {
    each(current, (ctrl, i5) => {
      const update22 = is.fun(values) ? values(i5, ctrl) : values;
      if (update22) {
        ctrl.set(update22);
      }
    });
  };
  SpringRef2.start = function(props) {
    const results = [];
    each(current, (ctrl, i5) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = this._getProps(props, ctrl, i5);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef2.stop = function() {
    each(current, (ctrl) => ctrl.stop(...arguments));
    return this;
  };
  SpringRef2.update = function(props) {
    each(current, (ctrl, i5) => ctrl.update(this._getProps(props, ctrl, i5)));
    return this;
  };
  const _getProps = function(arg, ctrl, index) {
    return is.fun(arg) ? arg(index, ctrl) : arg;
  };
  SpringRef2._getProps = _getProps;
  return SpringRef2;
};
function useSprings(length, props, deps) {
  const propsFn = is.fun(props) && props;
  if (propsFn && !deps)
    deps = [];
  const ref = (0, import_react10.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const layoutId = (0, import_react10.useRef)(0);
  const forceUpdate = useForceUpdate();
  const state = (0, import_react10.useMemo)(
    () => ({
      ctrls: [],
      queue: [],
      flush(ctrl, updates2) {
        const springs2 = getSprings(ctrl, updates2);
        const canFlushSync = layoutId.current > 0 && !state.queue.length && !Object.keys(springs2).some((key) => !ctrl.springs[key]);
        return canFlushSync ? flushUpdateQueue(ctrl, updates2) : new Promise((resolve) => {
          setSprings(ctrl, springs2);
          state.queue.push(() => {
            resolve(flushUpdateQueue(ctrl, updates2));
          });
          forceUpdate();
        });
      }
    }),
    []
  );
  const ctrls = (0, import_react10.useRef)([...state.ctrls]);
  const updates = [];
  const prevLength = usePrev(length) || 0;
  (0, import_react10.useMemo)(() => {
    each(ctrls.current.slice(length, prevLength), (ctrl) => {
      detachRefs(ctrl, ref);
      ctrl.stop(true);
    });
    ctrls.current.length = length;
    declareUpdates(prevLength, length);
  }, [length]);
  (0, import_react10.useMemo)(() => {
    declareUpdates(0, Math.min(prevLength, length));
  }, deps);
  function declareUpdates(startIndex, endIndex) {
    for (let i5 = startIndex; i5 < endIndex; i5++) {
      const ctrl = ctrls.current[i5] || (ctrls.current[i5] = new Controller(null, state.flush));
      const update22 = propsFn ? propsFn(i5, ctrl) : props[i5];
      if (update22) {
        updates[i5] = declareUpdate(update22);
      }
    }
  }
  const springs = ctrls.current.map((ctrl, i5) => getSprings(ctrl, updates[i5]));
  const context = (0, import_react10.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    layoutId.current++;
    state.ctrls = ctrls.current;
    const { queue } = state;
    if (queue.length) {
      state.queue = [];
      each(queue, (cb) => cb());
    }
    each(ctrls.current, (ctrl, i5) => {
      ref == null ? void 0 : ref.add(ctrl);
      if (hasContext) {
        ctrl.start({ default: context });
      }
      const update22 = updates[i5];
      if (update22) {
        replaceRef(ctrl, update22.ref);
        if (ctrl.ref) {
          ctrl.queue.push(update22);
        } else {
          ctrl.start(update22);
        }
      }
    });
  });
  useOnce(() => () => {
    each(state.ctrls, (ctrl) => ctrl.stop(true));
  });
  const values = springs.map((x3) => ({ ...x3 }));
  return ref ? [values, ref] : values;
}
function useSpring(props, deps) {
  const isFn = is.fun(props);
  const [[values], ref] = useSprings(
    1,
    isFn ? props : [props],
    isFn ? deps || [] : deps
  );
  return isFn || arguments.length == 2 ? [values, ref] : values;
}
function useTransition(data, props, deps) {
  const propsFn = is.fun(props) && props;
  const {
    reset,
    sort,
    trail = 0,
    expires = true,
    exitBeforeEnter = false,
    onDestroyed,
    ref: propsRef,
    config: propsConfig
  } = propsFn ? propsFn() : props;
  const ref = (0, import_react13.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const items = toArray(data);
  const transitions = [];
  const usedTransitions = (0, import_react13.useRef)(null);
  const prevTransitions = reset ? null : usedTransitions.current;
  useIsomorphicLayoutEffect(() => {
    usedTransitions.current = transitions;
  });
  useOnce(() => {
    each(transitions, (t6) => {
      ref == null ? void 0 : ref.add(t6.ctrl);
      t6.ctrl.ref = ref;
    });
    return () => {
      each(usedTransitions.current, (t6) => {
        if (t6.expired) {
          clearTimeout(t6.expirationId);
        }
        detachRefs(t6.ctrl, ref);
        t6.ctrl.stop(true);
      });
    };
  });
  const keys = getKeys(items, propsFn ? propsFn() : props, prevTransitions);
  const expired = reset && usedTransitions.current || [];
  useIsomorphicLayoutEffect(
    () => each(expired, ({ ctrl, item, key }) => {
      detachRefs(ctrl, ref);
      callProp(onDestroyed, item, key);
    })
  );
  const reused = [];
  if (prevTransitions)
    each(prevTransitions, (t6, i5) => {
      if (t6.expired) {
        clearTimeout(t6.expirationId);
        expired.push(t6);
      } else {
        i5 = reused[i5] = keys.indexOf(t6.key);
        if (~i5)
          transitions[i5] = t6;
      }
    });
  each(items, (item, i5) => {
    if (!transitions[i5]) {
      transitions[i5] = {
        key: keys[i5],
        item,
        phase: "mount",
        ctrl: new Controller()
      };
      transitions[i5].ctrl.item = item;
    }
  });
  if (reused.length) {
    let i5 = -1;
    const { leave } = propsFn ? propsFn() : props;
    each(reused, (keyIndex, prevIndex) => {
      const t6 = prevTransitions[prevIndex];
      if (~keyIndex) {
        i5 = transitions.indexOf(t6);
        transitions[i5] = { ...t6, item: items[keyIndex] };
      } else if (leave) {
        transitions.splice(++i5, 0, t6);
      }
    });
  }
  if (is.fun(sort)) {
    transitions.sort((a4, b4) => sort(a4.item, b4.item));
  }
  let delay = -trail;
  const forceUpdate = useForceUpdate();
  const defaultProps = getDefaultProps(props);
  const changes = /* @__PURE__ */ new Map();
  const exitingTransitions = (0, import_react13.useRef)(/* @__PURE__ */ new Map());
  const forceChange = (0, import_react13.useRef)(false);
  each(transitions, (t6, i5) => {
    const key = t6.key;
    const prevPhase = t6.phase;
    const p4 = propsFn ? propsFn() : props;
    let to22;
    let phase;
    const propsDelay = callProp(p4.delay || 0, key);
    if (prevPhase == "mount") {
      to22 = p4.enter;
      phase = "enter";
    } else {
      const isLeave = keys.indexOf(key) < 0;
      if (prevPhase != "leave") {
        if (isLeave) {
          to22 = p4.leave;
          phase = "leave";
        } else if (to22 = p4.update) {
          phase = "update";
        } else
          return;
      } else if (!isLeave) {
        to22 = p4.enter;
        phase = "enter";
      } else
        return;
    }
    to22 = callProp(to22, t6.item, i5);
    to22 = is.obj(to22) ? inferTo(to22) : { to: to22 };
    if (!to22.config) {
      const config2 = propsConfig || defaultProps.config;
      to22.config = callProp(config2, t6.item, i5, phase);
    }
    delay += trail;
    const payload = {
      ...defaultProps,
      // we need to add our props.delay value you here.
      delay: propsDelay + delay,
      ref: propsRef,
      immediate: p4.immediate,
      // This prevents implied resets.
      reset: false,
      // Merge any phase-specific props.
      ...to22
    };
    if (phase == "enter" && is.und(payload.from)) {
      const p22 = propsFn ? propsFn() : props;
      const from = is.und(p22.initial) || prevTransitions ? p22.from : p22.initial;
      payload.from = callProp(from, t6.item, i5);
    }
    const { onResolve } = payload;
    payload.onResolve = (result) => {
      callProp(onResolve, result);
      const transitions2 = usedTransitions.current;
      const t22 = transitions2.find((t32) => t32.key === key);
      if (!t22)
        return;
      if (result.cancelled && t22.phase != "update") {
        return;
      }
      if (t22.ctrl.idle) {
        const idle = transitions2.every((t32) => t32.ctrl.idle);
        if (t22.phase == "leave") {
          const expiry = callProp(expires, t22.item);
          if (expiry !== false) {
            const expiryMs = expiry === true ? 0 : expiry;
            t22.expired = true;
            if (!idle && expiryMs > 0) {
              if (expiryMs <= 2147483647)
                t22.expirationId = setTimeout(forceUpdate, expiryMs);
              return;
            }
          }
        }
        if (idle && transitions2.some((t32) => t32.expired)) {
          exitingTransitions.current.delete(t22);
          if (exitBeforeEnter) {
            forceChange.current = true;
          }
          forceUpdate();
        }
      }
    };
    const springs = getSprings(t6.ctrl, payload);
    if (phase === "leave" && exitBeforeEnter) {
      exitingTransitions.current.set(t6, { phase, springs, payload });
    } else {
      changes.set(t6, { phase, springs, payload });
    }
  });
  const context = (0, import_react13.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    if (hasContext) {
      each(transitions, (t6) => {
        t6.ctrl.start({ default: context });
      });
    }
  }, [context]);
  each(changes, (_2, t6) => {
    if (exitingTransitions.current.size) {
      const ind = transitions.findIndex((state) => state.key === t6.key);
      transitions.splice(ind, 1);
    }
  });
  useIsomorphicLayoutEffect(
    () => {
      each(
        exitingTransitions.current.size ? exitingTransitions.current : changes,
        ({ phase, payload }, t6) => {
          const { ctrl } = t6;
          t6.phase = phase;
          ref == null ? void 0 : ref.add(ctrl);
          if (hasContext && phase == "enter") {
            ctrl.start({ default: context });
          }
          if (payload) {
            replaceRef(ctrl, payload.ref);
            if ((ctrl.ref || ref) && !forceChange.current) {
              ctrl.update(payload);
            } else {
              ctrl.start(payload);
              if (forceChange.current) {
                forceChange.current = false;
              }
            }
          }
        }
      );
    },
    reset ? void 0 : deps
  );
  const renderTransitions = (render) => React22.createElement(React22.Fragment, null, transitions.map((t6, i5) => {
    const { springs } = changes.get(t6) || t6.ctrl;
    const elem = render({ ...springs }, t6.item, t6, i5);
    return elem && elem.type ? React22.createElement(
      elem.type,
      {
        ...elem.props,
        key: is.str(t6.key) || is.num(t6.key) ? t6.key : t6.ctrl.id,
        ref: elem.ref
      }
    ) : elem;
  }));
  return ref ? [renderTransitions, ref] : renderTransitions;
}
var nextKey = 1;
function getKeys(items, { key, keys = key }, prevTransitions) {
  if (keys === null) {
    const reused = /* @__PURE__ */ new Set();
    return items.map((item) => {
      const t6 = prevTransitions && prevTransitions.find(
        (t22) => t22.item === item && t22.phase !== "leave" && !reused.has(t22)
      );
      if (t6) {
        reused.add(t6);
        return t6.key;
      }
      return nextKey++;
    });
  }
  return is.und(keys) ? items : is.fun(keys) ? items.map(keys) : toArray(keys);
}
var Interpolation = class extends FrameValue {
  constructor(source, args) {
    super();
    this.source = source;
    this.idle = true;
    this._active = /* @__PURE__ */ new Set();
    this.calc = createInterpolator(...args);
    const value = this._get();
    const nodeType = getAnimatedType(value);
    setAnimated(this, nodeType.create(value));
  }
  advance(_dt) {
    const value = this._get();
    const oldValue = this.get();
    if (!isEqual(value, oldValue)) {
      getAnimated(this).setValue(value);
      this._onChange(value, this.idle);
    }
    if (!this.idle && checkIdle(this._active)) {
      becomeIdle(this);
    }
  }
  _get() {
    const inputs = is.arr(this.source) ? this.source.map(getFluidValue) : toArray(getFluidValue(this.source));
    return this.calc(...inputs);
  }
  _start() {
    if (this.idle && !checkIdle(this._active)) {
      this.idle = false;
      each(getPayload(this), (node) => {
        node.done = false;
      });
      if (globals_exports.skipAnimation) {
        raf.batchedUpdates(() => this.advance());
        becomeIdle(this);
      } else {
        frameLoop.start(this);
      }
    }
  }
  // Observe our sources only when we're observed.
  _attach() {
    let priority2 = 1;
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        addFluidObserver(source, this);
      }
      if (isFrameValue(source)) {
        if (!source.idle) {
          this._active.add(source);
        }
        priority2 = Math.max(priority2, source.priority + 1);
      }
    });
    this.priority = priority2;
    this._start();
  }
  // Stop observing our sources once we have no observers.
  _detach() {
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        removeFluidObserver(source, this);
      }
    });
    this._active.clear();
    becomeIdle(this);
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      if (event.idle) {
        this.advance();
      } else {
        this._active.add(event.parent);
        this._start();
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else if (event.type == "priority") {
      this.priority = toArray(this.source).reduce(
        (highest, parent) => Math.max(highest, (isFrameValue(parent) ? parent.priority : 0) + 1),
        0
      );
    }
  }
};
function isIdle(source) {
  return source.idle !== false;
}
function checkIdle(active) {
  return !active.size || Array.from(active).every(isIdle);
}
function becomeIdle(self) {
  if (!self.idle) {
    self.idle = true;
    each(getPayload(self), (node) => {
      node.done = true;
    });
    callFluidObservers(self, {
      type: "idle",
      parent: self
    });
  }
}
var to2 = (source, ...args) => new Interpolation(source, args);
globals_exports.assign({
  createStringInterpolator: createStringInterpolator2,
  to: (source, args) => new Interpolation(source, args)
});
var update2 = frameLoop.advance;

// node_modules/@react-spring/web/dist/react-spring_web.modern.mjs
var import_react_dom = __toESM(require_react_dom(), 1);
var isCustomPropRE = /^--/;
function dangerousStyleValue(name, value) {
  if (value == null || typeof value === "boolean" || value === "")
    return "";
  if (typeof value === "number" && value !== 0 && !isCustomPropRE.test(name) && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]))
    return value + "px";
  return ("" + value).trim();
}
var attributeCache = {};
function applyAnimatedValues(instance, props) {
  if (!instance.nodeType || !instance.setAttribute) {
    return false;
  }
  const isFilterElement = instance.nodeName === "filter" || instance.parentNode && instance.parentNode.nodeName === "filter";
  const {
    className,
    style,
    children,
    scrollTop,
    scrollLeft,
    viewBox,
    ...attributes
  } = props;
  const values = Object.values(attributes);
  const names = Object.keys(attributes).map(
    (name) => isFilterElement || instance.hasAttribute(name) ? name : attributeCache[name] || (attributeCache[name] = name.replace(
      /([A-Z])/g,
      // Attributes are written in dash case
      (n6) => "-" + n6.toLowerCase()
    ))
  );
  if (children !== void 0) {
    instance.textContent = children;
  }
  for (const name in style) {
    if (style.hasOwnProperty(name)) {
      const value = dangerousStyleValue(name, style[name]);
      if (isCustomPropRE.test(name)) {
        instance.style.setProperty(name, value);
      } else {
        instance.style[name] = value;
      }
    }
  }
  names.forEach((name, i5) => {
    instance.setAttribute(name, values[i5]);
  });
  if (className !== void 0) {
    instance.className = className;
  }
  if (scrollTop !== void 0) {
    instance.scrollTop = scrollTop;
  }
  if (scrollLeft !== void 0) {
    instance.scrollLeft = scrollLeft;
  }
  if (viewBox !== void 0) {
    instance.setAttribute("viewBox", viewBox);
  }
}
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
var prefixKey = (prefix2, key) => prefix2 + key.charAt(0).toUpperCase() + key.substring(1);
var prefixes = ["Webkit", "Ms", "Moz", "O"];
isUnitlessNumber = Object.keys(isUnitlessNumber).reduce((acc, prop) => {
  prefixes.forEach((prefix2) => acc[prefixKey(prefix2, prop)] = acc[prop]);
  return acc;
}, isUnitlessNumber);
var domTransforms = /^(matrix|translate|scale|rotate|skew)/;
var pxTransforms = /^(translate)/;
var degTransforms = /^(rotate|skew)/;
var addUnit = (value, unit) => is.num(value) && value !== 0 ? value + unit : value;
var isValueIdentity = (value, id) => is.arr(value) ? value.every((v5) => isValueIdentity(v5, id)) : is.num(value) ? value === id : parseFloat(value) === id;
var AnimatedStyle = class extends AnimatedObject {
  constructor({ x: x3, y: y3, z: z4, ...style }) {
    const inputs = [];
    const transforms = [];
    if (x3 || y3 || z4) {
      inputs.push([x3 || 0, y3 || 0, z4 || 0]);
      transforms.push((xyz) => [
        `translate3d(${xyz.map((v5) => addUnit(v5, "px")).join(",")})`,
        // prettier-ignore
        isValueIdentity(xyz, 0)
      ]);
    }
    eachProp(style, (value, key) => {
      if (key === "transform") {
        inputs.push([value || ""]);
        transforms.push((transform) => [transform, transform === ""]);
      } else if (domTransforms.test(key)) {
        delete style[key];
        if (is.und(value))
          return;
        const unit = pxTransforms.test(key) ? "px" : degTransforms.test(key) ? "deg" : "";
        inputs.push(toArray(value));
        transforms.push(
          key === "rotate3d" ? ([x22, y22, z22, deg]) => [
            `rotate3d(${x22},${y22},${z22},${addUnit(deg, unit)})`,
            isValueIdentity(deg, 0)
          ] : (input) => [
            `${key}(${input.map((v5) => addUnit(v5, unit)).join(",")})`,
            isValueIdentity(input, key.startsWith("scale") ? 1 : 0)
          ]
        );
      }
    });
    if (inputs.length) {
      style.transform = new FluidTransform(inputs, transforms);
    }
    super(style);
  }
};
var FluidTransform = class extends FluidValue {
  constructor(inputs, transforms) {
    super();
    this.inputs = inputs;
    this.transforms = transforms;
    this._value = null;
  }
  get() {
    return this._value || (this._value = this._get());
  }
  _get() {
    let transform = "";
    let identity = true;
    each(this.inputs, (input, i5) => {
      const arg1 = getFluidValue(input[0]);
      const [t6, id] = this.transforms[i5](
        is.arr(arg1) ? arg1 : input.map(getFluidValue)
      );
      transform += " " + t6;
      identity = identity && id;
    });
    return identity ? "none" : transform;
  }
  // Start observing our inputs once we have an observer.
  observerAdded(count) {
    if (count == 1)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && addFluidObserver(value, this)
        )
      );
  }
  // Stop observing our inputs once we have no observers.
  observerRemoved(count) {
    if (count == 0)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && removeFluidObserver(value, this)
        )
      );
  }
  eventObserved(event) {
    if (event.type == "change") {
      this._value = null;
    }
    callFluidObservers(this, event);
  }
};
var primitives = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
globals_exports.assign({
  batchedUpdates: import_react_dom.unstable_batchedUpdates,
  createStringInterpolator: createStringInterpolator2,
  colors: colors2
});
var host = createHost(primitives, {
  applyAnimatedValues,
  createAnimatedStyle: (style) => new AnimatedStyle(style),
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  getComponentProps: ({ scrollTop, scrollLeft, ...props }) => props
});
var animated = host.animated;

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_react15 = __toESM(require_react());
var import_prop_types = __toESM(require_prop_types());
var import_merge = __toESM(require_merge());
var import_get = __toESM(require_get());
var import_set = __toESM(require_set());
var import_isString = __toESM(require_isString());
var import_jsx_runtime = __toESM(require_jsx_runtime());
var import_last = __toESM(require_last());
var import_isArray = __toESM(require_isArray());

// node_modules/d3-scale-chromatic/src/colors.js
function colors_default(specifier) {
  var n6 = specifier.length / 6 | 0, colors3 = new Array(n6), i5 = 0;
  while (i5 < n6) colors3[i5] = "#" + specifier.slice(i5 * 6, ++i5 * 6);
  return colors3;
}

// node_modules/d3-scale-chromatic/src/categorical/category10.js
var category10_default = colors_default("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf");

// node_modules/d3-scale-chromatic/src/categorical/Accent.js
var Accent_default = colors_default("7fc97fbeaed4fdc086ffff99386cb0f0027fbf5b17666666");

// node_modules/d3-scale-chromatic/src/categorical/Dark2.js
var Dark2_default = colors_default("1b9e77d95f027570b3e7298a66a61ee6ab02a6761d666666");

// node_modules/d3-scale-chromatic/src/categorical/observable10.js
var observable10_default = colors_default("4269d0efb118ff725c6cc5b03ca951ff8ab7a463f297bbf59c6b4e9498a0");

// node_modules/d3-scale-chromatic/src/categorical/Paired.js
var Paired_default = colors_default("a6cee31f78b4b2df8a33a02cfb9a99e31a1cfdbf6fff7f00cab2d66a3d9affff99b15928");

// node_modules/d3-scale-chromatic/src/categorical/Pastel1.js
var Pastel1_default = colors_default("fbb4aeb3cde3ccebc5decbe4fed9a6ffffcce5d8bdfddaecf2f2f2");

// node_modules/d3-scale-chromatic/src/categorical/Pastel2.js
var Pastel2_default = colors_default("b3e2cdfdcdaccbd5e8f4cae4e6f5c9fff2aef1e2cccccccc");

// node_modules/d3-scale-chromatic/src/categorical/Set1.js
var Set1_default = colors_default("e41a1c377eb84daf4a984ea3ff7f00ffff33a65628f781bf999999");

// node_modules/d3-scale-chromatic/src/categorical/Set2.js
var Set2_default = colors_default("66c2a5fc8d628da0cbe78ac3a6d854ffd92fe5c494b3b3b3");

// node_modules/d3-scale-chromatic/src/categorical/Set3.js
var Set3_default = colors_default("8dd3c7ffffb3bebadafb807280b1d3fdb462b3de69fccde5d9d9d9bc80bdccebc5ffed6f");

// node_modules/d3-scale-chromatic/src/categorical/Tableau10.js
var Tableau10_default = colors_default("4e79a7f28e2ce1575976b7b259a14fedc949af7aa1ff9da79c755fbab0ab");

// node_modules/d3-scale-chromatic/src/ramp.js
var ramp_default = (scheme28) => rgbBasis(scheme28[scheme28.length - 1]);

// node_modules/d3-scale-chromatic/src/diverging/BrBG.js
var scheme = new Array(3).concat(
  "d8b365f5f5f55ab4ac",
  "a6611adfc27d80cdc1018571",
  "a6611adfc27df5f5f580cdc1018571",
  "8c510ad8b365f6e8c3c7eae55ab4ac01665e",
  "8c510ad8b365f6e8c3f5f5f5c7eae55ab4ac01665e",
  "8c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e",
  "8c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e",
  "5430058c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e003c30",
  "5430058c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e003c30"
).map(colors_default);
var BrBG_default = ramp_default(scheme);

// node_modules/d3-scale-chromatic/src/diverging/PRGn.js
var scheme2 = new Array(3).concat(
  "af8dc3f7f7f77fbf7b",
  "7b3294c2a5cfa6dba0008837",
  "7b3294c2a5cff7f7f7a6dba0008837",
  "762a83af8dc3e7d4e8d9f0d37fbf7b1b7837",
  "762a83af8dc3e7d4e8f7f7f7d9f0d37fbf7b1b7837",
  "762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b7837",
  "762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b7837",
  "40004b762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b783700441b",
  "40004b762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b783700441b"
).map(colors_default);
var PRGn_default = ramp_default(scheme2);

// node_modules/d3-scale-chromatic/src/diverging/PiYG.js
var scheme3 = new Array(3).concat(
  "e9a3c9f7f7f7a1d76a",
  "d01c8bf1b6dab8e1864dac26",
  "d01c8bf1b6daf7f7f7b8e1864dac26",
  "c51b7de9a3c9fde0efe6f5d0a1d76a4d9221",
  "c51b7de9a3c9fde0eff7f7f7e6f5d0a1d76a4d9221",
  "c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221",
  "c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221",
  "8e0152c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221276419",
  "8e0152c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221276419"
).map(colors_default);
var PiYG_default = ramp_default(scheme3);

// node_modules/d3-scale-chromatic/src/diverging/PuOr.js
var scheme4 = new Array(3).concat(
  "998ec3f7f7f7f1a340",
  "5e3c99b2abd2fdb863e66101",
  "5e3c99b2abd2f7f7f7fdb863e66101",
  "542788998ec3d8daebfee0b6f1a340b35806",
  "542788998ec3d8daebf7f7f7fee0b6f1a340b35806",
  "5427888073acb2abd2d8daebfee0b6fdb863e08214b35806",
  "5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b35806",
  "2d004b5427888073acb2abd2d8daebfee0b6fdb863e08214b358067f3b08",
  "2d004b5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b358067f3b08"
).map(colors_default);
var PuOr_default = ramp_default(scheme4);

// node_modules/d3-scale-chromatic/src/diverging/RdBu.js
var scheme5 = new Array(3).concat(
  "ef8a62f7f7f767a9cf",
  "ca0020f4a58292c5de0571b0",
  "ca0020f4a582f7f7f792c5de0571b0",
  "b2182bef8a62fddbc7d1e5f067a9cf2166ac",
  "b2182bef8a62fddbc7f7f7f7d1e5f067a9cf2166ac",
  "b2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac",
  "b2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac",
  "67001fb2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac053061",
  "67001fb2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac053061"
).map(colors_default);
var RdBu_default = ramp_default(scheme5);

// node_modules/d3-scale-chromatic/src/diverging/RdGy.js
var scheme6 = new Array(3).concat(
  "ef8a62ffffff999999",
  "ca0020f4a582bababa404040",
  "ca0020f4a582ffffffbababa404040",
  "b2182bef8a62fddbc7e0e0e09999994d4d4d",
  "b2182bef8a62fddbc7ffffffe0e0e09999994d4d4d",
  "b2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d",
  "b2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d",
  "67001fb2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d1a1a1a",
  "67001fb2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d1a1a1a"
).map(colors_default);
var RdGy_default = ramp_default(scheme6);

// node_modules/d3-scale-chromatic/src/diverging/RdYlBu.js
var scheme7 = new Array(3).concat(
  "fc8d59ffffbf91bfdb",
  "d7191cfdae61abd9e92c7bb6",
  "d7191cfdae61ffffbfabd9e92c7bb6",
  "d73027fc8d59fee090e0f3f891bfdb4575b4",
  "d73027fc8d59fee090ffffbfe0f3f891bfdb4575b4",
  "d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4",
  "d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4",
  "a50026d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4313695",
  "a50026d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4313695"
).map(colors_default);
var RdYlBu_default = ramp_default(scheme7);

// node_modules/d3-scale-chromatic/src/diverging/RdYlGn.js
var scheme8 = new Array(3).concat(
  "fc8d59ffffbf91cf60",
  "d7191cfdae61a6d96a1a9641",
  "d7191cfdae61ffffbfa6d96a1a9641",
  "d73027fc8d59fee08bd9ef8b91cf601a9850",
  "d73027fc8d59fee08bffffbfd9ef8b91cf601a9850",
  "d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850",
  "d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850",
  "a50026d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850006837",
  "a50026d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850006837"
).map(colors_default);
var RdYlGn_default = ramp_default(scheme8);

// node_modules/d3-scale-chromatic/src/diverging/Spectral.js
var scheme9 = new Array(3).concat(
  "fc8d59ffffbf99d594",
  "d7191cfdae61abdda42b83ba",
  "d7191cfdae61ffffbfabdda42b83ba",
  "d53e4ffc8d59fee08be6f59899d5943288bd",
  "d53e4ffc8d59fee08bffffbfe6f59899d5943288bd",
  "d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd",
  "d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd",
  "9e0142d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd5e4fa2",
  "9e0142d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd5e4fa2"
).map(colors_default);
var Spectral_default = ramp_default(scheme9);

// node_modules/d3-scale-chromatic/src/sequential-multi/BuGn.js
var scheme10 = new Array(3).concat(
  "e5f5f999d8c92ca25f",
  "edf8fbb2e2e266c2a4238b45",
  "edf8fbb2e2e266c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45006d2c00441b"
).map(colors_default);
var BuGn_default = ramp_default(scheme10);

// node_modules/d3-scale-chromatic/src/sequential-multi/BuPu.js
var scheme11 = new Array(3).concat(
  "e0ecf49ebcda8856a7",
  "edf8fbb3cde38c96c688419d",
  "edf8fbb3cde38c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d810f7c4d004b"
).map(colors_default);
var BuPu_default = ramp_default(scheme11);

// node_modules/d3-scale-chromatic/src/sequential-multi/GnBu.js
var scheme12 = new Array(3).concat(
  "e0f3dba8ddb543a2ca",
  "f0f9e8bae4bc7bccc42b8cbe",
  "f0f9e8bae4bc7bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe0868ac084081"
).map(colors_default);
var GnBu_default = ramp_default(scheme12);

// node_modules/d3-scale-chromatic/src/sequential-multi/OrRd.js
var scheme13 = new Array(3).concat(
  "fee8c8fdbb84e34a33",
  "fef0d9fdcc8afc8d59d7301f",
  "fef0d9fdcc8afc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301fb300007f0000"
).map(colors_default);
var OrRd_default = ramp_default(scheme13);

// node_modules/d3-scale-chromatic/src/sequential-multi/PuBuGn.js
var scheme14 = new Array(3).concat(
  "ece2f0a6bddb1c9099",
  "f6eff7bdc9e167a9cf02818a",
  "f6eff7bdc9e167a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016c59014636"
).map(colors_default);
var PuBuGn_default = ramp_default(scheme14);

// node_modules/d3-scale-chromatic/src/sequential-multi/PuBu.js
var scheme15 = new Array(3).concat(
  "ece7f2a6bddb2b8cbe",
  "f1eef6bdc9e174a9cf0570b0",
  "f1eef6bdc9e174a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0045a8d023858"
).map(colors_default);
var PuBu_default = ramp_default(scheme15);

// node_modules/d3-scale-chromatic/src/sequential-multi/PuRd.js
var scheme16 = new Array(3).concat(
  "e7e1efc994c7dd1c77",
  "f1eef6d7b5d8df65b0ce1256",
  "f1eef6d7b5d8df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125698004367001f"
).map(colors_default);
var PuRd_default = ramp_default(scheme16);

// node_modules/d3-scale-chromatic/src/sequential-multi/RdPu.js
var scheme17 = new Array(3).concat(
  "fde0ddfa9fb5c51b8a",
  "feebe2fbb4b9f768a1ae017e",
  "feebe2fbb4b9f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a017749006a"
).map(colors_default);
var RdPu_default = ramp_default(scheme17);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlGnBu.js
var scheme18 = new Array(3).concat(
  "edf8b17fcdbb2c7fb8",
  "ffffcca1dab441b6c4225ea8",
  "ffffcca1dab441b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea8253494081d58"
).map(colors_default);
var YlGnBu_default = ramp_default(scheme18);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlGn.js
var scheme19 = new Array(3).concat(
  "f7fcb9addd8e31a354",
  "ffffccc2e69978c679238443",
  "ffffccc2e69978c67931a354006837",
  "ffffccd9f0a3addd8e78c67931a354006837",
  "ffffccd9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443006837004529"
).map(colors_default);
var YlGn_default = ramp_default(scheme19);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlOrBr.js
var scheme20 = new Array(3).concat(
  "fff7bcfec44fd95f0e",
  "ffffd4fed98efe9929cc4c02",
  "ffffd4fed98efe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c02993404662506"
).map(colors_default);
var YlOrBr_default = ramp_default(scheme20);

// node_modules/d3-scale-chromatic/src/sequential-multi/YlOrRd.js
var scheme21 = new Array(3).concat(
  "ffeda0feb24cf03b20",
  "ffffb2fecc5cfd8d3ce31a1c",
  "ffffb2fecc5cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cbd0026800026"
).map(colors_default);
var YlOrRd_default = ramp_default(scheme21);

// node_modules/d3-scale-chromatic/src/sequential-single/Blues.js
var scheme22 = new Array(3).concat(
  "deebf79ecae13182bd",
  "eff3ffbdd7e76baed62171b5",
  "eff3ffbdd7e76baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b508519c08306b"
).map(colors_default);
var Blues_default = ramp_default(scheme22);

// node_modules/d3-scale-chromatic/src/sequential-single/Greens.js
var scheme23 = new Array(3).concat(
  "e5f5e0a1d99b31a354",
  "edf8e9bae4b374c476238b45",
  "edf8e9bae4b374c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45006d2c00441b"
).map(colors_default);
var Greens_default = ramp_default(scheme23);

// node_modules/d3-scale-chromatic/src/sequential-single/Greys.js
var scheme24 = new Array(3).concat(
  "f0f0f0bdbdbd636363",
  "f7f7f7cccccc969696525252",
  "f7f7f7cccccc969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525000000"
).map(colors_default);
var Greys_default = ramp_default(scheme24);

// node_modules/d3-scale-chromatic/src/sequential-single/Purples.js
var scheme25 = new Array(3).concat(
  "efedf5bcbddc756bb1",
  "f2f0f7cbc9e29e9ac86a51a3",
  "f2f0f7cbc9e29e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a354278f3f007d"
).map(colors_default);
var Purples_default = ramp_default(scheme25);

// node_modules/d3-scale-chromatic/src/sequential-single/Reds.js
var scheme26 = new Array(3).concat(
  "fee0d2fc9272de2d26",
  "fee5d9fcae91fb6a4acb181d",
  "fee5d9fcae91fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181da50f1567000d"
).map(colors_default);
var Reds_default = ramp_default(scheme26);

// node_modules/d3-scale-chromatic/src/sequential-single/Oranges.js
var scheme27 = new Array(3).concat(
  "fee6cefdae6be6550d",
  "feeddefdbe85fd8d3cd94701",
  "feeddefdbe85fd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d94801a636037f2704"
).map(colors_default);
var Oranges_default = ramp_default(scheme27);

// node_modules/d3-scale-chromatic/src/sequential-multi/cividis.js
function cividis_default(t6) {
  t6 = Math.max(0, Math.min(1, t6));
  return "rgb(" + Math.max(0, Math.min(255, Math.round(-4.54 - t6 * (35.34 - t6 * (2381.73 - t6 * (6402.7 - t6 * (7024.72 - t6 * 2710.57))))))) + ", " + Math.max(0, Math.min(255, Math.round(32.49 + t6 * (170.73 + t6 * (52.82 - t6 * (131.46 - t6 * (176.58 - t6 * 67.37))))))) + ", " + Math.max(0, Math.min(255, Math.round(81.24 + t6 * (442.36 - t6 * (2482.43 - t6 * (6167.24 - t6 * (6614.94 - t6 * 2475.67))))))) + ")";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/cubehelix.js
var cubehelix_default = cubehelixLong(cubehelix(300, 0.5, 0), cubehelix(-240, 0.5, 1));

// node_modules/d3-scale-chromatic/src/sequential-multi/rainbow.js
var warm = cubehelixLong(cubehelix(-100, 0.75, 0.35), cubehelix(80, 1.5, 0.8));
var cool = cubehelixLong(cubehelix(260, 0.75, 0.35), cubehelix(80, 1.5, 0.8));
var c = cubehelix();
function rainbow_default(t6) {
  if (t6 < 0 || t6 > 1) t6 -= Math.floor(t6);
  var ts2 = Math.abs(t6 - 0.5);
  c.h = 360 * t6 - 100;
  c.s = 1.5 - 1.5 * ts2;
  c.l = 0.8 - 0.9 * ts2;
  return c + "";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/sinebow.js
var c6 = rgb();
var pi_1_3 = Math.PI / 3;
var pi_2_3 = Math.PI * 2 / 3;
function sinebow_default(t6) {
  var x3;
  t6 = (0.5 - t6) * Math.PI;
  c6.r = 255 * (x3 = Math.sin(t6)) * x3;
  c6.g = 255 * (x3 = Math.sin(t6 + pi_1_3)) * x3;
  c6.b = 255 * (x3 = Math.sin(t6 + pi_2_3)) * x3;
  return c6 + "";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/turbo.js
function turbo_default(t6) {
  t6 = Math.max(0, Math.min(1, t6));
  return "rgb(" + Math.max(0, Math.min(255, Math.round(34.61 + t6 * (1172.33 - t6 * (10793.56 - t6 * (33300.12 - t6 * (38394.49 - t6 * 14825.05))))))) + ", " + Math.max(0, Math.min(255, Math.round(23.31 + t6 * (557.33 + t6 * (1225.33 - t6 * (3574.96 - t6 * (1073.77 + t6 * 707.56))))))) + ", " + Math.max(0, Math.min(255, Math.round(27.2 + t6 * (3211.1 - t6 * (15327.97 - t6 * (27814 - t6 * (22569.18 - t6 * 6838.66))))))) + ")";
}

// node_modules/d3-scale-chromatic/src/sequential-multi/viridis.js
function ramp(range) {
  var n6 = range.length;
  return function(t6) {
    return range[Math.max(0, Math.min(n6 - 1, Math.floor(t6 * n6)))];
  };
}
var viridis_default = ramp(colors_default(""));
var magma = ramp(colors_default(""));
var inferno = ramp(colors_default(""));
var plasma = ramp(colors_default(""));

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_isFunction = __toESM(require_isFunction());
var import_without = __toESM(require_without());
var import_isPlainObject = __toESM(require_isPlainObject());
var import_pick = __toESM(require_pick());
var import_isEqual = __toESM(require_isEqual());
var Pr = { background: "transparent", text: { fontFamily: "sans-serif", fontSize: 11, fill: "#333333", outlineWidth: 0, outlineColor: "transparent", outlineOpacity: 1 }, axis: { domain: { line: { stroke: "transparent", strokeWidth: 1 } }, ticks: { line: { stroke: "#777777", strokeWidth: 1 }, text: {} }, legend: { text: { fontSize: 12 } } }, grid: { line: { stroke: "#dddddd", strokeWidth: 1 } }, legends: { hidden: { symbol: { fill: "#333333", opacity: 0.6 }, text: { fill: "#333333", opacity: 0.6 } }, text: {}, ticks: { line: { stroke: "#777777", strokeWidth: 1 }, text: { fontSize: 10 } }, title: { text: {} } }, labels: { text: {} }, markers: { lineColor: "#000000", lineStrokeWidth: 1, text: {} }, dots: { text: {} }, tooltip: { container: { background: "white", color: "inherit", fontSize: "inherit", borderRadius: "2px", boxShadow: "0 1px 2px rgba(0, 0, 0, 0.25)", padding: "5px 9px" }, basic: { whiteSpace: "pre", display: "flex", alignItems: "center" }, chip: { marginRight: 7 }, table: {}, tableCell: { padding: "3px 5px" }, tableCellValue: { fontWeight: "bold" } }, crosshair: { line: { stroke: "#000000", strokeWidth: 1, strokeOpacity: 0.75, strokeDasharray: "6 6" } }, annotations: { text: { fontSize: 13, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, link: { stroke: "#000000", strokeWidth: 1, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, outline: { fill: "none", stroke: "#000000", strokeWidth: 2, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, symbol: { fill: "#000000", outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 } } };
function jr() {
  return jr = Object.assign ? Object.assign.bind() : function(e6) {
    for (var r6 = 1; r6 < arguments.length; r6++) {
      var t6 = arguments[r6];
      for (var n6 in t6) Object.prototype.hasOwnProperty.call(t6, n6) && (e6[n6] = t6[n6]);
    }
    return e6;
  }, jr.apply(this, arguments);
}
function Br(e6, r6) {
  if (null == e6) return {};
  var t6, n6, i5 = {}, o4 = Object.keys(e6);
  for (n6 = 0; n6 < o4.length; n6++) t6 = o4[n6], r6.indexOf(t6) >= 0 || (i5[t6] = e6[t6]);
  return i5;
}
var Gr = ["axis.ticks.text", "axis.legend.text", "legends.title.text", "legends.text", "legends.ticks.text", "legends.title.text", "labels.text", "dots.text", "markers.text", "annotations.text"];
var Lr = function(e6, r6) {
  return jr({}, r6, e6);
};
var Ir = function(e6, r6) {
  var t6 = (0, import_merge.default)({}, e6, r6);
  return Gr.forEach(function(e7) {
    (0, import_set.default)(t6, e7, Lr((0, import_get.default)(t6, e7), t6.text));
  }), t6;
};
var Yr = (0, import_react15.createContext)();
var Ar = function(e6) {
  var t6 = e6.children, n6 = e6.animate, i5 = void 0 === n6 || n6, o4 = e6.config, l4 = void 0 === o4 ? "default" : o4, a4 = (0, import_react15.useMemo)(function() {
    var e7 = (0, import_isString.default)(l4) ? config[l4] : l4;
    return { animate: i5, config: e7 };
  }, [i5, l4]);
  return (0, import_jsx_runtime.jsx)(Yr.Provider, { value: a4, children: t6 });
};
var Er = { animate: import_prop_types.default.bool, motionConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(Object.keys(config)), import_prop_types.default.shape({ mass: import_prop_types.default.number, tension: import_prop_types.default.number, friction: import_prop_types.default.number, clamp: import_prop_types.default.bool, precision: import_prop_types.default.number, velocity: import_prop_types.default.number, duration: import_prop_types.default.number, easing: import_prop_types.default.func })]) };
Ar.propTypes = { children: import_prop_types.default.node.isRequired, animate: Er.animate, config: Er.motionConfig };
var Ur = function() {
  return (0, import_react15.useContext)(Yr);
};
var Fr = function(e6) {
  var t6 = Ur(), o4 = t6.animate, l4 = t6.config, a4 = function(e7) {
    var r6 = (0, import_react15.useRef)();
    return (0, import_react15.useEffect)(function() {
      r6.current = e7;
    }, [e7]), r6.current;
  }(e6), d4 = (0, import_react15.useMemo)(function() {
    return string_default(a4, e6);
  }, [a4, e6]), s3 = useSpring({ from: { value: 0 }, to: { value: 1 }, reset: true, config: l4, immediate: !o4 }).value;
  return to2(s3, d4);
};
var Xr = { nivo: ["#d76445", "#f47560", "#e8c1a0", "#97e3d5", "#61cdbb", "#00b0a7"], BrBG: (0, import_last.default)(scheme), PRGn: (0, import_last.default)(scheme2), PiYG: (0, import_last.default)(scheme3), PuOr: (0, import_last.default)(scheme4), RdBu: (0, import_last.default)(scheme5), RdGy: (0, import_last.default)(scheme6), RdYlBu: (0, import_last.default)(scheme7), RdYlGn: (0, import_last.default)(scheme8), spectral: (0, import_last.default)(scheme9), blues: (0, import_last.default)(scheme22), greens: (0, import_last.default)(scheme23), greys: (0, import_last.default)(scheme24), oranges: (0, import_last.default)(scheme27), purples: (0, import_last.default)(scheme25), reds: (0, import_last.default)(scheme26), BuGn: (0, import_last.default)(scheme10), BuPu: (0, import_last.default)(scheme11), GnBu: (0, import_last.default)(scheme12), OrRd: (0, import_last.default)(scheme13), PuBuGn: (0, import_last.default)(scheme14), PuBu: (0, import_last.default)(scheme15), PuRd: (0, import_last.default)(scheme16), RdPu: (0, import_last.default)(scheme17), YlGnBu: (0, import_last.default)(scheme18), YlGn: (0, import_last.default)(scheme19), YlOrBr: (0, import_last.default)(scheme20), YlOrRd: (0, import_last.default)(scheme21) };
var Nr = Object.keys(Xr);
var Kr = { nivo: ["#e8c1a0", "#f47560", "#f1e15b", "#e8a838", "#61cdbb", "#97e3d5"], category10: category10_default, accent: Accent_default, dark2: Dark2_default, paired: Paired_default, pastel1: Pastel1_default, pastel2: Pastel2_default, set1: Set1_default, set2: Set2_default, set3: Set3_default, brown_blueGreen: (0, import_last.default)(scheme), purpleRed_green: (0, import_last.default)(scheme2), pink_yellowGreen: (0, import_last.default)(scheme3), purple_orange: (0, import_last.default)(scheme4), red_blue: (0, import_last.default)(scheme5), red_grey: (0, import_last.default)(scheme6), red_yellow_blue: (0, import_last.default)(scheme7), red_yellow_green: (0, import_last.default)(scheme8), spectral: (0, import_last.default)(scheme9), blues: (0, import_last.default)(scheme22), greens: (0, import_last.default)(scheme23), greys: (0, import_last.default)(scheme24), oranges: (0, import_last.default)(scheme27), purples: (0, import_last.default)(scheme25), reds: (0, import_last.default)(scheme26), blue_green: (0, import_last.default)(scheme10), blue_purple: (0, import_last.default)(scheme11), green_blue: (0, import_last.default)(scheme12), orange_red: (0, import_last.default)(scheme13), purple_blue_green: (0, import_last.default)(scheme14), purple_blue: (0, import_last.default)(scheme15), purple_red: (0, import_last.default)(scheme16), red_purple: (0, import_last.default)(scheme17), yellow_green_blue: (0, import_last.default)(scheme18), yellow_green: (0, import_last.default)(scheme19), yellow_orange_brown: (0, import_last.default)(scheme20), yellow_orange_red: (0, import_last.default)(scheme21) };
var et = import_prop_types.default.oneOfType([import_prop_types.default.oneOf(Nr), import_prop_types.default.func, import_prop_types.default.arrayOf(import_prop_types.default.string)]);
var rt = { basis: basis_default, basisClosed: basisClosed_default, basisOpen: basisOpen_default, bundle: bundle_default, cardinal: cardinal_default, cardinalClosed: cardinalClosed_default, cardinalOpen: cardinalOpen_default, catmullRom: catmullRom_default, catmullRomClosed: catmullRomClosed_default, catmullRomOpen: catmullRomOpen_default, linear: linear_default, linearClosed: linearClosed_default, monotoneX, monotoneY, natural: natural_default, step: step_default, stepAfter, stepBefore };
var tt = Object.keys(rt);
var nt = tt.filter(function(e6) {
  return e6.endsWith("Closed");
});
var it = (0, import_without.default)(tt, "bundle", "basisClosed", "basisOpen", "cardinalClosed", "cardinalOpen", "catmullRomClosed", "catmullRomOpen", "linearClosed");
var ot = (0, import_without.default)(tt, "bundle", "basisClosed", "basisOpen", "cardinalClosed", "cardinalOpen", "catmullRomClosed", "catmullRomOpen", "linearClosed");
var lt = function(e6) {
  if (!rt[e6]) throw new TypeError("'" + e6 + "', is not a valid curve interpolator identifier.");
  return rt[e6];
};
var at = { ascending: ascending_default, descending: descending_default, insideOut: insideOut_default, none: none_default2, reverse: reverse_default };
var dt = Object.keys(at);
var ut = { expand: expand_default, diverging: diverging_default, none: none_default, silhouette: silhouette_default, wiggle: wiggle_default };
var ct = Object.keys(ut);
var pt = import_prop_types.default.shape({ top: import_prop_types.default.number, right: import_prop_types.default.number, bottom: import_prop_types.default.number, left: import_prop_types.default.number }).isRequired;
var ht = ["normal", "multiply", "screen", "overlay", "darken", "lighten", "color-dodge", "color-burn", "hard-light", "soft-light", "difference", "exclusion", "hue", "saturation", "color", "luminosity"];
var gt = import_prop_types.default.oneOf(ht);
var vt = ordinal(Set3_default);
var _t = { top: 0, right: 0, bottom: 0, left: 0 };
var wt = function(e6, t6, n6) {
  return void 0 === n6 && (n6 = {}), (0, import_react15.useMemo)(function() {
    var r6 = jr({}, _t, n6);
    return { margin: r6, innerWidth: e6 - r6.left - r6.right, innerHeight: t6 - r6.top - r6.bottom, outerWidth: e6, outerHeight: t6 };
  }, [e6, t6, n6.top, n6.right, n6.bottom, n6.left]);
};
var kt = function() {
  var e6 = (0, import_react15.useRef)(null), r6 = (0, import_react15.useState)({ left: 0, top: 0, width: 0, height: 0 }), t6 = r6[0], l4 = r6[1], a4 = (0, import_react15.useState)(function() {
    return "undefined" == typeof ResizeObserver ? null : new ResizeObserver(function(e7) {
      var r7 = e7[0];
      return l4(r7.contentRect);
    });
  })[0];
  return (0, import_react15.useEffect)(function() {
    return e6.current && null !== a4 && a4.observe(e6.current), function() {
      null !== a4 && a4.disconnect();
    };
  }, []), [e6, t6];
};
var Rt = function(e6) {
  return (0, import_react15.useMemo)(function() {
    return Ir(Pr, e6);
  }, [e6]);
};
var xt = function(e6) {
  return "function" == typeof e6 ? e6 : "string" == typeof e6 ? 0 === e6.indexOf("time:") ? timeFormat(e6.slice("5")) : format(e6) : function(e7) {
    return "" + e7;
  };
};
var Ot = function(e6) {
  return (0, import_react15.useMemo)(function() {
    return xt(e6);
  }, [e6]);
};
var qt = (0, import_react15.createContext)();
var Ct = {};
var Wt = function(e6) {
  var r6 = e6.theme, t6 = void 0 === r6 ? Ct : r6, n6 = e6.children, i5 = Rt(t6);
  return (0, import_jsx_runtime.jsx)(qt.Provider, { value: i5, children: n6 });
};
Wt.propTypes = { children: import_prop_types.default.node.isRequired, theme: import_prop_types.default.object };
var zt = function() {
  return (0, import_react15.useContext)(qt);
};
var Tt = ["outlineWidth", "outlineColor", "outlineOpacity"];
var Mt = function(e6) {
  return e6.outlineWidth, e6.outlineColor, e6.outlineOpacity, Br(e6, Tt);
};
var Pt = function(e6) {
  var r6 = e6.children, t6 = e6.condition, n6 = e6.wrapper;
  return t6 ? (0, import_react15.cloneElement)(n6, {}, r6) : r6;
};
Pt.propTypes = { children: import_prop_types.default.node.isRequired, condition: import_prop_types.default.bool.isRequired, wrapper: import_prop_types.default.element.isRequired };
var jt = { position: "relative" };
var St = function(e6) {
  var r6 = e6.children, t6 = e6.theme, i5 = e6.renderWrapper, o4 = void 0 === i5 || i5, l4 = e6.isInteractive, a4 = void 0 === l4 || l4, d4 = e6.animate, s3 = e6.motionConfig, u3 = (0, import_react15.useRef)(null);
  return (0, import_jsx_runtime.jsx)(Wt, { theme: t6, children: (0, import_jsx_runtime.jsx)(Ar, { animate: d4, config: s3, children: (0, import_jsx_runtime.jsx)(M, { container: u3, children: (0, import_jsx_runtime.jsxs)(Pt, { condition: o4, wrapper: (0, import_jsx_runtime.jsx)("div", { style: jt, ref: u3 }), children: [r6, a4 && (0, import_jsx_runtime.jsx)(F, {})] }) }) }) });
};
St.propTypes = { children: import_prop_types.default.element.isRequired, isInteractive: import_prop_types.default.bool, renderWrapper: import_prop_types.default.bool, theme: import_prop_types.default.object, animate: import_prop_types.default.bool, motionConfig: import_prop_types.default.oneOfType([import_prop_types.default.string, Er.motionConfig]) };
var Bt = function() {
};
var Gt = { position: "relative" };
var Lt = function(e6) {
  var t6 = e6.children, i5 = e6.theme, o4 = e6.isInteractive, l4 = void 0 === o4 || o4, d4 = e6.renderWrapper, s3 = void 0 === d4 || d4, u3 = e6.animate, c9 = e6.motionConfig, f3 = (0, import_react15.useRef)(null), m4 = V(f3), y3 = m4.actions, v5 = m4.state, _2 = (0, import_react15.useCallback)(function(e7, r6) {
    return y3.showTooltipFromEvent(e7, r6);
  }, [y3.showTooltipFromEvent]), w4 = (0, import_react15.useMemo)(function() {
    return { showTooltip: l4 ? _2 : Bt, hideTooltip: l4 ? y3.hideTooltip : Bt };
  }, [y3.hideTooltip, l4, _2]);
  return (0, import_jsx_runtime.jsx)(Wt, { theme: i5, children: (0, import_jsx_runtime.jsx)(Ar, { animate: u3, config: c9, children: (0, import_jsx_runtime.jsx)(j.Provider, { value: y3, children: (0, import_jsx_runtime.jsx)(O.Provider, { value: v5, children: (0, import_jsx_runtime.jsxs)(Pt, { condition: s3, wrapper: (0, import_jsx_runtime.jsx)("div", { style: Gt, ref: f3 }), children: [t6(w4), l4 && (0, import_jsx_runtime.jsx)(F, {})] }) }) }) }) });
};
Lt.propTypes = { children: import_prop_types.default.func.isRequired, isInteractive: import_prop_types.default.bool, renderWrapper: import_prop_types.default.bool, theme: import_prop_types.default.object.isRequired, animate: import_prop_types.default.bool.isRequired, motionConfig: import_prop_types.default.oneOfType([import_prop_types.default.string, Er.motionConfig]) };
var It = function(e6) {
  var r6 = e6.children, t6 = kt(), n6 = t6[0], i5 = t6[1], o4 = i5.width > 0 && i5.height > 0;
  return (0, import_jsx_runtime.jsx)("div", { ref: n6, style: { width: "100%", height: "100%" }, children: o4 && r6({ width: i5.width, height: i5.height }) });
};
It.propTypes = { children: import_prop_types.default.func.isRequired };
var Yt = ["id", "colors"];
var Dt = function(e6) {
  var r6 = e6.id, t6 = e6.colors, n6 = Br(e6, Yt);
  return (0, import_jsx_runtime.jsx)("linearGradient", jr({ id: r6, x1: 0, x2: 0, y1: 0, y2: 1 }, n6, { children: t6.map(function(e7) {
    var r7 = e7.offset, t7 = e7.color, n7 = e7.opacity;
    return (0, import_jsx_runtime.jsx)("stop", { offset: r7 + "%", stopColor: t7, stopOpacity: void 0 !== n7 ? n7 : 1 }, r7);
  }) }));
};
Dt.propTypes = { id: import_prop_types.default.string.isRequired, colors: import_prop_types.default.arrayOf(import_prop_types.default.shape({ offset: import_prop_types.default.number.isRequired, color: import_prop_types.default.string.isRequired, opacity: import_prop_types.default.number })).isRequired, gradientTransform: import_prop_types.default.string };
var Et = { linearGradient: Dt };
var Ut = { color: "#000000", background: "#ffffff", size: 4, padding: 4, stagger: false };
var Ft = (0, import_react15.memo)(function(e6) {
  var r6 = e6.id, t6 = e6.background, n6 = void 0 === t6 ? Ut.background : t6, i5 = e6.color, o4 = void 0 === i5 ? Ut.color : i5, l4 = e6.size, a4 = void 0 === l4 ? Ut.size : l4, d4 = e6.padding, s3 = void 0 === d4 ? Ut.padding : d4, u3 = e6.stagger, c9 = void 0 === u3 ? Ut.stagger : u3, f3 = a4 + s3, p4 = a4 / 2, h2 = s3 / 2;
  return true === c9 && (f3 = 2 * a4 + 2 * s3), (0, import_jsx_runtime.jsxs)("pattern", { id: r6, width: f3, height: f3, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime.jsx)("rect", { width: f3, height: f3, fill: n6 }), (0, import_jsx_runtime.jsx)("circle", { cx: h2 + p4, cy: h2 + p4, r: p4, fill: o4 }), c9 && (0, import_jsx_runtime.jsx)("circle", { cx: 1.5 * s3 + a4 + p4, cy: 1.5 * s3 + a4 + p4, r: p4, fill: o4 })] });
});
Ft.displayName = "PatternDots", Ft.propTypes = { id: import_prop_types.default.string.isRequired, color: import_prop_types.default.string.isRequired, background: import_prop_types.default.string.isRequired, size: import_prop_types.default.number.isRequired, padding: import_prop_types.default.number.isRequired, stagger: import_prop_types.default.bool.isRequired };
var Nt = 2 * Math.PI;
var Ht = function(e6) {
  return e6 * Math.PI / 180;
};
var rn = { svg: { align: { left: "start", center: "middle", right: "end", start: "start", middle: "middle", end: "end" }, baseline: { top: "text-before-edge", center: "central", bottom: "alphabetic" } }, canvas: { align: { left: "left", center: "center", right: "right", start: "left", middle: "center", end: "right" }, baseline: { top: "top", center: "middle", bottom: "bottom" } } };
var nn = { spacing: 5, rotation: 0, background: "#000000", color: "#ffffff", lineWidth: 2 };
var on = (0, import_react15.memo)(function(e6) {
  var r6 = e6.id, t6 = e6.spacing, n6 = void 0 === t6 ? nn.spacing : t6, i5 = e6.rotation, o4 = void 0 === i5 ? nn.rotation : i5, l4 = e6.background, a4 = void 0 === l4 ? nn.background : l4, d4 = e6.color, s3 = void 0 === d4 ? nn.color : d4, u3 = e6.lineWidth, c9 = void 0 === u3 ? nn.lineWidth : u3, f3 = Math.round(o4) % 360, p4 = Math.abs(n6);
  f3 > 180 ? f3 -= 360 : f3 > 90 ? f3 -= 180 : f3 < -180 ? f3 += 360 : f3 < -90 && (f3 += 180);
  var h2, g4 = p4, b4 = p4;
  return 0 === f3 ? h2 = "\n                M 0 0 L " + g4 + " 0\n                M 0 " + b4 + " L " + g4 + " " + b4 + "\n            " : 90 === f3 ? h2 = "\n                M 0 0 L 0 " + b4 + "\n                M " + g4 + " 0 L " + g4 + " " + b4 + "\n            " : (g4 = Math.abs(p4 / Math.sin(Ht(f3))), b4 = p4 / Math.sin(Ht(90 - f3)), h2 = f3 > 0 ? "\n                    M 0 " + -b4 + " L " + 2 * g4 + " " + b4 + "\n                    M " + -g4 + " " + -b4 + " L " + g4 + " " + b4 + "\n                    M " + -g4 + " 0 L " + g4 + " " + 2 * b4 + "\n                " : "\n                    M " + -g4 + " " + b4 + " L " + g4 + " " + -b4 + "\n                    M " + -g4 + " " + 2 * b4 + " L " + 2 * g4 + " " + -b4 + "\n                    M 0 " + 2 * b4 + " L " + 2 * g4 + " 0\n                "), (0, import_jsx_runtime.jsxs)("pattern", { id: r6, width: g4, height: b4, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime.jsx)("rect", { width: g4, height: b4, fill: a4, stroke: "rgba(255, 0, 0, 0.1)", strokeWidth: 0 }), (0, import_jsx_runtime.jsx)("path", { d: h2, strokeWidth: c9, stroke: s3, strokeLinecap: "square" })] });
});
on.displayName = "PatternLines", on.propTypes = { id: import_prop_types.default.string.isRequired, spacing: import_prop_types.default.number.isRequired, rotation: import_prop_types.default.number.isRequired, background: import_prop_types.default.string.isRequired, color: import_prop_types.default.string.isRequired, lineWidth: import_prop_types.default.number.isRequired };
var an = { color: "#000000", background: "#ffffff", size: 4, padding: 4, stagger: false };
var dn = (0, import_react15.memo)(function(e6) {
  var r6 = e6.id, t6 = e6.color, n6 = void 0 === t6 ? an.color : t6, i5 = e6.background, o4 = void 0 === i5 ? an.background : i5, l4 = e6.size, a4 = void 0 === l4 ? an.size : l4, d4 = e6.padding, s3 = void 0 === d4 ? an.padding : d4, u3 = e6.stagger, c9 = void 0 === u3 ? an.stagger : u3, f3 = a4 + s3, p4 = s3 / 2;
  return true === c9 && (f3 = 2 * a4 + 2 * s3), (0, import_jsx_runtime.jsxs)("pattern", { id: r6, width: f3, height: f3, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime.jsx)("rect", { width: f3, height: f3, fill: o4 }), (0, import_jsx_runtime.jsx)("rect", { x: p4, y: p4, width: a4, height: a4, fill: n6 }), c9 && (0, import_jsx_runtime.jsx)("rect", { x: 1.5 * s3 + a4, y: 1.5 * s3 + a4, width: a4, height: a4, fill: n6 })] });
});
dn.displayName = "PatternSquares", dn.propTypes = { id: import_prop_types.default.string.isRequired, color: import_prop_types.default.string.isRequired, background: import_prop_types.default.string.isRequired, size: import_prop_types.default.number.isRequired, padding: import_prop_types.default.number.isRequired, stagger: import_prop_types.default.bool.isRequired };
var un = { patternDots: Ft, patternLines: on, patternSquares: dn };
var cn = ["type"];
var fn = jr({}, Et, un);
var pn = function(e6) {
  var r6 = e6.defs;
  return !r6 || r6.length < 1 ? null : (0, import_jsx_runtime.jsx)("defs", { "aria-hidden": true, children: r6.map(function(e7) {
    var r7 = e7.type, t6 = Br(e7, cn);
    return fn[r7] ? (0, import_react15.createElement)(fn[r7], jr({ key: t6.id }, t6)) : null;
  }) });
};
pn.propTypes = { defs: import_prop_types.default.arrayOf(import_prop_types.default.shape({ type: import_prop_types.default.oneOf(Object.keys(fn)).isRequired, id: import_prop_types.default.string.isRequired })) };
var hn = (0, import_react15.memo)(pn);
var gn = function(e6) {
  var r6 = e6.width, t6 = e6.height, n6 = e6.margin, i5 = e6.defs, o4 = e6.children, l4 = e6.role, a4 = e6.ariaLabel, d4 = e6.ariaLabelledBy, s3 = e6.ariaDescribedBy, u3 = e6.isFocusable, c9 = zt();
  return (0, import_jsx_runtime.jsxs)("svg", { xmlns: "http://www.w3.org/2000/svg", width: r6, height: t6, role: l4, "aria-label": a4, "aria-labelledby": d4, "aria-describedby": s3, focusable: u3, tabIndex: u3 ? 0 : void 0, children: [(0, import_jsx_runtime.jsx)(hn, { defs: i5 }), (0, import_jsx_runtime.jsx)("rect", { width: r6, height: t6, fill: c9.background }), (0, import_jsx_runtime.jsx)("g", { transform: "translate(" + n6.left + "," + n6.top + ")", children: o4 })] });
};
gn.propTypes = { width: import_prop_types.default.number.isRequired, height: import_prop_types.default.number.isRequired, margin: import_prop_types.default.shape({ top: import_prop_types.default.number.isRequired, left: import_prop_types.default.number.isRequired }).isRequired, defs: import_prop_types.default.array, children: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.node), import_prop_types.default.node]).isRequired, role: import_prop_types.default.string, isFocusable: import_prop_types.default.bool, ariaLabel: import_prop_types.default.string, ariaLabelledBy: import_prop_types.default.string, ariaDescribedBy: import_prop_types.default.string };
var bn = function(e6) {
  var r6 = e6.size, t6 = e6.color, n6 = e6.borderWidth, i5 = e6.borderColor;
  return (0, import_jsx_runtime.jsx)("circle", { r: r6 / 2, fill: t6, stroke: i5, strokeWidth: n6, style: { pointerEvents: "none" } });
};
bn.propTypes = { size: import_prop_types.default.number.isRequired, color: import_prop_types.default.string.isRequired, borderWidth: import_prop_types.default.number.isRequired, borderColor: import_prop_types.default.string.isRequired };
var mn = (0, import_react15.memo)(bn);
var yn = function(e6) {
  var r6 = e6.x, t6 = e6.y, n6 = e6.symbol, i5 = void 0 === n6 ? mn : n6, o4 = e6.size, l4 = e6.datum, a4 = e6.color, d4 = e6.borderWidth, u3 = e6.borderColor, c9 = e6.label, f3 = e6.labelTextAnchor, p4 = void 0 === f3 ? "middle" : f3, h2 = e6.labelYOffset, g4 = void 0 === h2 ? -12 : h2, b4 = zt(), m4 = Ur(), y3 = m4.animate, v5 = m4.config, _2 = useSpring({ transform: "translate(" + r6 + ", " + t6 + ")", config: v5, immediate: !y3 });
  return (0, import_jsx_runtime.jsxs)(animated.g, { transform: _2.transform, style: { pointerEvents: "none" }, children: [(0, import_react15.createElement)(i5, { size: o4, color: a4, datum: l4, borderWidth: d4, borderColor: u3 }), c9 && (0, import_jsx_runtime.jsx)("text", { textAnchor: p4, y: g4, style: Mt(b4.dots.text), children: c9 })] });
};
yn.propTypes = { x: import_prop_types.default.number.isRequired, y: import_prop_types.default.number.isRequired, datum: import_prop_types.default.object.isRequired, size: import_prop_types.default.number.isRequired, color: import_prop_types.default.string.isRequired, borderWidth: import_prop_types.default.number.isRequired, borderColor: import_prop_types.default.string.isRequired, symbol: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.object]), label: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.number]), labelTextAnchor: import_prop_types.default.oneOf(["start", "middle", "end"]), labelYOffset: import_prop_types.default.number };
var vn = (0, import_react15.memo)(yn);
var _n = function(e6) {
  var r6 = e6.width, t6 = e6.height, n6 = e6.axis, i5 = e6.scale, o4 = e6.value, l4 = e6.lineStyle, a4 = e6.textStyle, d4 = e6.legend, s3 = e6.legendNode, u3 = e6.legendPosition, c9 = void 0 === u3 ? "top-right" : u3, f3 = e6.legendOffsetX, p4 = void 0 === f3 ? 14 : f3, h2 = e6.legendOffsetY, g4 = void 0 === h2 ? 14 : h2, b4 = e6.legendOrientation, m4 = void 0 === b4 ? "horizontal" : b4, y3 = zt(), v5 = 0, _2 = 0, w4 = 0, k4 = 0;
  if ("y" === n6 ? (w4 = i5(o4), _2 = r6) : (v5 = i5(o4), k4 = t6), d4 && !s3) {
    var R2 = function(e7) {
      var r7 = e7.axis, t7 = e7.width, n7 = e7.height, i6 = e7.position, o5 = e7.offsetX, l5 = e7.offsetY, a5 = e7.orientation, d5 = 0, s4 = 0, u4 = "vertical" === a5 ? -90 : 0, c10 = "start";
      if ("x" === r7) switch (i6) {
        case "top-left":
          d5 = -o5, s4 = l5, c10 = "end";
          break;
        case "top":
          s4 = -l5, c10 = "horizontal" === a5 ? "middle" : "start";
          break;
        case "top-right":
          d5 = o5, s4 = l5, c10 = "horizontal" === a5 ? "start" : "end";
          break;
        case "right":
          d5 = o5, s4 = n7 / 2, c10 = "horizontal" === a5 ? "start" : "middle";
          break;
        case "bottom-right":
          d5 = o5, s4 = n7 - l5, c10 = "start";
          break;
        case "bottom":
          s4 = n7 + l5, c10 = "horizontal" === a5 ? "middle" : "end";
          break;
        case "bottom-left":
          s4 = n7 - l5, d5 = -o5, c10 = "horizontal" === a5 ? "end" : "start";
          break;
        case "left":
          d5 = -o5, s4 = n7 / 2, c10 = "horizontal" === a5 ? "end" : "middle";
      }
      else switch (i6) {
        case "top-left":
          d5 = o5, s4 = -l5, c10 = "start";
          break;
        case "top":
          d5 = t7 / 2, s4 = -l5, c10 = "horizontal" === a5 ? "middle" : "start";
          break;
        case "top-right":
          d5 = t7 - o5, s4 = -l5, c10 = "horizontal" === a5 ? "end" : "start";
          break;
        case "right":
          d5 = t7 + o5, c10 = "horizontal" === a5 ? "start" : "middle";
          break;
        case "bottom-right":
          d5 = t7 - o5, s4 = l5, c10 = "end";
          break;
        case "bottom":
          d5 = t7 / 2, s4 = l5, c10 = "horizontal" === a5 ? "middle" : "end";
          break;
        case "bottom-left":
          d5 = o5, s4 = l5, c10 = "horizontal" === a5 ? "start" : "end";
          break;
        case "left":
          d5 = -o5, c10 = "horizontal" === a5 ? "end" : "middle";
      }
      return { x: d5, y: s4, rotation: u4, textAnchor: c10 };
    }({ axis: n6, width: r6, height: t6, position: c9, offsetX: p4, offsetY: g4, orientation: m4 });
    s3 = (0, import_jsx_runtime.jsx)("text", { transform: "translate(" + R2.x + ", " + R2.y + ") rotate(" + R2.rotation + ")", textAnchor: R2.textAnchor, dominantBaseline: "central", style: a4, children: d4 });
  }
  return (0, import_jsx_runtime.jsxs)("g", { transform: "translate(" + v5 + ", " + w4 + ")", children: [(0, import_jsx_runtime.jsx)("line", { x1: 0, x2: _2, y1: 0, y2: k4, stroke: y3.markers.lineColor, strokeWidth: y3.markers.lineStrokeWidth, style: l4 }), s3] });
};
_n.propTypes = { width: import_prop_types.default.number.isRequired, height: import_prop_types.default.number.isRequired, axis: import_prop_types.default.oneOf(["x", "y"]).isRequired, scale: import_prop_types.default.func.isRequired, value: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string, import_prop_types.default.instanceOf(Date)]).isRequired, lineStyle: import_prop_types.default.object, textStyle: import_prop_types.default.object, legend: import_prop_types.default.string, legendPosition: import_prop_types.default.oneOf(["top-left", "top", "top-right", "right", "bottom-right", "bottom", "bottom-left", "left"]), legendOffsetX: import_prop_types.default.number.isRequired, legendOffsetY: import_prop_types.default.number.isRequired, legendOrientation: import_prop_types.default.oneOf(["horizontal", "vertical"]).isRequired };
var wn = (0, import_react15.memo)(_n);
var kn = function(e6) {
  var r6 = e6.markers, t6 = e6.width, n6 = e6.height, i5 = e6.xScale, o4 = e6.yScale;
  return r6 && 0 !== r6.length ? r6.map(function(e7, r7) {
    return (0, import_jsx_runtime.jsx)(wn, jr({}, e7, { width: t6, height: n6, scale: "y" === e7.axis ? o4 : i5 }), r7);
  }) : null;
};
kn.propTypes = { width: import_prop_types.default.number.isRequired, height: import_prop_types.default.number.isRequired, xScale: import_prop_types.default.func.isRequired, yScale: import_prop_types.default.func.isRequired, markers: import_prop_types.default.arrayOf(import_prop_types.default.shape({ axis: import_prop_types.default.oneOf(["x", "y"]).isRequired, value: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string, import_prop_types.default.instanceOf(Date)]).isRequired, lineStyle: import_prop_types.default.object, textStyle: import_prop_types.default.object })) };
var Rn = (0, import_react15.memo)(kn);
var Bn = Object.keys(Et);
var Gn = Object.keys(un);

// node_modules/@nivo/tooltip/dist/nivo-tooltip.es.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
function v2() {
  return v2 = Object.assign ? Object.assign.bind() : function(t6) {
    for (var i5 = 1; i5 < arguments.length; i5++) {
      var o4 = arguments[i5];
      for (var n6 in o4) Object.prototype.hasOwnProperty.call(o4, n6) && (t6[n6] = o4[n6]);
    }
    return t6;
  }, v2.apply(this, arguments);
}
var x = { pointerEvents: "none", position: "absolute", zIndex: 10, top: 0, left: 0 };
var m2 = function(t6, i5) {
  return "translate(" + t6 + "px, " + i5 + "px)";
};
var b = (0, import_react16.memo)(function(t6) {
  var o4, n6 = t6.position, r6 = t6.anchor, e6 = t6.children, l4 = zt(), d4 = Ur(), y3 = d4.animate, f3 = d4.config, b4 = kt(), g4 = b4[0], w4 = b4[1], T4 = (0, import_react16.useRef)(false), C4 = void 0, E3 = false, P3 = w4.width > 0 && w4.height > 0, j3 = Math.round(n6[0]), N2 = Math.round(n6[1]);
  P3 && ("top" === r6 ? (j3 -= w4.width / 2, N2 -= w4.height + 14) : "right" === r6 ? (j3 += 14, N2 -= w4.height / 2) : "bottom" === r6 ? (j3 -= w4.width / 2, N2 += 14) : "left" === r6 ? (j3 -= w4.width + 14, N2 -= w4.height / 2) : "center" === r6 && (j3 -= w4.width / 2, N2 -= w4.height / 2), C4 = { transform: m2(j3, N2) }, T4.current || (E3 = true), T4.current = [j3, N2]);
  var O6 = useSpring({ to: C4, config: f3, immediate: !y3 || E3 }), V3 = v2({}, x, l4.tooltip.wrapper, { transform: null != (o4 = O6.transform) ? o4 : m2(j3, N2), opacity: O6.transform ? 1 : 0 });
  return (0, import_jsx_runtime2.jsx)(animated.div, { ref: g4, style: V3, children: e6 });
});
b.displayName = "TooltipWrapper";
var g = (0, import_react16.memo)(function(t6) {
  var i5 = t6.size, o4 = void 0 === i5 ? 12 : i5, n6 = t6.color, r6 = t6.style;
  return (0, import_jsx_runtime2.jsx)("span", { style: v2({ display: "block", width: o4, height: o4, background: n6 }, void 0 === r6 ? {} : r6) });
});
var w = (0, import_react16.memo)(function(t6) {
  var i5, o4 = t6.id, n6 = t6.value, r6 = t6.format, e6 = t6.enableChip, l4 = void 0 !== e6 && e6, a4 = t6.color, c9 = t6.renderContent, h2 = zt(), u3 = Ot(r6);
  if ("function" == typeof c9) i5 = c9();
  else {
    var f3 = n6;
    void 0 !== u3 && void 0 !== f3 && (f3 = u3(f3)), i5 = (0, import_jsx_runtime2.jsxs)("div", { style: h2.tooltip.basic, children: [l4 && (0, import_jsx_runtime2.jsx)(g, { color: a4, style: h2.tooltip.chip }), void 0 !== f3 ? (0, import_jsx_runtime2.jsxs)("span", { children: [o4, ": ", (0, import_jsx_runtime2.jsx)("strong", { children: "" + f3 })] }) : o4] });
  }
  return (0, import_jsx_runtime2.jsx)("div", { style: h2.tooltip.container, children: i5 });
});
var T = { width: "100%", borderCollapse: "collapse" };
var C2 = (0, import_react16.memo)(function(t6) {
  var i5, o4 = t6.title, n6 = t6.rows, r6 = void 0 === n6 ? [] : n6, e6 = t6.renderContent, l4 = zt();
  return r6.length ? (i5 = "function" == typeof e6 ? e6() : (0, import_jsx_runtime2.jsxs)("div", { children: [o4 && o4, (0, import_jsx_runtime2.jsx)("table", { style: v2({}, T, l4.tooltip.table), children: (0, import_jsx_runtime2.jsx)("tbody", { children: r6.map(function(t7, i6) {
    return (0, import_jsx_runtime2.jsx)("tr", { children: t7.map(function(t8, i7) {
      return (0, import_jsx_runtime2.jsx)("td", { style: l4.tooltip.tableCell, children: t8 }, i7);
    }) }, i6);
  }) }) })] }), (0, import_jsx_runtime2.jsx)("div", { style: l4.tooltip.container, children: i5 })) : null;
});
C2.displayName = "TableTooltip";
var E = (0, import_react16.memo)(function(t6) {
  var i5 = t6.x0, n6 = t6.x1, r6 = t6.y0, e6 = t6.y1, l4 = zt(), u3 = Ur(), d4 = u3.animate, y3 = u3.config, f3 = (0, import_react16.useMemo)(function() {
    return v2({}, l4.crosshair.line, { pointerEvents: "none" });
  }, [l4.crosshair.line]), x3 = useSpring({ x1: i5, x2: n6, y1: r6, y2: e6, config: y3, immediate: !d4 });
  return (0, import_jsx_runtime2.jsx)(animated.line, v2({}, x3, { fill: "none", style: f3 }));
});
E.displayName = "CrosshairLine";
var P = (0, import_react16.memo)(function(t6) {
  var i5, o4, n6 = t6.width, r6 = t6.height, e6 = t6.type, l4 = t6.x, a4 = t6.y;
  return "cross" === e6 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: r6 }, o4 = { x0: 0, x1: n6, y0: a4, y1: a4 }) : "top-left" === e6 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: a4 }, o4 = { x0: 0, x1: l4, y0: a4, y1: a4 }) : "top" === e6 ? i5 = { x0: l4, x1: l4, y0: 0, y1: a4 } : "top-right" === e6 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: a4 }, o4 = { x0: l4, x1: n6, y0: a4, y1: a4 }) : "right" === e6 ? o4 = { x0: l4, x1: n6, y0: a4, y1: a4 } : "bottom-right" === e6 ? (i5 = { x0: l4, x1: l4, y0: a4, y1: r6 }, o4 = { x0: l4, x1: n6, y0: a4, y1: a4 }) : "bottom" === e6 ? i5 = { x0: l4, x1: l4, y0: a4, y1: r6 } : "bottom-left" === e6 ? (i5 = { x0: l4, x1: l4, y0: a4, y1: r6 }, o4 = { x0: 0, x1: l4, y0: a4, y1: a4 }) : "left" === e6 ? o4 = { x0: 0, x1: l4, y0: a4, y1: a4 } : "x" === e6 ? i5 = { x0: l4, x1: l4, y0: 0, y1: r6 } : "y" === e6 && (o4 = { x0: 0, x1: n6, y0: a4, y1: a4 }), (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [i5 && (0, import_jsx_runtime2.jsx)(E, { x0: i5.x0, x1: i5.x1, y0: i5.y0, y1: i5.y1 }), o4 && (0, import_jsx_runtime2.jsx)(E, { x0: o4.x0, x1: o4.x1, y0: o4.y0, y1: o4.y1 })] });
});
P.displayName = "Crosshair";
var j = (0, import_react16.createContext)({ showTooltipAt: function() {
}, showTooltipFromEvent: function() {
}, hideTooltip: function() {
} });
var N = { isVisible: false, position: [null, null], content: null, anchor: null };
var O = (0, import_react16.createContext)(N);
var V = function(t6) {
  var i5 = (0, import_react16.useState)(N), n6 = i5[0], l4 = i5[1], a4 = (0, import_react16.useCallback)(function(t7, i6, o4) {
    var n7 = i6[0], r6 = i6[1];
    void 0 === o4 && (o4 = "top"), l4({ isVisible: true, position: [n7, r6], anchor: o4, content: t7 });
  }, [l4]), c9 = (0, import_react16.useCallback)(function(i6, o4, n7) {
    void 0 === n7 && (n7 = "top");
    var r6 = t6.current.getBoundingClientRect(), e6 = t6.current.offsetWidth, a5 = e6 === r6.width ? 1 : e6 / r6.width, c10 = "touches" in o4 ? o4.touches[0] : o4, s4 = c10.clientX, h2 = c10.clientY, u3 = (s4 - r6.left) * a5, d4 = (h2 - r6.top) * a5;
    "left" !== n7 && "right" !== n7 || (n7 = u3 < r6.width / 2 ? "right" : "left"), l4({ isVisible: true, position: [u3, d4], anchor: n7, content: i6 });
  }, [t6, l4]), s3 = (0, import_react16.useCallback)(function() {
    l4(N);
  }, [l4]);
  return { actions: (0, import_react16.useMemo)(function() {
    return { showTooltipAt: a4, showTooltipFromEvent: c9, hideTooltip: s3 };
  }, [a4, c9, s3]), state: n6 };
};
var k = function() {
  var t6 = (0, import_react16.useContext)(j);
  if (void 0 === t6) throw new Error("useTooltip must be used within a TooltipProvider");
  return t6;
};
var z2 = function() {
  var t6 = (0, import_react16.useContext)(O);
  if (void 0 === t6) throw new Error("useTooltipState must be used within a TooltipProvider");
  return t6;
};
var A = function(t6) {
  return t6.isVisible;
};
var F = function() {
  var t6 = z2();
  return A(t6) ? (0, import_jsx_runtime2.jsx)(b, { position: t6.position, anchor: t6.anchor, children: t6.content }) : null;
};
var M = function(t6) {
  var i5 = t6.container, o4 = t6.children, n6 = V(i5), r6 = n6.actions, e6 = n6.state;
  return (0, import_jsx_runtime2.jsx)(j.Provider, { value: r6, children: (0, import_jsx_runtime2.jsx)(O.Provider, { value: e6, children: o4 }) });
};

// node_modules/@nivo/parallel-coordinates/dist/nivo-parallel-coordinates.es.js
var import_jsx_runtime5 = __toESM(require_jsx_runtime());
var import_react20 = __toESM(require_react());

// node_modules/@nivo/axes/dist/nivo-axes.es.js
var t4 = __toESM(require_react());
var import_react17 = __toESM(require_react());

// node_modules/@nivo/scales/dist/nivo-scales.es.js
var import_uniq = __toESM(require_uniq());
var import_uniqBy = __toESM(require_uniqBy());
var import_sortBy = __toESM(require_sortBy());
var import_last2 = __toESM(require_last());
var import_isDate = __toESM(require_isDate());
var J = [function(n6) {
  return n6.setMilliseconds(0);
}, function(n6) {
  return n6.setSeconds(0);
}, function(n6) {
  return n6.setMinutes(0);
}, function(n6) {
  return n6.setHours(0);
}, function(n6) {
  return n6.setDate(1);
}, function(n6) {
  return n6.setMonth(0);
}];
var K = { millisecond: [], second: J.slice(0, 1), minute: J.slice(0, 2), hour: J.slice(0, 3), day: J.slice(0, 4), month: J.slice(0, 5), year: J.slice(0, 6) };
var X = function(n6, t6) {
  void 0 === t6 && (t6 = false);
  var r6 = n6;
  return r6.type = "linear", r6.stacked = t6, r6;
};
var Z = function(n6) {
  var t6 = n6;
  return t6.type = "point", t6;
};
var pn2 = function(n6) {
  var t6 = n6.bandwidth();
  if (0 === t6) return n6;
  var r6 = t6 / 2;
  return n6.round() && (r6 = Math.round(r6)), function(t7) {
    var e6;
    return (null != (e6 = n6(t7)) ? e6 : 0) + r6;
  };
};
var hn2 = { millisecond: [millisecond_default, millisecond_default], second: [second_default, second_default], minute: [minute_default, utcMinute_default], hour: [hour_default, utcHour_default], day: [newInterval(function(n6) {
  return n6.setHours(0, 0, 0, 0);
}, function(n6, t6) {
  return n6.setDate(n6.getDate() + t6);
}, function(n6, t6) {
  return (t6.getTime() - n6.getTime()) / 864e5;
}, function(n6) {
  return Math.floor(n6.getTime() / 864e5);
}), newInterval(function(n6) {
  return n6.setUTCHours(0, 0, 0, 0);
}, function(n6, t6) {
  return n6.setUTCDate(n6.getUTCDate() + t6);
}, function(n6, t6) {
  return (t6.getTime() - n6.getTime()) / 864e5;
}, function(n6) {
  return Math.floor(n6.getTime() / 864e5);
})], week: [sunday, utcSunday], sunday: [sunday, utcSunday], monday: [monday, utcMonday], tuesday: [tuesday, utcTuesday], wednesday: [wednesday, utcWednesday], thursday: [thursday, utcThursday], friday: [friday, utcFriday], saturday: [saturday, utcSaturday], month: [month_default, utcMonth_default], year: [year_default, utcYear_default] };
var gn2 = Object.keys(hn2);
var xn = new RegExp("^every\\s*(\\d+)?\\s*(" + gn2.join("|") + ")s?$", "i");
var kn2 = function(n6, t6) {
  if (Array.isArray(t6)) return t6;
  if ("string" == typeof t6 && "useUTC" in n6) {
    var r6 = t6.match(xn);
    if (r6) {
      var e6 = r6[1], a4 = r6[2], i5 = hn2[a4][n6.useUTC ? 1 : 0];
      if ("day" === a4) {
        var o4, u3, c9 = n6.domain(), s3 = c9[0], d4 = c9[1], f3 = new Date(d4);
        return f3.setDate(f3.getDate() + 1), null != (o4 = null == (u3 = i5.every(Number(null != e6 ? e6 : 1))) ? void 0 : u3.range(s3, f3)) ? o4 : [];
      }
      if (void 0 === e6) return n6.ticks(i5);
      var l4 = i5.every(Number(e6));
      if (l4) return n6.ticks(l4);
    }
    throw new Error("Invalid tickValues: " + t6);
  }
  if ("ticks" in n6) {
    if (void 0 === t6) return n6.ticks();
    if ("number" == typeof (m4 = t6) && isFinite(m4) && Math.floor(m4) === m4) return n6.ticks(t6);
  }
  var m4;
  return n6.domain();
};

// node_modules/@nivo/axes/dist/nivo-axes.es.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
function p2() {
  return p2 = Object.assign ? Object.assign.bind() : function(t6) {
    for (var e6 = 1; e6 < arguments.length; e6++) {
      var i5 = arguments[e6];
      for (var n6 in i5) Object.prototype.hasOwnProperty.call(i5, n6) && (t6[n6] = i5[n6]);
    }
    return t6;
  }, p2.apply(this, arguments);
}
var b2 = function(t6) {
  var e6, i5 = t6.axis, n6 = t6.scale, r6 = t6.ticksPosition, o4 = t6.tickValues, l4 = t6.tickSize, s3 = t6.tickPadding, c9 = t6.tickRotation, f3 = t6.truncateTickAt, u3 = t6.engine, d4 = void 0 === u3 ? "svg" : u3, x3 = kn2(n6, o4), m4 = rn[d4], k4 = "bandwidth" in n6 ? pn2(n6) : n6, g4 = { lineX: 0, lineY: 0 }, v5 = { textX: 0, textY: 0 }, b4 = "object" == typeof document && "rtl" === document.dir, P3 = m4.align.center, T4 = m4.baseline.center;
  "x" === i5 ? (e6 = function(t7) {
    var e7;
    return { x: null != (e7 = k4(t7)) ? e7 : 0, y: 0 };
  }, g4.lineY = l4 * ("after" === r6 ? 1 : -1), v5.textY = (l4 + s3) * ("after" === r6 ? 1 : -1), T4 = "after" === r6 ? m4.baseline.top : m4.baseline.bottom, 0 === c9 ? P3 = m4.align.center : "after" === r6 && c9 < 0 || "before" === r6 && c9 > 0 ? (P3 = m4.align[b4 ? "left" : "right"], T4 = m4.baseline.center) : ("after" === r6 && c9 > 0 || "before" === r6 && c9 < 0) && (P3 = m4.align[b4 ? "right" : "left"], T4 = m4.baseline.center)) : (e6 = function(t7) {
    var e7;
    return { x: 0, y: null != (e7 = k4(t7)) ? e7 : 0 };
  }, g4.lineX = l4 * ("after" === r6 ? 1 : -1), v5.textX = (l4 + s3) * ("after" === r6 ? 1 : -1), P3 = "after" === r6 ? m4.align.left : m4.align.right);
  return { ticks: x3.map(function(t7) {
    var i6 = "string" == typeof t7 ? function(t8) {
      var e7 = String(t8).length;
      return f3 && f3 > 0 && e7 > f3 ? "" + String(t8).slice(0, f3).concat("...") : "" + t8;
    }(t7) : t7;
    return p2({ key: t7 instanceof Date ? "" + t7.valueOf() : "" + t7, value: i6 }, e6(t7), g4, v5);
  }), textAlign: P3, textBaseline: T4 };
};
var P2 = function(t6, e6) {
  if (void 0 === t6 || "function" == typeof t6) return t6;
  if ("time" === e6.type) {
    var i5 = timeFormat(t6);
    return function(t7) {
      return i5(t7 instanceof Date ? t7 : new Date(t7));
    };
  }
  return format(t6);
};
var T2 = function(t6) {
  var e6, i5 = t6.width, n6 = t6.height, r6 = t6.scale, a4 = t6.axis, o4 = t6.values, l4 = (e6 = o4, Array.isArray(e6) ? o4 : void 0) || kn2(r6, o4), s3 = "bandwidth" in r6 ? pn2(r6) : r6, c9 = "x" === a4 ? l4.map(function(t7) {
    var e7, i6;
    return { key: t7 instanceof Date ? "" + t7.valueOf() : "" + t7, x1: null != (e7 = s3(t7)) ? e7 : 0, x2: null != (i6 = s3(t7)) ? i6 : 0, y1: 0, y2: n6 };
  }) : l4.map(function(t7) {
    var e7, n7;
    return { key: t7 instanceof Date ? "" + t7.valueOf() : "" + t7, x1: 0, x2: i5, y1: null != (e7 = s3(t7)) ? e7 : 0, y2: null != (n7 = s3(t7)) ? n7 : 0 };
  });
  return c9;
};
var A2 = (0, import_react17.memo)(function(t6) {
  var e6, n6 = t6.value, r6 = t6.format, a4 = t6.lineX, s3 = t6.lineY, c9 = t6.onClick, u3 = t6.textBaseline, d4 = t6.textAnchor, x3 = t6.animatedProps, m4 = zt(), y3 = m4.axis.ticks.line, h2 = m4.axis.ticks.text, v5 = null != (e6 = null == r6 ? void 0 : r6(n6)) ? e6 : n6, b4 = (0, import_react17.useMemo)(function() {
    var t7 = { opacity: x3.opacity };
    return c9 ? { style: p2({}, t7, { cursor: "pointer" }), onClick: function(t8) {
      return c9(t8, v5);
    } } : { style: t7 };
  }, [x3.opacity, c9, v5]);
  return (0, import_jsx_runtime3.jsxs)(animated.g, p2({ transform: x3.transform }, b4, { children: [(0, import_jsx_runtime3.jsx)("line", { x1: 0, x2: a4, y1: 0, y2: s3, style: y3 }), h2.outlineWidth > 0 && (0, import_jsx_runtime3.jsx)(animated.text, { dominantBaseline: u3, textAnchor: d4, transform: x3.textTransform, style: h2, strokeWidth: 2 * h2.outlineWidth, stroke: h2.outlineColor, strokeLinejoin: "round", children: "" + v5 }), (0, import_jsx_runtime3.jsx)(animated.text, { dominantBaseline: u3, textAnchor: d4, transform: x3.textTransform, style: Mt(h2), children: "" + v5 })] }));
});
var S = function(e6) {
  var r6 = e6.axis, a4 = e6.scale, l4 = e6.x, c9 = void 0 === l4 ? 0 : l4, x3 = e6.y, m4 = void 0 === x3 ? 0 : x3, y3 = e6.length, h2 = e6.ticksPosition, T4 = e6.tickValues, S3 = e6.tickSize, W3 = void 0 === S3 ? 5 : S3, w4 = e6.tickPadding, B4 = void 0 === w4 ? 5 : w4, X4 = e6.tickRotation, Y3 = void 0 === X4 ? 0 : X4, C4 = e6.format, O6 = e6.renderTick, j3 = void 0 === O6 ? A2 : O6, z4 = e6.truncateTickAt, V3 = e6.legend, D2 = e6.legendPosition, R2 = void 0 === D2 ? "end" : D2, E3 = e6.legendOffset, q3 = void 0 === E3 ? 0 : E3, F3 = e6.onClick, L = e6.ariaHidden, N2 = zt(), H3 = N2.axis.legend.text, I = (0, import_react17.useMemo)(function() {
    return P2(C4, a4);
  }, [C4, a4]), J2 = b2({ axis: r6, scale: a4, ticksPosition: h2, tickValues: T4, tickSize: W3, tickPadding: B4, tickRotation: Y3, truncateTickAt: z4 }), G2 = J2.ticks, K2 = J2.textAlign, M3 = J2.textBaseline, Q = null;
  if (void 0 !== V3) {
    var U2, Z2 = 0, $2 = 0, _2 = 0;
    "y" === r6 ? (_2 = -90, Z2 = q3, "start" === R2 ? (U2 = "start", $2 = y3) : "middle" === R2 ? (U2 = "middle", $2 = y3 / 2) : "end" === R2 && (U2 = "end")) : ($2 = q3, "start" === R2 ? U2 = "start" : "middle" === R2 ? (U2 = "middle", Z2 = y3 / 2) : "end" === R2 && (U2 = "end", Z2 = y3)), Q = (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [H3.outlineWidth > 0 && (0, import_jsx_runtime3.jsx)("text", { transform: "translate(" + Z2 + ", " + $2 + ") rotate(" + _2 + ")", textAnchor: U2, style: p2({ dominantBaseline: "central" }, H3), strokeWidth: 2 * H3.outlineWidth, stroke: H3.outlineColor, strokeLinejoin: "round", children: V3 }), (0, import_jsx_runtime3.jsx)("text", { transform: "translate(" + Z2 + ", " + $2 + ") rotate(" + _2 + ")", textAnchor: U2, style: p2({ dominantBaseline: "central" }, H3), children: V3 })] });
  }
  var tt2 = Ur(), et2 = tt2.animate, it2 = tt2.config, nt2 = useSpring({ transform: "translate(" + c9 + "," + m4 + ")", lineX2: "x" === r6 ? y3 : 0, lineY2: "x" === r6 ? 0 : y3, config: it2, immediate: !et2 }), rt2 = (0, import_react17.useCallback)(function(t6) {
    return { opacity: 1, transform: "translate(" + t6.x + "," + t6.y + ")", textTransform: "translate(" + t6.textX + "," + t6.textY + ") rotate(" + Y3 + ")" };
  }, [Y3]), at2 = (0, import_react17.useCallback)(function(t6) {
    return { opacity: 0, transform: "translate(" + t6.x + "," + t6.y + ")", textTransform: "translate(" + t6.textX + "," + t6.textY + ") rotate(" + Y3 + ")" };
  }, [Y3]), ot2 = useTransition(G2, { keys: function(t6) {
    return t6.key;
  }, initial: rt2, from: at2, enter: rt2, update: rt2, leave: { opacity: 0 }, config: it2, immediate: !et2 });
  return (0, import_jsx_runtime3.jsxs)(animated.g, { transform: nt2.transform, "aria-hidden": L, children: [ot2(function(e7, i5, n6, r7) {
    return t4.createElement(j3, p2({ tickIndex: r7, format: I, rotate: Y3, textBaseline: M3, textAnchor: K2, truncateTickAt: z4, animatedProps: e7 }, i5, F3 ? { onClick: F3 } : {}));
  }), (0, import_jsx_runtime3.jsx)(animated.line, { style: N2.axis.domain.line, x1: 0, x2: nt2.lineX2, y1: 0, y2: nt2.lineY2 }), Q] });
};
var W2 = (0, import_react17.memo)(S);
var w2 = ["top", "right", "bottom", "left"];
var B = (0, import_react17.memo)(function(t6) {
  var e6 = t6.xScale, i5 = t6.yScale, n6 = t6.width, r6 = t6.height, a4 = { top: t6.top, right: t6.right, bottom: t6.bottom, left: t6.left };
  return (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: w2.map(function(t7) {
    var o4 = a4[t7];
    if (!o4) return null;
    var l4 = "top" === t7 || "bottom" === t7;
    return (0, import_jsx_runtime3.jsx)(W2, p2({}, o4, { axis: l4 ? "x" : "y", x: "right" === t7 ? n6 : 0, y: "bottom" === t7 ? r6 : 0, scale: l4 ? e6 : i5, length: l4 ? n6 : r6, ticksPosition: "top" === t7 || "left" === t7 ? "before" : "after", truncateTickAt: o4.truncateTickAt }), t7);
  }) });
});
var X2 = (0, import_react17.memo)(function(t6) {
  var e6 = t6.animatedProps, i5 = zt();
  return (0, import_jsx_runtime3.jsx)(animated.line, p2({}, e6, i5.grid.line));
});
var Y = (0, import_react17.memo)(function(t6) {
  var e6 = t6.lines, i5 = Ur(), n6 = i5.animate, a4 = i5.config, o4 = useTransition(e6, { keys: function(t7) {
    return t7.key;
  }, initial: function(t7) {
    return { opacity: 1, x1: t7.x1, x2: t7.x2, y1: t7.y1, y2: t7.y2 };
  }, from: function(t7) {
    return { opacity: 0, x1: t7.x1, x2: t7.x2, y1: t7.y1, y2: t7.y2 };
  }, enter: function(t7) {
    return { opacity: 1, x1: t7.x1, x2: t7.x2, y1: t7.y1, y2: t7.y2 };
  }, update: function(t7) {
    return { opacity: 1, x1: t7.x1, x2: t7.x2, y1: t7.y1, y2: t7.y2 };
  }, leave: { opacity: 0 }, config: a4, immediate: !n6 });
  return (0, import_jsx_runtime3.jsx)("g", { children: o4(function(t7, e7) {
    return (0, import_react17.createElement)(X2, p2({}, e7, { key: e7.key, animatedProps: t7 }));
  }) });
});
var C3 = (0, import_react17.memo)(function(t6) {
  var e6 = t6.width, n6 = t6.height, r6 = t6.xScale, a4 = t6.yScale, o4 = t6.xValues, l4 = t6.yValues, s3 = (0, import_react17.useMemo)(function() {
    return !!r6 && T2({ width: e6, height: n6, scale: r6, axis: "x", values: o4 });
  }, [r6, o4, e6, n6]), c9 = (0, import_react17.useMemo)(function() {
    return !!a4 && T2({ width: e6, height: n6, scale: a4, axis: "y", values: l4 });
  }, [n6, e6, a4, l4]);
  return (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [s3 && (0, import_jsx_runtime3.jsx)(Y, { lines: s3 }), c9 && (0, import_jsx_runtime3.jsx)(Y, { lines: c9 })] });
});
var O3 = function(t6, e6) {
  var i5, n6 = e6.axis, r6 = e6.scale, a4 = e6.x, o4 = void 0 === a4 ? 0 : a4, l4 = e6.y, s3 = void 0 === l4 ? 0 : l4, f3 = e6.length, u3 = e6.ticksPosition, d4 = e6.tickValues, x3 = e6.tickSize, m4 = void 0 === x3 ? 5 : x3, y3 = e6.tickPadding, h2 = void 0 === y3 ? 5 : y3, k4 = e6.tickRotation, g4 = void 0 === k4 ? 0 : k4, v5 = e6.format, p4 = e6.legend, P3 = e6.legendPosition, T4 = void 0 === P3 ? "end" : P3, A4 = e6.legendOffset, S3 = void 0 === A4 ? 0 : A4, W3 = e6.theme, w4 = b2({ axis: n6, scale: r6, ticksPosition: u3, tickValues: d4, tickSize: m4, tickPadding: h2, tickRotation: g4, engine: "canvas" }), B4 = w4.ticks, X4 = w4.textAlign, Y3 = w4.textBaseline;
  t6.save(), t6.translate(o4, s3), t6.textAlign = X4, t6.textBaseline = Y3;
  var C4 = W3.axis.ticks.text;
  t6.font = (C4.fontWeight ? C4.fontWeight + " " : "") + C4.fontSize + "px " + C4.fontFamily, (null != (i5 = W3.axis.domain.line.strokeWidth) ? i5 : 0) > 0 && (t6.lineWidth = Number(W3.axis.domain.line.strokeWidth), t6.lineCap = "square", W3.axis.domain.line.stroke && (t6.strokeStyle = W3.axis.domain.line.stroke), t6.beginPath(), t6.moveTo(0, 0), t6.lineTo("x" === n6 ? f3 : 0, "x" === n6 ? 0 : f3), t6.stroke());
  var O6 = "function" == typeof v5 ? v5 : function(t7) {
    return "" + t7;
  };
  if (B4.forEach(function(e7) {
    var i6;
    (null != (i6 = W3.axis.ticks.line.strokeWidth) ? i6 : 0) > 0 && (t6.lineWidth = Number(W3.axis.ticks.line.strokeWidth), t6.lineCap = "square", W3.axis.ticks.line.stroke && (t6.strokeStyle = W3.axis.ticks.line.stroke), t6.beginPath(), t6.moveTo(e7.x, e7.y), t6.lineTo(e7.x + e7.lineX, e7.y + e7.lineY), t6.stroke());
    var n7 = O6(e7.value);
    t6.save(), t6.translate(e7.x + e7.textX, e7.y + e7.textY), t6.rotate(Ht(g4)), C4.outlineWidth > 0 && (t6.strokeStyle = C4.outlineColor, t6.lineWidth = 2 * C4.outlineWidth, t6.lineJoin = "round", t6.strokeText("" + n7, 0, 0)), W3.axis.ticks.text.fill && (t6.fillStyle = C4.fill), t6.fillText("" + n7, 0, 0), t6.restore();
  }), void 0 !== p4) {
    var j3 = 0, z4 = 0, V3 = 0, D2 = "center";
    "y" === n6 ? (V3 = -90, j3 = S3, "start" === T4 ? (D2 = "start", z4 = f3) : "middle" === T4 ? (D2 = "center", z4 = f3 / 2) : "end" === T4 && (D2 = "end")) : (z4 = S3, "start" === T4 ? D2 = "start" : "middle" === T4 ? (D2 = "center", j3 = f3 / 2) : "end" === T4 && (D2 = "end", j3 = f3)), t6.translate(j3, z4), t6.rotate(Ht(V3)), t6.font = (W3.axis.legend.text.fontWeight ? W3.axis.legend.text.fontWeight + " " : "") + W3.axis.legend.text.fontSize + "px " + W3.axis.legend.text.fontFamily, W3.axis.legend.text.fill && (t6.fillStyle = W3.axis.legend.text.fill), t6.textAlign = D2, t6.textBaseline = "middle", t6.fillText(p4, 0, 0);
  }
  t6.restore();
};

// node_modules/@nivo/legends/dist/nivo-legends.es.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime());

// node_modules/@nivo/colors/dist/nivo-colors.es.js
var import_react18 = __toESM(require_react());
var import_get2 = __toESM(require_get());
var import_isPlainObject2 = __toESM(require_isPlainObject());
var import_prop_types2 = __toESM(require_prop_types());
function qe() {
  return qe = Object.assign ? Object.assign.bind() : function(e6) {
    for (var r6 = 1; r6 < arguments.length; r6++) {
      var n6 = arguments[r6];
      for (var t6 in n6) Object.prototype.hasOwnProperty.call(n6, t6) && (e6[t6] = n6[t6]);
    }
    return e6;
  }, qe.apply(this, arguments);
}
var Re = { nivo: ["#e8c1a0", "#f47560", "#f1e15b", "#e8a838", "#61cdbb", "#97e3d5"], category10: category10_default, accent: Accent_default, dark2: Dark2_default, paired: Paired_default, pastel1: Pastel1_default, pastel2: Pastel2_default, set1: Set1_default, set2: Set2_default, set3: Set3_default, tableau10: Tableau10_default };
var Ve = Object.keys(Re);
var Pe = { brown_blueGreen: scheme, purpleRed_green: scheme2, pink_yellowGreen: scheme3, purple_orange: scheme4, red_blue: scheme5, red_grey: scheme6, red_yellow_blue: scheme7, red_yellow_green: scheme8, spectral: scheme9 };
var Te = Object.keys(Pe);
var Ue = { brown_blueGreen: BrBG_default, purpleRed_green: PRGn_default, pink_yellowGreen: PiYG_default, purple_orange: PuOr_default, red_blue: RdBu_default, red_grey: RdGy_default, red_yellow_blue: RdYlBu_default, red_yellow_green: RdYlGn_default, spectral: Spectral_default };
var De = { blues: scheme22, greens: scheme23, greys: scheme24, oranges: scheme27, purples: scheme25, reds: scheme26, blue_green: scheme10, blue_purple: scheme11, green_blue: scheme12, orange_red: scheme13, purple_blue_green: scheme14, purple_blue: scheme15, purple_red: scheme16, red_purple: scheme17, yellow_green_blue: scheme18, yellow_green: scheme19, yellow_orange_brown: scheme20, yellow_orange_red: scheme21 };
var Me = Object.keys(De);
var $e = { blues: Blues_default, greens: Greens_default, greys: Greys_default, oranges: Oranges_default, purples: Purples_default, reds: Reds_default, turbo: turbo_default, viridis: viridis_default, inferno, magma, plasma, cividis: cividis_default, warm, cool, cubehelixDefault: cubehelix_default, blue_green: BuGn_default, blue_purple: BuPu_default, green_blue: GnBu_default, orange_red: OrRd_default, purple_blue_green: PuBuGn_default, purple_blue: PuBu_default, purple_red: PuRd_default, red_purple: RdPu_default, yellow_green_blue: YlGnBu_default, yellow_green: YlGn_default, yellow_orange_brown: YlOrBr_default, yellow_orange_red: YlOrRd_default };
var Be = qe({}, Re, Pe, De);
var Fe = Object.keys(Be);
var He = function(e6) {
  return Ve.includes(e6);
};
var Je = function(e6) {
  return Te.includes(e6);
};
var Ke = function(e6) {
  return Me.includes(e6);
};
var Le = { rainbow: rainbow_default, sinebow: sinebow_default };
var Ne = qe({}, Ue, $e, Le);
var Qe2 = Object.keys(Ne);
var Ye = import_prop_types2.default.oneOfType([import_prop_types2.default.string, import_prop_types2.default.func, import_prop_types2.default.shape({ theme: import_prop_types2.default.string.isRequired }), import_prop_types2.default.shape({ from: import_prop_types2.default.string.isRequired, modifiers: import_prop_types2.default.arrayOf(import_prop_types2.default.array) })]);
var fr = function(e6, r6) {
  if ("function" == typeof e6) return e6;
  var n6 = "function" == typeof r6 ? r6 : function(e7) {
    return (0, import_get2.default)(e7, r6);
  };
  if (Array.isArray(e6)) {
    var t6 = ordinal(e6), o4 = function(e7) {
      return t6(n6(e7));
    };
    return o4.scale = t6, o4;
  }
  if ((0, import_isPlainObject2.default)(e6)) {
    if (function(e7) {
      return void 0 !== e7.datum;
    }(e6)) return function(r7) {
      return (0, import_get2.default)(r7, e6.datum);
    };
    if (function(e7) {
      return void 0 !== e7.scheme;
    }(e6)) {
      if (He(e6.scheme)) {
        var i5 = ordinal(Be[e6.scheme]), u3 = function(e7) {
          return i5(n6(e7));
        };
        return u3.scale = i5, u3;
      }
      if (Je(e6.scheme)) {
        if (void 0 !== e6.size && (e6.size < 3 || e6.size > 11)) throw new Error("Invalid size '" + e6.size + "' for diverging color scheme '" + e6.scheme + "', must be between 3~11");
        var a4 = ordinal(Be[e6.scheme][e6.size || 11]), l4 = function(e7) {
          return a4(n6(e7));
        };
        return l4.scale = a4, l4;
      }
      if (Ke(e6.scheme)) {
        if (void 0 !== e6.size && (e6.size < 3 || e6.size > 9)) throw new Error("Invalid size '" + e6.size + "' for sequential color scheme '" + e6.scheme + "', must be between 3~9");
        var c9 = ordinal(Be[e6.scheme][e6.size || 9]), s3 = function(e7) {
          return c9(n6(e7));
        };
        return s3.scale = c9, s3;
      }
    }
    throw new Error("Invalid colors, when using an object, you should either pass a 'datum' or a 'scheme' property");
  }
  return function() {
    return e6;
  };
};
var pr = function(e6, r6) {
  return (0, import_react18.useMemo)(function() {
    return fr(e6, r6);
  }, [e6, r6]);
};

// node_modules/@nivo/legends/dist/nivo-legends.es.js
var c8 = __toESM(require_react());
var import_react19 = __toESM(require_react());
var f2 = function(e6) {
  var i5 = e6.x, n6 = e6.y, o4 = e6.size, r6 = e6.fill, l4 = e6.opacity, a4 = void 0 === l4 ? 1 : l4, c9 = e6.borderWidth, d4 = void 0 === c9 ? 0 : c9, s3 = e6.borderColor;
  return (0, import_jsx_runtime4.jsx)("circle", { r: o4 / 2, cx: i5 + o4 / 2, cy: n6 + o4 / 2, fill: r6, opacity: a4, strokeWidth: d4, stroke: void 0 === s3 ? "transparent" : s3, style: { pointerEvents: "none" } });
};
var m3 = function(e6) {
  var i5 = e6.x, n6 = e6.y, o4 = e6.size, r6 = e6.fill, l4 = e6.opacity, a4 = void 0 === l4 ? 1 : l4, c9 = e6.borderWidth, d4 = void 0 === c9 ? 0 : c9, s3 = e6.borderColor;
  return (0, import_jsx_runtime4.jsx)("g", { transform: "translate(" + i5 + "," + n6 + ")", children: (0, import_jsx_runtime4.jsx)("path", { d: "\n                    M" + o4 / 2 + " 0\n                    L" + 0.8 * o4 + " " + o4 / 2 + "\n                    L" + o4 / 2 + " " + o4 + "\n                    L" + 0.2 * o4 + " " + o4 / 2 + "\n                    L" + o4 / 2 + " 0\n                ", fill: r6, opacity: a4, strokeWidth: d4, stroke: void 0 === s3 ? "transparent" : s3, style: { pointerEvents: "none" } }) });
};
var v4 = function(e6) {
  var i5 = e6.x, n6 = e6.y, o4 = e6.size, r6 = e6.fill, l4 = e6.opacity, a4 = void 0 === l4 ? 1 : l4, c9 = e6.borderWidth, d4 = void 0 === c9 ? 0 : c9, s3 = e6.borderColor;
  return (0, import_jsx_runtime4.jsx)("rect", { x: i5, y: n6, fill: r6, opacity: a4, strokeWidth: d4, stroke: void 0 === s3 ? "transparent" : s3, width: o4, height: o4, style: { pointerEvents: "none" } });
};
var u2 = function(e6) {
  var i5 = e6.x, n6 = e6.y, o4 = e6.size, r6 = e6.fill, l4 = e6.opacity, a4 = void 0 === l4 ? 1 : l4, c9 = e6.borderWidth, d4 = void 0 === c9 ? 0 : c9, s3 = e6.borderColor;
  return (0, import_jsx_runtime4.jsx)("g", { transform: "translate(" + i5 + "," + n6 + ")", children: (0, import_jsx_runtime4.jsx)("path", { d: "\n                M" + o4 / 2 + " 0\n                L" + o4 + " " + o4 + "\n                L0 " + o4 + "\n                L" + o4 / 2 + " 0\n            ", fill: r6, opacity: a4, strokeWidth: d4, stroke: void 0 === s3 ? "transparent" : s3, style: { pointerEvents: "none" } }) });
};
function p3() {
  return p3 = Object.assign ? Object.assign.bind() : function(t6) {
    for (var e6 = 1; e6 < arguments.length; e6++) {
      var i5 = arguments[e6];
      for (var n6 in i5) Object.prototype.hasOwnProperty.call(i5, n6) && (t6[n6] = i5[n6]);
    }
    return t6;
  }, p3.apply(this, arguments);
}
var k3 = { top: 0, right: 0, bottom: 0, left: 0 };
var x2 = function(t6) {
  var e6, i5 = t6.direction, n6 = t6.itemsSpacing, o4 = t6.padding, r6 = t6.itemCount, l4 = t6.itemWidth, a4 = t6.itemHeight;
  if ("number" != typeof o4 && ("object" != typeof (e6 = o4) || Array.isArray(e6) || null === e6)) throw new Error("Invalid property padding, must be one of: number, object");
  var c9 = "number" == typeof o4 ? { top: o4, right: o4, bottom: o4, left: o4 } : p3({}, k3, o4), d4 = c9.left + c9.right, s3 = c9.top + c9.bottom, h2 = l4 + d4, g4 = a4 + s3, f3 = (r6 - 1) * n6;
  return "row" === i5 ? h2 = l4 * r6 + f3 + d4 : "column" === i5 && (g4 = a4 * r6 + f3 + s3), { width: h2, height: g4, padding: c9 };
};
var b3 = function(t6) {
  var e6 = t6.anchor, i5 = t6.translateX, n6 = t6.translateY, o4 = t6.containerWidth, r6 = t6.containerHeight, l4 = t6.width, a4 = t6.height, c9 = i5, d4 = n6;
  switch (e6) {
    case "top":
      c9 += (o4 - l4) / 2;
      break;
    case "top-right":
      c9 += o4 - l4;
      break;
    case "right":
      c9 += o4 - l4, d4 += (r6 - a4) / 2;
      break;
    case "bottom-right":
      c9 += o4 - l4, d4 += r6 - a4;
      break;
    case "bottom":
      c9 += (o4 - l4) / 2, d4 += r6 - a4;
      break;
    case "bottom-left":
      d4 += r6 - a4;
      break;
    case "left":
      d4 += (r6 - a4) / 2;
      break;
    case "center":
      c9 += (o4 - l4) / 2, d4 += (r6 - a4) / 2;
  }
  return { x: c9, y: d4 };
};
var S2 = function(t6) {
  var e6, i5, n6, o4, r6, l4, a4 = t6.direction, c9 = t6.justify, d4 = t6.symbolSize, s3 = t6.symbolSpacing, h2 = t6.width, g4 = t6.height;
  switch (a4) {
    case "left-to-right":
      e6 = 0, i5 = (g4 - d4) / 2, o4 = g4 / 2, l4 = "central", c9 ? (n6 = h2, r6 = "end") : (n6 = d4 + s3, r6 = "start");
      break;
    case "right-to-left":
      e6 = h2 - d4, i5 = (g4 - d4) / 2, o4 = g4 / 2, l4 = "central", c9 ? (n6 = 0, r6 = "start") : (n6 = h2 - d4 - s3, r6 = "end");
      break;
    case "top-to-bottom":
      e6 = (h2 - d4) / 2, i5 = 0, n6 = h2 / 2, r6 = "middle", c9 ? (o4 = g4, l4 = "alphabetic") : (o4 = d4 + s3, l4 = "text-before-edge");
      break;
    case "bottom-to-top":
      e6 = (h2 - d4) / 2, i5 = g4 - d4, n6 = h2 / 2, r6 = "middle", c9 ? (o4 = 0, l4 = "text-before-edge") : (o4 = g4 - d4 - s3, l4 = "alphabetic");
  }
  return { symbolX: e6, symbolY: i5, labelX: n6, labelY: o4, labelAnchor: r6, labelAlignment: l4 };
};
var w3 = { circle: f2, diamond: m3, square: v4, triangle: u2 };
var X3 = function(i5) {
  var n6, l4, a4, d4, g4, f3, m4, v5, u3, y3, k4, x3 = i5.x, b4 = i5.y, A4 = i5.width, W3 = i5.height, z4 = i5.data, C4 = i5.direction, X4 = void 0 === C4 ? "left-to-right" : C4, Y3 = i5.justify, O6 = void 0 !== Y3 && Y3, B4 = i5.textColor, H3 = i5.background, E3 = void 0 === H3 ? "transparent" : H3, j3 = i5.opacity, L = void 0 === j3 ? 1 : j3, M3 = i5.symbolShape, F3 = void 0 === M3 ? "square" : M3, T4 = i5.symbolSize, P3 = void 0 === T4 ? 16 : T4, V3 = i5.symbolSpacing, R2 = void 0 === V3 ? 8 : V3, D2 = i5.symbolBorderWidth, q3 = void 0 === D2 ? 0 : D2, G2 = i5.symbolBorderColor, I = void 0 === G2 ? "transparent" : G2, N2 = i5.onClick, _2 = i5.onMouseEnter, J2 = i5.onMouseLeave, K2 = i5.toggleSerie, Q = i5.effects, U2 = (0, import_react19.useState)({}), Z2 = U2[0], $2 = U2[1], tt2 = zt(), et2 = (0, import_react19.useCallback)(function(t6) {
    if (Q) {
      var e6 = Q.filter(function(t7) {
        return "hover" === t7.on;
      }).reduce(function(t7, e7) {
        return p3({}, t7, e7.style);
      }, {});
      $2(e6);
    }
    null == _2 || _2(z4, t6);
  }, [_2, z4, Q]), it2 = (0, import_react19.useCallback)(function(t6) {
    if (Q) {
      var e6 = Q.filter(function(t7) {
        return "hover" !== t7.on;
      }).reduce(function(t7, e7) {
        return p3({}, t7, e7.style);
      }, {});
      $2(e6);
    }
    null == J2 || J2(z4, t6);
  }, [J2, z4, Q]), nt2 = S2({ direction: X4, justify: O6, symbolSize: null != (n6 = Z2.symbolSize) ? n6 : P3, symbolSpacing: R2, width: A4, height: W3 }), ot2 = nt2.symbolX, rt2 = nt2.symbolY, lt2 = nt2.labelX, at2 = nt2.labelY, ct2 = nt2.labelAnchor, dt2 = nt2.labelAlignment, st = [N2, _2, J2, K2].some(function(t6) {
    return void 0 !== t6;
  }), ht2 = "function" == typeof F3 ? F3 : w3[F3];
  return (0, import_jsx_runtime4.jsxs)("g", { transform: "translate(" + x3 + "," + b4 + ")", style: { opacity: null != (l4 = Z2.itemOpacity) ? l4 : L }, children: [(0, import_jsx_runtime4.jsx)("rect", { width: A4, height: W3, fill: null != (a4 = Z2.itemBackground) ? a4 : E3, style: { cursor: st ? "pointer" : "auto" }, onClick: function(t6) {
    null == N2 || N2(z4, t6), null == K2 || K2(z4.id);
  }, onMouseEnter: et2, onMouseLeave: it2 }), c8.createElement(ht2, p3({ id: z4.id, x: ot2, y: rt2, size: null != (d4 = Z2.symbolSize) ? d4 : P3, fill: null != (g4 = null != (f3 = z4.fill) ? f3 : z4.color) ? g4 : "black", borderWidth: null != (m4 = Z2.symbolBorderWidth) ? m4 : q3, borderColor: null != (v5 = Z2.symbolBorderColor) ? v5 : I }, z4.hidden ? tt2.legends.hidden.symbol : void 0)), (0, import_jsx_runtime4.jsx)("text", { textAnchor: ct2, style: p3({}, Mt(tt2.legends.text), { fill: null != (u3 = null != (y3 = null != (k4 = Z2.itemTextColor) ? k4 : B4) ? y3 : tt2.legends.text.fill) ? u3 : "black", dominantBaseline: dt2, pointerEvents: "none", userSelect: "none" }, z4.hidden ? tt2.legends.hidden.text : void 0), x: lt2, y: at2, children: z4.label })] });
};
var Y2 = function(e6) {
  var i5 = e6.data, n6 = e6.x, o4 = e6.y, r6 = e6.direction, l4 = e6.padding, a4 = void 0 === l4 ? 0 : l4, c9 = e6.justify, d4 = e6.effects, s3 = e6.itemWidth, h2 = e6.itemHeight, g4 = e6.itemDirection, f3 = void 0 === g4 ? "left-to-right" : g4, m4 = e6.itemsSpacing, v5 = void 0 === m4 ? 0 : m4, u3 = e6.itemTextColor, p4 = e6.itemBackground, y3 = void 0 === p4 ? "transparent" : p4, k4 = e6.itemOpacity, b4 = void 0 === k4 ? 1 : k4, S3 = e6.symbolShape, A4 = e6.symbolSize, W3 = e6.symbolSpacing, z4 = e6.symbolBorderWidth, C4 = e6.symbolBorderColor, w4 = e6.onClick, Y3 = e6.onMouseEnter, O6 = e6.onMouseLeave, B4 = e6.toggleSerie, H3 = x2({ itemCount: i5.length, itemWidth: s3, itemHeight: h2, itemsSpacing: v5, direction: r6, padding: a4 }).padding, E3 = "row" === r6 ? s3 + v5 : 0, j3 = "column" === r6 ? h2 + v5 : 0;
  return (0, import_jsx_runtime4.jsx)("g", { transform: "translate(" + n6 + "," + o4 + ")", children: i5.map(function(e7, i6) {
    return (0, import_jsx_runtime4.jsx)(X3, { data: e7, x: i6 * E3 + H3.left, y: i6 * j3 + H3.top, width: s3, height: h2, direction: f3, justify: c9, effects: d4, textColor: u3, background: y3, opacity: b4, symbolShape: S3, symbolSize: A4, symbolSpacing: W3, symbolBorderWidth: z4, symbolBorderColor: C4, onClick: w4, onMouseEnter: Y3, onMouseLeave: O6, toggleSerie: B4 }, i6);
  }) });
};
var O4 = function(e6) {
  var i5 = e6.data, n6 = e6.containerWidth, o4 = e6.containerHeight, r6 = e6.translateX, l4 = void 0 === r6 ? 0 : r6, a4 = e6.translateY, c9 = void 0 === a4 ? 0 : a4, d4 = e6.anchor, s3 = e6.direction, h2 = e6.padding, g4 = void 0 === h2 ? 0 : h2, f3 = e6.justify, m4 = e6.itemsSpacing, v5 = void 0 === m4 ? 0 : m4, u3 = e6.itemWidth, p4 = e6.itemHeight, y3 = e6.itemDirection, k4 = e6.itemTextColor, S3 = e6.itemBackground, A4 = e6.itemOpacity, W3 = e6.symbolShape, z4 = e6.symbolSize, C4 = e6.symbolSpacing, w4 = e6.symbolBorderWidth, X4 = e6.symbolBorderColor, O6 = e6.onClick, B4 = e6.onMouseEnter, H3 = e6.onMouseLeave, E3 = e6.toggleSerie, j3 = e6.effects, L = x2({ itemCount: i5.length, itemsSpacing: v5, itemWidth: u3, itemHeight: p4, direction: s3, padding: g4 }), M3 = L.width, F3 = L.height, T4 = b3({ anchor: d4, translateX: l4, translateY: c9, containerWidth: n6, containerHeight: o4, width: M3, height: F3 }), P3 = T4.x, V3 = T4.y;
  return (0, import_jsx_runtime4.jsx)(Y2, { data: i5, x: P3, y: V3, direction: s3, padding: g4, justify: f3, effects: j3, itemsSpacing: v5, itemWidth: u3, itemHeight: p4, itemDirection: y3, itemTextColor: k4, itemBackground: S3, itemOpacity: A4, symbolShape: W3, symbolSize: z4, symbolSpacing: C4, symbolBorderWidth: w4, symbolBorderColor: X4, onClick: O6, onMouseEnter: B4, onMouseLeave: H3, toggleSerie: "boolean" == typeof E3 ? void 0 : E3 });
};
var B2 = { start: "left", middle: "center", end: "right" };
var H = function(t6, e6) {
  var i5 = e6.data, n6 = e6.containerWidth, o4 = e6.containerHeight, r6 = e6.translateX, l4 = void 0 === r6 ? 0 : r6, a4 = e6.translateY, c9 = void 0 === a4 ? 0 : a4, d4 = e6.anchor, s3 = e6.direction, h2 = e6.padding, g4 = void 0 === h2 ? 0 : h2, f3 = e6.justify, m4 = void 0 !== f3 && f3, v5 = e6.itemsSpacing, u3 = void 0 === v5 ? 0 : v5, p4 = e6.itemWidth, y3 = e6.itemHeight, k4 = e6.itemDirection, A4 = void 0 === k4 ? "left-to-right" : k4, W3 = e6.itemTextColor, z4 = e6.symbolSize, C4 = void 0 === z4 ? 16 : z4, w4 = e6.symbolSpacing, X4 = void 0 === w4 ? 8 : w4, Y3 = e6.theme, O6 = x2({ itemCount: i5.length, itemWidth: p4, itemHeight: y3, itemsSpacing: u3, direction: s3, padding: g4 }), H3 = O6.width, E3 = O6.height, j3 = O6.padding, L = b3({ anchor: d4, translateX: l4, translateY: c9, containerWidth: n6, containerHeight: o4, width: H3, height: E3 }), M3 = L.x, F3 = L.y, T4 = "row" === s3 ? p4 + u3 : 0, P3 = "column" === s3 ? y3 + u3 : 0;
  t6.save(), t6.translate(M3, F3), t6.font = Y3.legends.text.fontSize + "px " + (Y3.legends.text.fontFamily || "sans-serif"), i5.forEach(function(e7, i6) {
    var n7, o5, r7 = i6 * T4 + j3.left, l5 = i6 * P3 + j3.top, a5 = S2({ direction: A4, justify: m4, symbolSize: C4, symbolSpacing: X4, width: p4, height: y3 }), c10 = a5.symbolX, d5 = a5.symbolY, s4 = a5.labelX, h3 = a5.labelY, g5 = a5.labelAnchor, f4 = a5.labelAlignment;
    t6.fillStyle = null != (n7 = e7.color) ? n7 : "black", t6.fillRect(r7 + c10, l5 + d5, C4, C4), t6.textAlign = B2[g5], "central" === f4 && (t6.textBaseline = "middle"), t6.fillStyle = null != (o5 = null != W3 ? W3 : Y3.legends.text.fill) ? o5 : "black", t6.fillText(String(e7.label), r7 + s4, l5 + h3);
  }), t6.restore();
};

// node_modules/@nivo/parallel-coordinates/dist/nivo-parallel-coordinates.es.js
function z3() {
  return z3 = Object.assign ? Object.assign.bind() : function(e6) {
    for (var t6 = 1; t6 < arguments.length; t6++) {
      var i5 = arguments[t6];
      for (var r6 in i5) Object.prototype.hasOwnProperty.call(i5, r6) && (e6[r6] = i5[r6]);
    }
    return e6;
  }, z3.apply(this, arguments);
}
function B3(e6, t6) {
  if (null == e6) return {};
  var i5, r6, n6 = {}, o4 = Object.keys(e6);
  for (r6 = 0; r6 < o4.length; r6++) i5 = o4[r6], t6.indexOf(i5) >= 0 || (n6[i5] = e6[i5]);
  return n6;
}
function D(e6, t6) {
  (null == t6 || t6 > e6.length) && (t6 = e6.length);
  for (var i5 = 0, r6 = new Array(t6); i5 < t6; i5++) r6[i5] = e6[i5];
  return r6;
}
function O5(e6, t6) {
  var i5 = "undefined" != typeof Symbol && e6[Symbol.iterator] || e6["@@iterator"];
  if (i5) return (i5 = i5.call(e6)).next.bind(i5);
  if (Array.isArray(e6) || (i5 = function(e7, t7) {
    if (e7) {
      if ("string" == typeof e7) return D(e7, t7);
      var i6 = Object.prototype.toString.call(e7).slice(8, -1);
      return "Object" === i6 && e7.constructor && (i6 = e7.constructor.name), "Map" === i6 || "Set" === i6 ? Array.from(e7) : "Arguments" === i6 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(i6) ? D(e7, t7) : void 0;
    }
  }(e6)) || t6 && e6 && "number" == typeof e6.length) {
    i5 && (e6 = i5);
    var r6 = 0;
    return function() {
      return r6 >= e6.length ? { done: true } : { done: false, value: e6[r6++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
var M2;
var j2 = { layout: "horizontal", curve: "linear", role: "img", colors: { scheme: "category10" }, lineWidth: 2, lineOpacity: 0.5, axesTicksPosition: "after", isInteractive: true, tooltip: function(t6) {
  var r6 = t6.datum, n6 = t6.variables;
  return (0, import_jsx_runtime5.jsx)(C2, { rows: n6.map(function(e6) {
    return [e6.label || e6.id, (0, import_jsx_runtime5.jsx)("strong", { children: r6.data[e6.value] })];
  }) });
}, legends: [], animate: true, motionConfig: "gentle" };
var A3 = z3({}, j2, { layers: ["lines", "axes", "legends"] });
var G = z3({}, j2, { layers: ["lines", "axes", "legends"], pixelRatio: "undefined" != typeof window && null != (M2 = window.devicePixelRatio) ? M2 : 1 });
var R = function(e6) {
  var t6 = e6.width, i5 = e6.height, a4 = e6.data, l4 = e6.variables, d4 = e6.groupBy, u3 = e6.groups, s3 = e6.layout, v5 = void 0 === s3 ? j2.layout : s3, h2 = e6.curve, f3 = void 0 === h2 ? j2.curve : h2, p4 = e6.colors, g4 = void 0 === p4 ? j2.colors : p4, m4 = e6.forwardLegendData, y3 = pr(g4, "id"), b4 = (0, import_react20.useMemo)(function() {
    if (void 0 !== d4) return function(e7) {
      var t7, i6 = e7.data, r6 = e7.groupBy, n6 = e7.predefinedGroups, o4 = e7.getColor;
      if (void 0 !== n6) t7 = n6.map(function(e8) {
        return { id: e8.id, label: e8.label || e8.id, color: o4(e8) };
      });
      else {
        for (var a5, l5 = /* @__PURE__ */ new Set(), d5 = O5(i6); !(a5 = d5()).done; ) {
          var c9 = a5.value;
          l5.add(c9[r6]);
        }
        t7 = Array.from(l5, function(e8) {
          var t8 = { id: e8, label: e8 };
          return z3({}, t8, { color: o4(t8) });
        });
      }
      return t7;
    }({ data: a4, groupBy: d4, predefinedGroups: u3, getColor: y3 });
  }, [a4, d4, u3, y3]), x3 = (0, import_react20.useMemo)(function() {
    var e7 = l4.map(function(e8) {
      return e8.id;
    });
    return Z(point().range("horizontal" === v5 ? [0, t6] : [i5, 0]).domain(e7));
  }, [l4, v5, t6, i5]), P3 = (0, import_react20.useMemo)(function() {
    return function(e7) {
      var t7 = e7.width, i6 = e7.height, r6 = e7.data, n6 = e7.variables, o4 = "horizontal" === e7.layout ? [i6, 0] : [0, t7];
      return n6.map(function(e8) {
        var t8 = r6.map(function(t9) {
          return t9[e8.value];
        }), i7 = void 0 !== e8.min && "auto" !== e8.min ? e8.min : Math.min.apply(Math, t8), n7 = void 0 !== e8.max && "auto" !== e8.max ? e8.max : Math.max.apply(Math, t8), a5 = X(linear().rangeRound(o4).domain([i7, n7]));
        return z3({}, e8, { scale: a5 });
      });
    }({ width: t6, height: i5, data: a4, variables: l4, layout: v5 });
  }, [t6, i5, a4, l4, v5]), L = (0, import_react20.useMemo)(function() {
    return function(e7) {
      var t7 = e7.data, i6 = e7.groupBy, r6 = e7.groups, n6 = e7.variablesScale, o4 = e7.variablesWithScale, a5 = e7.layout, l5 = e7.getColor;
      return t7.map(function(e8, t8) {
        var d5 = o4.map(function(t9) {
          return ["horizontal" === a5 ? n6(t9.id) : t9.scale(e8[t9.value]), "horizontal" === a5 ? t9.scale(e8[t9.value]) : n6(t9.id)];
        }), c9 = void 0;
        if (void 0 !== i6) {
          var u4 = e8[i6];
          c9 = null == r6 ? void 0 : r6.find(function(e9) {
            return e9.id === u4;
          });
        }
        var s4 = { id: e8.id, index: t8, points: d5, data: e8, color: c9 ? c9.color : l5(e8) };
        return c9 ? z3({}, s4, { group: c9 }) : s4;
      });
    }({ data: a4, groupBy: d4, groups: b4, variablesScale: x3, variablesWithScale: P3, layout: v5, getColor: y3 });
  }, [a4, d4, b4, x3, P3, v5, y3]), B4 = (0, import_react20.useMemo)(function() {
    return b4 ? b4.map(function(e7) {
      return { id: e7.id, label: e7.label || e7.id, color: e7.color, data: e7 };
    }) : L.map(function(e7) {
      return { id: e7.id, label: e7.id, color: e7.color, data: e7 };
    });
  }, [b4, L]), D2 = (0, import_react20.useRef)(m4);
  (0, import_react20.useEffect)(function() {
    "function" == typeof D2.current && D2.current(B4);
  }, [D2, B4]);
  var M3 = (0, import_react20.useMemo)(function() {
    return line_default().curve(lt(f3));
  }, [f3]), A4 = (0, import_react20.useMemo)(function() {
    return { computedData: L, variables: l4, lineGenerator: M3 };
  }, [L, l4, M3]);
  return { variablesScale: x3, variablesWithScale: P3, computedData: L, lineGenerator: M3, legendData: B4, customLayerContext: A4 };
};
var E2 = function(e6) {
  var r6 = e6.datum, n6 = e6.variables, o4 = e6.lineGenerator, d4 = e6.lineWidth, c9 = e6.opacity, v5 = e6.tooltip, h2 = e6.testIdPrefix, f3 = k(), p4 = f3.showTooltipFromEvent, g4 = f3.hideTooltip, m4 = (0, import_react20.useCallback)(function(e7) {
    p4((0, import_react20.createElement)(v5, { datum: r6, variables: n6 }), e7);
  }, [p4, r6, n6]), y3 = Ur(), b4 = y3.animate, x3 = y3.config, w4 = Fr(o4(r6.points)), W3 = useSpring({ color: r6.color, opacity: c9, config: x3, immediate: !b4 });
  return (0, import_jsx_runtime5.jsx)(animated.path, { d: w4, stroke: W3.color, strokeWidth: d4, strokeLinecap: "round", opacity: W3.opacity, fill: "none", onMouseEnter: m4, onMouseMove: m4, onMouseLeave: g4, "data-test-id": h2 ? h2 + ".line_" + r6.id : void 0 });
};
var T3 = ["isInteractive", "animate", "motionConfig", "theme", "renderWrapper"];
var H2 = function(e6) {
  var t6 = e6.data, r6 = e6.variables, n6 = e6.groupBy, o4 = e6.groups, a4 = e6.width, c9 = e6.height, u3 = e6.margin, s3 = e6.layout, v5 = void 0 === s3 ? A3.layout : s3, p4 = e6.curve, g4 = void 0 === p4 ? A3.curve : p4, y3 = e6.axesTicksPosition, x3 = e6.lineWidth, w4 = void 0 === x3 ? A3.lineWidth : x3, W3 = e6.lineOpacity, k4 = void 0 === W3 ? A3.lineOpacity : W3, S3 = e6.colors, C4 = void 0 === S3 ? A3.colors : S3, I = e6.layers, P3 = void 0 === I ? A3.layers : I, L = e6.tooltip, B4 = void 0 === L ? A3.tooltip : L, D2 = e6.legends, O6 = void 0 === D2 ? A3.legends : D2, M3 = e6.forwardLegendData, j3 = e6.role, G2 = void 0 === j3 ? A3.role : j3, T4 = e6.ariaLabel, H3 = e6.ariaLabelledBy, F3 = e6.ariaDescribedBy, V3 = e6.testIdPrefix, U2 = wt(a4, c9, u3), $2 = U2.outerWidth, _2 = U2.outerHeight, q3 = U2.margin, J2 = U2.innerWidth, K2 = U2.innerHeight, N2 = R({ width: J2, height: K2, data: t6, variables: r6, groupBy: n6, groups: o4, layout: v5, colors: C4, curve: g4, forwardLegendData: M3 }), Q = N2.variablesScale, X4 = N2.variablesWithScale, Y3 = N2.computedData, Z2 = N2.lineGenerator, ee = N2.legendData, te = N2.customLayerContext, ie = { axes: null, lines: null, legends: null };
  return P3.includes("axes") && (ie.axes = (0, import_jsx_runtime5.jsx)("g", { children: X4.map(function(e7) {
    return (0, import_jsx_runtime5.jsx)(W2, { axis: "horizontal" === v5 ? "y" : "x", length: "horizontal" === v5 ? K2 : J2, x: "horizontal" === v5 ? Q(e7.id) : 0, y: "horizontal" === v5 ? 0 : Q(e7.id), scale: e7.scale, ticksPosition: e7.ticksPosition || y3, tickValues: e7.tickValues, tickSize: e7.tickSize, tickPadding: e7.tickPadding, tickRotation: e7.tickRotation, format: e7.tickFormat, legend: e7.label || e7.id, legendPosition: e7.legendPosition, legendOffset: e7.legendOffset }, e7.id);
  }) }, "axes")), P3.includes("lines") && (ie.lines = (0, import_jsx_runtime5.jsx)("g", { children: Y3.map(function(e7) {
    return (0, import_jsx_runtime5.jsx)(E2, { datum: e7, variables: r6, lineGenerator: Z2, lineWidth: w4, opacity: k4, tooltip: B4, testIdPrefix: V3 }, "" + ("group" in e7 ? e7.group.id : "") + e7.id);
  }) }, "lines")), P3.includes("legends") && (ie.legends = (0, import_jsx_runtime5.jsx)("g", { children: O6.map(function(e7, t7) {
    return (0, import_jsx_runtime5.jsx)(O4, z3({}, e7, { containerWidth: J2, containerHeight: K2, data: ee }), t7);
  }) }, "legends")), (0, import_jsx_runtime5.jsx)(gn, { width: $2, height: _2, margin: q3, role: G2, ariaLabel: T4, ariaLabelledBy: H3, ariaDescribedBy: F3, children: P3.map(function(e7, t7) {
    var r7;
    return "function" == typeof e7 ? (0, import_jsx_runtime5.jsx)(import_react20.Fragment, { children: (0, import_react20.createElement)(e7, te) }, t7) : null != (r7 = null == ie ? void 0 : ie[e7]) ? r7 : null;
  }) });
};
var F2 = function(e6) {
  var t6 = e6.isInteractive, r6 = void 0 === t6 ? A3.isInteractive : t6, n6 = e6.animate, o4 = void 0 === n6 ? A3.animate : n6, a4 = e6.motionConfig, l4 = void 0 === a4 ? A3.motionConfig : a4, d4 = e6.theme, c9 = e6.renderWrapper, u3 = B3(e6, T3);
  return (0, import_jsx_runtime5.jsx)(St, { animate: o4, isInteractive: r6, motionConfig: l4, renderWrapper: c9, theme: d4, children: (0, import_jsx_runtime5.jsx)(H2, z3({ isInteractive: r6 }, u3)) });
};
var V2 = function(e6) {
  return (0, import_jsx_runtime5.jsx)(It, { children: function(t6) {
    var r6 = t6.width, n6 = t6.height;
    return (0, import_jsx_runtime5.jsx)(F2, z3({ width: r6, height: n6 }, e6));
  } });
};
var U = ["theme", "isInteractive", "animate", "motionConfig", "renderWrapper"];
var $ = function(e6) {
  var t6 = e6.data, r6 = e6.layout, a4 = void 0 === r6 ? G.layout : r6, l4 = e6.variables, d4 = e6.groupBy, c9 = e6.groups, u3 = e6.width, s3 = e6.height, v5 = e6.margin, f3 = e6.curve, p4 = void 0 === f3 ? G.curve : f3, m4 = e6.colors, b4 = void 0 === m4 ? G.colors : m4, w4 = e6.lineOpacity, W3 = void 0 === w4 ? G.lineOpacity : w4, k4 = e6.lineWidth, S3 = void 0 === k4 ? G.lineWidth : k4, C4 = e6.axesTicksPosition, I = void 0 === C4 ? G.axesTicksPosition : C4, P3 = e6.legends, L = void 0 === P3 ? G.legends : P3, B4 = e6.forwardLegendData, D2 = e6.layers, O6 = void 0 === D2 ? G.layers : D2, M3 = e6.role, j3 = void 0 === M3 ? G.role : M3, A4 = e6.ariaLabel, E3 = e6.ariaLabelledBy, T4 = e6.ariaDescribedBy, H3 = e6.pixelRatio, F3 = void 0 === H3 ? G.pixelRatio : H3, V3 = (0, import_react20.useRef)(null), U2 = wt(u3, s3, v5), $2 = U2.margin, _2 = U2.innerWidth, q3 = U2.innerHeight, J2 = U2.outerWidth, K2 = U2.outerHeight, N2 = R({ width: _2, height: q3, data: t6, variables: l4, groupBy: d4, groups: c9, layout: a4, colors: b4, curve: p4, forwardLegendData: B4 }), Q = N2.variablesScale, X4 = N2.variablesWithScale, Y3 = N2.computedData, Z2 = N2.lineGenerator, ee = N2.legendData, te = N2.customLayerContext, ie = zt();
  return (0, import_react20.useEffect)(function() {
    if (null !== V3.current) {
      var e7 = V3.current.getContext("2d");
      null !== e7 && (V3.current.width = J2 * F3, V3.current.height = K2 * F3, e7.scale(F3, F3), e7.fillStyle = ie.background, e7.fillRect(0, 0, J2, K2), e7.translate($2.left, $2.top), O6.forEach(function(t7) {
        "axes" === t7 ? X4.forEach(function(t8) {
          O3(e7, { axis: "horizontal" === a4 ? "y" : "x", scale: t8.scale, x: "horizontal" === a4 ? Q(t8.id) : 0, y: "horizontal" === a4 ? 0 : Q(t8.id), length: "horizontal" === a4 ? q3 : _2, ticksPosition: I, theme: ie });
        }) : "lines" === t7 ? (Z2.context(e7), Y3.forEach(function(t8) {
          e7.save(), e7.globalAlpha = W3, e7.beginPath(), Z2(t8.points), e7.strokeStyle = t8.color, e7.lineWidth = S3, e7.stroke(), e7.restore();
        })) : "legends" === t7 ? L.forEach(function(t8) {
          H(e7, z3({}, t8, { data: ee, containerWidth: _2, containerHeight: q3, theme: ie }));
        }) : "function" == typeof t7 && t7(e7, te);
      }));
    }
  }, [V3, J2, K2, _2, q3, $2, O6, te, Z2, W3, S3, Y3, Q, X4, a4, I, L, ee, ie, F3]), (0, import_jsx_runtime5.jsx)("canvas", { ref: V3, width: J2 * F3, height: K2 * F3, style: { width: J2, height: K2 }, role: j3, "aria-label": A4, "aria-labelledby": E3, "aria-describedby": T4 });
};
var _ = function(e6) {
  var t6 = e6.theme, r6 = e6.isInteractive, n6 = void 0 === r6 ? G.isInteractive : r6, o4 = e6.animate, a4 = void 0 === o4 ? G.animate : o4, l4 = e6.motionConfig, d4 = void 0 === l4 ? G.motionConfig : l4, c9 = e6.renderWrapper, u3 = B3(e6, U);
  return (0, import_jsx_runtime5.jsx)(St, { isInteractive: n6, animate: a4, motionConfig: d4, theme: t6, renderWrapper: c9, children: (0, import_jsx_runtime5.jsx)($, z3({ isInteractive: n6 }, u3)) });
};
var q2 = function(e6) {
  return (0, import_jsx_runtime5.jsx)(It, { children: function(t6) {
    var r6 = t6.width, n6 = t6.height;
    return (0, import_jsx_runtime5.jsx)(_, z3({ width: r6, height: n6 }, e6));
  } });
};
export {
  $ as InnerParallelCoordinatesCanvas,
  F2 as ParallelCoordinates,
  _ as ParallelCoordinatesCanvas,
  V2 as ResponsiveParallelCoordinates,
  q2 as ResponsiveParallelCoordinatesCanvas,
  G as canvasDefaultProps,
  j2 as commonDefaultProps,
  A3 as svgDefaultProps
};
//# sourceMappingURL=@nivo_parallel-coordinates.js.map
