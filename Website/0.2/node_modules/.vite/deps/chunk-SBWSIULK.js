import {
  require_baseEach,
  require_baseIteratee,
  require_baseUniq,
  require_isNumber,
  require_omit,
  require_sortBy,
  require_uniqBy
} from "./chunk-IP3WKTXL.js";
import {
  Dr,
  It,
  M,
  R,
  a,
  b,
  ct,
  d,
  ft,
  gt,
  ht,
  ut,
  w
} from "./chunk-UIKR6S4V.js";
import {
  band,
  format,
  friday,
  hour_default,
  linear,
  log,
  millisecond_default,
  minute_default,
  monday,
  month_default,
  newInterval,
  number_default,
  point,
  require_arrayFilter,
  require_baseGetTag,
  require_baseUnary,
  require_isArray,
  require_isObjectLike,
  require_last,
  require_nodeUtil,
  round_default,
  saturday,
  second_default,
  sunday,
  symlog,
  thursday,
  time,
  timeFormat,
  timeParse,
  tuesday,
  utcFriday,
  utcHour_default,
  utcMinute_default,
  utcMonday,
  utcMonth_default,
  utcParse,
  utcSaturday,
  utcSunday,
  utcThursday,
  utcTime,
  utcTuesday,
  utcWednesday,
  utcYear_default,
  wednesday,
  year_default
} from "./chunk-DNTQEE5E.js";
import {
  require_react_dom
} from "./chunk-NXESFFTV.js";
import {
  require_jsx_runtime
} from "./chunk-6PXSGDAH.js";
import {
  require_react
} from "./chunk-DRWLMN53.js";
import {
  __commonJS,
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/lodash/uniq.js
var require_uniq = __commonJS({
  "node_modules/lodash/uniq.js"(exports, module) {
    var baseUniq = require_baseUniq();
    function uniq(array) {
      return array && array.length ? baseUniq(array) : [];
    }
    module.exports = uniq;
  }
});

// node_modules/lodash/_baseIsDate.js
var require_baseIsDate = __commonJS({
  "node_modules/lodash/_baseIsDate.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var dateTag = "[object Date]";
    function baseIsDate(value) {
      return isObjectLike(value) && baseGetTag(value) == dateTag;
    }
    module.exports = baseIsDate;
  }
});

// node_modules/lodash/isDate.js
var require_isDate = __commonJS({
  "node_modules/lodash/isDate.js"(exports, module) {
    var baseIsDate = require_baseIsDate();
    var baseUnary = require_baseUnary();
    var nodeUtil = require_nodeUtil();
    var nodeIsDate = nodeUtil && nodeUtil.isDate;
    var isDate = nodeIsDate ? baseUnary(nodeIsDate) : baseIsDate;
    module.exports = isDate;
  }
});

// node_modules/lodash/_baseFilter.js
var require_baseFilter = __commonJS({
  "node_modules/lodash/_baseFilter.js"(exports, module) {
    var baseEach = require_baseEach();
    function baseFilter(collection, predicate) {
      var result = [];
      baseEach(collection, function(value, index, collection2) {
        if (predicate(value, index, collection2)) {
          result.push(value);
        }
      });
      return result;
    }
    module.exports = baseFilter;
  }
});

// node_modules/lodash/filter.js
var require_filter = __commonJS({
  "node_modules/lodash/filter.js"(exports, module) {
    var arrayFilter = require_arrayFilter();
    var baseFilter = require_baseFilter();
    var baseIteratee = require_baseIteratee();
    var isArray = require_isArray();
    function filter(collection, predicate) {
      var func = isArray(collection) ? arrayFilter : baseFilter;
      return func(collection, baseIteratee(predicate, 3));
    }
    module.exports = filter;
  }
});

// node_modules/@nivo/scales/dist/nivo-scales.mjs
var import_uniq = __toESM(require_uniq(), 1);
var import_uniqBy = __toESM(require_uniqBy(), 1);
var import_sortBy = __toESM(require_sortBy(), 1);
var import_last = __toESM(require_last(), 1);
var import_isDate = __toESM(require_isDate(), 1);
function J() {
  return J = Object.assign ? Object.assign.bind() : function(n4) {
    for (var t4 = 1; t4 < arguments.length; t4++) {
      var e4 = arguments[t4];
      for (var r3 in e4) ({}).hasOwnProperty.call(e4, r3) && (n4[r3] = e4[r3]);
    }
    return n4;
  }, J.apply(null, arguments);
}
var L = [function(n4) {
  return n4.setMilliseconds(0);
}, function(n4) {
  return n4.setSeconds(0);
}, function(n4) {
  return n4.setMinutes(0);
}, function(n4) {
  return n4.setHours(0);
}, function(n4) {
  return n4.setDate(1);
}, function(n4) {
  return n4.setMonth(0);
}];
var Q = { millisecond: [], second: L.slice(0, 1), minute: L.slice(0, 2), hour: L.slice(0, 3), day: L.slice(0, 4), month: L.slice(0, 5), year: L.slice(0, 6) };
var W = function(n4) {
  return function(t4) {
    return Q[n4].forEach(function(n5) {
      n5(t4);
    }), t4;
  };
};
var X = function(n4) {
  var t4 = n4.format, e4 = void 0 === t4 ? "native" : t4, r3 = n4.precision, i4 = void 0 === r3 ? "millisecond" : r3, u = n4.useUTC, c = void 0 === u || u, s = W(i4);
  return function(n5) {
    if (void 0 === n5) return n5;
    if ("native" === e4 || n5 instanceof Date) return s(n5);
    var t5 = c ? utcParse(e4) : timeParse(e4);
    return s(t5(n5));
  };
};
var Y = { type: "linear", min: 0, max: "auto", stacked: false, reverse: false, clamp: false, nice: true, round: false };
var Z = function(n4, t4, e4, r3) {
  var i4, a3, o2, c, s = n4.min, d2 = void 0 === s ? Y.min : s, m2 = n4.max, f = void 0 === m2 ? Y.max : m2, l = n4.stacked, y = void 0 === l ? Y.stacked : l, h = n4.reverse, g2 = void 0 === h ? Y.reverse : h, x2 = n4.clamp, k2 = void 0 === x2 ? Y.clamp : x2, T3 = n4.nice, b4 = void 0 === T3 ? Y.nice : T3, M3 = n4.round, w4 = void 0 === M3 ? Y.round : M3;
  "auto" === d2 ? i4 = true === y ? null != (a3 = t4.minStacked) ? a3 : 0 : t4.min : i4 = d2;
  "auto" === f ? o2 = true === y ? null != (c = t4.maxStacked) ? c : 0 : t4.max : o2 = f;
  var E3 = linear().range("x" === r3 ? [0, e4] : [e4, 0]).interpolate(w4 ? round_default : number_default).domain(g2 ? [o2, i4] : [i4, o2]).clamp(k2);
  return true === b4 ? E3.nice() : "number" == typeof b4 && E3.nice(b4), _(E3, y);
};
var _ = function(n4, t4) {
  void 0 === t4 && (t4 = false);
  var e4 = n4;
  return e4.type = "linear", e4.stacked = t4, e4;
};
var nn = function(n4, t4, e4) {
  var r3 = point().range([0, e4]).domain(t4.all);
  return r3.type = "point", r3;
};
var en = { type: "band", round: false };
var rn = function(n4, t4, e4, r3) {
  var i4 = n4.round, a3 = void 0 === i4 ? en.round : i4, o2 = band().range("x" === r3 ? [0, e4] : [e4, 0]).domain(t4.all).round(a3);
  return an(o2);
};
var an = function(n4) {
  var t4 = n4;
  return t4.type = "band", t4;
};
var on = { type: "time", format: "native", precision: "millisecond", min: "auto", max: "auto", useUTC: true, nice: false };
var un = function(n4, t4, e4) {
  var r3, i4, a3 = n4.format, o2 = void 0 === a3 ? on.format : a3, u = n4.precision, c = void 0 === u ? on.precision : u, s = n4.min, f = void 0 === s ? on.min : s, l = n4.max, v2 = void 0 === l ? on.max : l, p2 = n4.useUTC, y = void 0 === p2 ? on.useUTC : p2, h = n4.nice, g2 = void 0 === h ? on.nice : h, x2 = X({ format: o2, precision: c, useUTC: y });
  r3 = "auto" === f ? x2(t4.min) : "native" !== o2 ? x2(f) : f, i4 = "auto" === v2 ? x2(t4.max) : "native" !== o2 ? x2(v2) : v2;
  var k2 = y ? utcTime() : time();
  k2.range([0, e4]), r3 && i4 && k2.domain([r3, i4]), true === g2 ? k2.nice() : "object" != typeof g2 && "number" != typeof g2 || k2.nice(g2);
  var T3 = k2;
  return T3.type = "time", T3.useUTC = y, T3;
};
var cn = { type: "log", base: 10, min: "auto", max: "auto", round: false, reverse: false, nice: true };
var sn = function(n4, t4, e4, r3) {
  var i4, a3 = n4.base, o2 = void 0 === a3 ? cn.base : a3, u = n4.min, c = void 0 === u ? cn.min : u, s = n4.max, d2 = void 0 === s ? cn.max : s, m2 = n4.round, l = void 0 === m2 ? cn.round : m2, v2 = n4.reverse, p2 = void 0 === v2 ? cn.reverse : v2, y = n4.nice, h = void 0 === y ? cn.nice : y;
  if (t4.all.some(function(n5) {
    return 0 === n5;
  })) throw new Error("a log scale domain must not include or cross zero");
  var g2, x2, k2 = false;
  if (t4.all.filter(function(n5) {
    return null != n5;
  }).forEach(function(n5) {
    k2 || (void 0 === i4 ? i4 = Math.sign(n5) : Math.sign(n5) !== i4 && (k2 = true));
  }), k2) throw new Error("a log scale domain must be strictly-positive or strictly-negative");
  g2 = "auto" === c ? t4.min : c, x2 = "auto" === d2 ? t4.max : d2;
  var T3 = log().base(o2), b4 = "x" === r3 ? [0, e4] : [e4, 0];
  true === l ? T3.rangeRound(b4) : T3.range(b4), true === p2 ? T3.domain([x2, g2]) : T3.domain([g2, x2]), true === h ? T3.nice() : "number" == typeof h && T3.nice(h);
  var M3 = T3;
  return M3.type = "log", M3;
};
var dn = { type: "symlog", constant: 1, min: "auto", max: "auto", round: false, reverse: false, nice: true };
var mn = function(n4, t4, e4, r3) {
  var i4, a3, o2 = n4.constant, u = void 0 === o2 ? dn.constant : o2, c = n4.min, s = void 0 === c ? dn.min : c, d2 = n4.max, m2 = void 0 === d2 ? dn.max : d2, f = n4.round, v2 = void 0 === f ? dn.round : f, p2 = n4.reverse, y = void 0 === p2 ? dn.reverse : p2, h = n4.nice, g2 = void 0 === h ? dn.nice : h;
  i4 = "auto" === s ? t4.min : s, a3 = "auto" === m2 ? t4.max : m2;
  var x2 = symlog().constant(u), k2 = "x" === r3 ? [0, e4] : [e4, 0];
  true === v2 ? x2.rangeRound(k2) : x2.range(k2), true === y ? x2.domain([a3, i4]) : x2.domain([i4, a3]), true === g2 ? x2.nice() : "number" == typeof g2 && x2.nice(g2);
  var T3 = x2;
  return T3.type = "symlog", T3;
};
var fn = function(n4) {
  return "x" === n4 ? "y" : "x";
};
var ln = function(n4, t4) {
  return n4 === t4;
};
var vn = function(n4, t4) {
  return n4.getTime() === t4.getTime();
};
function pn(n4, t4, e4, r3) {
  switch (n4.type) {
    case "linear":
      return Z(n4, t4, e4, r3);
    case "point":
      return nn(0, t4, e4);
    case "band":
      return rn(n4, t4, e4, r3);
    case "time":
      return un(n4, t4, e4);
    case "log":
      return sn(n4, t4, e4, r3);
    case "symlog":
      return mn(n4, t4, e4, r3);
    default:
      throw new Error("invalid scale spec");
  }
}
var yn = function(n4, t4, e4) {
  var r3;
  if ("stacked" in e4 && e4.stacked) {
    var i4 = n4.data["x" === t4 ? "xStacked" : "yStacked"];
    return null == i4 ? null : e4(i4);
  }
  return null != (r3 = e4(n4.data[t4])) ? r3 : null;
};
var hn = function(n4, t4, e4, r3, i4) {
  var a3 = n4.map(function(n5) {
    return function(n6) {
      return J({}, n6, { data: n6.data.map(function(n7) {
        return { data: J({}, n7) };
      }) });
    }(n5);
  }), o2 = gn(a3, t4, e4);
  "stacked" in t4 && true === t4.stacked && Tn(o2, a3), "stacked" in e4 && true === e4.stacked && bn(o2, a3);
  var u = pn(t4, o2.x, r3, "x"), c = pn(e4, o2.y, i4, "y"), s = a3.map(function(n5) {
    return J({}, n5, { data: n5.data.map(function(n6) {
      return J({}, n6, { position: { x: yn(n6, "x", u), y: yn(n6, "y", c) } });
    }) });
  });
  return J({}, o2, { series: s, xScale: u, yScale: c });
};
var gn = function(n4, t4, e4) {
  return { x: xn(n4, "x", t4), y: xn(n4, "y", e4) };
};
var xn = function(i4, a3, o2, u) {
  var c = void 0 === u ? {} : u, s = c.getValue, d2 = void 0 === s ? function(n4) {
    return n4.data[a3];
  } : s, m2 = c.setValue, f = void 0 === m2 ? function(n4, t4) {
    n4.data[a3] = t4;
  } : m2;
  if ("linear" === o2.type) i4.forEach(function(n4) {
    n4.data.forEach(function(n5) {
      var t4 = d2(n5);
      t4 && f(n5, parseFloat(String(t4)));
    });
  });
  else if ("time" === o2.type && "native" !== o2.format) {
    var l = X(o2);
    i4.forEach(function(n4) {
      n4.data.forEach(function(n5) {
        var t4 = d2(n5);
        t4 && f(n5, l(t4));
      });
    });
  }
  var v2 = [];
  switch (i4.forEach(function(n4) {
    n4.data.forEach(function(n5) {
      v2.push(d2(n5));
    });
  }), o2.type) {
    case "linear":
      var p2 = (0, import_sortBy.default)((0, import_uniq.default)(v2).filter(function(n4) {
        return null !== n4;
      }), function(n4) {
        return n4;
      });
      return { all: p2, min: Math.min.apply(Math, p2), max: Math.max.apply(Math, p2) };
    case "time":
      var y = (0, import_uniqBy.default)(v2, function(n4) {
        return n4.getTime();
      }).slice(0).sort(function(n4, t4) {
        return t4.getTime() - n4.getTime();
      }).reverse();
      return { all: y, min: y[0], max: (0, import_last.default)(y) };
    default:
      var h = (0, import_uniq.default)(v2);
      return { all: h, min: h[0], max: (0, import_last.default)(h) };
  }
};
var kn = function(n4, t4, e4) {
  var a3 = fn(n4), o2 = [];
  t4[a3].all.forEach(function(t5) {
    var u = (0, import_isDate.default)(t5) ? vn : ln, c = [];
    e4.forEach(function(e5) {
      var i4 = e5.data.find(function(n5) {
        return u(n5.data[a3], t5);
      }), s = null, d2 = null;
      if (void 0 !== i4) {
        if (null !== (s = i4.data[n4])) {
          var m2 = (0, import_last.default)(c);
          void 0 === m2 ? d2 = s : null !== m2 && (d2 = m2 + s);
        }
        i4.data["x" === n4 ? "xStacked" : "yStacked"] = d2;
      }
      c.push(d2), null !== d2 && o2.push(d2);
    });
  }), t4[n4].minStacked = Math.min.apply(Math, o2), t4[n4].maxStacked = Math.max.apply(Math, o2);
};
var Tn = function(n4, t4) {
  return kn("x", n4, t4);
};
var bn = function(n4, t4) {
  return kn("y", n4, t4);
};
var Mn = function(n4) {
  var t4 = n4.bandwidth();
  if (0 === t4) return n4;
  var e4 = t4 / 2;
  return n4.round() && (e4 = Math.round(e4)), function(t5) {
    var r3;
    return (null != (r3 = n4(t5)) ? r3 : 0) + e4;
  };
};
var wn = { millisecond: [millisecond_default, millisecond_default], second: [second_default, second_default], minute: [minute_default, utcMinute_default], hour: [hour_default, utcHour_default], day: [newInterval(function(n4) {
  return n4.setHours(0, 0, 0, 0);
}, function(n4, t4) {
  return n4.setDate(n4.getDate() + t4);
}, function(n4, t4) {
  return (t4.getTime() - n4.getTime()) / 864e5;
}, function(n4) {
  return Math.floor(n4.getTime() / 864e5);
}), newInterval(function(n4) {
  return n4.setUTCHours(0, 0, 0, 0);
}, function(n4, t4) {
  return n4.setUTCDate(n4.getUTCDate() + t4);
}, function(n4, t4) {
  return (t4.getTime() - n4.getTime()) / 864e5;
}, function(n4) {
  return Math.floor(n4.getTime() / 864e5);
})], week: [sunday, utcSunday], sunday: [sunday, utcSunday], monday: [monday, utcMonday], tuesday: [tuesday, utcTuesday], wednesday: [wednesday, utcWednesday], thursday: [thursday, utcThursday], friday: [friday, utcFriday], saturday: [saturday, utcSaturday], month: [month_default, utcMonth_default], year: [year_default, utcYear_default] };
var En = Object.keys(wn);
var Sn = new RegExp("^every\\s*(\\d+)?\\s*(" + En.join("|") + ")s?$", "i");
var Cn = function(n4, t4) {
  if (Array.isArray(t4)) return t4;
  if ("string" == typeof t4 && "useUTC" in n4) {
    var e4 = t4.match(Sn);
    if (e4) {
      var r3 = e4[1], i4 = e4[2], a3 = wn[i4][n4.useUTC ? 1 : 0];
      if ("day" === i4) {
        var o2, u, c = n4.domain(), s = c[0], d2 = c[1], m2 = new Date(d2);
        return m2.setDate(m2.getDate() + 1), null != (o2 = null == (u = a3.every(Number(null != r3 ? r3 : 1))) ? void 0 : u.range(s, m2)) ? o2 : [];
      }
      if (void 0 === r3) return n4.ticks(a3);
      var f = a3.every(Number(r3));
      if (f) return n4.ticks(f);
    }
    throw new Error("Invalid tickValues: " + t4);
  }
  if ("ticks" in n4) {
    if (void 0 === t4) return n4.ticks();
    if ("number" == typeof (l = t4) && isFinite(l) && Math.floor(l) === l) return n4.ticks(t4);
  }
  var l;
  return n4.domain();
};

// node_modules/@nivo/axes/dist/nivo-axes.mjs
var t2 = __toESM(require_react(), 1);
var import_react15 = __toESM(require_react(), 1);

// node_modules/@nivo/axes/node_modules/@react-spring/rafz/dist/react-spring_rafz.modern.mjs
var updateQueue = makeQueue();
var raf = (fn2) => schedule(fn2, updateQueue);
var writeQueue = makeQueue();
raf.write = (fn2) => schedule(fn2, writeQueue);
var onStartQueue = makeQueue();
raf.onStart = (fn2) => schedule(fn2, onStartQueue);
var onFrameQueue = makeQueue();
raf.onFrame = (fn2) => schedule(fn2, onFrameQueue);
var onFinishQueue = makeQueue();
raf.onFinish = (fn2) => schedule(fn2, onFinishQueue);
var timeouts = [];
raf.setTimeout = (handler, ms) => {
  const time2 = raf.now() + ms;
  const cancel = () => {
    const i4 = timeouts.findIndex((t4) => t4.cancel == cancel);
    if (~i4) timeouts.splice(i4, 1);
    pendingCount -= ~i4 ? 1 : 0;
  };
  const timeout = { time: time2, handler, cancel };
  timeouts.splice(findTimeout(time2), 0, timeout);
  pendingCount += 1;
  start();
  return timeout;
};
var findTimeout = (time2) => ~(~timeouts.findIndex((t4) => t4.time > time2) || ~timeouts.length);
raf.cancel = (fn2) => {
  onStartQueue.delete(fn2);
  onFrameQueue.delete(fn2);
  onFinishQueue.delete(fn2);
  updateQueue.delete(fn2);
  writeQueue.delete(fn2);
};
raf.sync = (fn2) => {
  sync = true;
  raf.batchedUpdates(fn2);
  sync = false;
};
raf.throttle = (fn2) => {
  let lastArgs;
  function queuedFn() {
    try {
      fn2(...lastArgs);
    } finally {
      lastArgs = null;
    }
  }
  function throttled(...args) {
    lastArgs = args;
    raf.onStart(queuedFn);
  }
  throttled.handler = fn2;
  throttled.cancel = () => {
    onStartQueue.delete(queuedFn);
    lastArgs = null;
  };
  return throttled;
};
var nativeRaf = typeof window != "undefined" ? window.requestAnimationFrame : (
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  () => {
  }
);
raf.use = (impl) => nativeRaf = impl;
raf.now = typeof performance != "undefined" ? () => performance.now() : Date.now;
raf.batchedUpdates = (fn2) => fn2();
raf.catch = console.error;
raf.frameLoop = "always";
raf.advance = () => {
  if (raf.frameLoop !== "demand") {
    console.warn(
      "Cannot call the manual advancement of rafz whilst frameLoop is not set as demand"
    );
  } else {
    update();
  }
};
var ts = -1;
var pendingCount = 0;
var sync = false;
function schedule(fn2, queue) {
  if (sync) {
    queue.delete(fn2);
    fn2(0);
  } else {
    queue.add(fn2);
    start();
  }
}
function start() {
  if (ts < 0) {
    ts = 0;
    if (raf.frameLoop !== "demand") {
      nativeRaf(loop);
    }
  }
}
function stop() {
  ts = -1;
}
function loop() {
  if (~ts) {
    nativeRaf(loop);
    raf.batchedUpdates(update);
  }
}
function update() {
  const prevTs = ts;
  ts = raf.now();
  const count = findTimeout(ts);
  if (count) {
    eachSafely(timeouts.splice(0, count), (t4) => t4.handler());
    pendingCount -= count;
  }
  if (!pendingCount) {
    stop();
    return;
  }
  onStartQueue.flush();
  updateQueue.flush(prevTs ? Math.min(64, ts - prevTs) : 16.667);
  onFrameQueue.flush();
  writeQueue.flush();
  onFinishQueue.flush();
}
function makeQueue() {
  let next = /* @__PURE__ */ new Set();
  let current = next;
  return {
    add(fn2) {
      pendingCount += current == next && !next.has(fn2) ? 1 : 0;
      next.add(fn2);
    },
    delete(fn2) {
      pendingCount -= current == next && next.has(fn2) ? 1 : 0;
      return next.delete(fn2);
    },
    flush(arg) {
      if (current.size) {
        next = /* @__PURE__ */ new Set();
        pendingCount -= current.size;
        eachSafely(current, (fn2) => fn2(arg) && next.add(fn2));
        pendingCount += next.size;
        current = next;
      }
    }
  };
}
function eachSafely(values, each3) {
  values.forEach((value) => {
    try {
      each3(value);
    } catch (e4) {
      raf.catch(e4);
    }
  });
}

// node_modules/@nivo/axes/node_modules/@react-spring/shared/dist/react-spring_shared.modern.mjs
var import_react = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_react3 = __toESM(require_react(), 1);
var import_react4 = __toESM(require_react(), 1);
var import_react5 = __toESM(require_react(), 1);
var import_react6 = __toESM(require_react(), 1);
var import_react7 = __toESM(require_react(), 1);
var import_react8 = __toESM(require_react(), 1);
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var globals_exports = {};
__export(globals_exports, {
  assign: () => assign,
  colors: () => colors,
  createStringInterpolator: () => createStringInterpolator,
  skipAnimation: () => skipAnimation,
  to: () => to,
  willAdvance: () => willAdvance
});
function noop() {
}
var defineHidden = (obj, key, value) => Object.defineProperty(obj, key, { value, writable: true, configurable: true });
var is = {
  arr: Array.isArray,
  obj: (a3) => !!a3 && a3.constructor.name === "Object",
  fun: (a3) => typeof a3 === "function",
  str: (a3) => typeof a3 === "string",
  num: (a3) => typeof a3 === "number",
  und: (a3) => a3 === void 0
};
function isEqual(a3, b4) {
  if (is.arr(a3)) {
    if (!is.arr(b4) || a3.length !== b4.length) return false;
    for (let i4 = 0; i4 < a3.length; i4++) {
      if (a3[i4] !== b4[i4]) return false;
    }
    return true;
  }
  return a3 === b4;
}
var each = (obj, fn2) => obj.forEach(fn2);
function eachProp(obj, fn2, ctx) {
  if (is.arr(obj)) {
    for (let i4 = 0; i4 < obj.length; i4++) {
      fn2.call(ctx, obj[i4], `${i4}`);
    }
    return;
  }
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      fn2.call(ctx, obj[key], key);
    }
  }
}
var toArray = (a3) => is.und(a3) ? [] : is.arr(a3) ? a3 : [a3];
function flush(queue, iterator) {
  if (queue.size) {
    const items = Array.from(queue);
    queue.clear();
    each(items, iterator);
  }
}
var flushCalls = (queue, ...args) => flush(queue, (fn2) => fn2(...args));
var isSSR = () => typeof window === "undefined" || !window.navigator || /ServerSideRendering|^Deno\//.test(window.navigator.userAgent);
var createStringInterpolator;
var to;
var colors = null;
var skipAnimation = false;
var willAdvance = noop;
var assign = (globals) => {
  if (globals.to) to = globals.to;
  if (globals.now) raf.now = globals.now;
  if (globals.colors !== void 0) colors = globals.colors;
  if (globals.skipAnimation != null) skipAnimation = globals.skipAnimation;
  if (globals.createStringInterpolator)
    createStringInterpolator = globals.createStringInterpolator;
  if (globals.requestAnimationFrame) raf.use(globals.requestAnimationFrame);
  if (globals.batchedUpdates) raf.batchedUpdates = globals.batchedUpdates;
  if (globals.willAdvance) willAdvance = globals.willAdvance;
  if (globals.frameLoop) raf.frameLoop = globals.frameLoop;
};
var startQueue = /* @__PURE__ */ new Set();
var currentFrame = [];
var prevFrame = [];
var priority = 0;
var frameLoop = {
  get idle() {
    return !startQueue.size && !currentFrame.length;
  },
  /** Advance the given animation on every frame until idle. */
  start(animation) {
    if (priority > animation.priority) {
      startQueue.add(animation);
      raf.onStart(flushStartQueue);
    } else {
      startSafely(animation);
      raf(advance);
    }
  },
  /** Advance all animations by the given time. */
  advance,
  /** Call this when an animation's priority changes. */
  sort(animation) {
    if (priority) {
      raf.onFrame(() => frameLoop.sort(animation));
    } else {
      const prevIndex = currentFrame.indexOf(animation);
      if (~prevIndex) {
        currentFrame.splice(prevIndex, 1);
        startUnsafely(animation);
      }
    }
  },
  /**
   * Clear all animations. For testing purposes.
   *
   * ☠️ Never call this from within the frameloop.
   */
  clear() {
    currentFrame = [];
    startQueue.clear();
  }
};
function flushStartQueue() {
  startQueue.forEach(startSafely);
  startQueue.clear();
  raf(advance);
}
function startSafely(animation) {
  if (!currentFrame.includes(animation)) startUnsafely(animation);
}
function startUnsafely(animation) {
  currentFrame.splice(
    findIndex(currentFrame, (other) => other.priority > animation.priority),
    0,
    animation
  );
}
function advance(dt) {
  const nextFrame = prevFrame;
  for (let i4 = 0; i4 < currentFrame.length; i4++) {
    const animation = currentFrame[i4];
    priority = animation.priority;
    if (!animation.idle) {
      willAdvance(animation);
      animation.advance(dt);
      if (!animation.idle) {
        nextFrame.push(animation);
      }
    }
  }
  priority = 0;
  prevFrame = currentFrame;
  prevFrame.length = 0;
  currentFrame = nextFrame;
  return currentFrame.length > 0;
}
function findIndex(arr, test) {
  const index = arr.findIndex(test);
  return index < 0 ? arr.length : index;
}
var clamp = (min, max, v2) => Math.min(Math.max(v2, min), max);
var colors2 = {
  transparent: 0,
  aliceblue: 4042850303,
  antiquewhite: 4209760255,
  aqua: 16777215,
  aquamarine: 2147472639,
  azure: 4043309055,
  beige: 4126530815,
  bisque: 4293182719,
  black: 255,
  blanchedalmond: 4293643775,
  blue: 65535,
  blueviolet: 2318131967,
  brown: 2771004159,
  burlywood: 3736635391,
  burntsienna: 3934150143,
  cadetblue: 1604231423,
  chartreuse: 2147418367,
  chocolate: 3530104575,
  coral: 4286533887,
  cornflowerblue: 1687547391,
  cornsilk: 4294499583,
  crimson: 3692313855,
  cyan: 16777215,
  darkblue: 35839,
  darkcyan: 9145343,
  darkgoldenrod: 3095792639,
  darkgray: 2846468607,
  darkgreen: 6553855,
  darkgrey: 2846468607,
  darkkhaki: 3182914559,
  darkmagenta: 2332068863,
  darkolivegreen: 1433087999,
  darkorange: 4287365375,
  darkorchid: 2570243327,
  darkred: 2332033279,
  darksalmon: 3918953215,
  darkseagreen: 2411499519,
  darkslateblue: 1211993087,
  darkslategray: 793726975,
  darkslategrey: 793726975,
  darkturquoise: 13554175,
  darkviolet: 2483082239,
  deeppink: 4279538687,
  deepskyblue: 12582911,
  dimgray: 1768516095,
  dimgrey: 1768516095,
  dodgerblue: 512819199,
  firebrick: 2988581631,
  floralwhite: 4294635775,
  forestgreen: 579543807,
  fuchsia: 4278255615,
  gainsboro: 3705462015,
  ghostwhite: 4177068031,
  gold: 4292280575,
  goldenrod: 3668254975,
  gray: 2155905279,
  green: 8388863,
  greenyellow: 2919182335,
  grey: 2155905279,
  honeydew: 4043305215,
  hotpink: 4285117695,
  indianred: 3445382399,
  indigo: 1258324735,
  ivory: 4294963455,
  khaki: 4041641215,
  lavender: 3873897215,
  lavenderblush: 4293981695,
  lawngreen: 2096890111,
  lemonchiffon: 4294626815,
  lightblue: 2916673279,
  lightcoral: 4034953471,
  lightcyan: 3774873599,
  lightgoldenrodyellow: 4210742015,
  lightgray: 3553874943,
  lightgreen: 2431553791,
  lightgrey: 3553874943,
  lightpink: 4290167295,
  lightsalmon: 4288707327,
  lightseagreen: 548580095,
  lightskyblue: 2278488831,
  lightslategray: 2005441023,
  lightslategrey: 2005441023,
  lightsteelblue: 2965692159,
  lightyellow: 4294959359,
  lime: 16711935,
  limegreen: 852308735,
  linen: 4210091775,
  magenta: 4278255615,
  maroon: 2147483903,
  mediumaquamarine: 1724754687,
  mediumblue: 52735,
  mediumorchid: 3126187007,
  mediumpurple: 2473647103,
  mediumseagreen: 1018393087,
  mediumslateblue: 2070474495,
  mediumspringgreen: 16423679,
  mediumturquoise: 1221709055,
  mediumvioletred: 3340076543,
  midnightblue: 421097727,
  mintcream: 4127193855,
  mistyrose: 4293190143,
  moccasin: 4293178879,
  navajowhite: 4292783615,
  navy: 33023,
  oldlace: 4260751103,
  olive: 2155872511,
  olivedrab: 1804477439,
  orange: 4289003775,
  orangered: 4282712319,
  orchid: 3664828159,
  palegoldenrod: 4008225535,
  palegreen: 2566625535,
  paleturquoise: 2951671551,
  palevioletred: 3681588223,
  papayawhip: 4293907967,
  peachpuff: 4292524543,
  peru: 3448061951,
  pink: 4290825215,
  plum: 3718307327,
  powderblue: 2967529215,
  purple: 2147516671,
  rebeccapurple: 1714657791,
  red: 4278190335,
  rosybrown: 3163525119,
  royalblue: 1097458175,
  saddlebrown: 2336560127,
  salmon: 4202722047,
  sandybrown: 4104413439,
  seagreen: 780883967,
  seashell: 4294307583,
  sienna: 2689740287,
  silver: 3233857791,
  skyblue: 2278484991,
  slateblue: 1784335871,
  slategray: 1887473919,
  slategrey: 1887473919,
  snow: 4294638335,
  springgreen: 16744447,
  steelblue: 1182971135,
  tan: 3535047935,
  teal: 8421631,
  thistle: 3636451583,
  tomato: 4284696575,
  turquoise: 1088475391,
  violet: 4001558271,
  wheat: 4125012991,
  white: 4294967295,
  whitesmoke: 4126537215,
  yellow: 4294902015,
  yellowgreen: 2597139199
};
var NUMBER = "[-+]?\\d*\\.?\\d+";
var PERCENTAGE = NUMBER + "%";
function call(...parts) {
  return "\\(\\s*(" + parts.join(")\\s*,\\s*(") + ")\\s*\\)";
}
var rgb = new RegExp("rgb" + call(NUMBER, NUMBER, NUMBER));
var rgba = new RegExp("rgba" + call(NUMBER, NUMBER, NUMBER, NUMBER));
var hsl = new RegExp("hsl" + call(NUMBER, PERCENTAGE, PERCENTAGE));
var hsla = new RegExp(
  "hsla" + call(NUMBER, PERCENTAGE, PERCENTAGE, NUMBER)
);
var hex3 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex4 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex6 = /^#([0-9a-fA-F]{6})$/;
var hex8 = /^#([0-9a-fA-F]{8})$/;
function normalizeColor(color) {
  let match;
  if (typeof color === "number") {
    return color >>> 0 === color && color >= 0 && color <= 4294967295 ? color : null;
  }
  if (match = hex6.exec(color))
    return parseInt(match[1] + "ff", 16) >>> 0;
  if (colors && colors[color] !== void 0) {
    return colors[color];
  }
  if (match = rgb.exec(color)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    255) >>> // a
    0;
  }
  if (match = rgba.exec(color)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    parse1(match[4])) >>> // a
    0;
  }
  if (match = hex3.exec(color)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      "ff",
      // a
      16
    ) >>> 0;
  }
  if (match = hex8.exec(color)) return parseInt(match[1], 16) >>> 0;
  if (match = hex4.exec(color)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      match[4] + match[4],
      // a
      16
    ) >>> 0;
  }
  if (match = hsl.exec(color)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | 255) >>> // a
    0;
  }
  if (match = hsla.exec(color)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | parse1(match[4])) >>> // a
    0;
  }
  return null;
}
function hue2rgb(p2, q3, t4) {
  if (t4 < 0) t4 += 1;
  if (t4 > 1) t4 -= 1;
  if (t4 < 1 / 6) return p2 + (q3 - p2) * 6 * t4;
  if (t4 < 1 / 2) return q3;
  if (t4 < 2 / 3) return p2 + (q3 - p2) * (2 / 3 - t4) * 6;
  return p2;
}
function hslToRgb(h, s, l) {
  const q3 = l < 0.5 ? l * (1 + s) : l + s - l * s;
  const p2 = 2 * l - q3;
  const r3 = hue2rgb(p2, q3, h + 1 / 3);
  const g2 = hue2rgb(p2, q3, h);
  const b4 = hue2rgb(p2, q3, h - 1 / 3);
  return Math.round(r3 * 255) << 24 | Math.round(g2 * 255) << 16 | Math.round(b4 * 255) << 8;
}
function parse255(str) {
  const int = parseInt(str, 10);
  if (int < 0) return 0;
  if (int > 255) return 255;
  return int;
}
function parse360(str) {
  const int = parseFloat(str);
  return (int % 360 + 360) % 360 / 360;
}
function parse1(str) {
  const num = parseFloat(str);
  if (num < 0) return 0;
  if (num > 1) return 255;
  return Math.round(num * 255);
}
function parsePercentage(str) {
  const int = parseFloat(str);
  if (int < 0) return 0;
  if (int > 100) return 1;
  return int / 100;
}
function colorToRgba(input) {
  let int32Color = normalizeColor(input);
  if (int32Color === null) return input;
  int32Color = int32Color || 0;
  const r3 = (int32Color & 4278190080) >>> 24;
  const g2 = (int32Color & 16711680) >>> 16;
  const b4 = (int32Color & 65280) >>> 8;
  const a3 = (int32Color & 255) / 255;
  return `rgba(${r3}, ${g2}, ${b4}, ${a3})`;
}
var createInterpolator = (range, output, extrapolate) => {
  if (is.fun(range)) {
    return range;
  }
  if (is.arr(range)) {
    return createInterpolator({
      range,
      output,
      extrapolate
    });
  }
  if (is.str(range.output[0])) {
    return createStringInterpolator(range);
  }
  const config3 = range;
  const outputRange = config3.output;
  const inputRange = config3.range || [0, 1];
  const extrapolateLeft = config3.extrapolateLeft || config3.extrapolate || "extend";
  const extrapolateRight = config3.extrapolateRight || config3.extrapolate || "extend";
  const easing = config3.easing || ((t4) => t4);
  return (input) => {
    const range2 = findRange(input, inputRange);
    return interpolate(
      input,
      inputRange[range2],
      inputRange[range2 + 1],
      outputRange[range2],
      outputRange[range2 + 1],
      easing,
      extrapolateLeft,
      extrapolateRight,
      config3.map
    );
  };
};
function interpolate(input, inputMin, inputMax, outputMin, outputMax, easing, extrapolateLeft, extrapolateRight, map) {
  let result = map ? map(input) : input;
  if (result < inputMin) {
    if (extrapolateLeft === "identity") return result;
    else if (extrapolateLeft === "clamp") result = inputMin;
  }
  if (result > inputMax) {
    if (extrapolateRight === "identity") return result;
    else if (extrapolateRight === "clamp") result = inputMax;
  }
  if (outputMin === outputMax) return outputMin;
  if (inputMin === inputMax) return input <= inputMin ? outputMin : outputMax;
  if (inputMin === -Infinity) result = -result;
  else if (inputMax === Infinity) result = result - inputMin;
  else result = (result - inputMin) / (inputMax - inputMin);
  result = easing(result);
  if (outputMin === -Infinity) result = -result;
  else if (outputMax === Infinity) result = result + outputMin;
  else result = result * (outputMax - outputMin) + outputMin;
  return result;
}
function findRange(input, inputRange) {
  for (var i4 = 1; i4 < inputRange.length - 1; ++i4)
    if (inputRange[i4] >= input) break;
  return i4 - 1;
}
var steps = (steps22, direction = "end") => (progress2) => {
  progress2 = direction === "end" ? Math.min(progress2, 0.999) : Math.max(progress2, 1e-3);
  const expanded = progress2 * steps22;
  const rounded = direction === "end" ? Math.floor(expanded) : Math.ceil(expanded);
  return clamp(0, 1, rounded / steps22);
};
var c1 = 1.70158;
var c2 = c1 * 1.525;
var c3 = c1 + 1;
var c4 = 2 * Math.PI / 3;
var c5 = 2 * Math.PI / 4.5;
var bounceOut = (x2) => {
  const n1 = 7.5625;
  const d1 = 2.75;
  if (x2 < 1 / d1) {
    return n1 * x2 * x2;
  } else if (x2 < 2 / d1) {
    return n1 * (x2 -= 1.5 / d1) * x2 + 0.75;
  } else if (x2 < 2.5 / d1) {
    return n1 * (x2 -= 2.25 / d1) * x2 + 0.9375;
  } else {
    return n1 * (x2 -= 2.625 / d1) * x2 + 0.984375;
  }
};
var easings = {
  linear: (x2) => x2,
  easeInQuad: (x2) => x2 * x2,
  easeOutQuad: (x2) => 1 - (1 - x2) * (1 - x2),
  easeInOutQuad: (x2) => x2 < 0.5 ? 2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 2) / 2,
  easeInCubic: (x2) => x2 * x2 * x2,
  easeOutCubic: (x2) => 1 - Math.pow(1 - x2, 3),
  easeInOutCubic: (x2) => x2 < 0.5 ? 4 * x2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 3) / 2,
  easeInQuart: (x2) => x2 * x2 * x2 * x2,
  easeOutQuart: (x2) => 1 - Math.pow(1 - x2, 4),
  easeInOutQuart: (x2) => x2 < 0.5 ? 8 * x2 * x2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 4) / 2,
  easeInQuint: (x2) => x2 * x2 * x2 * x2 * x2,
  easeOutQuint: (x2) => 1 - Math.pow(1 - x2, 5),
  easeInOutQuint: (x2) => x2 < 0.5 ? 16 * x2 * x2 * x2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 5) / 2,
  easeInSine: (x2) => 1 - Math.cos(x2 * Math.PI / 2),
  easeOutSine: (x2) => Math.sin(x2 * Math.PI / 2),
  easeInOutSine: (x2) => -(Math.cos(Math.PI * x2) - 1) / 2,
  easeInExpo: (x2) => x2 === 0 ? 0 : Math.pow(2, 10 * x2 - 10),
  easeOutExpo: (x2) => x2 === 1 ? 1 : 1 - Math.pow(2, -10 * x2),
  easeInOutExpo: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : x2 < 0.5 ? Math.pow(2, 20 * x2 - 10) / 2 : (2 - Math.pow(2, -20 * x2 + 10)) / 2,
  easeInCirc: (x2) => 1 - Math.sqrt(1 - Math.pow(x2, 2)),
  easeOutCirc: (x2) => Math.sqrt(1 - Math.pow(x2 - 1, 2)),
  easeInOutCirc: (x2) => x2 < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x2, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x2 + 2, 2)) + 1) / 2,
  easeInBack: (x2) => c3 * x2 * x2 * x2 - c1 * x2 * x2,
  easeOutBack: (x2) => 1 + c3 * Math.pow(x2 - 1, 3) + c1 * Math.pow(x2 - 1, 2),
  easeInOutBack: (x2) => x2 < 0.5 ? Math.pow(2 * x2, 2) * ((c2 + 1) * 2 * x2 - c2) / 2 : (Math.pow(2 * x2 - 2, 2) * ((c2 + 1) * (x2 * 2 - 2) + c2) + 2) / 2,
  easeInElastic: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : -Math.pow(2, 10 * x2 - 10) * Math.sin((x2 * 10 - 10.75) * c4),
  easeOutElastic: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : Math.pow(2, -10 * x2) * Math.sin((x2 * 10 - 0.75) * c4) + 1,
  easeInOutElastic: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : x2 < 0.5 ? -(Math.pow(2, 20 * x2 - 10) * Math.sin((20 * x2 - 11.125) * c5)) / 2 : Math.pow(2, -20 * x2 + 10) * Math.sin((20 * x2 - 11.125) * c5) / 2 + 1,
  easeInBounce: (x2) => 1 - bounceOut(1 - x2),
  easeOutBounce: bounceOut,
  easeInOutBounce: (x2) => x2 < 0.5 ? (1 - bounceOut(1 - 2 * x2)) / 2 : (1 + bounceOut(2 * x2 - 1)) / 2,
  steps
};
var $get = Symbol.for("FluidValue.get");
var $observers = Symbol.for("FluidValue.observers");
var hasFluidValue = (arg) => Boolean(arg && arg[$get]);
var getFluidValue = (arg) => arg && arg[$get] ? arg[$get]() : arg;
var getFluidObservers = (target) => target[$observers] || null;
function callFluidObserver(observer2, event) {
  if (observer2.eventObserved) {
    observer2.eventObserved(event);
  } else {
    observer2(event);
  }
}
function callFluidObservers(target, event) {
  const observers = target[$observers];
  if (observers) {
    observers.forEach((observer2) => {
      callFluidObserver(observer2, event);
    });
  }
}
var FluidValue = class {
  constructor(get) {
    if (!get && !(get = this.get)) {
      throw Error("Unknown getter");
    }
    setFluidGetter(this, get);
  }
};
var setFluidGetter = (target, get) => setHidden(target, $get, get);
function addFluidObserver(target, observer2) {
  if (target[$get]) {
    let observers = target[$observers];
    if (!observers) {
      setHidden(target, $observers, observers = /* @__PURE__ */ new Set());
    }
    if (!observers.has(observer2)) {
      observers.add(observer2);
      if (target.observerAdded) {
        target.observerAdded(observers.size, observer2);
      }
    }
  }
  return observer2;
}
function removeFluidObserver(target, observer2) {
  const observers = target[$observers];
  if (observers && observers.has(observer2)) {
    const count = observers.size - 1;
    if (count) {
      observers.delete(observer2);
    } else {
      target[$observers] = null;
    }
    if (target.observerRemoved) {
      target.observerRemoved(count, observer2);
    }
  }
}
var setHidden = (target, key, value) => Object.defineProperty(target, key, {
  value,
  writable: true,
  configurable: true
});
var numberRegex = /[+\-]?(?:0|[1-9]\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?/g;
var colorRegex = /(#(?:[0-9a-f]{2}){2,4}|(#[0-9a-f]{3})|(rgb|hsl)a?\((-?\d+%?[,\s]+){2,3}\s*[\d\.]+%?\))/gi;
var unitRegex = new RegExp(`(${numberRegex.source})(%|[a-z]+)`, "i");
var rgbaRegex = /rgba\(([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+)\)/gi;
var cssVariableRegex = /var\((--[a-zA-Z0-9-_]+),? ?([a-zA-Z0-9 ()%#.,-]+)?\)/;
var variableToRgba = (input) => {
  const [token, fallback] = parseCSSVariable(input);
  if (!token || isSSR()) {
    return input;
  }
  const value = window.getComputedStyle(document.documentElement).getPropertyValue(token);
  if (value) {
    return value.trim();
  } else if (fallback && fallback.startsWith("--")) {
    const value2 = window.getComputedStyle(document.documentElement).getPropertyValue(fallback);
    if (value2) {
      return value2;
    } else {
      return input;
    }
  } else if (fallback && cssVariableRegex.test(fallback)) {
    return variableToRgba(fallback);
  } else if (fallback) {
    return fallback;
  }
  return input;
};
var parseCSSVariable = (current) => {
  const match = cssVariableRegex.exec(current);
  if (!match) return [,];
  const [, token, fallback] = match;
  return [token, fallback];
};
var namedColorRegex;
var rgbaRound = (_2, p1, p2, p3, p4) => `rgba(${Math.round(p1)}, ${Math.round(p2)}, ${Math.round(p3)}, ${p4})`;
var createStringInterpolator2 = (config3) => {
  if (!namedColorRegex)
    namedColorRegex = colors ? (
      // match color names, ignore partial matches
      new RegExp(`(${Object.keys(colors).join("|")})(?!\\w)`, "g")
    ) : (
      // never match
      /^\b$/
    );
  const output = config3.output.map((value) => {
    return getFluidValue(value).replace(cssVariableRegex, variableToRgba).replace(colorRegex, colorToRgba).replace(namedColorRegex, colorToRgba);
  });
  const keyframes = output.map((value) => value.match(numberRegex).map(Number));
  const outputRanges = keyframes[0].map(
    (_2, i4) => keyframes.map((values) => {
      if (!(i4 in values)) {
        throw Error('The arity of each "output" value must be equal');
      }
      return values[i4];
    })
  );
  const interpolators = outputRanges.map(
    (output2) => createInterpolator({ ...config3, output: output2 })
  );
  return (input) => {
    var _a;
    const missingUnit = !unitRegex.test(output[0]) && ((_a = output.find((value) => unitRegex.test(value))) == null ? void 0 : _a.replace(numberRegex, ""));
    let i4 = 0;
    return output[0].replace(
      numberRegex,
      () => `${interpolators[i4++](input)}${missingUnit || ""}`
    ).replace(rgbaRegex, rgbaRound);
  };
};
var prefix = "react-spring: ";
var once = (fn2) => {
  const func = fn2;
  let called = false;
  if (typeof func != "function") {
    throw new TypeError(`${prefix}once requires a function parameter`);
  }
  return (...args) => {
    if (!called) {
      func(...args);
      called = true;
    }
  };
};
var warnInterpolate = once(console.warn);
function deprecateInterpolate() {
  warnInterpolate(
    `${prefix}The "interpolate" function is deprecated in v9 (use "to" instead)`
  );
}
var warnDirectCall = once(console.warn);
function deprecateDirectCall() {
  warnDirectCall(
    `${prefix}Directly calling start instead of using the api object is deprecated in v9 (use ".start" instead), this will be removed in later 0.X.0 versions`
  );
}
function isAnimatedString(value) {
  return is.str(value) && (value[0] == "#" || /\d/.test(value) || // Do not identify a CSS variable as an AnimatedString if its SSR
  !isSSR() && cssVariableRegex.test(value) || value in (colors || {}));
}
var useIsomorphicLayoutEffect = isSSR() ? import_react4.useEffect : import_react4.useLayoutEffect;
var useIsMounted = () => {
  const isMounted = (0, import_react3.useRef)(false);
  useIsomorphicLayoutEffect(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
function useForceUpdate() {
  const update5 = (0, import_react2.useState)()[1];
  const isMounted = useIsMounted();
  return () => {
    if (isMounted.current) {
      update5(Math.random());
    }
  };
}
var useOnce = (effect) => (0, import_react6.useEffect)(effect, emptyDeps);
var emptyDeps = [];
function usePrev(value) {
  const prevRef = (0, import_react7.useRef)(void 0);
  (0, import_react7.useEffect)(() => {
    prevRef.current = value;
  });
  return prevRef.current;
}

// node_modules/@nivo/axes/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var import_react10 = __toESM(require_react(), 1);

// node_modules/@nivo/axes/node_modules/@react-spring/animated/dist/react-spring_animated.modern.mjs
var React = __toESM(require_react(), 1);
var import_react9 = __toESM(require_react(), 1);
var $node = Symbol.for("Animated:node");
var isAnimated = (value) => !!value && value[$node] === value;
var getAnimated = (owner) => owner && owner[$node];
var setAnimated = (owner, node) => defineHidden(owner, $node, node);
var getPayload = (owner) => owner && owner[$node] && owner[$node].getPayload();
var Animated = class {
  constructor() {
    setAnimated(this, this);
  }
  /** Get every `AnimatedValue` used by this node. */
  getPayload() {
    return this.payload || [];
  }
};
var AnimatedValue = class _AnimatedValue extends Animated {
  constructor(_value) {
    super();
    this._value = _value;
    this.done = true;
    this.durationProgress = 0;
    if (is.num(this._value)) {
      this.lastPosition = this._value;
    }
  }
  /** @internal */
  static create(value) {
    return new _AnimatedValue(value);
  }
  getPayload() {
    return [this];
  }
  getValue() {
    return this._value;
  }
  setValue(value, step) {
    if (is.num(value)) {
      this.lastPosition = value;
      if (step) {
        value = Math.round(value / step) * step;
        if (this.done) {
          this.lastPosition = value;
        }
      }
    }
    if (this._value === value) {
      return false;
    }
    this._value = value;
    return true;
  }
  reset() {
    const { done } = this;
    this.done = false;
    if (is.num(this._value)) {
      this.elapsedTime = 0;
      this.durationProgress = 0;
      this.lastPosition = this._value;
      if (done) this.lastVelocity = null;
      this.v0 = null;
    }
  }
};
var AnimatedString = class _AnimatedString extends AnimatedValue {
  constructor(value) {
    super(0);
    this._string = null;
    this._toString = createInterpolator({
      output: [value, value]
    });
  }
  /** @internal */
  static create(value) {
    return new _AnimatedString(value);
  }
  getValue() {
    const value = this._string;
    return value == null ? this._string = this._toString(this._value) : value;
  }
  setValue(value) {
    if (is.str(value)) {
      if (value == this._string) {
        return false;
      }
      this._string = value;
      this._value = 1;
    } else if (super.setValue(value)) {
      this._string = null;
    } else {
      return false;
    }
    return true;
  }
  reset(goal) {
    if (goal) {
      this._toString = createInterpolator({
        output: [this.getValue(), goal]
      });
    }
    this._value = 0;
    super.reset();
  }
};
var TreeContext = { dependencies: null };
var AnimatedObject = class extends Animated {
  constructor(source) {
    super();
    this.source = source;
    this.setValue(source);
  }
  getValue(animated3) {
    const values = {};
    eachProp(this.source, (source, key) => {
      if (isAnimated(source)) {
        values[key] = source.getValue(animated3);
      } else if (hasFluidValue(source)) {
        values[key] = getFluidValue(source);
      } else if (!animated3) {
        values[key] = source;
      }
    });
    return values;
  }
  /** Replace the raw object data */
  setValue(source) {
    this.source = source;
    this.payload = this._makePayload(source);
  }
  reset() {
    if (this.payload) {
      each(this.payload, (node) => node.reset());
    }
  }
  /** Create a payload set. */
  _makePayload(source) {
    if (source) {
      const payload = /* @__PURE__ */ new Set();
      eachProp(source, this._addToPayload, payload);
      return Array.from(payload);
    }
  }
  /** Add to a payload set. */
  _addToPayload(source) {
    if (TreeContext.dependencies && hasFluidValue(source)) {
      TreeContext.dependencies.add(source);
    }
    const payload = getPayload(source);
    if (payload) {
      each(payload, (node) => this.add(node));
    }
  }
};
var AnimatedArray = class _AnimatedArray extends AnimatedObject {
  constructor(source) {
    super(source);
  }
  /** @internal */
  static create(source) {
    return new _AnimatedArray(source);
  }
  getValue() {
    return this.source.map((node) => node.getValue());
  }
  setValue(source) {
    const payload = this.getPayload();
    if (source.length == payload.length) {
      return payload.map((node, i4) => node.setValue(source[i4])).some(Boolean);
    }
    super.setValue(source.map(makeAnimated));
    return true;
  }
};
function makeAnimated(value) {
  const nodeType = isAnimatedString(value) ? AnimatedString : AnimatedValue;
  return nodeType.create(value);
}
function getAnimatedType(value) {
  const parentNode = getAnimated(value);
  return parentNode ? parentNode.constructor : is.arr(value) ? AnimatedArray : isAnimatedString(value) ? AnimatedString : AnimatedValue;
}
var withAnimated = (Component, host3) => {
  const hasInstance = (
    // Function components must use "forwardRef" to avoid being
    // re-rendered on every animation frame.
    !is.fun(Component) || Component.prototype && Component.prototype.isReactComponent
  );
  return (0, import_react9.forwardRef)((givenProps, givenRef) => {
    const instanceRef = (0, import_react9.useRef)(null);
    const ref = hasInstance && // eslint-disable-next-line react-hooks/rules-of-hooks
    (0, import_react9.useCallback)(
      (value) => {
        instanceRef.current = updateRef(givenRef, value);
      },
      [givenRef]
    );
    const [props, deps] = getAnimatedState(givenProps, host3);
    const forceUpdate = useForceUpdate();
    const callback = () => {
      const instance = instanceRef.current;
      if (hasInstance && !instance) {
        return;
      }
      const didUpdate = instance ? host3.applyAnimatedValues(instance, props.getValue(true)) : false;
      if (didUpdate === false) {
        forceUpdate();
      }
    };
    const observer = new PropsObserver(callback, deps);
    const observerRef = (0, import_react9.useRef)(void 0);
    useIsomorphicLayoutEffect(() => {
      observerRef.current = observer;
      each(deps, (dep) => addFluidObserver(dep, observer));
      return () => {
        if (observerRef.current) {
          each(
            observerRef.current.deps,
            (dep) => removeFluidObserver(dep, observerRef.current)
          );
          raf.cancel(observerRef.current.update);
        }
      };
    });
    (0, import_react9.useEffect)(callback, []);
    useOnce(() => () => {
      const observer2 = observerRef.current;
      each(observer2.deps, (dep) => removeFluidObserver(dep, observer2));
    });
    const usedProps = host3.getComponentProps(props.getValue());
    return React.createElement(Component, { ...usedProps, ref });
  });
};
var PropsObserver = class {
  constructor(update5, deps) {
    this.update = update5;
    this.deps = deps;
  }
  eventObserved(event) {
    if (event.type == "change") {
      raf.write(this.update);
    }
  }
};
function getAnimatedState(props, host3) {
  const dependencies = /* @__PURE__ */ new Set();
  TreeContext.dependencies = dependencies;
  if (props.style)
    props = {
      ...props,
      style: host3.createAnimatedStyle(props.style)
    };
  props = new AnimatedObject(props);
  TreeContext.dependencies = null;
  return [props, dependencies];
}
function updateRef(ref, value) {
  if (ref) {
    if (is.fun(ref)) ref(value);
    else ref.current = value;
  }
  return value;
}
var cacheKey = Symbol.for("AnimatedComponent");
var createHost = (components, {
  applyAnimatedValues: applyAnimatedValues3 = () => false,
  createAnimatedStyle = (style) => new AnimatedObject(style),
  getComponentProps = (props) => props
} = {}) => {
  const hostConfig = {
    applyAnimatedValues: applyAnimatedValues3,
    createAnimatedStyle,
    getComponentProps
  };
  const animated3 = (Component) => {
    const displayName = getDisplayName(Component) || "Anonymous";
    if (is.str(Component)) {
      Component = animated3[Component] || (animated3[Component] = withAnimated(Component, hostConfig));
    } else {
      Component = Component[cacheKey] || (Component[cacheKey] = withAnimated(Component, hostConfig));
    }
    Component.displayName = `Animated(${displayName})`;
    return Component;
  };
  eachProp(components, (Component, key) => {
    if (is.arr(components)) {
      key = getDisplayName(Component);
    }
    animated3[key] = animated3(Component);
  });
  return {
    animated: animated3
  };
};
var getDisplayName = (arg) => is.str(arg) ? arg : arg && is.str(arg.displayName) ? arg.displayName : is.fun(arg) && arg.name || null;

// node_modules/@nivo/axes/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var React2 = __toESM(require_react(), 1);
var import_react11 = __toESM(require_react(), 1);
var import_react12 = __toESM(require_react(), 1);
var React22 = __toESM(require_react(), 1);
var import_react13 = __toESM(require_react(), 1);
var import_react14 = __toESM(require_react(), 1);
function callProp(value, ...args) {
  return is.fun(value) ? value(...args) : value;
}
var matchProp = (value, key) => value === true || !!(key && value && (is.fun(value) ? value(key) : toArray(value).includes(key)));
var resolveProp = (prop, key) => is.obj(prop) ? key && prop[key] : prop;
var getDefaultProp = (props, key) => props.default === true ? props[key] : props.default ? props.default[key] : void 0;
var noopTransform = (value) => value;
var getDefaultProps = (props, transform = noopTransform) => {
  let keys = DEFAULT_PROPS;
  if (props.default && props.default !== true) {
    props = props.default;
    keys = Object.keys(props);
  }
  const defaults22 = {};
  for (const key of keys) {
    const value = transform(props[key], key);
    if (!is.und(value)) {
      defaults22[key] = value;
    }
  }
  return defaults22;
};
var DEFAULT_PROPS = [
  "config",
  "onProps",
  "onStart",
  "onChange",
  "onPause",
  "onResume",
  "onRest"
];
var RESERVED_PROPS = {
  config: 1,
  from: 1,
  to: 1,
  ref: 1,
  loop: 1,
  reset: 1,
  pause: 1,
  cancel: 1,
  reverse: 1,
  immediate: 1,
  default: 1,
  delay: 1,
  onProps: 1,
  onStart: 1,
  onChange: 1,
  onPause: 1,
  onResume: 1,
  onRest: 1,
  onResolve: 1,
  // Transition props
  items: 1,
  trail: 1,
  sort: 1,
  expires: 1,
  initial: 1,
  enter: 1,
  update: 1,
  leave: 1,
  children: 1,
  onDestroyed: 1,
  // Internal props
  keys: 1,
  callId: 1,
  parentId: 1
};
function getForwardProps(props) {
  const forward = {};
  let count = 0;
  eachProp(props, (value, prop) => {
    if (!RESERVED_PROPS[prop]) {
      forward[prop] = value;
      count++;
    }
  });
  if (count) {
    return forward;
  }
}
function inferTo(props) {
  const to22 = getForwardProps(props);
  if (to22) {
    const out = { to: to22 };
    eachProp(props, (val, key) => key in to22 || (out[key] = val));
    return out;
  }
  return { ...props };
}
function computeGoal(value) {
  value = getFluidValue(value);
  return is.arr(value) ? value.map(computeGoal) : isAnimatedString(value) ? globals_exports.createStringInterpolator({
    range: [0, 1],
    output: [value, value]
  })(1) : value;
}
function hasProps(props) {
  for (const _2 in props) return true;
  return false;
}
function isAsyncTo(to22) {
  return is.fun(to22) || is.arr(to22) && is.obj(to22[0]);
}
function detachRefs(ctrl, ref) {
  var _a;
  (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
  ref == null ? void 0 : ref.delete(ctrl);
}
function replaceRef(ctrl, ref) {
  var _a;
  if (ref && ctrl.ref !== ref) {
    (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
    ref.add(ctrl);
    ctrl.ref = ref;
  }
}
var config = {
  default: { tension: 170, friction: 26 },
  gentle: { tension: 120, friction: 14 },
  wobbly: { tension: 180, friction: 12 },
  stiff: { tension: 210, friction: 20 },
  slow: { tension: 280, friction: 60 },
  molasses: { tension: 280, friction: 120 }
};
var defaults = {
  ...config.default,
  mass: 1,
  damping: 1,
  easing: easings.linear,
  clamp: false
};
var AnimationConfig = class {
  constructor() {
    this.velocity = 0;
    Object.assign(this, defaults);
  }
};
function mergeConfig(config22, newConfig, defaultConfig) {
  if (defaultConfig) {
    defaultConfig = { ...defaultConfig };
    sanitizeConfig(defaultConfig, newConfig);
    newConfig = { ...defaultConfig, ...newConfig };
  }
  sanitizeConfig(config22, newConfig);
  Object.assign(config22, newConfig);
  for (const key in defaults) {
    if (config22[key] == null) {
      config22[key] = defaults[key];
    }
  }
  let { frequency, damping } = config22;
  const { mass } = config22;
  if (!is.und(frequency)) {
    if (frequency < 0.01) frequency = 0.01;
    if (damping < 0) damping = 0;
    config22.tension = Math.pow(2 * Math.PI / frequency, 2) * mass;
    config22.friction = 4 * Math.PI * damping * mass / frequency;
  }
  return config22;
}
function sanitizeConfig(config22, props) {
  if (!is.und(props.decay)) {
    config22.duration = void 0;
  } else {
    const isTensionConfig = !is.und(props.tension) || !is.und(props.friction);
    if (isTensionConfig || !is.und(props.frequency) || !is.und(props.damping) || !is.und(props.mass)) {
      config22.duration = void 0;
      config22.decay = void 0;
    }
    if (isTensionConfig) {
      config22.frequency = void 0;
    }
  }
}
var emptyArray = [];
var Animation = class {
  constructor() {
    this.changed = false;
    this.values = emptyArray;
    this.toValues = null;
    this.fromValues = emptyArray;
    this.config = new AnimationConfig();
    this.immediate = false;
  }
};
function scheduleProps(callId, { key, props, defaultProps, state, actions }) {
  return new Promise((resolve, reject) => {
    let delay;
    let timeout;
    let cancel = matchProp(props.cancel ?? (defaultProps == null ? void 0 : defaultProps.cancel), key);
    if (cancel) {
      onStart();
    } else {
      if (!is.und(props.pause)) {
        state.paused = matchProp(props.pause, key);
      }
      let pause = defaultProps == null ? void 0 : defaultProps.pause;
      if (pause !== true) {
        pause = state.paused || matchProp(pause, key);
      }
      delay = callProp(props.delay || 0, key);
      if (pause) {
        state.resumeQueue.add(onResume);
        actions.pause();
      } else {
        actions.resume();
        onResume();
      }
    }
    function onPause() {
      state.resumeQueue.add(onResume);
      state.timeouts.delete(timeout);
      timeout.cancel();
      delay = timeout.time - raf.now();
    }
    function onResume() {
      if (delay > 0 && !globals_exports.skipAnimation) {
        state.delayed = true;
        timeout = raf.setTimeout(onStart, delay);
        state.pauseQueue.add(onPause);
        state.timeouts.add(timeout);
      } else {
        onStart();
      }
    }
    function onStart() {
      if (state.delayed) {
        state.delayed = false;
      }
      state.pauseQueue.delete(onPause);
      state.timeouts.delete(timeout);
      if (callId <= (state.cancelId || 0)) {
        cancel = true;
      }
      try {
        actions.start({ ...props, callId, cancel }, resolve);
      } catch (err) {
        reject(err);
      }
    }
  });
}
var getCombinedResult = (target, results) => results.length == 1 ? results[0] : results.some((result) => result.cancelled) ? getCancelledResult(target.get()) : results.every((result) => result.noop) ? getNoopResult(target.get()) : getFinishedResult(
  target.get(),
  results.every((result) => result.finished)
);
var getNoopResult = (value) => ({
  value,
  noop: true,
  finished: true,
  cancelled: false
});
var getFinishedResult = (value, finished, cancelled = false) => ({
  value,
  finished,
  cancelled
});
var getCancelledResult = (value) => ({
  value,
  cancelled: true,
  finished: false
});
function runAsync(to22, props, state, target) {
  const { callId, parentId, onRest } = props;
  const { asyncTo: prevTo, promise: prevPromise } = state;
  if (!parentId && to22 === prevTo && !props.reset) {
    return prevPromise;
  }
  return state.promise = (async () => {
    state.asyncId = callId;
    state.asyncTo = to22;
    const defaultProps = getDefaultProps(
      props,
      (value, key) => (
        // The `onRest` prop is only called when the `runAsync` promise is resolved.
        key === "onRest" ? void 0 : value
      )
    );
    let preventBail;
    let bail;
    const bailPromise = new Promise(
      (resolve, reject) => (preventBail = resolve, bail = reject)
    );
    const bailIfEnded = (bailSignal) => {
      const bailResult = (
        // The `cancel` prop or `stop` method was used.
        callId <= (state.cancelId || 0) && getCancelledResult(target) || // The async `to` prop was replaced.
        callId !== state.asyncId && getFinishedResult(target, false)
      );
      if (bailResult) {
        bailSignal.result = bailResult;
        bail(bailSignal);
        throw bailSignal;
      }
    };
    const animate = (arg1, arg2) => {
      const bailSignal = new BailSignal();
      const skipAnimationSignal = new SkipAnimationSignal();
      return (async () => {
        if (globals_exports.skipAnimation) {
          stopAsync(state);
          skipAnimationSignal.result = getFinishedResult(target, false);
          bail(skipAnimationSignal);
          throw skipAnimationSignal;
        }
        bailIfEnded(bailSignal);
        const props2 = is.obj(arg1) ? { ...arg1 } : { ...arg2, to: arg1 };
        props2.parentId = callId;
        eachProp(defaultProps, (value, key) => {
          if (is.und(props2[key])) {
            props2[key] = value;
          }
        });
        const result2 = await target.start(props2);
        bailIfEnded(bailSignal);
        if (state.paused) {
          await new Promise((resume) => {
            state.resumeQueue.add(resume);
          });
        }
        return result2;
      })();
    };
    let result;
    if (globals_exports.skipAnimation) {
      stopAsync(state);
      return getFinishedResult(target, false);
    }
    try {
      let animating;
      if (is.arr(to22)) {
        animating = (async (queue) => {
          for (const props2 of queue) {
            await animate(props2);
          }
        })(to22);
      } else {
        animating = Promise.resolve(to22(animate, target.stop.bind(target)));
      }
      await Promise.all([animating.then(preventBail), bailPromise]);
      result = getFinishedResult(target.get(), true, false);
    } catch (err) {
      if (err instanceof BailSignal) {
        result = err.result;
      } else if (err instanceof SkipAnimationSignal) {
        result = err.result;
      } else {
        throw err;
      }
    } finally {
      if (callId == state.asyncId) {
        state.asyncId = parentId;
        state.asyncTo = parentId ? prevTo : void 0;
        state.promise = parentId ? prevPromise : void 0;
      }
    }
    if (is.fun(onRest)) {
      raf.batchedUpdates(() => {
        onRest(result, target, target.item);
      });
    }
    return result;
  })();
}
function stopAsync(state, cancelId) {
  flush(state.timeouts, (t4) => t4.cancel());
  state.pauseQueue.clear();
  state.resumeQueue.clear();
  state.asyncId = state.asyncTo = state.promise = void 0;
  if (cancelId) state.cancelId = cancelId;
}
var BailSignal = class extends Error {
  constructor() {
    super(
      "An async animation has been interrupted. You see this error because you forgot to use `await` or `.catch(...)` on its returned promise."
    );
  }
};
var SkipAnimationSignal = class extends Error {
  constructor() {
    super("SkipAnimationSignal");
  }
};
var isFrameValue = (value) => value instanceof FrameValue;
var nextId = 1;
var FrameValue = class extends FluidValue {
  constructor() {
    super(...arguments);
    this.id = nextId++;
    this._priority = 0;
  }
  get priority() {
    return this._priority;
  }
  set priority(priority3) {
    if (this._priority != priority3) {
      this._priority = priority3;
      this._onPriorityChange(priority3);
    }
  }
  /** Get the current value */
  get() {
    const node = getAnimated(this);
    return node && node.getValue();
  }
  /** Create a spring that maps our value to another value */
  to(...args) {
    return globals_exports.to(this, args);
  }
  /** @deprecated Use the `to` method instead. */
  interpolate(...args) {
    deprecateInterpolate();
    return globals_exports.to(this, args);
  }
  toJSON() {
    return this.get();
  }
  observerAdded(count) {
    if (count == 1) this._attach();
  }
  observerRemoved(count) {
    if (count == 0) this._detach();
  }
  /** Called when the first child is added. */
  _attach() {
  }
  /** Called when the last child is removed. */
  _detach() {
  }
  /** Tell our children about our new value */
  _onChange(value, idle = false) {
    callFluidObservers(this, {
      type: "change",
      parent: this,
      value,
      idle
    });
  }
  /** Tell our children about our new priority */
  _onPriorityChange(priority3) {
    if (!this.idle) {
      frameLoop.sort(this);
    }
    callFluidObservers(this, {
      type: "priority",
      parent: this,
      priority: priority3
    });
  }
};
var $P = Symbol.for("SpringPhase");
var HAS_ANIMATED = 1;
var IS_ANIMATING = 2;
var IS_PAUSED = 4;
var hasAnimated = (target) => (target[$P] & HAS_ANIMATED) > 0;
var isAnimating = (target) => (target[$P] & IS_ANIMATING) > 0;
var isPaused = (target) => (target[$P] & IS_PAUSED) > 0;
var setActiveBit = (target, active) => active ? target[$P] |= IS_ANIMATING | HAS_ANIMATED : target[$P] &= ~IS_ANIMATING;
var setPausedBit = (target, paused) => paused ? target[$P] |= IS_PAUSED : target[$P] &= ~IS_PAUSED;
var SpringValue = class extends FrameValue {
  constructor(arg1, arg2) {
    super();
    this.animation = new Animation();
    this.defaultProps = {};
    this._state = {
      paused: false,
      delayed: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._pendingCalls = /* @__PURE__ */ new Set();
    this._lastCallId = 0;
    this._lastToId = 0;
    this._memoizedDuration = 0;
    if (!is.und(arg1) || !is.und(arg2)) {
      const props = is.obj(arg1) ? { ...arg1 } : { ...arg2, from: arg1 };
      if (is.und(props.default)) {
        props.default = true;
      }
      this.start(props);
    }
  }
  /** Equals true when not advancing on each frame. */
  get idle() {
    return !(isAnimating(this) || this._state.asyncTo) || isPaused(this);
  }
  get goal() {
    return getFluidValue(this.animation.to);
  }
  get velocity() {
    const node = getAnimated(this);
    return node instanceof AnimatedValue ? node.lastVelocity || 0 : node.getPayload().map((node2) => node2.lastVelocity || 0);
  }
  /**
   * When true, this value has been animated at least once.
   */
  get hasAnimated() {
    return hasAnimated(this);
  }
  /**
   * When true, this value has an unfinished animation,
   * which is either active or paused.
   */
  get isAnimating() {
    return isAnimating(this);
  }
  /**
   * When true, all current and future animations are paused.
   */
  get isPaused() {
    return isPaused(this);
  }
  /**
   *
   *
   */
  get isDelayed() {
    return this._state.delayed;
  }
  /** Advance the current animation by a number of milliseconds */
  advance(dt) {
    let idle = true;
    let changed = false;
    const anim = this.animation;
    let { toValues } = anim;
    const { config: config22 } = anim;
    const payload = getPayload(anim.to);
    if (!payload && hasFluidValue(anim.to)) {
      toValues = toArray(getFluidValue(anim.to));
    }
    anim.values.forEach((node2, i4) => {
      if (node2.done) return;
      const to22 = (
        // Animated strings always go from 0 to 1.
        node2.constructor == AnimatedString ? 1 : payload ? payload[i4].lastPosition : toValues[i4]
      );
      let finished = anim.immediate;
      let position = to22;
      if (!finished) {
        position = node2.lastPosition;
        if (config22.tension <= 0) {
          node2.done = true;
          return;
        }
        let elapsed = node2.elapsedTime += dt;
        const from = anim.fromValues[i4];
        const v0 = node2.v0 != null ? node2.v0 : node2.v0 = is.arr(config22.velocity) ? config22.velocity[i4] : config22.velocity;
        let velocity;
        const precision = config22.precision || (from == to22 ? 5e-3 : Math.min(1, Math.abs(to22 - from) * 1e-3));
        if (!is.und(config22.duration)) {
          let p2 = 1;
          if (config22.duration > 0) {
            if (this._memoizedDuration !== config22.duration) {
              this._memoizedDuration = config22.duration;
              if (node2.durationProgress > 0) {
                node2.elapsedTime = config22.duration * node2.durationProgress;
                elapsed = node2.elapsedTime += dt;
              }
            }
            p2 = (config22.progress || 0) + elapsed / this._memoizedDuration;
            p2 = p2 > 1 ? 1 : p2 < 0 ? 0 : p2;
            node2.durationProgress = p2;
          }
          position = from + config22.easing(p2) * (to22 - from);
          velocity = (position - node2.lastPosition) / dt;
          finished = p2 == 1;
        } else if (config22.decay) {
          const decay = config22.decay === true ? 0.998 : config22.decay;
          const e4 = Math.exp(-(1 - decay) * elapsed);
          position = from + v0 / (1 - decay) * (1 - e4);
          finished = Math.abs(node2.lastPosition - position) <= precision;
          velocity = v0 * e4;
        } else {
          velocity = node2.lastVelocity == null ? v0 : node2.lastVelocity;
          const restVelocity = config22.restVelocity || precision / 10;
          const bounceFactor = config22.clamp ? 0 : config22.bounce;
          const canBounce = !is.und(bounceFactor);
          const isGrowing = from == to22 ? node2.v0 > 0 : from < to22;
          let isMoving;
          let isBouncing = false;
          const step = 1;
          const numSteps = Math.ceil(dt / step);
          for (let n4 = 0; n4 < numSteps; ++n4) {
            isMoving = Math.abs(velocity) > restVelocity;
            if (!isMoving) {
              finished = Math.abs(to22 - position) <= precision;
              if (finished) {
                break;
              }
            }
            if (canBounce) {
              isBouncing = position == to22 || position > to22 == isGrowing;
              if (isBouncing) {
                velocity = -velocity * bounceFactor;
                position = to22;
              }
            }
            const springForce = -config22.tension * 1e-6 * (position - to22);
            const dampingForce = -config22.friction * 1e-3 * velocity;
            const acceleration = (springForce + dampingForce) / config22.mass;
            velocity = velocity + acceleration * step;
            position = position + velocity * step;
          }
        }
        node2.lastVelocity = velocity;
        if (Number.isNaN(position)) {
          console.warn(`Got NaN while animating:`, this);
          finished = true;
        }
      }
      if (payload && !payload[i4].done) {
        finished = false;
      }
      if (finished) {
        node2.done = true;
      } else {
        idle = false;
      }
      if (node2.setValue(position, config22.round)) {
        changed = true;
      }
    });
    const node = getAnimated(this);
    const currVal = node.getValue();
    if (idle) {
      const finalVal = getFluidValue(anim.to);
      if ((currVal !== finalVal || changed) && !config22.decay) {
        node.setValue(finalVal);
        this._onChange(finalVal);
      } else if (changed && config22.decay) {
        this._onChange(currVal);
      }
      this._stop();
    } else if (changed) {
      this._onChange(currVal);
    }
  }
  /** Set the current value, while stopping the current animation */
  set(value) {
    raf.batchedUpdates(() => {
      this._stop();
      this._focus(value);
      this._set(value);
    });
    return this;
  }
  /**
   * Freeze the active animation in time, as well as any updates merged
   * before `resume` is called.
   */
  pause() {
    this._update({ pause: true });
  }
  /** Resume the animation if paused. */
  resume() {
    this._update({ pause: false });
  }
  /** Skip to the end of the current animation. */
  finish() {
    if (isAnimating(this)) {
      const { to: to22, config: config22 } = this.animation;
      raf.batchedUpdates(() => {
        this._onStart();
        if (!config22.decay) {
          this._set(to22, false);
        }
        this._stop();
      });
    }
    return this;
  }
  /** Push props into the pending queue. */
  update(props) {
    const queue = this.queue || (this.queue = []);
    queue.push(props);
    return this;
  }
  start(to22, arg2) {
    let queue;
    if (!is.und(to22)) {
      queue = [is.obj(to22) ? to22 : { ...arg2, to: to22 }];
    } else {
      queue = this.queue || [];
      this.queue = [];
    }
    return Promise.all(
      queue.map((props) => {
        const up = this._update(props);
        return up;
      })
    ).then((results) => getCombinedResult(this, results));
  }
  /**
   * Stop the current animation, and cancel any delayed updates.
   *
   * Pass `true` to call `onRest` with `cancelled: true`.
   */
  stop(cancel) {
    const { to: to22 } = this.animation;
    this._focus(this.get());
    stopAsync(this._state, cancel && this._lastCallId);
    raf.batchedUpdates(() => this._stop(to22, cancel));
    return this;
  }
  /** Restart the animation. */
  reset() {
    this._update({ reset: true });
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._start();
    } else if (event.type == "priority") {
      this.priority = event.priority + 1;
    }
  }
  /**
   * Parse the `to` and `from` range from the given `props` object.
   *
   * This also ensures the initial value is available to animated components
   * during the render phase.
   */
  _prepareNode(props) {
    const key = this.key || "";
    let { to: to22, from } = props;
    to22 = is.obj(to22) ? to22[key] : to22;
    if (to22 == null || isAsyncTo(to22)) {
      to22 = void 0;
    }
    from = is.obj(from) ? from[key] : from;
    if (from == null) {
      from = void 0;
    }
    const range = { to: to22, from };
    if (!hasAnimated(this)) {
      if (props.reverse) [to22, from] = [from, to22];
      from = getFluidValue(from);
      if (!is.und(from)) {
        this._set(from);
      } else if (!getAnimated(this)) {
        this._set(to22);
      }
    }
    return range;
  }
  /** Every update is processed by this method before merging. */
  _update({ ...props }, isLoop) {
    const { key, defaultProps } = this;
    if (props.default)
      Object.assign(
        defaultProps,
        getDefaultProps(
          props,
          (value, prop) => /^on/.test(prop) ? resolveProp(value, key) : value
        )
      );
    mergeActiveFn(this, props, "onProps");
    sendEvent(this, "onProps", props, this);
    const range = this._prepareNode(props);
    if (Object.isFrozen(this)) {
      throw Error(
        "Cannot animate a `SpringValue` object that is frozen. Did you forget to pass your component to `animated(...)` before animating its props?"
      );
    }
    const state = this._state;
    return scheduleProps(++this._lastCallId, {
      key,
      props,
      defaultProps,
      state,
      actions: {
        pause: () => {
          if (!isPaused(this)) {
            setPausedBit(this, true);
            flushCalls(state.pauseQueue);
            sendEvent(
              this,
              "onPause",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        resume: () => {
          if (isPaused(this)) {
            setPausedBit(this, false);
            if (isAnimating(this)) {
              this._resume();
            }
            flushCalls(state.resumeQueue);
            sendEvent(
              this,
              "onResume",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        start: this._merge.bind(this, range)
      }
    }).then((result) => {
      if (props.loop && result.finished && !(isLoop && result.noop)) {
        const nextProps = createLoopUpdate(props);
        if (nextProps) {
          return this._update(nextProps, true);
        }
      }
      return result;
    });
  }
  /** Merge props into the current animation */
  _merge(range, props, resolve) {
    if (props.cancel) {
      this.stop(true);
      return resolve(getCancelledResult(this));
    }
    const hasToProp = !is.und(range.to);
    const hasFromProp = !is.und(range.from);
    if (hasToProp || hasFromProp) {
      if (props.callId > this._lastToId) {
        this._lastToId = props.callId;
      } else {
        return resolve(getCancelledResult(this));
      }
    }
    const { key, defaultProps, animation: anim } = this;
    const { to: prevTo, from: prevFrom } = anim;
    let { to: to22 = prevTo, from = prevFrom } = range;
    if (hasFromProp && !hasToProp && (!props.default || is.und(to22))) {
      to22 = from;
    }
    if (props.reverse) [to22, from] = [from, to22];
    const hasFromChanged = !isEqual(from, prevFrom);
    if (hasFromChanged) {
      anim.from = from;
    }
    from = getFluidValue(from);
    const hasToChanged = !isEqual(to22, prevTo);
    if (hasToChanged) {
      this._focus(to22);
    }
    const hasAsyncTo = isAsyncTo(props.to);
    const { config: config22 } = anim;
    const { decay, velocity } = config22;
    if (hasToProp || hasFromProp) {
      config22.velocity = 0;
    }
    if (props.config && !hasAsyncTo) {
      mergeConfig(
        config22,
        callProp(props.config, key),
        // Avoid calling the same "config" prop twice.
        props.config !== defaultProps.config ? callProp(defaultProps.config, key) : void 0
      );
    }
    let node = getAnimated(this);
    if (!node || is.und(to22)) {
      return resolve(getFinishedResult(this, true));
    }
    const reset = (
      // When `reset` is undefined, the `from` prop implies `reset: true`,
      // except for declarative updates. When `reset` is defined, there
      // must exist a value to animate from.
      is.und(props.reset) ? hasFromProp && !props.default : !is.und(from) && matchProp(props.reset, key)
    );
    const value = reset ? from : this.get();
    const goal = computeGoal(to22);
    const isAnimatable = is.num(goal) || is.arr(goal) || isAnimatedString(goal);
    const immediate = !hasAsyncTo && (!isAnimatable || matchProp(defaultProps.immediate || props.immediate, key));
    if (hasToChanged) {
      const nodeType = getAnimatedType(to22);
      if (nodeType !== node.constructor) {
        if (immediate) {
          node = this._set(goal);
        } else
          throw Error(
            `Cannot animate between ${node.constructor.name} and ${nodeType.name}, as the "to" prop suggests`
          );
      }
    }
    const goalType = node.constructor;
    let started = hasFluidValue(to22);
    let finished = false;
    if (!started) {
      const hasValueChanged = reset || !hasAnimated(this) && hasFromChanged;
      if (hasToChanged || hasValueChanged) {
        finished = isEqual(computeGoal(value), goal);
        started = !finished;
      }
      if (!isEqual(anim.immediate, immediate) && !immediate || !isEqual(config22.decay, decay) || !isEqual(config22.velocity, velocity)) {
        started = true;
      }
    }
    if (finished && isAnimating(this)) {
      if (anim.changed && !reset) {
        started = true;
      } else if (!started) {
        this._stop(prevTo);
      }
    }
    if (!hasAsyncTo) {
      if (started || hasFluidValue(prevTo)) {
        anim.values = node.getPayload();
        anim.toValues = hasFluidValue(to22) ? null : goalType == AnimatedString ? [1] : toArray(goal);
      }
      if (anim.immediate != immediate) {
        anim.immediate = immediate;
        if (!immediate && !reset) {
          this._set(prevTo);
        }
      }
      if (started) {
        const { onRest } = anim;
        each(ACTIVE_EVENTS, (type) => mergeActiveFn(this, props, type));
        const result = getFinishedResult(this, checkFinished(this, prevTo));
        flushCalls(this._pendingCalls, result);
        this._pendingCalls.add(resolve);
        if (anim.changed)
          raf.batchedUpdates(() => {
            var _a;
            anim.changed = !reset;
            onRest == null ? void 0 : onRest(result, this);
            if (reset) {
              callProp(defaultProps.onRest, result);
            } else {
              (_a = anim.onStart) == null ? void 0 : _a.call(anim, result, this);
            }
          });
      }
    }
    if (reset) {
      this._set(value);
    }
    if (hasAsyncTo) {
      resolve(runAsync(props.to, props, this._state, this));
    } else if (started) {
      this._start();
    } else if (isAnimating(this) && !hasToChanged) {
      this._pendingCalls.add(resolve);
    } else {
      resolve(getNoopResult(value));
    }
  }
  /** Update the `animation.to` value, which might be a `FluidValue` */
  _focus(value) {
    const anim = this.animation;
    if (value !== anim.to) {
      if (getFluidObservers(this)) {
        this._detach();
      }
      anim.to = value;
      if (getFluidObservers(this)) {
        this._attach();
      }
    }
  }
  _attach() {
    let priority3 = 0;
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      addFluidObserver(to22, this);
      if (isFrameValue(to22)) {
        priority3 = to22.priority + 1;
      }
    }
    this.priority = priority3;
  }
  _detach() {
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      removeFluidObserver(to22, this);
    }
  }
  /**
   * Update the current value from outside the frameloop,
   * and return the `Animated` node.
   */
  _set(arg, idle = true) {
    const value = getFluidValue(arg);
    if (!is.und(value)) {
      const oldNode = getAnimated(this);
      if (!oldNode || !isEqual(value, oldNode.getValue())) {
        const nodeType = getAnimatedType(value);
        if (!oldNode || oldNode.constructor != nodeType) {
          setAnimated(this, nodeType.create(value));
        } else {
          oldNode.setValue(value);
        }
        if (oldNode) {
          raf.batchedUpdates(() => {
            this._onChange(value, idle);
          });
        }
      }
    }
    return getAnimated(this);
  }
  _onStart() {
    const anim = this.animation;
    if (!anim.changed) {
      anim.changed = true;
      sendEvent(
        this,
        "onStart",
        getFinishedResult(this, checkFinished(this, anim.to)),
        this
      );
    }
  }
  _onChange(value, idle) {
    if (!idle) {
      this._onStart();
      callProp(this.animation.onChange, value, this);
    }
    callProp(this.defaultProps.onChange, value, this);
    super._onChange(value, idle);
  }
  // This method resets the animation state (even if already animating) to
  // ensure the latest from/to range is used, and it also ensures this spring
  // is added to the frameloop.
  _start() {
    const anim = this.animation;
    getAnimated(this).reset(getFluidValue(anim.to));
    if (!anim.immediate) {
      anim.fromValues = anim.values.map((node) => node.lastPosition);
    }
    if (!isAnimating(this)) {
      setActiveBit(this, true);
      if (!isPaused(this)) {
        this._resume();
      }
    }
  }
  _resume() {
    if (globals_exports.skipAnimation) {
      this.finish();
    } else {
      frameLoop.start(this);
    }
  }
  /**
   * Exit the frameloop and notify `onRest` listeners.
   *
   * Always wrap `_stop` calls with `batchedUpdates`.
   */
  _stop(goal, cancel) {
    if (isAnimating(this)) {
      setActiveBit(this, false);
      const anim = this.animation;
      each(anim.values, (node) => {
        node.done = true;
      });
      if (anim.toValues) {
        anim.onChange = anim.onPause = anim.onResume = void 0;
      }
      callFluidObservers(this, {
        type: "idle",
        parent: this
      });
      const result = cancel ? getCancelledResult(this.get()) : getFinishedResult(this.get(), checkFinished(this, goal ?? anim.to));
      flushCalls(this._pendingCalls, result);
      if (anim.changed) {
        anim.changed = false;
        sendEvent(this, "onRest", result, this);
      }
    }
  }
};
function checkFinished(target, to22) {
  const goal = computeGoal(to22);
  const value = computeGoal(target.get());
  return isEqual(value, goal);
}
function createLoopUpdate(props, loop3 = props.loop, to22 = props.to) {
  const loopRet = callProp(loop3);
  if (loopRet) {
    const overrides = loopRet !== true && inferTo(loopRet);
    const reverse = (overrides || props).reverse;
    const reset = !overrides || overrides.reset;
    return createUpdate({
      ...props,
      loop: loop3,
      // Avoid updating default props when looping.
      default: false,
      // Never loop the `pause` prop.
      pause: void 0,
      // For the "reverse" prop to loop as expected, the "to" prop
      // must be undefined. The "reverse" prop is ignored when the
      // "to" prop is an array or function.
      to: !reverse || isAsyncTo(to22) ? to22 : void 0,
      // Ignore the "from" prop except on reset.
      from: reset ? props.from : void 0,
      reset,
      // The "loop" prop can return a "useSpring" props object to
      // override any of the original props.
      ...overrides
    });
  }
}
function createUpdate(props) {
  const { to: to22, from } = props = inferTo(props);
  const keys = /* @__PURE__ */ new Set();
  if (is.obj(to22)) findDefined(to22, keys);
  if (is.obj(from)) findDefined(from, keys);
  props.keys = keys.size ? Array.from(keys) : null;
  return props;
}
function declareUpdate(props) {
  const update22 = createUpdate(props);
  if (is.und(update22.default)) {
    update22.default = getDefaultProps(update22);
  }
  return update22;
}
function findDefined(values, keys) {
  eachProp(values, (value, key) => value != null && keys.add(key));
}
var ACTIVE_EVENTS = [
  "onStart",
  "onRest",
  "onChange",
  "onPause",
  "onResume"
];
function mergeActiveFn(target, props, type) {
  target.animation[type] = props[type] !== getDefaultProp(props, type) ? resolveProp(props[type], target.key) : void 0;
}
function sendEvent(target, type, ...args) {
  var _a, _b, _c, _d;
  (_b = (_a = target.animation)[type]) == null ? void 0 : _b.call(_a, ...args);
  (_d = (_c = target.defaultProps)[type]) == null ? void 0 : _d.call(_c, ...args);
}
var BATCHED_EVENTS = ["onStart", "onChange", "onRest"];
var nextId2 = 1;
var Controller = class {
  constructor(props, flush3) {
    this.id = nextId2++;
    this.springs = {};
    this.queue = [];
    this._lastAsyncId = 0;
    this._active = /* @__PURE__ */ new Set();
    this._changed = /* @__PURE__ */ new Set();
    this._started = false;
    this._state = {
      paused: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._events = {
      onStart: /* @__PURE__ */ new Map(),
      onChange: /* @__PURE__ */ new Map(),
      onRest: /* @__PURE__ */ new Map()
    };
    this._onFrame = this._onFrame.bind(this);
    if (flush3) {
      this._flush = flush3;
    }
    if (props) {
      this.start({ default: true, ...props });
    }
  }
  /**
   * Equals `true` when no spring values are in the frameloop, and
   * no async animation is currently active.
   */
  get idle() {
    return !this._state.asyncTo && Object.values(this.springs).every((spring) => {
      return spring.idle && !spring.isDelayed && !spring.isPaused;
    });
  }
  get item() {
    return this._item;
  }
  set item(item) {
    this._item = item;
  }
  /** Get the current values of our springs */
  get() {
    const values = {};
    this.each((spring, key) => values[key] = spring.get());
    return values;
  }
  /** Set the current values without animating. */
  set(values) {
    for (const key in values) {
      const value = values[key];
      if (!is.und(value)) {
        this.springs[key].set(value);
      }
    }
  }
  /** Push an update onto the queue of each value. */
  update(props) {
    if (props) {
      this.queue.push(createUpdate(props));
    }
    return this;
  }
  /**
   * Start the queued animations for every spring, and resolve the returned
   * promise once all queued animations have finished or been cancelled.
   *
   * When you pass a queue (instead of nothing), that queue is used instead of
   * the queued animations added with the `update` method, which are left alone.
   */
  start(props) {
    let { queue } = this;
    if (props) {
      queue = toArray(props).map(createUpdate);
    } else {
      this.queue = [];
    }
    if (this._flush) {
      return this._flush(this, queue);
    }
    prepareKeys(this, queue);
    return flushUpdateQueue(this, queue);
  }
  /** @internal */
  stop(arg, keys) {
    if (arg !== !!arg) {
      keys = arg;
    }
    if (keys) {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].stop(!!arg));
    } else {
      stopAsync(this._state, this._lastAsyncId);
      this.each((spring) => spring.stop(!!arg));
    }
    return this;
  }
  /** Freeze the active animation in time */
  pause(keys) {
    if (is.und(keys)) {
      this.start({ pause: true });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].pause());
    }
    return this;
  }
  /** Resume the animation if paused. */
  resume(keys) {
    if (is.und(keys)) {
      this.start({ pause: false });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].resume());
    }
    return this;
  }
  /** Call a function once per spring value */
  each(iterator) {
    eachProp(this.springs, iterator);
  }
  /** @internal Called at the end of every animation frame */
  _onFrame() {
    const { onStart, onChange, onRest } = this._events;
    const active = this._active.size > 0;
    const changed = this._changed.size > 0;
    if (active && !this._started || changed && !this._started) {
      this._started = true;
      flush(onStart, ([onStart2, result]) => {
        result.value = this.get();
        onStart2(result, this, this._item);
      });
    }
    const idle = !active && this._started;
    const values = changed || idle && onRest.size ? this.get() : null;
    if (changed && onChange.size) {
      flush(onChange, ([onChange2, result]) => {
        result.value = values;
        onChange2(result, this, this._item);
      });
    }
    if (idle) {
      this._started = false;
      flush(onRest, ([onRest2, result]) => {
        result.value = values;
        onRest2(result, this, this._item);
      });
    }
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._changed.add(event.parent);
      if (!event.idle) {
        this._active.add(event.parent);
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else return;
    raf.onFrame(this._onFrame);
  }
};
function flushUpdateQueue(ctrl, queue) {
  return Promise.all(queue.map((props) => flushUpdate(ctrl, props))).then(
    (results) => getCombinedResult(ctrl, results)
  );
}
async function flushUpdate(ctrl, props, isLoop) {
  const { keys, to: to22, from, loop: loop3, onRest, onResolve } = props;
  const defaults22 = is.obj(props.default) && props.default;
  if (loop3) {
    props.loop = false;
  }
  if (to22 === false) props.to = null;
  if (from === false) props.from = null;
  const asyncTo = is.arr(to22) || is.fun(to22) ? to22 : void 0;
  if (asyncTo) {
    props.to = void 0;
    props.onRest = void 0;
    if (defaults22) {
      defaults22.onRest = void 0;
    }
  } else {
    each(BATCHED_EVENTS, (key) => {
      const handler = props[key];
      if (is.fun(handler)) {
        const queue = ctrl["_events"][key];
        props[key] = ({ finished, cancelled }) => {
          const result2 = queue.get(handler);
          if (result2) {
            if (!finished) result2.finished = false;
            if (cancelled) result2.cancelled = true;
          } else {
            queue.set(handler, {
              value: null,
              finished: finished || false,
              cancelled: cancelled || false
            });
          }
        };
        if (defaults22) {
          defaults22[key] = props[key];
        }
      }
    });
  }
  const state = ctrl["_state"];
  if (props.pause === !state.paused) {
    state.paused = props.pause;
    flushCalls(props.pause ? state.pauseQueue : state.resumeQueue);
  } else if (state.paused) {
    props.pause = true;
  }
  const promises = (keys || Object.keys(ctrl.springs)).map(
    (key) => ctrl.springs[key].start(props)
  );
  const cancel = props.cancel === true || getDefaultProp(props, "cancel") === true;
  if (asyncTo || cancel && state.asyncId) {
    promises.push(
      scheduleProps(++ctrl["_lastAsyncId"], {
        props,
        state,
        actions: {
          pause: noop,
          resume: noop,
          start(props2, resolve) {
            if (cancel) {
              stopAsync(state, ctrl["_lastAsyncId"]);
              resolve(getCancelledResult(ctrl));
            } else {
              props2.onRest = onRest;
              resolve(
                runAsync(
                  asyncTo,
                  props2,
                  state,
                  ctrl
                )
              );
            }
          }
        }
      })
    );
  }
  if (state.paused) {
    await new Promise((resume) => {
      state.resumeQueue.add(resume);
    });
  }
  const result = getCombinedResult(ctrl, await Promise.all(promises));
  if (loop3 && result.finished && !(isLoop && result.noop)) {
    const nextProps = createLoopUpdate(props, loop3, to22);
    if (nextProps) {
      prepareKeys(ctrl, [nextProps]);
      return flushUpdate(ctrl, nextProps, true);
    }
  }
  if (onResolve) {
    raf.batchedUpdates(() => onResolve(result, ctrl, ctrl.item));
  }
  return result;
}
function getSprings(ctrl, props) {
  const springs = { ...ctrl.springs };
  if (props) {
    each(toArray(props), (props2) => {
      if (is.und(props2.keys)) {
        props2 = createUpdate(props2);
      }
      if (!is.obj(props2.to)) {
        props2 = { ...props2, to: void 0 };
      }
      prepareSprings(springs, props2, (key) => {
        return createSpring(key);
      });
    });
  }
  setSprings(ctrl, springs);
  return springs;
}
function setSprings(ctrl, springs) {
  eachProp(springs, (spring, key) => {
    if (!ctrl.springs[key]) {
      ctrl.springs[key] = spring;
      addFluidObserver(spring, ctrl);
    }
  });
}
function createSpring(key, observer) {
  const spring = new SpringValue();
  spring.key = key;
  if (observer) {
    addFluidObserver(spring, observer);
  }
  return spring;
}
function prepareSprings(springs, props, create) {
  if (props.keys) {
    each(props.keys, (key) => {
      const spring = springs[key] || (springs[key] = create(key));
      spring["_prepareNode"](props);
    });
  }
}
function prepareKeys(ctrl, queue) {
  each(queue, (props) => {
    prepareSprings(ctrl.springs, props, (key) => {
      return createSpring(key, ctrl);
    });
  });
}
var SpringContext = React2.createContext({
  pause: false,
  immediate: false
});
var SpringRef = () => {
  const current = [];
  const SpringRef22 = function(props) {
    deprecateDirectCall();
    const results = [];
    each(current, (ctrl, i4) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = _getProps(props, ctrl, i4);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.current = current;
  SpringRef22.add = function(ctrl) {
    if (!current.includes(ctrl)) {
      current.push(ctrl);
    }
  };
  SpringRef22.delete = function(ctrl) {
    const i4 = current.indexOf(ctrl);
    if (~i4) current.splice(i4, 1);
  };
  SpringRef22.pause = function() {
    each(current, (ctrl) => ctrl.pause(...arguments));
    return this;
  };
  SpringRef22.resume = function() {
    each(current, (ctrl) => ctrl.resume(...arguments));
    return this;
  };
  SpringRef22.set = function(values) {
    each(current, (ctrl, i4) => {
      const update22 = is.fun(values) ? values(i4, ctrl) : values;
      if (update22) {
        ctrl.set(update22);
      }
    });
  };
  SpringRef22.start = function(props) {
    const results = [];
    each(current, (ctrl, i4) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = this._getProps(props, ctrl, i4);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.stop = function() {
    each(current, (ctrl) => ctrl.stop(...arguments));
    return this;
  };
  SpringRef22.update = function(props) {
    each(current, (ctrl, i4) => ctrl.update(this._getProps(props, ctrl, i4)));
    return this;
  };
  const _getProps = function(arg, ctrl, index) {
    return is.fun(arg) ? arg(index, ctrl) : arg;
  };
  SpringRef22._getProps = _getProps;
  return SpringRef22;
};
function useSprings(length, props, deps) {
  const propsFn = is.fun(props) && props;
  if (propsFn && !deps) deps = [];
  const ref = (0, import_react10.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const layoutId = (0, import_react10.useRef)(0);
  const forceUpdate = useForceUpdate();
  const state = (0, import_react10.useMemo)(
    () => ({
      ctrls: [],
      queue: [],
      flush(ctrl, updates2) {
        const springs2 = getSprings(ctrl, updates2);
        const canFlushSync = layoutId.current > 0 && !state.queue.length && !Object.keys(springs2).some((key) => !ctrl.springs[key]);
        return canFlushSync ? flushUpdateQueue(ctrl, updates2) : new Promise((resolve) => {
          setSprings(ctrl, springs2);
          state.queue.push(() => {
            resolve(flushUpdateQueue(ctrl, updates2));
          });
          forceUpdate();
        });
      }
    }),
    []
  );
  const ctrls = (0, import_react10.useRef)([...state.ctrls]);
  const updates = (0, import_react10.useRef)([]);
  const prevLength = usePrev(length) || 0;
  (0, import_react10.useMemo)(() => {
    each(ctrls.current.slice(length, prevLength), (ctrl) => {
      detachRefs(ctrl, ref);
      ctrl.stop(true);
    });
    ctrls.current.length = length;
    declareUpdates(prevLength, length);
  }, [length]);
  (0, import_react10.useMemo)(() => {
    declareUpdates(0, Math.min(prevLength, length));
  }, deps);
  function declareUpdates(startIndex, endIndex) {
    for (let i4 = startIndex; i4 < endIndex; i4++) {
      const ctrl = ctrls.current[i4] || (ctrls.current[i4] = new Controller(null, state.flush));
      const update22 = propsFn ? propsFn(i4, ctrl) : props[i4];
      if (update22) {
        updates.current[i4] = declareUpdate(update22);
      }
    }
  }
  const springs = ctrls.current.map(
    (ctrl, i4) => getSprings(ctrl, updates.current[i4])
  );
  const context = (0, import_react10.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    layoutId.current++;
    state.ctrls = ctrls.current;
    const { queue } = state;
    if (queue.length) {
      state.queue = [];
      each(queue, (cb) => cb());
    }
    each(ctrls.current, (ctrl, i4) => {
      ref == null ? void 0 : ref.add(ctrl);
      if (hasContext) {
        ctrl.start({ default: context });
      }
      const update22 = updates.current[i4];
      if (update22) {
        replaceRef(ctrl, update22.ref);
        if (ctrl.ref) {
          ctrl.queue.push(update22);
        } else {
          ctrl.start(update22);
        }
      }
    });
  });
  useOnce(() => () => {
    each(state.ctrls, (ctrl) => ctrl.stop(true));
  });
  const values = springs.map((x2) => ({ ...x2 }));
  return ref ? [values, ref] : values;
}
function useSpring(props, deps) {
  const isFn = is.fun(props);
  const [[values], ref] = useSprings(
    1,
    isFn ? props : [props],
    isFn ? deps || [] : deps
  );
  return isFn || arguments.length == 2 ? [values, ref] : values;
}
function useTransition(data, props, deps) {
  const propsFn = is.fun(props) && props;
  const {
    reset,
    sort,
    trail = 0,
    expires = true,
    exitBeforeEnter = false,
    onDestroyed,
    ref: propsRef,
    config: propsConfig
  } = propsFn ? propsFn() : props;
  const ref = (0, import_react13.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const items = toArray(data);
  const transitions = [];
  const usedTransitions = (0, import_react13.useRef)(null);
  const prevTransitions = reset ? null : usedTransitions.current;
  useIsomorphicLayoutEffect(() => {
    usedTransitions.current = transitions;
  });
  useOnce(() => {
    each(transitions, (t4) => {
      ref == null ? void 0 : ref.add(t4.ctrl);
      t4.ctrl.ref = ref;
    });
    return () => {
      each(usedTransitions.current, (t4) => {
        if (t4.expired) {
          clearTimeout(t4.expirationId);
        }
        detachRefs(t4.ctrl, ref);
        t4.ctrl.stop(true);
      });
    };
  });
  const keys = getKeys(items, propsFn ? propsFn() : props, prevTransitions);
  const expired = reset && usedTransitions.current || [];
  useIsomorphicLayoutEffect(
    () => each(expired, ({ ctrl, item, key }) => {
      detachRefs(ctrl, ref);
      callProp(onDestroyed, item, key);
    })
  );
  const reused = [];
  if (prevTransitions)
    each(prevTransitions, (t4, i4) => {
      if (t4.expired) {
        clearTimeout(t4.expirationId);
        expired.push(t4);
      } else {
        i4 = reused[i4] = keys.indexOf(t4.key);
        if (~i4) transitions[i4] = t4;
      }
    });
  each(items, (item, i4) => {
    if (!transitions[i4]) {
      transitions[i4] = {
        key: keys[i4],
        item,
        phase: "mount",
        ctrl: new Controller()
      };
      transitions[i4].ctrl.item = item;
    }
  });
  if (reused.length) {
    let i4 = -1;
    const { leave } = propsFn ? propsFn() : props;
    each(reused, (keyIndex, prevIndex) => {
      const t4 = prevTransitions[prevIndex];
      if (~keyIndex) {
        i4 = transitions.indexOf(t4);
        transitions[i4] = { ...t4, item: items[keyIndex] };
      } else if (leave) {
        transitions.splice(++i4, 0, t4);
      }
    });
  }
  if (is.fun(sort)) {
    transitions.sort((a3, b4) => sort(a3.item, b4.item));
  }
  let delay = -trail;
  const forceUpdate = useForceUpdate();
  const defaultProps = getDefaultProps(props);
  const changes = /* @__PURE__ */ new Map();
  const exitingTransitions = (0, import_react13.useRef)(/* @__PURE__ */ new Map());
  const forceChange = (0, import_react13.useRef)(false);
  each(transitions, (t4, i4) => {
    const key = t4.key;
    const prevPhase = t4.phase;
    const p2 = propsFn ? propsFn() : props;
    let to22;
    let phase;
    const propsDelay = callProp(p2.delay || 0, key);
    if (prevPhase == "mount") {
      to22 = p2.enter;
      phase = "enter";
    } else {
      const isLeave = keys.indexOf(key) < 0;
      if (prevPhase != "leave") {
        if (isLeave) {
          to22 = p2.leave;
          phase = "leave";
        } else if (to22 = p2.update) {
          phase = "update";
        } else return;
      } else if (!isLeave) {
        to22 = p2.enter;
        phase = "enter";
      } else return;
    }
    to22 = callProp(to22, t4.item, i4);
    to22 = is.obj(to22) ? inferTo(to22) : { to: to22 };
    if (!to22.config) {
      const config22 = propsConfig || defaultProps.config;
      to22.config = callProp(config22, t4.item, i4, phase);
    }
    delay += trail;
    const payload = {
      ...defaultProps,
      // we need to add our props.delay value you here.
      delay: propsDelay + delay,
      ref: propsRef,
      immediate: p2.immediate,
      // This prevents implied resets.
      reset: false,
      // Merge any phase-specific props.
      ...to22
    };
    if (phase == "enter" && is.und(payload.from)) {
      const p22 = propsFn ? propsFn() : props;
      const from = is.und(p22.initial) || prevTransitions ? p22.from : p22.initial;
      payload.from = callProp(from, t4.item, i4);
    }
    const { onResolve } = payload;
    payload.onResolve = (result) => {
      callProp(onResolve, result);
      const transitions2 = usedTransitions.current;
      const t22 = transitions2.find((t32) => t32.key === key);
      if (!t22) return;
      if (result.cancelled && t22.phase != "update") {
        return;
      }
      if (t22.ctrl.idle) {
        const idle = transitions2.every((t32) => t32.ctrl.idle);
        if (t22.phase == "leave") {
          const expiry = callProp(expires, t22.item);
          if (expiry !== false) {
            const expiryMs = expiry === true ? 0 : expiry;
            t22.expired = true;
            if (!idle && expiryMs > 0) {
              if (expiryMs <= 2147483647)
                t22.expirationId = setTimeout(forceUpdate, expiryMs);
              return;
            }
          }
        }
        if (idle && transitions2.some((t32) => t32.expired)) {
          exitingTransitions.current.delete(t22);
          if (exitBeforeEnter) {
            forceChange.current = true;
          }
          forceUpdate();
        }
      }
    };
    const springs = getSprings(t4.ctrl, payload);
    if (phase === "leave" && exitBeforeEnter) {
      exitingTransitions.current.set(t4, { phase, springs, payload });
    } else {
      changes.set(t4, { phase, springs, payload });
    }
  });
  const context = (0, import_react13.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    if (hasContext) {
      each(transitions, (t4) => {
        t4.ctrl.start({ default: context });
      });
    }
  }, [context]);
  each(changes, (_2, t4) => {
    if (exitingTransitions.current.size) {
      const ind = transitions.findIndex((state) => state.key === t4.key);
      transitions.splice(ind, 1);
    }
  });
  useIsomorphicLayoutEffect(
    () => {
      each(
        exitingTransitions.current.size ? exitingTransitions.current : changes,
        ({ phase, payload }, t4) => {
          const { ctrl } = t4;
          t4.phase = phase;
          ref == null ? void 0 : ref.add(ctrl);
          if (hasContext && phase == "enter") {
            ctrl.start({ default: context });
          }
          if (payload) {
            replaceRef(ctrl, payload.ref);
            if ((ctrl.ref || ref) && !forceChange.current) {
              ctrl.update(payload);
            } else {
              ctrl.start(payload);
              if (forceChange.current) {
                forceChange.current = false;
              }
            }
          }
        }
      );
    },
    reset ? void 0 : deps
  );
  const renderTransitions = (render) => React22.createElement(React22.Fragment, null, transitions.map((t4, i4) => {
    const { springs } = changes.get(t4) || t4.ctrl;
    const elem = render({ ...springs }, t4.item, t4, i4);
    return elem && elem.type ? React22.createElement(
      elem.type,
      {
        ...elem.props,
        key: is.str(t4.key) || is.num(t4.key) ? t4.key : t4.ctrl.id,
        ref: elem.ref
      }
    ) : elem;
  }));
  return ref ? [renderTransitions, ref] : renderTransitions;
}
var nextKey = 1;
function getKeys(items, { key, keys = key }, prevTransitions) {
  if (keys === null) {
    const reused = /* @__PURE__ */ new Set();
    return items.map((item) => {
      const t4 = prevTransitions && prevTransitions.find(
        (t22) => t22.item === item && t22.phase !== "leave" && !reused.has(t22)
      );
      if (t4) {
        reused.add(t4);
        return t4.key;
      }
      return nextKey++;
    });
  }
  return is.und(keys) ? items : is.fun(keys) ? items.map(keys) : toArray(keys);
}
var Interpolation = class extends FrameValue {
  constructor(source, args) {
    super();
    this.source = source;
    this.idle = true;
    this._active = /* @__PURE__ */ new Set();
    this.calc = createInterpolator(...args);
    const value = this._get();
    const nodeType = getAnimatedType(value);
    setAnimated(this, nodeType.create(value));
  }
  advance(_dt) {
    const value = this._get();
    const oldValue = this.get();
    if (!isEqual(value, oldValue)) {
      getAnimated(this).setValue(value);
      this._onChange(value, this.idle);
    }
    if (!this.idle && checkIdle(this._active)) {
      becomeIdle(this);
    }
  }
  _get() {
    const inputs = is.arr(this.source) ? this.source.map(getFluidValue) : toArray(getFluidValue(this.source));
    return this.calc(...inputs);
  }
  _start() {
    if (this.idle && !checkIdle(this._active)) {
      this.idle = false;
      each(getPayload(this), (node) => {
        node.done = false;
      });
      if (globals_exports.skipAnimation) {
        raf.batchedUpdates(() => this.advance());
        becomeIdle(this);
      } else {
        frameLoop.start(this);
      }
    }
  }
  // Observe our sources only when we're observed.
  _attach() {
    let priority3 = 1;
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        addFluidObserver(source, this);
      }
      if (isFrameValue(source)) {
        if (!source.idle) {
          this._active.add(source);
        }
        priority3 = Math.max(priority3, source.priority + 1);
      }
    });
    this.priority = priority3;
    this._start();
  }
  // Stop observing our sources once we have no observers.
  _detach() {
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        removeFluidObserver(source, this);
      }
    });
    this._active.clear();
    becomeIdle(this);
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      if (event.idle) {
        this.advance();
      } else {
        this._active.add(event.parent);
        this._start();
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else if (event.type == "priority") {
      this.priority = toArray(this.source).reduce(
        (highest, parent) => Math.max(highest, (isFrameValue(parent) ? parent.priority : 0) + 1),
        0
      );
    }
  }
};
function isIdle(source) {
  return source.idle !== false;
}
function checkIdle(active) {
  return !active.size || Array.from(active).every(isIdle);
}
function becomeIdle(self) {
  if (!self.idle) {
    self.idle = true;
    each(getPayload(self), (node) => {
      node.done = true;
    });
    callFluidObservers(self, {
      type: "idle",
      parent: self
    });
  }
}
globals_exports.assign({
  createStringInterpolator: createStringInterpolator2,
  to: (source, args) => new Interpolation(source, args)
});
var update2 = frameLoop.advance;

// node_modules/@nivo/axes/node_modules/@react-spring/web/dist/react-spring_web.modern.mjs
var import_react_dom = __toESM(require_react_dom(), 1);
var isCustomPropRE = /^--/;
function dangerousStyleValue(name, value) {
  if (value == null || typeof value === "boolean" || value === "") return "";
  if (typeof value === "number" && value !== 0 && !isCustomPropRE.test(name) && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]))
    return value + "px";
  return ("" + value).trim();
}
var attributeCache = {};
function applyAnimatedValues(instance, props) {
  if (!instance.nodeType || !instance.setAttribute) {
    return false;
  }
  const isFilterElement = instance.nodeName === "filter" || instance.parentNode && instance.parentNode.nodeName === "filter";
  const {
    className,
    style,
    children,
    scrollTop,
    scrollLeft,
    viewBox,
    ...attributes
  } = props;
  const values = Object.values(attributes);
  const names = Object.keys(attributes).map(
    (name) => isFilterElement || instance.hasAttribute(name) ? name : attributeCache[name] || (attributeCache[name] = name.replace(
      /([A-Z])/g,
      // Attributes are written in dash case
      (n4) => "-" + n4.toLowerCase()
    ))
  );
  if (children !== void 0) {
    instance.textContent = children;
  }
  for (const name in style) {
    if (style.hasOwnProperty(name)) {
      const value = dangerousStyleValue(name, style[name]);
      if (isCustomPropRE.test(name)) {
        instance.style.setProperty(name, value);
      } else {
        instance.style[name] = value;
      }
    }
  }
  names.forEach((name, i4) => {
    instance.setAttribute(name, values[i4]);
  });
  if (className !== void 0) {
    instance.className = className;
  }
  if (scrollTop !== void 0) {
    instance.scrollTop = scrollTop;
  }
  if (scrollLeft !== void 0) {
    instance.scrollLeft = scrollLeft;
  }
  if (viewBox !== void 0) {
    instance.setAttribute("viewBox", viewBox);
  }
}
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
var prefixKey = (prefix3, key) => prefix3 + key.charAt(0).toUpperCase() + key.substring(1);
var prefixes = ["Webkit", "Ms", "Moz", "O"];
isUnitlessNumber = Object.keys(isUnitlessNumber).reduce((acc, prop) => {
  prefixes.forEach((prefix3) => acc[prefixKey(prefix3, prop)] = acc[prop]);
  return acc;
}, isUnitlessNumber);
var domTransforms = /^(matrix|translate|scale|rotate|skew)/;
var pxTransforms = /^(translate)/;
var degTransforms = /^(rotate|skew)/;
var addUnit = (value, unit) => is.num(value) && value !== 0 ? value + unit : value;
var isValueIdentity = (value, id) => is.arr(value) ? value.every((v2) => isValueIdentity(v2, id)) : is.num(value) ? value === id : parseFloat(value) === id;
var AnimatedStyle = class extends AnimatedObject {
  constructor({ x: x2, y, z: z3, ...style }) {
    const inputs = [];
    const transforms = [];
    if (x2 || y || z3) {
      inputs.push([x2 || 0, y || 0, z3 || 0]);
      transforms.push((xyz) => [
        `translate3d(${xyz.map((v2) => addUnit(v2, "px")).join(",")})`,
        // prettier-ignore
        isValueIdentity(xyz, 0)
      ]);
    }
    eachProp(style, (value, key) => {
      if (key === "transform") {
        inputs.push([value || ""]);
        transforms.push((transform) => [transform, transform === ""]);
      } else if (domTransforms.test(key)) {
        delete style[key];
        if (is.und(value)) return;
        const unit = pxTransforms.test(key) ? "px" : degTransforms.test(key) ? "deg" : "";
        inputs.push(toArray(value));
        transforms.push(
          key === "rotate3d" ? ([x22, y2, z22, deg]) => [
            `rotate3d(${x22},${y2},${z22},${addUnit(deg, unit)})`,
            isValueIdentity(deg, 0)
          ] : (input) => [
            `${key}(${input.map((v2) => addUnit(v2, unit)).join(",")})`,
            isValueIdentity(input, key.startsWith("scale") ? 1 : 0)
          ]
        );
      }
    });
    if (inputs.length) {
      style.transform = new FluidTransform(inputs, transforms);
    }
    super(style);
  }
};
var FluidTransform = class extends FluidValue {
  constructor(inputs, transforms) {
    super();
    this.inputs = inputs;
    this.transforms = transforms;
    this._value = null;
  }
  get() {
    return this._value || (this._value = this._get());
  }
  _get() {
    let transform = "";
    let identity = true;
    each(this.inputs, (input, i4) => {
      const arg1 = getFluidValue(input[0]);
      const [t4, id] = this.transforms[i4](
        is.arr(arg1) ? arg1 : input.map(getFluidValue)
      );
      transform += " " + t4;
      identity = identity && id;
    });
    return identity ? "none" : transform;
  }
  // Start observing our inputs once we have an observer.
  observerAdded(count) {
    if (count == 1)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && addFluidObserver(value, this)
        )
      );
  }
  // Stop observing our inputs once we have no observers.
  observerRemoved(count) {
    if (count == 0)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && removeFluidObserver(value, this)
        )
      );
  }
  eventObserved(event) {
    if (event.type == "change") {
      this._value = null;
    }
    callFluidObservers(this, event);
  }
};
var primitives = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
globals_exports.assign({
  batchedUpdates: import_react_dom.unstable_batchedUpdates,
  createStringInterpolator: createStringInterpolator2,
  colors: colors2
});
var host = createHost(primitives, {
  applyAnimatedValues,
  createAnimatedStyle: (style) => new AnimatedStyle(style),
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  getComponentProps: ({ scrollTop, scrollLeft, ...props }) => props
});
var animated = host.animated;

// node_modules/@nivo/axes/dist/nivo-axes.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
function A() {
  return A = Object.assign ? Object.assign.bind() : function(t4) {
    for (var e4 = 1; e4 < arguments.length; e4++) {
      var i4 = arguments[e4];
      for (var n4 in i4) ({}).hasOwnProperty.call(i4, n4) && (t4[n4] = i4[n4]);
    }
    return t4;
  }, A.apply(null, arguments);
}
var T = function(t4) {
  var e4, i4 = t4.axis, n4 = t4.scale, r3 = t4.ticksPosition, o2 = t4.tickValues, l = t4.tickSize, c = t4.tickPadding, s = t4.tickRotation, f = t4.truncateTickAt, d2 = t4.engine, u = void 0 === d2 ? "svg" : d2, x2 = Cn(n4, o2), m2 = gt[u], y = "bandwidth" in n4 ? Mn(n4) : n4, g2 = { lineX: 0, lineY: 0 }, v2 = { textX: 0, textY: 0 }, k2 = "object" == typeof document && "rtl" === document.dir, b4 = m2.align.center, P3 = m2.baseline.center;
  "x" === i4 ? (e4 = function(t5) {
    var e5;
    return { x: null != (e5 = y(t5)) ? e5 : 0, y: 0 };
  }, g2.lineY = l * ("after" === r3 ? 1 : -1), v2.textY = (l + c) * ("after" === r3 ? 1 : -1), P3 = "after" === r3 ? m2.baseline.top : m2.baseline.bottom, 0 === s ? b4 = m2.align.center : "after" === r3 && s < 0 || "before" === r3 && s > 0 ? (b4 = m2.align[k2 ? "left" : "right"], P3 = m2.baseline.center) : ("after" === r3 && s > 0 || "before" === r3 && s < 0) && (b4 = m2.align[k2 ? "right" : "left"], P3 = m2.baseline.center)) : (e4 = function(t5) {
    var e5;
    return { x: 0, y: null != (e5 = y(t5)) ? e5 : 0 };
  }, g2.lineX = l * ("after" === r3 ? 1 : -1), v2.textX = (l + c) * ("after" === r3 ? 1 : -1), b4 = "after" === r3 ? m2.align.left : m2.align.right);
  return { ticks: x2.map(function(t5) {
    var i5 = "string" == typeof t5 ? function(t6) {
      var e5 = String(t6).length;
      return f && f > 0 && e5 > f ? "" + String(t6).slice(0, f).concat("...") : "" + t6;
    }(t5) : t5;
    return A({ key: t5 instanceof Date ? "" + t5.valueOf() : "" + t5, value: i5 }, e4(t5), g2, v2);
  }), textAlign: b4, textBaseline: P3 };
};
var w2 = function(t4, e4) {
  if (void 0 === t4 || "function" == typeof t4) return t4;
  if ("time" === e4.type) {
    var i4 = timeFormat(t4);
    return function(t5) {
      return i4(t5 instanceof Date ? t5 : new Date(t5));
    };
  }
  return format(t4);
};
var O = function(t4) {
  var e4, i4 = t4.width, n4 = t4.height, r3 = t4.scale, a3 = t4.axis, o2 = t4.values, l = (e4 = o2, Array.isArray(e4) ? o2 : void 0) || Cn(r3, o2), c = "bandwidth" in r3 ? Mn(r3) : r3, s = "x" === a3 ? l.map(function(t5) {
    var e5, i5;
    return { key: t5 instanceof Date ? "" + t5.valueOf() : "" + t5, x1: null != (e5 = c(t5)) ? e5 : 0, x2: null != (i5 = c(t5)) ? i5 : 0, y1: 0, y2: n4 };
  }) : l.map(function(t5) {
    var e5, n5;
    return { key: t5 instanceof Date ? "" + t5.valueOf() : "" + t5, x1: 0, x2: i4, y1: null != (e5 = c(t5)) ? e5 : 0, y2: null != (n5 = c(t5)) ? n5 : 0 };
  });
  return s;
};
var X2 = (0, import_react15.memo)(function(t4) {
  var e4, n4 = t4.value, r3 = t4.format, a3 = t4.lineX, o2 = t4.lineY, l = t4.onClick, c = t4.textBaseline, s = t4.textAnchor, f = t4.theme, u = t4.animatedProps, x2 = null != (e4 = null == r3 ? void 0 : r3(n4)) ? e4 : n4, y = (0, import_react15.useMemo)(function() {
    var t5 = { opacity: u.opacity };
    return l ? { style: A({}, t5, { cursor: "pointer" }), onClick: function(t6) {
      return l(t6, x2);
    } } : { style: t5 };
  }, [u.opacity, l, x2]);
  return (0, import_jsx_runtime.jsxs)(animated.g, A({ transform: u.transform }, y, { children: [(0, import_jsx_runtime.jsx)("line", { x1: 0, x2: a3, y1: 0, y2: o2, style: f.line }), (0, import_jsx_runtime.jsx)(b, { dominantBaseline: c, textAnchor: s, transform: u.textTransform, style: f.text, children: "" + x2 })] }));
});
var Y2 = { tickSize: 5, tickPadding: 5, tickRotation: 0, legendPosition: "middle", legendOffset: 0 };
var B = function(e4) {
  var r3 = e4.axis, a3 = e4.scale, l = e4.x, f = void 0 === l ? 0 : l, u = e4.y, x2 = void 0 === u ? 0 : u, v2 = e4.length, k2 = e4.ticksPosition, h = e4.tickValues, p2 = e4.tickSize, O3 = void 0 === p2 ? Y2.tickSize : p2, B2 = e4.tickPadding, z3 = void 0 === B2 ? Y2.tickPadding : B2, R4 = e4.tickRotation, V2 = void 0 === R4 ? Y2.tickRotation : R4, C3 = e4.format, D3 = e4.renderTick, j3 = void 0 === D3 ? X2 : D3, E3 = e4.truncateTickAt, W4 = e4.legend, q3 = e4.legendPosition, H = void 0 === q3 ? Y2.legendPosition : q3, I2 = e4.legendOffset, F = void 0 === I2 ? Y2.legendOffset : I2, G = e4.style, J3 = e4.onClick, K = e4.ariaHidden, L3 = M(), M3 = w(L3.axis, G), N = (0, import_react15.useMemo)(function() {
    return w2(C3, a3);
  }, [C3, a3]), Q2 = T({ axis: r3, scale: a3, ticksPosition: k2, tickValues: h, tickSize: O3, tickPadding: z3, tickRotation: V2, truncateTickAt: E3 }), U = Q2.ticks, Z2 = Q2.textAlign, $ = Q2.textBaseline, _2 = null;
  if (void 0 !== W4) {
    var tt, et = 0, it = 0, nt = 0;
    "y" === r3 ? (nt = -90, et = F, "start" === H ? (tt = "start", it = v2) : "middle" === H ? (tt = "middle", it = v2 / 2) : "end" === H && (tt = "end")) : (it = F, "start" === H ? tt = "start" : "middle" === H ? (tt = "middle", et = v2 / 2) : "end" === H && (tt = "end", et = v2)), _2 = (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: (0, import_jsx_runtime.jsx)(b, { transform: "translate(" + et + ", " + it + ") rotate(" + nt + ")", textAnchor: tt, style: A({}, M3.legend.text, { dominantBaseline: "central" }), children: W4 }) });
  }
  var rt = Dr(), at = rt.animate, ot = rt.config, lt = useSpring({ transform: "translate(" + f + "," + x2 + ")", lineX2: "x" === r3 ? v2 : 0, lineY2: "x" === r3 ? 0 : v2, config: ot, immediate: !at }), ct2 = (0, import_react15.useCallback)(function(t4) {
    return { opacity: 1, transform: "translate(" + t4.x + "," + t4.y + ")", textTransform: "translate(" + t4.textX + "," + t4.textY + ") rotate(" + V2 + ")" };
  }, [V2]), st = (0, import_react15.useCallback)(function(t4) {
    return { opacity: 0, transform: "translate(" + t4.x + "," + t4.y + ")", textTransform: "translate(" + t4.textX + "," + t4.textY + ") rotate(" + V2 + ")" };
  }, [V2]), ft2 = useTransition(U, { keys: function(t4) {
    return t4.key;
  }, initial: ct2, from: st, enter: ct2, update: ct2, leave: { opacity: 0 }, config: ot, immediate: !at });
  return (0, import_jsx_runtime.jsxs)(animated.g, { transform: lt.transform, "aria-hidden": K, children: [ft2(function(e5, i4, n4, r4) {
    return t2.createElement(j3, A({ tickIndex: r4, format: N, rotate: V2, textBaseline: $, textAnchor: Z2, truncateTickAt: E3, animatedProps: e5, theme: M3.ticks }, i4, J3 ? { onClick: J3 } : {}));
  }), (0, import_jsx_runtime.jsx)(animated.line, { style: M3.domain.line, x1: 0, x2: lt.lineX2, y1: 0, y2: lt.lineY2 }), _2] });
};
var z = (0, import_react15.memo)(B);
var R2 = ["top", "right", "bottom", "left"];
var V = (0, import_react15.memo)(function(t4) {
  var e4 = t4.xScale, i4 = t4.yScale, n4 = t4.width, r3 = t4.height, a3 = { top: t4.top, right: t4.right, bottom: t4.bottom, left: t4.left };
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: R2.map(function(t5) {
    var o2 = a3[t5];
    if (!o2) return null;
    var l = "top" === t5 || "bottom" === t5;
    return (0, import_jsx_runtime.jsx)(z, A({}, o2, { axis: l ? "x" : "y", x: "right" === t5 ? n4 : 0, y: "bottom" === t5 ? r3 : 0, scale: l ? e4 : i4, length: l ? n4 : r3, ticksPosition: "top" === t5 || "left" === t5 ? "before" : "after", truncateTickAt: o2.truncateTickAt }), t5);
  }) });
});
var C = (0, import_react15.memo)(function(t4) {
  var e4 = t4.animatedProps, i4 = M();
  return (0, import_jsx_runtime.jsx)(animated.line, A({}, e4, i4.grid.line));
});
var D = (0, import_react15.memo)(function(t4) {
  var e4 = t4.lines, i4 = Dr(), n4 = i4.animate, a3 = i4.config, l = useTransition(e4, { keys: function(t5) {
    return t5.key;
  }, initial: function(t5) {
    return { opacity: 1, x1: t5.x1, x2: t5.x2, y1: t5.y1, y2: t5.y2 };
  }, from: function(t5) {
    return { opacity: 0, x1: t5.x1, x2: t5.x2, y1: t5.y1, y2: t5.y2 };
  }, enter: function(t5) {
    return { opacity: 1, x1: t5.x1, x2: t5.x2, y1: t5.y1, y2: t5.y2 };
  }, update: function(t5) {
    return { opacity: 1, x1: t5.x1, x2: t5.x2, y1: t5.y1, y2: t5.y2 };
  }, leave: { opacity: 0 }, config: a3, immediate: !n4 });
  return (0, import_jsx_runtime.jsx)("g", { children: l(function(t5, e5) {
    return (0, import_react15.createElement)(C, A({}, e5, { key: e5.key, animatedProps: t5 }));
  }) });
});
var j = (0, import_react15.memo)(function(t4) {
  var e4 = t4.width, n4 = t4.height, r3 = t4.xScale, a3 = t4.yScale, o2 = t4.xValues, l = t4.yValues, c = (0, import_react15.useMemo)(function() {
    return !!r3 && O({ width: e4, height: n4, scale: r3, axis: "x", values: o2 });
  }, [r3, o2, e4, n4]), s = (0, import_react15.useMemo)(function() {
    return !!a3 && O({ width: e4, height: n4, scale: a3, axis: "y", values: l });
  }, [n4, e4, a3, l]);
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [c && (0, import_jsx_runtime.jsx)(D, { lines: c }), s && (0, import_jsx_runtime.jsx)(D, { lines: s })] });
});
var E = function(t4, e4) {
  var i4, n4, r3 = e4.axis, a3 = e4.scale, o2 = e4.x, c = void 0 === o2 ? 0 : o2, s = e4.y, d2 = void 0 === s ? 0 : s, m2 = e4.length, y = e4.ticksPosition, g2 = e4.tickValues, v2 = e4.tickSize, k2 = void 0 === v2 ? Y2.tickSize : v2, h = e4.tickPadding, p2 = void 0 === h ? Y2.tickPadding : h, b4 = e4.tickRotation, P3 = void 0 === b4 ? Y2.tickRotation : b4, S3 = e4.format, A2 = e4.legend, w4 = e4.legendPosition, O3 = void 0 === w4 ? Y2.legendPosition : w4, X3 = e4.legendOffset, B2 = void 0 === X3 ? Y2.legendOffset : X3, z3 = e4.theme, R4 = e4.style, V2 = T({ axis: r3, scale: a3, ticksPosition: y, tickValues: g2, tickSize: k2, tickPadding: p2, tickRotation: P3, engine: "canvas" }), C3 = V2.ticks, D3 = V2.textAlign, j3 = V2.textBaseline;
  t4.save(), t4.translate(c, d2);
  var E3 = R(z3.axis, R4);
  t4.textAlign = D3, t4.textBaseline = j3, a(t4, E3.ticks.text);
  var W4 = null != (i4 = E3.domain.line.strokeWidth) ? i4 : 0;
  "string" != typeof W4 && W4 > 0 && (t4.lineWidth = W4, t4.lineCap = "square", E3.domain.line.stroke && (t4.strokeStyle = E3.domain.line.stroke), t4.beginPath(), t4.moveTo(0, 0), t4.lineTo("x" === r3 ? m2 : 0, "x" === r3 ? 0 : m2), t4.stroke());
  var q3 = "function" == typeof S3 ? S3 : function(t5) {
    return "" + t5;
  }, H = null != (n4 = E3.ticks.line.strokeWidth) ? n4 : 0, I2 = "string" != typeof H && H > 0;
  if (C3.forEach(function(e5) {
    I2 && (t4.lineWidth = H, t4.lineCap = "square", E3.ticks.line.stroke && (t4.strokeStyle = E3.ticks.line.stroke), t4.beginPath(), t4.moveTo(e5.x, e5.y), t4.lineTo(e5.x + e5.lineX, e5.y + e5.lineY), t4.stroke());
    var i5 = q3(e5.value);
    t4.save(), t4.translate(e5.x + e5.textX, e5.y + e5.textY), t4.rotate(ut(P3)), d(t4, E3.ticks.text, "" + i5), t4.fillText("" + i5, 0, 0), t4.restore();
  }), void 0 !== A2) {
    var F = 0, G = 0, J3 = 0, K = "center";
    "y" === r3 ? (J3 = -90, F = B2, "start" === O3 ? (K = "start", G = m2) : "middle" === O3 ? (K = "center", G = m2 / 2) : "end" === O3 && (K = "end")) : (G = B2, "start" === O3 ? K = "start" : "middle" === O3 ? (K = "center", F = m2 / 2) : "end" === O3 && (K = "end", F = m2)), t4.translate(F, G), t4.rotate(ut(J3)), a(t4, E3.legend.text), E3.legend.text.fill && (t4.fillStyle = E3.legend.text.fill), t4.textAlign = K, t4.textBaseline = "middle", d(t4, E3.legend.text, A2);
  }
  t4.restore();
};
var W2 = function(t4, e4) {
  var i4 = e4.xScale, n4 = e4.yScale, r3 = e4.width, a3 = e4.height, o2 = e4.top, l = e4.right, c = e4.bottom, s = e4.left, f = e4.theme, d2 = { top: o2, right: l, bottom: c, left: s };
  R2.forEach(function(e5) {
    var o3 = d2[e5];
    if (!o3) return null;
    var l2 = "top" === e5 || "bottom" === e5, c6 = "top" === e5 || "left" === e5 ? "before" : "after", s2 = l2 ? i4 : n4, u = w2(o3.format, s2);
    E(t4, A({}, o3, { axis: l2 ? "x" : "y", x: "right" === e5 ? r3 : 0, y: "bottom" === e5 ? a3 : 0, scale: s2, format: u, length: l2 ? r3 : a3, ticksPosition: c6, theme: f }));
  });
};
var q = function(t4, e4) {
  var i4 = e4.width, n4 = e4.height, r3 = e4.scale, a3 = e4.axis, o2 = e4.values;
  O({ width: i4, height: n4, scale: r3, axis: a3, values: o2 }).forEach(function(e5) {
    t4.beginPath(), t4.moveTo(e5.x1, e5.y1), t4.lineTo(e5.x2, e5.y2), t4.stroke();
  });
};

// node_modules/@nivo/annotations/dist/nivo-annotations.mjs
var import_react30 = __toESM(require_react(), 1);
var import_filter = __toESM(require_filter(), 1);
var import_isNumber = __toESM(require_isNumber(), 1);
var import_omit = __toESM(require_omit(), 1);

// node_modules/@nivo/annotations/node_modules/@react-spring/rafz/dist/react-spring_rafz.modern.mjs
var updateQueue2 = makeQueue2();
var raf2 = (fn2) => schedule2(fn2, updateQueue2);
var writeQueue2 = makeQueue2();
raf2.write = (fn2) => schedule2(fn2, writeQueue2);
var onStartQueue2 = makeQueue2();
raf2.onStart = (fn2) => schedule2(fn2, onStartQueue2);
var onFrameQueue2 = makeQueue2();
raf2.onFrame = (fn2) => schedule2(fn2, onFrameQueue2);
var onFinishQueue2 = makeQueue2();
raf2.onFinish = (fn2) => schedule2(fn2, onFinishQueue2);
var timeouts2 = [];
raf2.setTimeout = (handler, ms) => {
  const time2 = raf2.now() + ms;
  const cancel = () => {
    const i4 = timeouts2.findIndex((t4) => t4.cancel == cancel);
    if (~i4) timeouts2.splice(i4, 1);
    pendingCount2 -= ~i4 ? 1 : 0;
  };
  const timeout = { time: time2, handler, cancel };
  timeouts2.splice(findTimeout2(time2), 0, timeout);
  pendingCount2 += 1;
  start2();
  return timeout;
};
var findTimeout2 = (time2) => ~(~timeouts2.findIndex((t4) => t4.time > time2) || ~timeouts2.length);
raf2.cancel = (fn2) => {
  onStartQueue2.delete(fn2);
  onFrameQueue2.delete(fn2);
  onFinishQueue2.delete(fn2);
  updateQueue2.delete(fn2);
  writeQueue2.delete(fn2);
};
raf2.sync = (fn2) => {
  sync2 = true;
  raf2.batchedUpdates(fn2);
  sync2 = false;
};
raf2.throttle = (fn2) => {
  let lastArgs;
  function queuedFn() {
    try {
      fn2(...lastArgs);
    } finally {
      lastArgs = null;
    }
  }
  function throttled(...args) {
    lastArgs = args;
    raf2.onStart(queuedFn);
  }
  throttled.handler = fn2;
  throttled.cancel = () => {
    onStartQueue2.delete(queuedFn);
    lastArgs = null;
  };
  return throttled;
};
var nativeRaf2 = typeof window != "undefined" ? window.requestAnimationFrame : (
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  () => {
  }
);
raf2.use = (impl) => nativeRaf2 = impl;
raf2.now = typeof performance != "undefined" ? () => performance.now() : Date.now;
raf2.batchedUpdates = (fn2) => fn2();
raf2.catch = console.error;
raf2.frameLoop = "always";
raf2.advance = () => {
  if (raf2.frameLoop !== "demand") {
    console.warn(
      "Cannot call the manual advancement of rafz whilst frameLoop is not set as demand"
    );
  } else {
    update3();
  }
};
var ts2 = -1;
var pendingCount2 = 0;
var sync2 = false;
function schedule2(fn2, queue) {
  if (sync2) {
    queue.delete(fn2);
    fn2(0);
  } else {
    queue.add(fn2);
    start2();
  }
}
function start2() {
  if (ts2 < 0) {
    ts2 = 0;
    if (raf2.frameLoop !== "demand") {
      nativeRaf2(loop2);
    }
  }
}
function stop2() {
  ts2 = -1;
}
function loop2() {
  if (~ts2) {
    nativeRaf2(loop2);
    raf2.batchedUpdates(update3);
  }
}
function update3() {
  const prevTs = ts2;
  ts2 = raf2.now();
  const count = findTimeout2(ts2);
  if (count) {
    eachSafely2(timeouts2.splice(0, count), (t4) => t4.handler());
    pendingCount2 -= count;
  }
  if (!pendingCount2) {
    stop2();
    return;
  }
  onStartQueue2.flush();
  updateQueue2.flush(prevTs ? Math.min(64, ts2 - prevTs) : 16.667);
  onFrameQueue2.flush();
  writeQueue2.flush();
  onFinishQueue2.flush();
}
function makeQueue2() {
  let next = /* @__PURE__ */ new Set();
  let current = next;
  return {
    add(fn2) {
      pendingCount2 += current == next && !next.has(fn2) ? 1 : 0;
      next.add(fn2);
    },
    delete(fn2) {
      pendingCount2 -= current == next && next.has(fn2) ? 1 : 0;
      return next.delete(fn2);
    },
    flush(arg) {
      if (current.size) {
        next = /* @__PURE__ */ new Set();
        pendingCount2 -= current.size;
        eachSafely2(current, (fn2) => fn2(arg) && next.add(fn2));
        pendingCount2 += next.size;
        current = next;
      }
    }
  };
}
function eachSafely2(values, each3) {
  values.forEach((value) => {
    try {
      each3(value);
    } catch (e4) {
      raf2.catch(e4);
    }
  });
}

// node_modules/@nivo/annotations/node_modules/@react-spring/shared/dist/react-spring_shared.modern.mjs
var import_react16 = __toESM(require_react(), 1);
var import_react17 = __toESM(require_react(), 1);
var import_react18 = __toESM(require_react(), 1);
var import_react19 = __toESM(require_react(), 1);
var import_react20 = __toESM(require_react(), 1);
var import_react21 = __toESM(require_react(), 1);
var import_react22 = __toESM(require_react(), 1);
var import_react23 = __toESM(require_react(), 1);
var __defProp2 = Object.defineProperty;
var __export2 = (target, all) => {
  for (var name in all)
    __defProp2(target, name, { get: all[name], enumerable: true });
};
var globals_exports2 = {};
__export2(globals_exports2, {
  assign: () => assign2,
  colors: () => colors3,
  createStringInterpolator: () => createStringInterpolator3,
  skipAnimation: () => skipAnimation2,
  to: () => to2,
  willAdvance: () => willAdvance2
});
function noop2() {
}
var defineHidden2 = (obj, key, value) => Object.defineProperty(obj, key, { value, writable: true, configurable: true });
var is2 = {
  arr: Array.isArray,
  obj: (a3) => !!a3 && a3.constructor.name === "Object",
  fun: (a3) => typeof a3 === "function",
  str: (a3) => typeof a3 === "string",
  num: (a3) => typeof a3 === "number",
  und: (a3) => a3 === void 0
};
function isEqual2(a3, b4) {
  if (is2.arr(a3)) {
    if (!is2.arr(b4) || a3.length !== b4.length) return false;
    for (let i4 = 0; i4 < a3.length; i4++) {
      if (a3[i4] !== b4[i4]) return false;
    }
    return true;
  }
  return a3 === b4;
}
var each2 = (obj, fn2) => obj.forEach(fn2);
function eachProp2(obj, fn2, ctx) {
  if (is2.arr(obj)) {
    for (let i4 = 0; i4 < obj.length; i4++) {
      fn2.call(ctx, obj[i4], `${i4}`);
    }
    return;
  }
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      fn2.call(ctx, obj[key], key);
    }
  }
}
var toArray2 = (a3) => is2.und(a3) ? [] : is2.arr(a3) ? a3 : [a3];
function flush2(queue, iterator) {
  if (queue.size) {
    const items = Array.from(queue);
    queue.clear();
    each2(items, iterator);
  }
}
var flushCalls2 = (queue, ...args) => flush2(queue, (fn2) => fn2(...args));
var isSSR2 = () => typeof window === "undefined" || !window.navigator || /ServerSideRendering|^Deno\//.test(window.navigator.userAgent);
var createStringInterpolator3;
var to2;
var colors3 = null;
var skipAnimation2 = false;
var willAdvance2 = noop2;
var assign2 = (globals) => {
  if (globals.to) to2 = globals.to;
  if (globals.now) raf2.now = globals.now;
  if (globals.colors !== void 0) colors3 = globals.colors;
  if (globals.skipAnimation != null) skipAnimation2 = globals.skipAnimation;
  if (globals.createStringInterpolator)
    createStringInterpolator3 = globals.createStringInterpolator;
  if (globals.requestAnimationFrame) raf2.use(globals.requestAnimationFrame);
  if (globals.batchedUpdates) raf2.batchedUpdates = globals.batchedUpdates;
  if (globals.willAdvance) willAdvance2 = globals.willAdvance;
  if (globals.frameLoop) raf2.frameLoop = globals.frameLoop;
};
var startQueue2 = /* @__PURE__ */ new Set();
var currentFrame2 = [];
var prevFrame2 = [];
var priority2 = 0;
var frameLoop2 = {
  get idle() {
    return !startQueue2.size && !currentFrame2.length;
  },
  /** Advance the given animation on every frame until idle. */
  start(animation) {
    if (priority2 > animation.priority) {
      startQueue2.add(animation);
      raf2.onStart(flushStartQueue2);
    } else {
      startSafely2(animation);
      raf2(advance2);
    }
  },
  /** Advance all animations by the given time. */
  advance: advance2,
  /** Call this when an animation's priority changes. */
  sort(animation) {
    if (priority2) {
      raf2.onFrame(() => frameLoop2.sort(animation));
    } else {
      const prevIndex = currentFrame2.indexOf(animation);
      if (~prevIndex) {
        currentFrame2.splice(prevIndex, 1);
        startUnsafely2(animation);
      }
    }
  },
  /**
   * Clear all animations. For testing purposes.
   *
   * ☠️ Never call this from within the frameloop.
   */
  clear() {
    currentFrame2 = [];
    startQueue2.clear();
  }
};
function flushStartQueue2() {
  startQueue2.forEach(startSafely2);
  startQueue2.clear();
  raf2(advance2);
}
function startSafely2(animation) {
  if (!currentFrame2.includes(animation)) startUnsafely2(animation);
}
function startUnsafely2(animation) {
  currentFrame2.splice(
    findIndex2(currentFrame2, (other) => other.priority > animation.priority),
    0,
    animation
  );
}
function advance2(dt) {
  const nextFrame = prevFrame2;
  for (let i4 = 0; i4 < currentFrame2.length; i4++) {
    const animation = currentFrame2[i4];
    priority2 = animation.priority;
    if (!animation.idle) {
      willAdvance2(animation);
      animation.advance(dt);
      if (!animation.idle) {
        nextFrame.push(animation);
      }
    }
  }
  priority2 = 0;
  prevFrame2 = currentFrame2;
  prevFrame2.length = 0;
  currentFrame2 = nextFrame;
  return currentFrame2.length > 0;
}
function findIndex2(arr, test) {
  const index = arr.findIndex(test);
  return index < 0 ? arr.length : index;
}
var clamp2 = (min, max, v2) => Math.min(Math.max(v2, min), max);
var colors22 = {
  transparent: 0,
  aliceblue: 4042850303,
  antiquewhite: 4209760255,
  aqua: 16777215,
  aquamarine: 2147472639,
  azure: 4043309055,
  beige: 4126530815,
  bisque: 4293182719,
  black: 255,
  blanchedalmond: 4293643775,
  blue: 65535,
  blueviolet: 2318131967,
  brown: 2771004159,
  burlywood: 3736635391,
  burntsienna: 3934150143,
  cadetblue: 1604231423,
  chartreuse: 2147418367,
  chocolate: 3530104575,
  coral: 4286533887,
  cornflowerblue: 1687547391,
  cornsilk: 4294499583,
  crimson: 3692313855,
  cyan: 16777215,
  darkblue: 35839,
  darkcyan: 9145343,
  darkgoldenrod: 3095792639,
  darkgray: 2846468607,
  darkgreen: 6553855,
  darkgrey: 2846468607,
  darkkhaki: 3182914559,
  darkmagenta: 2332068863,
  darkolivegreen: 1433087999,
  darkorange: 4287365375,
  darkorchid: 2570243327,
  darkred: 2332033279,
  darksalmon: 3918953215,
  darkseagreen: 2411499519,
  darkslateblue: 1211993087,
  darkslategray: 793726975,
  darkslategrey: 793726975,
  darkturquoise: 13554175,
  darkviolet: 2483082239,
  deeppink: 4279538687,
  deepskyblue: 12582911,
  dimgray: 1768516095,
  dimgrey: 1768516095,
  dodgerblue: 512819199,
  firebrick: 2988581631,
  floralwhite: 4294635775,
  forestgreen: 579543807,
  fuchsia: 4278255615,
  gainsboro: 3705462015,
  ghostwhite: 4177068031,
  gold: 4292280575,
  goldenrod: 3668254975,
  gray: 2155905279,
  green: 8388863,
  greenyellow: 2919182335,
  grey: 2155905279,
  honeydew: 4043305215,
  hotpink: 4285117695,
  indianred: 3445382399,
  indigo: 1258324735,
  ivory: 4294963455,
  khaki: 4041641215,
  lavender: 3873897215,
  lavenderblush: 4293981695,
  lawngreen: 2096890111,
  lemonchiffon: 4294626815,
  lightblue: 2916673279,
  lightcoral: 4034953471,
  lightcyan: 3774873599,
  lightgoldenrodyellow: 4210742015,
  lightgray: 3553874943,
  lightgreen: 2431553791,
  lightgrey: 3553874943,
  lightpink: 4290167295,
  lightsalmon: 4288707327,
  lightseagreen: 548580095,
  lightskyblue: 2278488831,
  lightslategray: 2005441023,
  lightslategrey: 2005441023,
  lightsteelblue: 2965692159,
  lightyellow: 4294959359,
  lime: 16711935,
  limegreen: 852308735,
  linen: 4210091775,
  magenta: 4278255615,
  maroon: 2147483903,
  mediumaquamarine: 1724754687,
  mediumblue: 52735,
  mediumorchid: 3126187007,
  mediumpurple: 2473647103,
  mediumseagreen: 1018393087,
  mediumslateblue: 2070474495,
  mediumspringgreen: 16423679,
  mediumturquoise: 1221709055,
  mediumvioletred: 3340076543,
  midnightblue: 421097727,
  mintcream: 4127193855,
  mistyrose: 4293190143,
  moccasin: 4293178879,
  navajowhite: 4292783615,
  navy: 33023,
  oldlace: 4260751103,
  olive: 2155872511,
  olivedrab: 1804477439,
  orange: 4289003775,
  orangered: 4282712319,
  orchid: 3664828159,
  palegoldenrod: 4008225535,
  palegreen: 2566625535,
  paleturquoise: 2951671551,
  palevioletred: 3681588223,
  papayawhip: 4293907967,
  peachpuff: 4292524543,
  peru: 3448061951,
  pink: 4290825215,
  plum: 3718307327,
  powderblue: 2967529215,
  purple: 2147516671,
  rebeccapurple: 1714657791,
  red: 4278190335,
  rosybrown: 3163525119,
  royalblue: 1097458175,
  saddlebrown: 2336560127,
  salmon: 4202722047,
  sandybrown: 4104413439,
  seagreen: 780883967,
  seashell: 4294307583,
  sienna: 2689740287,
  silver: 3233857791,
  skyblue: 2278484991,
  slateblue: 1784335871,
  slategray: 1887473919,
  slategrey: 1887473919,
  snow: 4294638335,
  springgreen: 16744447,
  steelblue: 1182971135,
  tan: 3535047935,
  teal: 8421631,
  thistle: 3636451583,
  tomato: 4284696575,
  turquoise: 1088475391,
  violet: 4001558271,
  wheat: 4125012991,
  white: 4294967295,
  whitesmoke: 4126537215,
  yellow: 4294902015,
  yellowgreen: 2597139199
};
var NUMBER2 = "[-+]?\\d*\\.?\\d+";
var PERCENTAGE2 = NUMBER2 + "%";
function call2(...parts) {
  return "\\(\\s*(" + parts.join(")\\s*,\\s*(") + ")\\s*\\)";
}
var rgb2 = new RegExp("rgb" + call2(NUMBER2, NUMBER2, NUMBER2));
var rgba2 = new RegExp("rgba" + call2(NUMBER2, NUMBER2, NUMBER2, NUMBER2));
var hsl2 = new RegExp("hsl" + call2(NUMBER2, PERCENTAGE2, PERCENTAGE2));
var hsla2 = new RegExp(
  "hsla" + call2(NUMBER2, PERCENTAGE2, PERCENTAGE2, NUMBER2)
);
var hex32 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex42 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex62 = /^#([0-9a-fA-F]{6})$/;
var hex82 = /^#([0-9a-fA-F]{8})$/;
function normalizeColor2(color) {
  let match;
  if (typeof color === "number") {
    return color >>> 0 === color && color >= 0 && color <= 4294967295 ? color : null;
  }
  if (match = hex62.exec(color))
    return parseInt(match[1] + "ff", 16) >>> 0;
  if (colors3 && colors3[color] !== void 0) {
    return colors3[color];
  }
  if (match = rgb2.exec(color)) {
    return (parse2552(match[1]) << 24 | // r
    parse2552(match[2]) << 16 | // g
    parse2552(match[3]) << 8 | // b
    255) >>> // a
    0;
  }
  if (match = rgba2.exec(color)) {
    return (parse2552(match[1]) << 24 | // r
    parse2552(match[2]) << 16 | // g
    parse2552(match[3]) << 8 | // b
    parse12(match[4])) >>> // a
    0;
  }
  if (match = hex32.exec(color)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      "ff",
      // a
      16
    ) >>> 0;
  }
  if (match = hex82.exec(color)) return parseInt(match[1], 16) >>> 0;
  if (match = hex42.exec(color)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      match[4] + match[4],
      // a
      16
    ) >>> 0;
  }
  if (match = hsl2.exec(color)) {
    return (hslToRgb2(
      parse3602(match[1]),
      // h
      parsePercentage2(match[2]),
      // s
      parsePercentage2(match[3])
      // l
    ) | 255) >>> // a
    0;
  }
  if (match = hsla2.exec(color)) {
    return (hslToRgb2(
      parse3602(match[1]),
      // h
      parsePercentage2(match[2]),
      // s
      parsePercentage2(match[3])
      // l
    ) | parse12(match[4])) >>> // a
    0;
  }
  return null;
}
function hue2rgb2(p2, q3, t4) {
  if (t4 < 0) t4 += 1;
  if (t4 > 1) t4 -= 1;
  if (t4 < 1 / 6) return p2 + (q3 - p2) * 6 * t4;
  if (t4 < 1 / 2) return q3;
  if (t4 < 2 / 3) return p2 + (q3 - p2) * (2 / 3 - t4) * 6;
  return p2;
}
function hslToRgb2(h, s, l) {
  const q3 = l < 0.5 ? l * (1 + s) : l + s - l * s;
  const p2 = 2 * l - q3;
  const r3 = hue2rgb2(p2, q3, h + 1 / 3);
  const g2 = hue2rgb2(p2, q3, h);
  const b4 = hue2rgb2(p2, q3, h - 1 / 3);
  return Math.round(r3 * 255) << 24 | Math.round(g2 * 255) << 16 | Math.round(b4 * 255) << 8;
}
function parse2552(str) {
  const int = parseInt(str, 10);
  if (int < 0) return 0;
  if (int > 255) return 255;
  return int;
}
function parse3602(str) {
  const int = parseFloat(str);
  return (int % 360 + 360) % 360 / 360;
}
function parse12(str) {
  const num = parseFloat(str);
  if (num < 0) return 0;
  if (num > 1) return 255;
  return Math.round(num * 255);
}
function parsePercentage2(str) {
  const int = parseFloat(str);
  if (int < 0) return 0;
  if (int > 100) return 1;
  return int / 100;
}
function colorToRgba2(input) {
  let int32Color = normalizeColor2(input);
  if (int32Color === null) return input;
  int32Color = int32Color || 0;
  const r3 = (int32Color & 4278190080) >>> 24;
  const g2 = (int32Color & 16711680) >>> 16;
  const b4 = (int32Color & 65280) >>> 8;
  const a3 = (int32Color & 255) / 255;
  return `rgba(${r3}, ${g2}, ${b4}, ${a3})`;
}
var createInterpolator2 = (range, output, extrapolate) => {
  if (is2.fun(range)) {
    return range;
  }
  if (is2.arr(range)) {
    return createInterpolator2({
      range,
      output,
      extrapolate
    });
  }
  if (is2.str(range.output[0])) {
    return createStringInterpolator3(range);
  }
  const config3 = range;
  const outputRange = config3.output;
  const inputRange = config3.range || [0, 1];
  const extrapolateLeft = config3.extrapolateLeft || config3.extrapolate || "extend";
  const extrapolateRight = config3.extrapolateRight || config3.extrapolate || "extend";
  const easing = config3.easing || ((t4) => t4);
  return (input) => {
    const range2 = findRange2(input, inputRange);
    return interpolate2(
      input,
      inputRange[range2],
      inputRange[range2 + 1],
      outputRange[range2],
      outputRange[range2 + 1],
      easing,
      extrapolateLeft,
      extrapolateRight,
      config3.map
    );
  };
};
function interpolate2(input, inputMin, inputMax, outputMin, outputMax, easing, extrapolateLeft, extrapolateRight, map) {
  let result = map ? map(input) : input;
  if (result < inputMin) {
    if (extrapolateLeft === "identity") return result;
    else if (extrapolateLeft === "clamp") result = inputMin;
  }
  if (result > inputMax) {
    if (extrapolateRight === "identity") return result;
    else if (extrapolateRight === "clamp") result = inputMax;
  }
  if (outputMin === outputMax) return outputMin;
  if (inputMin === inputMax) return input <= inputMin ? outputMin : outputMax;
  if (inputMin === -Infinity) result = -result;
  else if (inputMax === Infinity) result = result - inputMin;
  else result = (result - inputMin) / (inputMax - inputMin);
  result = easing(result);
  if (outputMin === -Infinity) result = -result;
  else if (outputMax === Infinity) result = result + outputMin;
  else result = result * (outputMax - outputMin) + outputMin;
  return result;
}
function findRange2(input, inputRange) {
  for (var i4 = 1; i4 < inputRange.length - 1; ++i4)
    if (inputRange[i4] >= input) break;
  return i4 - 1;
}
var steps2 = (steps22, direction = "end") => (progress2) => {
  progress2 = direction === "end" ? Math.min(progress2, 0.999) : Math.max(progress2, 1e-3);
  const expanded = progress2 * steps22;
  const rounded = direction === "end" ? Math.floor(expanded) : Math.ceil(expanded);
  return clamp2(0, 1, rounded / steps22);
};
var c12 = 1.70158;
var c22 = c12 * 1.525;
var c32 = c12 + 1;
var c42 = 2 * Math.PI / 3;
var c52 = 2 * Math.PI / 4.5;
var bounceOut2 = (x2) => {
  const n1 = 7.5625;
  const d1 = 2.75;
  if (x2 < 1 / d1) {
    return n1 * x2 * x2;
  } else if (x2 < 2 / d1) {
    return n1 * (x2 -= 1.5 / d1) * x2 + 0.75;
  } else if (x2 < 2.5 / d1) {
    return n1 * (x2 -= 2.25 / d1) * x2 + 0.9375;
  } else {
    return n1 * (x2 -= 2.625 / d1) * x2 + 0.984375;
  }
};
var easings2 = {
  linear: (x2) => x2,
  easeInQuad: (x2) => x2 * x2,
  easeOutQuad: (x2) => 1 - (1 - x2) * (1 - x2),
  easeInOutQuad: (x2) => x2 < 0.5 ? 2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 2) / 2,
  easeInCubic: (x2) => x2 * x2 * x2,
  easeOutCubic: (x2) => 1 - Math.pow(1 - x2, 3),
  easeInOutCubic: (x2) => x2 < 0.5 ? 4 * x2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 3) / 2,
  easeInQuart: (x2) => x2 * x2 * x2 * x2,
  easeOutQuart: (x2) => 1 - Math.pow(1 - x2, 4),
  easeInOutQuart: (x2) => x2 < 0.5 ? 8 * x2 * x2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 4) / 2,
  easeInQuint: (x2) => x2 * x2 * x2 * x2 * x2,
  easeOutQuint: (x2) => 1 - Math.pow(1 - x2, 5),
  easeInOutQuint: (x2) => x2 < 0.5 ? 16 * x2 * x2 * x2 * x2 * x2 : 1 - Math.pow(-2 * x2 + 2, 5) / 2,
  easeInSine: (x2) => 1 - Math.cos(x2 * Math.PI / 2),
  easeOutSine: (x2) => Math.sin(x2 * Math.PI / 2),
  easeInOutSine: (x2) => -(Math.cos(Math.PI * x2) - 1) / 2,
  easeInExpo: (x2) => x2 === 0 ? 0 : Math.pow(2, 10 * x2 - 10),
  easeOutExpo: (x2) => x2 === 1 ? 1 : 1 - Math.pow(2, -10 * x2),
  easeInOutExpo: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : x2 < 0.5 ? Math.pow(2, 20 * x2 - 10) / 2 : (2 - Math.pow(2, -20 * x2 + 10)) / 2,
  easeInCirc: (x2) => 1 - Math.sqrt(1 - Math.pow(x2, 2)),
  easeOutCirc: (x2) => Math.sqrt(1 - Math.pow(x2 - 1, 2)),
  easeInOutCirc: (x2) => x2 < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x2, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x2 + 2, 2)) + 1) / 2,
  easeInBack: (x2) => c32 * x2 * x2 * x2 - c12 * x2 * x2,
  easeOutBack: (x2) => 1 + c32 * Math.pow(x2 - 1, 3) + c12 * Math.pow(x2 - 1, 2),
  easeInOutBack: (x2) => x2 < 0.5 ? Math.pow(2 * x2, 2) * ((c22 + 1) * 2 * x2 - c22) / 2 : (Math.pow(2 * x2 - 2, 2) * ((c22 + 1) * (x2 * 2 - 2) + c22) + 2) / 2,
  easeInElastic: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : -Math.pow(2, 10 * x2 - 10) * Math.sin((x2 * 10 - 10.75) * c42),
  easeOutElastic: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : Math.pow(2, -10 * x2) * Math.sin((x2 * 10 - 0.75) * c42) + 1,
  easeInOutElastic: (x2) => x2 === 0 ? 0 : x2 === 1 ? 1 : x2 < 0.5 ? -(Math.pow(2, 20 * x2 - 10) * Math.sin((20 * x2 - 11.125) * c52)) / 2 : Math.pow(2, -20 * x2 + 10) * Math.sin((20 * x2 - 11.125) * c52) / 2 + 1,
  easeInBounce: (x2) => 1 - bounceOut2(1 - x2),
  easeOutBounce: bounceOut2,
  easeInOutBounce: (x2) => x2 < 0.5 ? (1 - bounceOut2(1 - 2 * x2)) / 2 : (1 + bounceOut2(2 * x2 - 1)) / 2,
  steps: steps2
};
var $get2 = Symbol.for("FluidValue.get");
var $observers2 = Symbol.for("FluidValue.observers");
var hasFluidValue2 = (arg) => Boolean(arg && arg[$get2]);
var getFluidValue2 = (arg) => arg && arg[$get2] ? arg[$get2]() : arg;
var getFluidObservers2 = (target) => target[$observers2] || null;
function callFluidObserver2(observer2, event) {
  if (observer2.eventObserved) {
    observer2.eventObserved(event);
  } else {
    observer2(event);
  }
}
function callFluidObservers2(target, event) {
  const observers = target[$observers2];
  if (observers) {
    observers.forEach((observer2) => {
      callFluidObserver2(observer2, event);
    });
  }
}
var FluidValue2 = class {
  constructor(get) {
    if (!get && !(get = this.get)) {
      throw Error("Unknown getter");
    }
    setFluidGetter2(this, get);
  }
};
var setFluidGetter2 = (target, get) => setHidden2(target, $get2, get);
function addFluidObserver2(target, observer2) {
  if (target[$get2]) {
    let observers = target[$observers2];
    if (!observers) {
      setHidden2(target, $observers2, observers = /* @__PURE__ */ new Set());
    }
    if (!observers.has(observer2)) {
      observers.add(observer2);
      if (target.observerAdded) {
        target.observerAdded(observers.size, observer2);
      }
    }
  }
  return observer2;
}
function removeFluidObserver2(target, observer2) {
  const observers = target[$observers2];
  if (observers && observers.has(observer2)) {
    const count = observers.size - 1;
    if (count) {
      observers.delete(observer2);
    } else {
      target[$observers2] = null;
    }
    if (target.observerRemoved) {
      target.observerRemoved(count, observer2);
    }
  }
}
var setHidden2 = (target, key, value) => Object.defineProperty(target, key, {
  value,
  writable: true,
  configurable: true
});
var numberRegex2 = /[+\-]?(?:0|[1-9]\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?/g;
var colorRegex2 = /(#(?:[0-9a-f]{2}){2,4}|(#[0-9a-f]{3})|(rgb|hsl)a?\((-?\d+%?[,\s]+){2,3}\s*[\d\.]+%?\))/gi;
var unitRegex2 = new RegExp(`(${numberRegex2.source})(%|[a-z]+)`, "i");
var rgbaRegex2 = /rgba\(([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+)\)/gi;
var cssVariableRegex2 = /var\((--[a-zA-Z0-9-_]+),? ?([a-zA-Z0-9 ()%#.,-]+)?\)/;
var variableToRgba2 = (input) => {
  const [token, fallback] = parseCSSVariable2(input);
  if (!token || isSSR2()) {
    return input;
  }
  const value = window.getComputedStyle(document.documentElement).getPropertyValue(token);
  if (value) {
    return value.trim();
  } else if (fallback && fallback.startsWith("--")) {
    const value2 = window.getComputedStyle(document.documentElement).getPropertyValue(fallback);
    if (value2) {
      return value2;
    } else {
      return input;
    }
  } else if (fallback && cssVariableRegex2.test(fallback)) {
    return variableToRgba2(fallback);
  } else if (fallback) {
    return fallback;
  }
  return input;
};
var parseCSSVariable2 = (current) => {
  const match = cssVariableRegex2.exec(current);
  if (!match) return [,];
  const [, token, fallback] = match;
  return [token, fallback];
};
var namedColorRegex2;
var rgbaRound2 = (_2, p1, p2, p3, p4) => `rgba(${Math.round(p1)}, ${Math.round(p2)}, ${Math.round(p3)}, ${p4})`;
var createStringInterpolator22 = (config3) => {
  if (!namedColorRegex2)
    namedColorRegex2 = colors3 ? (
      // match color names, ignore partial matches
      new RegExp(`(${Object.keys(colors3).join("|")})(?!\\w)`, "g")
    ) : (
      // never match
      /^\b$/
    );
  const output = config3.output.map((value) => {
    return getFluidValue2(value).replace(cssVariableRegex2, variableToRgba2).replace(colorRegex2, colorToRgba2).replace(namedColorRegex2, colorToRgba2);
  });
  const keyframes = output.map((value) => value.match(numberRegex2).map(Number));
  const outputRanges = keyframes[0].map(
    (_2, i4) => keyframes.map((values) => {
      if (!(i4 in values)) {
        throw Error('The arity of each "output" value must be equal');
      }
      return values[i4];
    })
  );
  const interpolators = outputRanges.map(
    (output2) => createInterpolator2({ ...config3, output: output2 })
  );
  return (input) => {
    var _a;
    const missingUnit = !unitRegex2.test(output[0]) && ((_a = output.find((value) => unitRegex2.test(value))) == null ? void 0 : _a.replace(numberRegex2, ""));
    let i4 = 0;
    return output[0].replace(
      numberRegex2,
      () => `${interpolators[i4++](input)}${missingUnit || ""}`
    ).replace(rgbaRegex2, rgbaRound2);
  };
};
var prefix2 = "react-spring: ";
var once2 = (fn2) => {
  const func = fn2;
  let called = false;
  if (typeof func != "function") {
    throw new TypeError(`${prefix2}once requires a function parameter`);
  }
  return (...args) => {
    if (!called) {
      func(...args);
      called = true;
    }
  };
};
var warnInterpolate2 = once2(console.warn);
function deprecateInterpolate2() {
  warnInterpolate2(
    `${prefix2}The "interpolate" function is deprecated in v9 (use "to" instead)`
  );
}
var warnDirectCall2 = once2(console.warn);
function deprecateDirectCall2() {
  warnDirectCall2(
    `${prefix2}Directly calling start instead of using the api object is deprecated in v9 (use ".start" instead), this will be removed in later 0.X.0 versions`
  );
}
function isAnimatedString2(value) {
  return is2.str(value) && (value[0] == "#" || /\d/.test(value) || // Do not identify a CSS variable as an AnimatedString if its SSR
  !isSSR2() && cssVariableRegex2.test(value) || value in (colors3 || {}));
}
var useIsomorphicLayoutEffect2 = isSSR2() ? import_react19.useEffect : import_react19.useLayoutEffect;
var useIsMounted2 = () => {
  const isMounted = (0, import_react18.useRef)(false);
  useIsomorphicLayoutEffect2(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
function useForceUpdate2() {
  const update5 = (0, import_react17.useState)()[1];
  const isMounted = useIsMounted2();
  return () => {
    if (isMounted.current) {
      update5(Math.random());
    }
  };
}
var useOnce2 = (effect) => (0, import_react21.useEffect)(effect, emptyDeps2);
var emptyDeps2 = [];
function usePrev2(value) {
  const prevRef = (0, import_react22.useRef)(void 0);
  (0, import_react22.useEffect)(() => {
    prevRef.current = value;
  });
  return prevRef.current;
}

// node_modules/@nivo/annotations/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var import_react25 = __toESM(require_react(), 1);

// node_modules/@nivo/annotations/node_modules/@react-spring/animated/dist/react-spring_animated.modern.mjs
var React3 = __toESM(require_react(), 1);
var import_react24 = __toESM(require_react(), 1);
var $node2 = Symbol.for("Animated:node");
var isAnimated2 = (value) => !!value && value[$node2] === value;
var getAnimated2 = (owner) => owner && owner[$node2];
var setAnimated2 = (owner, node) => defineHidden2(owner, $node2, node);
var getPayload2 = (owner) => owner && owner[$node2] && owner[$node2].getPayload();
var Animated2 = class {
  constructor() {
    setAnimated2(this, this);
  }
  /** Get every `AnimatedValue` used by this node. */
  getPayload() {
    return this.payload || [];
  }
};
var AnimatedValue2 = class _AnimatedValue2 extends Animated2 {
  constructor(_value) {
    super();
    this._value = _value;
    this.done = true;
    this.durationProgress = 0;
    if (is2.num(this._value)) {
      this.lastPosition = this._value;
    }
  }
  /** @internal */
  static create(value) {
    return new _AnimatedValue2(value);
  }
  getPayload() {
    return [this];
  }
  getValue() {
    return this._value;
  }
  setValue(value, step) {
    if (is2.num(value)) {
      this.lastPosition = value;
      if (step) {
        value = Math.round(value / step) * step;
        if (this.done) {
          this.lastPosition = value;
        }
      }
    }
    if (this._value === value) {
      return false;
    }
    this._value = value;
    return true;
  }
  reset() {
    const { done } = this;
    this.done = false;
    if (is2.num(this._value)) {
      this.elapsedTime = 0;
      this.durationProgress = 0;
      this.lastPosition = this._value;
      if (done) this.lastVelocity = null;
      this.v0 = null;
    }
  }
};
var AnimatedString2 = class _AnimatedString2 extends AnimatedValue2 {
  constructor(value) {
    super(0);
    this._string = null;
    this._toString = createInterpolator2({
      output: [value, value]
    });
  }
  /** @internal */
  static create(value) {
    return new _AnimatedString2(value);
  }
  getValue() {
    const value = this._string;
    return value == null ? this._string = this._toString(this._value) : value;
  }
  setValue(value) {
    if (is2.str(value)) {
      if (value == this._string) {
        return false;
      }
      this._string = value;
      this._value = 1;
    } else if (super.setValue(value)) {
      this._string = null;
    } else {
      return false;
    }
    return true;
  }
  reset(goal) {
    if (goal) {
      this._toString = createInterpolator2({
        output: [this.getValue(), goal]
      });
    }
    this._value = 0;
    super.reset();
  }
};
var TreeContext2 = { dependencies: null };
var AnimatedObject2 = class extends Animated2 {
  constructor(source) {
    super();
    this.source = source;
    this.setValue(source);
  }
  getValue(animated3) {
    const values = {};
    eachProp2(this.source, (source, key) => {
      if (isAnimated2(source)) {
        values[key] = source.getValue(animated3);
      } else if (hasFluidValue2(source)) {
        values[key] = getFluidValue2(source);
      } else if (!animated3) {
        values[key] = source;
      }
    });
    return values;
  }
  /** Replace the raw object data */
  setValue(source) {
    this.source = source;
    this.payload = this._makePayload(source);
  }
  reset() {
    if (this.payload) {
      each2(this.payload, (node) => node.reset());
    }
  }
  /** Create a payload set. */
  _makePayload(source) {
    if (source) {
      const payload = /* @__PURE__ */ new Set();
      eachProp2(source, this._addToPayload, payload);
      return Array.from(payload);
    }
  }
  /** Add to a payload set. */
  _addToPayload(source) {
    if (TreeContext2.dependencies && hasFluidValue2(source)) {
      TreeContext2.dependencies.add(source);
    }
    const payload = getPayload2(source);
    if (payload) {
      each2(payload, (node) => this.add(node));
    }
  }
};
var AnimatedArray2 = class _AnimatedArray2 extends AnimatedObject2 {
  constructor(source) {
    super(source);
  }
  /** @internal */
  static create(source) {
    return new _AnimatedArray2(source);
  }
  getValue() {
    return this.source.map((node) => node.getValue());
  }
  setValue(source) {
    const payload = this.getPayload();
    if (source.length == payload.length) {
      return payload.map((node, i4) => node.setValue(source[i4])).some(Boolean);
    }
    super.setValue(source.map(makeAnimated2));
    return true;
  }
};
function makeAnimated2(value) {
  const nodeType = isAnimatedString2(value) ? AnimatedString2 : AnimatedValue2;
  return nodeType.create(value);
}
function getAnimatedType2(value) {
  const parentNode = getAnimated2(value);
  return parentNode ? parentNode.constructor : is2.arr(value) ? AnimatedArray2 : isAnimatedString2(value) ? AnimatedString2 : AnimatedValue2;
}
var withAnimated2 = (Component, host3) => {
  const hasInstance = (
    // Function components must use "forwardRef" to avoid being
    // re-rendered on every animation frame.
    !is2.fun(Component) || Component.prototype && Component.prototype.isReactComponent
  );
  return (0, import_react24.forwardRef)((givenProps, givenRef) => {
    const instanceRef = (0, import_react24.useRef)(null);
    const ref = hasInstance && // eslint-disable-next-line react-hooks/rules-of-hooks
    (0, import_react24.useCallback)(
      (value) => {
        instanceRef.current = updateRef2(givenRef, value);
      },
      [givenRef]
    );
    const [props, deps] = getAnimatedState2(givenProps, host3);
    const forceUpdate = useForceUpdate2();
    const callback = () => {
      const instance = instanceRef.current;
      if (hasInstance && !instance) {
        return;
      }
      const didUpdate = instance ? host3.applyAnimatedValues(instance, props.getValue(true)) : false;
      if (didUpdate === false) {
        forceUpdate();
      }
    };
    const observer = new PropsObserver2(callback, deps);
    const observerRef = (0, import_react24.useRef)(void 0);
    useIsomorphicLayoutEffect2(() => {
      observerRef.current = observer;
      each2(deps, (dep) => addFluidObserver2(dep, observer));
      return () => {
        if (observerRef.current) {
          each2(
            observerRef.current.deps,
            (dep) => removeFluidObserver2(dep, observerRef.current)
          );
          raf2.cancel(observerRef.current.update);
        }
      };
    });
    (0, import_react24.useEffect)(callback, []);
    useOnce2(() => () => {
      const observer2 = observerRef.current;
      each2(observer2.deps, (dep) => removeFluidObserver2(dep, observer2));
    });
    const usedProps = host3.getComponentProps(props.getValue());
    return React3.createElement(Component, { ...usedProps, ref });
  });
};
var PropsObserver2 = class {
  constructor(update5, deps) {
    this.update = update5;
    this.deps = deps;
  }
  eventObserved(event) {
    if (event.type == "change") {
      raf2.write(this.update);
    }
  }
};
function getAnimatedState2(props, host3) {
  const dependencies = /* @__PURE__ */ new Set();
  TreeContext2.dependencies = dependencies;
  if (props.style)
    props = {
      ...props,
      style: host3.createAnimatedStyle(props.style)
    };
  props = new AnimatedObject2(props);
  TreeContext2.dependencies = null;
  return [props, dependencies];
}
function updateRef2(ref, value) {
  if (ref) {
    if (is2.fun(ref)) ref(value);
    else ref.current = value;
  }
  return value;
}
var cacheKey2 = Symbol.for("AnimatedComponent");
var createHost2 = (components, {
  applyAnimatedValues: applyAnimatedValues3 = () => false,
  createAnimatedStyle = (style) => new AnimatedObject2(style),
  getComponentProps = (props) => props
} = {}) => {
  const hostConfig = {
    applyAnimatedValues: applyAnimatedValues3,
    createAnimatedStyle,
    getComponentProps
  };
  const animated3 = (Component) => {
    const displayName = getDisplayName2(Component) || "Anonymous";
    if (is2.str(Component)) {
      Component = animated3[Component] || (animated3[Component] = withAnimated2(Component, hostConfig));
    } else {
      Component = Component[cacheKey2] || (Component[cacheKey2] = withAnimated2(Component, hostConfig));
    }
    Component.displayName = `Animated(${displayName})`;
    return Component;
  };
  eachProp2(components, (Component, key) => {
    if (is2.arr(components)) {
      key = getDisplayName2(Component);
    }
    animated3[key] = animated3(Component);
  });
  return {
    animated: animated3
  };
};
var getDisplayName2 = (arg) => is2.str(arg) ? arg : arg && is2.str(arg.displayName) ? arg.displayName : is2.fun(arg) && arg.name || null;

// node_modules/@nivo/annotations/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var React4 = __toESM(require_react(), 1);
var import_react26 = __toESM(require_react(), 1);
var import_react27 = __toESM(require_react(), 1);
var React23 = __toESM(require_react(), 1);
var import_react28 = __toESM(require_react(), 1);
var import_react29 = __toESM(require_react(), 1);
function callProp2(value, ...args) {
  return is2.fun(value) ? value(...args) : value;
}
var matchProp2 = (value, key) => value === true || !!(key && value && (is2.fun(value) ? value(key) : toArray2(value).includes(key)));
var resolveProp2 = (prop, key) => is2.obj(prop) ? key && prop[key] : prop;
var getDefaultProp2 = (props, key) => props.default === true ? props[key] : props.default ? props.default[key] : void 0;
var noopTransform2 = (value) => value;
var getDefaultProps2 = (props, transform = noopTransform2) => {
  let keys = DEFAULT_PROPS2;
  if (props.default && props.default !== true) {
    props = props.default;
    keys = Object.keys(props);
  }
  const defaults22 = {};
  for (const key of keys) {
    const value = transform(props[key], key);
    if (!is2.und(value)) {
      defaults22[key] = value;
    }
  }
  return defaults22;
};
var DEFAULT_PROPS2 = [
  "config",
  "onProps",
  "onStart",
  "onChange",
  "onPause",
  "onResume",
  "onRest"
];
var RESERVED_PROPS2 = {
  config: 1,
  from: 1,
  to: 1,
  ref: 1,
  loop: 1,
  reset: 1,
  pause: 1,
  cancel: 1,
  reverse: 1,
  immediate: 1,
  default: 1,
  delay: 1,
  onProps: 1,
  onStart: 1,
  onChange: 1,
  onPause: 1,
  onResume: 1,
  onRest: 1,
  onResolve: 1,
  // Transition props
  items: 1,
  trail: 1,
  sort: 1,
  expires: 1,
  initial: 1,
  enter: 1,
  update: 1,
  leave: 1,
  children: 1,
  onDestroyed: 1,
  // Internal props
  keys: 1,
  callId: 1,
  parentId: 1
};
function getForwardProps2(props) {
  const forward = {};
  let count = 0;
  eachProp2(props, (value, prop) => {
    if (!RESERVED_PROPS2[prop]) {
      forward[prop] = value;
      count++;
    }
  });
  if (count) {
    return forward;
  }
}
function inferTo2(props) {
  const to22 = getForwardProps2(props);
  if (to22) {
    const out = { to: to22 };
    eachProp2(props, (val, key) => key in to22 || (out[key] = val));
    return out;
  }
  return { ...props };
}
function computeGoal2(value) {
  value = getFluidValue2(value);
  return is2.arr(value) ? value.map(computeGoal2) : isAnimatedString2(value) ? globals_exports2.createStringInterpolator({
    range: [0, 1],
    output: [value, value]
  })(1) : value;
}
function hasProps2(props) {
  for (const _2 in props) return true;
  return false;
}
function isAsyncTo2(to22) {
  return is2.fun(to22) || is2.arr(to22) && is2.obj(to22[0]);
}
function detachRefs2(ctrl, ref) {
  var _a;
  (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
  ref == null ? void 0 : ref.delete(ctrl);
}
function replaceRef2(ctrl, ref) {
  var _a;
  if (ref && ctrl.ref !== ref) {
    (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
    ref.add(ctrl);
    ctrl.ref = ref;
  }
}
var config2 = {
  default: { tension: 170, friction: 26 },
  gentle: { tension: 120, friction: 14 },
  wobbly: { tension: 180, friction: 12 },
  stiff: { tension: 210, friction: 20 },
  slow: { tension: 280, friction: 60 },
  molasses: { tension: 280, friction: 120 }
};
var defaults2 = {
  ...config2.default,
  mass: 1,
  damping: 1,
  easing: easings2.linear,
  clamp: false
};
var AnimationConfig2 = class {
  constructor() {
    this.velocity = 0;
    Object.assign(this, defaults2);
  }
};
function mergeConfig2(config22, newConfig, defaultConfig) {
  if (defaultConfig) {
    defaultConfig = { ...defaultConfig };
    sanitizeConfig2(defaultConfig, newConfig);
    newConfig = { ...defaultConfig, ...newConfig };
  }
  sanitizeConfig2(config22, newConfig);
  Object.assign(config22, newConfig);
  for (const key in defaults2) {
    if (config22[key] == null) {
      config22[key] = defaults2[key];
    }
  }
  let { frequency, damping } = config22;
  const { mass } = config22;
  if (!is2.und(frequency)) {
    if (frequency < 0.01) frequency = 0.01;
    if (damping < 0) damping = 0;
    config22.tension = Math.pow(2 * Math.PI / frequency, 2) * mass;
    config22.friction = 4 * Math.PI * damping * mass / frequency;
  }
  return config22;
}
function sanitizeConfig2(config22, props) {
  if (!is2.und(props.decay)) {
    config22.duration = void 0;
  } else {
    const isTensionConfig = !is2.und(props.tension) || !is2.und(props.friction);
    if (isTensionConfig || !is2.und(props.frequency) || !is2.und(props.damping) || !is2.und(props.mass)) {
      config22.duration = void 0;
      config22.decay = void 0;
    }
    if (isTensionConfig) {
      config22.frequency = void 0;
    }
  }
}
var emptyArray2 = [];
var Animation2 = class {
  constructor() {
    this.changed = false;
    this.values = emptyArray2;
    this.toValues = null;
    this.fromValues = emptyArray2;
    this.config = new AnimationConfig2();
    this.immediate = false;
  }
};
function scheduleProps2(callId, { key, props, defaultProps, state, actions }) {
  return new Promise((resolve, reject) => {
    let delay;
    let timeout;
    let cancel = matchProp2(props.cancel ?? (defaultProps == null ? void 0 : defaultProps.cancel), key);
    if (cancel) {
      onStart();
    } else {
      if (!is2.und(props.pause)) {
        state.paused = matchProp2(props.pause, key);
      }
      let pause = defaultProps == null ? void 0 : defaultProps.pause;
      if (pause !== true) {
        pause = state.paused || matchProp2(pause, key);
      }
      delay = callProp2(props.delay || 0, key);
      if (pause) {
        state.resumeQueue.add(onResume);
        actions.pause();
      } else {
        actions.resume();
        onResume();
      }
    }
    function onPause() {
      state.resumeQueue.add(onResume);
      state.timeouts.delete(timeout);
      timeout.cancel();
      delay = timeout.time - raf2.now();
    }
    function onResume() {
      if (delay > 0 && !globals_exports2.skipAnimation) {
        state.delayed = true;
        timeout = raf2.setTimeout(onStart, delay);
        state.pauseQueue.add(onPause);
        state.timeouts.add(timeout);
      } else {
        onStart();
      }
    }
    function onStart() {
      if (state.delayed) {
        state.delayed = false;
      }
      state.pauseQueue.delete(onPause);
      state.timeouts.delete(timeout);
      if (callId <= (state.cancelId || 0)) {
        cancel = true;
      }
      try {
        actions.start({ ...props, callId, cancel }, resolve);
      } catch (err) {
        reject(err);
      }
    }
  });
}
var getCombinedResult2 = (target, results) => results.length == 1 ? results[0] : results.some((result) => result.cancelled) ? getCancelledResult2(target.get()) : results.every((result) => result.noop) ? getNoopResult2(target.get()) : getFinishedResult2(
  target.get(),
  results.every((result) => result.finished)
);
var getNoopResult2 = (value) => ({
  value,
  noop: true,
  finished: true,
  cancelled: false
});
var getFinishedResult2 = (value, finished, cancelled = false) => ({
  value,
  finished,
  cancelled
});
var getCancelledResult2 = (value) => ({
  value,
  cancelled: true,
  finished: false
});
function runAsync2(to22, props, state, target) {
  const { callId, parentId, onRest } = props;
  const { asyncTo: prevTo, promise: prevPromise } = state;
  if (!parentId && to22 === prevTo && !props.reset) {
    return prevPromise;
  }
  return state.promise = (async () => {
    state.asyncId = callId;
    state.asyncTo = to22;
    const defaultProps = getDefaultProps2(
      props,
      (value, key) => (
        // The `onRest` prop is only called when the `runAsync` promise is resolved.
        key === "onRest" ? void 0 : value
      )
    );
    let preventBail;
    let bail;
    const bailPromise = new Promise(
      (resolve, reject) => (preventBail = resolve, bail = reject)
    );
    const bailIfEnded = (bailSignal) => {
      const bailResult = (
        // The `cancel` prop or `stop` method was used.
        callId <= (state.cancelId || 0) && getCancelledResult2(target) || // The async `to` prop was replaced.
        callId !== state.asyncId && getFinishedResult2(target, false)
      );
      if (bailResult) {
        bailSignal.result = bailResult;
        bail(bailSignal);
        throw bailSignal;
      }
    };
    const animate = (arg1, arg2) => {
      const bailSignal = new BailSignal2();
      const skipAnimationSignal = new SkipAnimationSignal2();
      return (async () => {
        if (globals_exports2.skipAnimation) {
          stopAsync2(state);
          skipAnimationSignal.result = getFinishedResult2(target, false);
          bail(skipAnimationSignal);
          throw skipAnimationSignal;
        }
        bailIfEnded(bailSignal);
        const props2 = is2.obj(arg1) ? { ...arg1 } : { ...arg2, to: arg1 };
        props2.parentId = callId;
        eachProp2(defaultProps, (value, key) => {
          if (is2.und(props2[key])) {
            props2[key] = value;
          }
        });
        const result2 = await target.start(props2);
        bailIfEnded(bailSignal);
        if (state.paused) {
          await new Promise((resume) => {
            state.resumeQueue.add(resume);
          });
        }
        return result2;
      })();
    };
    let result;
    if (globals_exports2.skipAnimation) {
      stopAsync2(state);
      return getFinishedResult2(target, false);
    }
    try {
      let animating;
      if (is2.arr(to22)) {
        animating = (async (queue) => {
          for (const props2 of queue) {
            await animate(props2);
          }
        })(to22);
      } else {
        animating = Promise.resolve(to22(animate, target.stop.bind(target)));
      }
      await Promise.all([animating.then(preventBail), bailPromise]);
      result = getFinishedResult2(target.get(), true, false);
    } catch (err) {
      if (err instanceof BailSignal2) {
        result = err.result;
      } else if (err instanceof SkipAnimationSignal2) {
        result = err.result;
      } else {
        throw err;
      }
    } finally {
      if (callId == state.asyncId) {
        state.asyncId = parentId;
        state.asyncTo = parentId ? prevTo : void 0;
        state.promise = parentId ? prevPromise : void 0;
      }
    }
    if (is2.fun(onRest)) {
      raf2.batchedUpdates(() => {
        onRest(result, target, target.item);
      });
    }
    return result;
  })();
}
function stopAsync2(state, cancelId) {
  flush2(state.timeouts, (t4) => t4.cancel());
  state.pauseQueue.clear();
  state.resumeQueue.clear();
  state.asyncId = state.asyncTo = state.promise = void 0;
  if (cancelId) state.cancelId = cancelId;
}
var BailSignal2 = class extends Error {
  constructor() {
    super(
      "An async animation has been interrupted. You see this error because you forgot to use `await` or `.catch(...)` on its returned promise."
    );
  }
};
var SkipAnimationSignal2 = class extends Error {
  constructor() {
    super("SkipAnimationSignal");
  }
};
var isFrameValue2 = (value) => value instanceof FrameValue2;
var nextId3 = 1;
var FrameValue2 = class extends FluidValue2 {
  constructor() {
    super(...arguments);
    this.id = nextId3++;
    this._priority = 0;
  }
  get priority() {
    return this._priority;
  }
  set priority(priority3) {
    if (this._priority != priority3) {
      this._priority = priority3;
      this._onPriorityChange(priority3);
    }
  }
  /** Get the current value */
  get() {
    const node = getAnimated2(this);
    return node && node.getValue();
  }
  /** Create a spring that maps our value to another value */
  to(...args) {
    return globals_exports2.to(this, args);
  }
  /** @deprecated Use the `to` method instead. */
  interpolate(...args) {
    deprecateInterpolate2();
    return globals_exports2.to(this, args);
  }
  toJSON() {
    return this.get();
  }
  observerAdded(count) {
    if (count == 1) this._attach();
  }
  observerRemoved(count) {
    if (count == 0) this._detach();
  }
  /** Called when the first child is added. */
  _attach() {
  }
  /** Called when the last child is removed. */
  _detach() {
  }
  /** Tell our children about our new value */
  _onChange(value, idle = false) {
    callFluidObservers2(this, {
      type: "change",
      parent: this,
      value,
      idle
    });
  }
  /** Tell our children about our new priority */
  _onPriorityChange(priority3) {
    if (!this.idle) {
      frameLoop2.sort(this);
    }
    callFluidObservers2(this, {
      type: "priority",
      parent: this,
      priority: priority3
    });
  }
};
var $P2 = Symbol.for("SpringPhase");
var HAS_ANIMATED2 = 1;
var IS_ANIMATING2 = 2;
var IS_PAUSED2 = 4;
var hasAnimated2 = (target) => (target[$P2] & HAS_ANIMATED2) > 0;
var isAnimating2 = (target) => (target[$P2] & IS_ANIMATING2) > 0;
var isPaused2 = (target) => (target[$P2] & IS_PAUSED2) > 0;
var setActiveBit2 = (target, active) => active ? target[$P2] |= IS_ANIMATING2 | HAS_ANIMATED2 : target[$P2] &= ~IS_ANIMATING2;
var setPausedBit2 = (target, paused) => paused ? target[$P2] |= IS_PAUSED2 : target[$P2] &= ~IS_PAUSED2;
var SpringValue2 = class extends FrameValue2 {
  constructor(arg1, arg2) {
    super();
    this.animation = new Animation2();
    this.defaultProps = {};
    this._state = {
      paused: false,
      delayed: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._pendingCalls = /* @__PURE__ */ new Set();
    this._lastCallId = 0;
    this._lastToId = 0;
    this._memoizedDuration = 0;
    if (!is2.und(arg1) || !is2.und(arg2)) {
      const props = is2.obj(arg1) ? { ...arg1 } : { ...arg2, from: arg1 };
      if (is2.und(props.default)) {
        props.default = true;
      }
      this.start(props);
    }
  }
  /** Equals true when not advancing on each frame. */
  get idle() {
    return !(isAnimating2(this) || this._state.asyncTo) || isPaused2(this);
  }
  get goal() {
    return getFluidValue2(this.animation.to);
  }
  get velocity() {
    const node = getAnimated2(this);
    return node instanceof AnimatedValue2 ? node.lastVelocity || 0 : node.getPayload().map((node2) => node2.lastVelocity || 0);
  }
  /**
   * When true, this value has been animated at least once.
   */
  get hasAnimated() {
    return hasAnimated2(this);
  }
  /**
   * When true, this value has an unfinished animation,
   * which is either active or paused.
   */
  get isAnimating() {
    return isAnimating2(this);
  }
  /**
   * When true, all current and future animations are paused.
   */
  get isPaused() {
    return isPaused2(this);
  }
  /**
   *
   *
   */
  get isDelayed() {
    return this._state.delayed;
  }
  /** Advance the current animation by a number of milliseconds */
  advance(dt) {
    let idle = true;
    let changed = false;
    const anim = this.animation;
    let { toValues } = anim;
    const { config: config22 } = anim;
    const payload = getPayload2(anim.to);
    if (!payload && hasFluidValue2(anim.to)) {
      toValues = toArray2(getFluidValue2(anim.to));
    }
    anim.values.forEach((node2, i4) => {
      if (node2.done) return;
      const to22 = (
        // Animated strings always go from 0 to 1.
        node2.constructor == AnimatedString2 ? 1 : payload ? payload[i4].lastPosition : toValues[i4]
      );
      let finished = anim.immediate;
      let position = to22;
      if (!finished) {
        position = node2.lastPosition;
        if (config22.tension <= 0) {
          node2.done = true;
          return;
        }
        let elapsed = node2.elapsedTime += dt;
        const from = anim.fromValues[i4];
        const v0 = node2.v0 != null ? node2.v0 : node2.v0 = is2.arr(config22.velocity) ? config22.velocity[i4] : config22.velocity;
        let velocity;
        const precision = config22.precision || (from == to22 ? 5e-3 : Math.min(1, Math.abs(to22 - from) * 1e-3));
        if (!is2.und(config22.duration)) {
          let p2 = 1;
          if (config22.duration > 0) {
            if (this._memoizedDuration !== config22.duration) {
              this._memoizedDuration = config22.duration;
              if (node2.durationProgress > 0) {
                node2.elapsedTime = config22.duration * node2.durationProgress;
                elapsed = node2.elapsedTime += dt;
              }
            }
            p2 = (config22.progress || 0) + elapsed / this._memoizedDuration;
            p2 = p2 > 1 ? 1 : p2 < 0 ? 0 : p2;
            node2.durationProgress = p2;
          }
          position = from + config22.easing(p2) * (to22 - from);
          velocity = (position - node2.lastPosition) / dt;
          finished = p2 == 1;
        } else if (config22.decay) {
          const decay = config22.decay === true ? 0.998 : config22.decay;
          const e4 = Math.exp(-(1 - decay) * elapsed);
          position = from + v0 / (1 - decay) * (1 - e4);
          finished = Math.abs(node2.lastPosition - position) <= precision;
          velocity = v0 * e4;
        } else {
          velocity = node2.lastVelocity == null ? v0 : node2.lastVelocity;
          const restVelocity = config22.restVelocity || precision / 10;
          const bounceFactor = config22.clamp ? 0 : config22.bounce;
          const canBounce = !is2.und(bounceFactor);
          const isGrowing = from == to22 ? node2.v0 > 0 : from < to22;
          let isMoving;
          let isBouncing = false;
          const step = 1;
          const numSteps = Math.ceil(dt / step);
          for (let n4 = 0; n4 < numSteps; ++n4) {
            isMoving = Math.abs(velocity) > restVelocity;
            if (!isMoving) {
              finished = Math.abs(to22 - position) <= precision;
              if (finished) {
                break;
              }
            }
            if (canBounce) {
              isBouncing = position == to22 || position > to22 == isGrowing;
              if (isBouncing) {
                velocity = -velocity * bounceFactor;
                position = to22;
              }
            }
            const springForce = -config22.tension * 1e-6 * (position - to22);
            const dampingForce = -config22.friction * 1e-3 * velocity;
            const acceleration = (springForce + dampingForce) / config22.mass;
            velocity = velocity + acceleration * step;
            position = position + velocity * step;
          }
        }
        node2.lastVelocity = velocity;
        if (Number.isNaN(position)) {
          console.warn(`Got NaN while animating:`, this);
          finished = true;
        }
      }
      if (payload && !payload[i4].done) {
        finished = false;
      }
      if (finished) {
        node2.done = true;
      } else {
        idle = false;
      }
      if (node2.setValue(position, config22.round)) {
        changed = true;
      }
    });
    const node = getAnimated2(this);
    const currVal = node.getValue();
    if (idle) {
      const finalVal = getFluidValue2(anim.to);
      if ((currVal !== finalVal || changed) && !config22.decay) {
        node.setValue(finalVal);
        this._onChange(finalVal);
      } else if (changed && config22.decay) {
        this._onChange(currVal);
      }
      this._stop();
    } else if (changed) {
      this._onChange(currVal);
    }
  }
  /** Set the current value, while stopping the current animation */
  set(value) {
    raf2.batchedUpdates(() => {
      this._stop();
      this._focus(value);
      this._set(value);
    });
    return this;
  }
  /**
   * Freeze the active animation in time, as well as any updates merged
   * before `resume` is called.
   */
  pause() {
    this._update({ pause: true });
  }
  /** Resume the animation if paused. */
  resume() {
    this._update({ pause: false });
  }
  /** Skip to the end of the current animation. */
  finish() {
    if (isAnimating2(this)) {
      const { to: to22, config: config22 } = this.animation;
      raf2.batchedUpdates(() => {
        this._onStart();
        if (!config22.decay) {
          this._set(to22, false);
        }
        this._stop();
      });
    }
    return this;
  }
  /** Push props into the pending queue. */
  update(props) {
    const queue = this.queue || (this.queue = []);
    queue.push(props);
    return this;
  }
  start(to22, arg2) {
    let queue;
    if (!is2.und(to22)) {
      queue = [is2.obj(to22) ? to22 : { ...arg2, to: to22 }];
    } else {
      queue = this.queue || [];
      this.queue = [];
    }
    return Promise.all(
      queue.map((props) => {
        const up = this._update(props);
        return up;
      })
    ).then((results) => getCombinedResult2(this, results));
  }
  /**
   * Stop the current animation, and cancel any delayed updates.
   *
   * Pass `true` to call `onRest` with `cancelled: true`.
   */
  stop(cancel) {
    const { to: to22 } = this.animation;
    this._focus(this.get());
    stopAsync2(this._state, cancel && this._lastCallId);
    raf2.batchedUpdates(() => this._stop(to22, cancel));
    return this;
  }
  /** Restart the animation. */
  reset() {
    this._update({ reset: true });
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._start();
    } else if (event.type == "priority") {
      this.priority = event.priority + 1;
    }
  }
  /**
   * Parse the `to` and `from` range from the given `props` object.
   *
   * This also ensures the initial value is available to animated components
   * during the render phase.
   */
  _prepareNode(props) {
    const key = this.key || "";
    let { to: to22, from } = props;
    to22 = is2.obj(to22) ? to22[key] : to22;
    if (to22 == null || isAsyncTo2(to22)) {
      to22 = void 0;
    }
    from = is2.obj(from) ? from[key] : from;
    if (from == null) {
      from = void 0;
    }
    const range = { to: to22, from };
    if (!hasAnimated2(this)) {
      if (props.reverse) [to22, from] = [from, to22];
      from = getFluidValue2(from);
      if (!is2.und(from)) {
        this._set(from);
      } else if (!getAnimated2(this)) {
        this._set(to22);
      }
    }
    return range;
  }
  /** Every update is processed by this method before merging. */
  _update({ ...props }, isLoop) {
    const { key, defaultProps } = this;
    if (props.default)
      Object.assign(
        defaultProps,
        getDefaultProps2(
          props,
          (value, prop) => /^on/.test(prop) ? resolveProp2(value, key) : value
        )
      );
    mergeActiveFn2(this, props, "onProps");
    sendEvent2(this, "onProps", props, this);
    const range = this._prepareNode(props);
    if (Object.isFrozen(this)) {
      throw Error(
        "Cannot animate a `SpringValue` object that is frozen. Did you forget to pass your component to `animated(...)` before animating its props?"
      );
    }
    const state = this._state;
    return scheduleProps2(++this._lastCallId, {
      key,
      props,
      defaultProps,
      state,
      actions: {
        pause: () => {
          if (!isPaused2(this)) {
            setPausedBit2(this, true);
            flushCalls2(state.pauseQueue);
            sendEvent2(
              this,
              "onPause",
              getFinishedResult2(this, checkFinished2(this, this.animation.to)),
              this
            );
          }
        },
        resume: () => {
          if (isPaused2(this)) {
            setPausedBit2(this, false);
            if (isAnimating2(this)) {
              this._resume();
            }
            flushCalls2(state.resumeQueue);
            sendEvent2(
              this,
              "onResume",
              getFinishedResult2(this, checkFinished2(this, this.animation.to)),
              this
            );
          }
        },
        start: this._merge.bind(this, range)
      }
    }).then((result) => {
      if (props.loop && result.finished && !(isLoop && result.noop)) {
        const nextProps = createLoopUpdate2(props);
        if (nextProps) {
          return this._update(nextProps, true);
        }
      }
      return result;
    });
  }
  /** Merge props into the current animation */
  _merge(range, props, resolve) {
    if (props.cancel) {
      this.stop(true);
      return resolve(getCancelledResult2(this));
    }
    const hasToProp = !is2.und(range.to);
    const hasFromProp = !is2.und(range.from);
    if (hasToProp || hasFromProp) {
      if (props.callId > this._lastToId) {
        this._lastToId = props.callId;
      } else {
        return resolve(getCancelledResult2(this));
      }
    }
    const { key, defaultProps, animation: anim } = this;
    const { to: prevTo, from: prevFrom } = anim;
    let { to: to22 = prevTo, from = prevFrom } = range;
    if (hasFromProp && !hasToProp && (!props.default || is2.und(to22))) {
      to22 = from;
    }
    if (props.reverse) [to22, from] = [from, to22];
    const hasFromChanged = !isEqual2(from, prevFrom);
    if (hasFromChanged) {
      anim.from = from;
    }
    from = getFluidValue2(from);
    const hasToChanged = !isEqual2(to22, prevTo);
    if (hasToChanged) {
      this._focus(to22);
    }
    const hasAsyncTo = isAsyncTo2(props.to);
    const { config: config22 } = anim;
    const { decay, velocity } = config22;
    if (hasToProp || hasFromProp) {
      config22.velocity = 0;
    }
    if (props.config && !hasAsyncTo) {
      mergeConfig2(
        config22,
        callProp2(props.config, key),
        // Avoid calling the same "config" prop twice.
        props.config !== defaultProps.config ? callProp2(defaultProps.config, key) : void 0
      );
    }
    let node = getAnimated2(this);
    if (!node || is2.und(to22)) {
      return resolve(getFinishedResult2(this, true));
    }
    const reset = (
      // When `reset` is undefined, the `from` prop implies `reset: true`,
      // except for declarative updates. When `reset` is defined, there
      // must exist a value to animate from.
      is2.und(props.reset) ? hasFromProp && !props.default : !is2.und(from) && matchProp2(props.reset, key)
    );
    const value = reset ? from : this.get();
    const goal = computeGoal2(to22);
    const isAnimatable = is2.num(goal) || is2.arr(goal) || isAnimatedString2(goal);
    const immediate = !hasAsyncTo && (!isAnimatable || matchProp2(defaultProps.immediate || props.immediate, key));
    if (hasToChanged) {
      const nodeType = getAnimatedType2(to22);
      if (nodeType !== node.constructor) {
        if (immediate) {
          node = this._set(goal);
        } else
          throw Error(
            `Cannot animate between ${node.constructor.name} and ${nodeType.name}, as the "to" prop suggests`
          );
      }
    }
    const goalType = node.constructor;
    let started = hasFluidValue2(to22);
    let finished = false;
    if (!started) {
      const hasValueChanged = reset || !hasAnimated2(this) && hasFromChanged;
      if (hasToChanged || hasValueChanged) {
        finished = isEqual2(computeGoal2(value), goal);
        started = !finished;
      }
      if (!isEqual2(anim.immediate, immediate) && !immediate || !isEqual2(config22.decay, decay) || !isEqual2(config22.velocity, velocity)) {
        started = true;
      }
    }
    if (finished && isAnimating2(this)) {
      if (anim.changed && !reset) {
        started = true;
      } else if (!started) {
        this._stop(prevTo);
      }
    }
    if (!hasAsyncTo) {
      if (started || hasFluidValue2(prevTo)) {
        anim.values = node.getPayload();
        anim.toValues = hasFluidValue2(to22) ? null : goalType == AnimatedString2 ? [1] : toArray2(goal);
      }
      if (anim.immediate != immediate) {
        anim.immediate = immediate;
        if (!immediate && !reset) {
          this._set(prevTo);
        }
      }
      if (started) {
        const { onRest } = anim;
        each2(ACTIVE_EVENTS2, (type) => mergeActiveFn2(this, props, type));
        const result = getFinishedResult2(this, checkFinished2(this, prevTo));
        flushCalls2(this._pendingCalls, result);
        this._pendingCalls.add(resolve);
        if (anim.changed)
          raf2.batchedUpdates(() => {
            var _a;
            anim.changed = !reset;
            onRest == null ? void 0 : onRest(result, this);
            if (reset) {
              callProp2(defaultProps.onRest, result);
            } else {
              (_a = anim.onStart) == null ? void 0 : _a.call(anim, result, this);
            }
          });
      }
    }
    if (reset) {
      this._set(value);
    }
    if (hasAsyncTo) {
      resolve(runAsync2(props.to, props, this._state, this));
    } else if (started) {
      this._start();
    } else if (isAnimating2(this) && !hasToChanged) {
      this._pendingCalls.add(resolve);
    } else {
      resolve(getNoopResult2(value));
    }
  }
  /** Update the `animation.to` value, which might be a `FluidValue` */
  _focus(value) {
    const anim = this.animation;
    if (value !== anim.to) {
      if (getFluidObservers2(this)) {
        this._detach();
      }
      anim.to = value;
      if (getFluidObservers2(this)) {
        this._attach();
      }
    }
  }
  _attach() {
    let priority3 = 0;
    const { to: to22 } = this.animation;
    if (hasFluidValue2(to22)) {
      addFluidObserver2(to22, this);
      if (isFrameValue2(to22)) {
        priority3 = to22.priority + 1;
      }
    }
    this.priority = priority3;
  }
  _detach() {
    const { to: to22 } = this.animation;
    if (hasFluidValue2(to22)) {
      removeFluidObserver2(to22, this);
    }
  }
  /**
   * Update the current value from outside the frameloop,
   * and return the `Animated` node.
   */
  _set(arg, idle = true) {
    const value = getFluidValue2(arg);
    if (!is2.und(value)) {
      const oldNode = getAnimated2(this);
      if (!oldNode || !isEqual2(value, oldNode.getValue())) {
        const nodeType = getAnimatedType2(value);
        if (!oldNode || oldNode.constructor != nodeType) {
          setAnimated2(this, nodeType.create(value));
        } else {
          oldNode.setValue(value);
        }
        if (oldNode) {
          raf2.batchedUpdates(() => {
            this._onChange(value, idle);
          });
        }
      }
    }
    return getAnimated2(this);
  }
  _onStart() {
    const anim = this.animation;
    if (!anim.changed) {
      anim.changed = true;
      sendEvent2(
        this,
        "onStart",
        getFinishedResult2(this, checkFinished2(this, anim.to)),
        this
      );
    }
  }
  _onChange(value, idle) {
    if (!idle) {
      this._onStart();
      callProp2(this.animation.onChange, value, this);
    }
    callProp2(this.defaultProps.onChange, value, this);
    super._onChange(value, idle);
  }
  // This method resets the animation state (even if already animating) to
  // ensure the latest from/to range is used, and it also ensures this spring
  // is added to the frameloop.
  _start() {
    const anim = this.animation;
    getAnimated2(this).reset(getFluidValue2(anim.to));
    if (!anim.immediate) {
      anim.fromValues = anim.values.map((node) => node.lastPosition);
    }
    if (!isAnimating2(this)) {
      setActiveBit2(this, true);
      if (!isPaused2(this)) {
        this._resume();
      }
    }
  }
  _resume() {
    if (globals_exports2.skipAnimation) {
      this.finish();
    } else {
      frameLoop2.start(this);
    }
  }
  /**
   * Exit the frameloop and notify `onRest` listeners.
   *
   * Always wrap `_stop` calls with `batchedUpdates`.
   */
  _stop(goal, cancel) {
    if (isAnimating2(this)) {
      setActiveBit2(this, false);
      const anim = this.animation;
      each2(anim.values, (node) => {
        node.done = true;
      });
      if (anim.toValues) {
        anim.onChange = anim.onPause = anim.onResume = void 0;
      }
      callFluidObservers2(this, {
        type: "idle",
        parent: this
      });
      const result = cancel ? getCancelledResult2(this.get()) : getFinishedResult2(this.get(), checkFinished2(this, goal ?? anim.to));
      flushCalls2(this._pendingCalls, result);
      if (anim.changed) {
        anim.changed = false;
        sendEvent2(this, "onRest", result, this);
      }
    }
  }
};
function checkFinished2(target, to22) {
  const goal = computeGoal2(to22);
  const value = computeGoal2(target.get());
  return isEqual2(value, goal);
}
function createLoopUpdate2(props, loop3 = props.loop, to22 = props.to) {
  const loopRet = callProp2(loop3);
  if (loopRet) {
    const overrides = loopRet !== true && inferTo2(loopRet);
    const reverse = (overrides || props).reverse;
    const reset = !overrides || overrides.reset;
    return createUpdate2({
      ...props,
      loop: loop3,
      // Avoid updating default props when looping.
      default: false,
      // Never loop the `pause` prop.
      pause: void 0,
      // For the "reverse" prop to loop as expected, the "to" prop
      // must be undefined. The "reverse" prop is ignored when the
      // "to" prop is an array or function.
      to: !reverse || isAsyncTo2(to22) ? to22 : void 0,
      // Ignore the "from" prop except on reset.
      from: reset ? props.from : void 0,
      reset,
      // The "loop" prop can return a "useSpring" props object to
      // override any of the original props.
      ...overrides
    });
  }
}
function createUpdate2(props) {
  const { to: to22, from } = props = inferTo2(props);
  const keys = /* @__PURE__ */ new Set();
  if (is2.obj(to22)) findDefined2(to22, keys);
  if (is2.obj(from)) findDefined2(from, keys);
  props.keys = keys.size ? Array.from(keys) : null;
  return props;
}
function declareUpdate2(props) {
  const update22 = createUpdate2(props);
  if (is2.und(update22.default)) {
    update22.default = getDefaultProps2(update22);
  }
  return update22;
}
function findDefined2(values, keys) {
  eachProp2(values, (value, key) => value != null && keys.add(key));
}
var ACTIVE_EVENTS2 = [
  "onStart",
  "onRest",
  "onChange",
  "onPause",
  "onResume"
];
function mergeActiveFn2(target, props, type) {
  target.animation[type] = props[type] !== getDefaultProp2(props, type) ? resolveProp2(props[type], target.key) : void 0;
}
function sendEvent2(target, type, ...args) {
  var _a, _b, _c, _d;
  (_b = (_a = target.animation)[type]) == null ? void 0 : _b.call(_a, ...args);
  (_d = (_c = target.defaultProps)[type]) == null ? void 0 : _d.call(_c, ...args);
}
var BATCHED_EVENTS2 = ["onStart", "onChange", "onRest"];
var nextId22 = 1;
var Controller2 = class {
  constructor(props, flush3) {
    this.id = nextId22++;
    this.springs = {};
    this.queue = [];
    this._lastAsyncId = 0;
    this._active = /* @__PURE__ */ new Set();
    this._changed = /* @__PURE__ */ new Set();
    this._started = false;
    this._state = {
      paused: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._events = {
      onStart: /* @__PURE__ */ new Map(),
      onChange: /* @__PURE__ */ new Map(),
      onRest: /* @__PURE__ */ new Map()
    };
    this._onFrame = this._onFrame.bind(this);
    if (flush3) {
      this._flush = flush3;
    }
    if (props) {
      this.start({ default: true, ...props });
    }
  }
  /**
   * Equals `true` when no spring values are in the frameloop, and
   * no async animation is currently active.
   */
  get idle() {
    return !this._state.asyncTo && Object.values(this.springs).every((spring) => {
      return spring.idle && !spring.isDelayed && !spring.isPaused;
    });
  }
  get item() {
    return this._item;
  }
  set item(item) {
    this._item = item;
  }
  /** Get the current values of our springs */
  get() {
    const values = {};
    this.each((spring, key) => values[key] = spring.get());
    return values;
  }
  /** Set the current values without animating. */
  set(values) {
    for (const key in values) {
      const value = values[key];
      if (!is2.und(value)) {
        this.springs[key].set(value);
      }
    }
  }
  /** Push an update onto the queue of each value. */
  update(props) {
    if (props) {
      this.queue.push(createUpdate2(props));
    }
    return this;
  }
  /**
   * Start the queued animations for every spring, and resolve the returned
   * promise once all queued animations have finished or been cancelled.
   *
   * When you pass a queue (instead of nothing), that queue is used instead of
   * the queued animations added with the `update` method, which are left alone.
   */
  start(props) {
    let { queue } = this;
    if (props) {
      queue = toArray2(props).map(createUpdate2);
    } else {
      this.queue = [];
    }
    if (this._flush) {
      return this._flush(this, queue);
    }
    prepareKeys2(this, queue);
    return flushUpdateQueue2(this, queue);
  }
  /** @internal */
  stop(arg, keys) {
    if (arg !== !!arg) {
      keys = arg;
    }
    if (keys) {
      const springs = this.springs;
      each2(toArray2(keys), (key) => springs[key].stop(!!arg));
    } else {
      stopAsync2(this._state, this._lastAsyncId);
      this.each((spring) => spring.stop(!!arg));
    }
    return this;
  }
  /** Freeze the active animation in time */
  pause(keys) {
    if (is2.und(keys)) {
      this.start({ pause: true });
    } else {
      const springs = this.springs;
      each2(toArray2(keys), (key) => springs[key].pause());
    }
    return this;
  }
  /** Resume the animation if paused. */
  resume(keys) {
    if (is2.und(keys)) {
      this.start({ pause: false });
    } else {
      const springs = this.springs;
      each2(toArray2(keys), (key) => springs[key].resume());
    }
    return this;
  }
  /** Call a function once per spring value */
  each(iterator) {
    eachProp2(this.springs, iterator);
  }
  /** @internal Called at the end of every animation frame */
  _onFrame() {
    const { onStart, onChange, onRest } = this._events;
    const active = this._active.size > 0;
    const changed = this._changed.size > 0;
    if (active && !this._started || changed && !this._started) {
      this._started = true;
      flush2(onStart, ([onStart2, result]) => {
        result.value = this.get();
        onStart2(result, this, this._item);
      });
    }
    const idle = !active && this._started;
    const values = changed || idle && onRest.size ? this.get() : null;
    if (changed && onChange.size) {
      flush2(onChange, ([onChange2, result]) => {
        result.value = values;
        onChange2(result, this, this._item);
      });
    }
    if (idle) {
      this._started = false;
      flush2(onRest, ([onRest2, result]) => {
        result.value = values;
        onRest2(result, this, this._item);
      });
    }
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._changed.add(event.parent);
      if (!event.idle) {
        this._active.add(event.parent);
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else return;
    raf2.onFrame(this._onFrame);
  }
};
function flushUpdateQueue2(ctrl, queue) {
  return Promise.all(queue.map((props) => flushUpdate2(ctrl, props))).then(
    (results) => getCombinedResult2(ctrl, results)
  );
}
async function flushUpdate2(ctrl, props, isLoop) {
  const { keys, to: to22, from, loop: loop3, onRest, onResolve } = props;
  const defaults22 = is2.obj(props.default) && props.default;
  if (loop3) {
    props.loop = false;
  }
  if (to22 === false) props.to = null;
  if (from === false) props.from = null;
  const asyncTo = is2.arr(to22) || is2.fun(to22) ? to22 : void 0;
  if (asyncTo) {
    props.to = void 0;
    props.onRest = void 0;
    if (defaults22) {
      defaults22.onRest = void 0;
    }
  } else {
    each2(BATCHED_EVENTS2, (key) => {
      const handler = props[key];
      if (is2.fun(handler)) {
        const queue = ctrl["_events"][key];
        props[key] = ({ finished, cancelled }) => {
          const result2 = queue.get(handler);
          if (result2) {
            if (!finished) result2.finished = false;
            if (cancelled) result2.cancelled = true;
          } else {
            queue.set(handler, {
              value: null,
              finished: finished || false,
              cancelled: cancelled || false
            });
          }
        };
        if (defaults22) {
          defaults22[key] = props[key];
        }
      }
    });
  }
  const state = ctrl["_state"];
  if (props.pause === !state.paused) {
    state.paused = props.pause;
    flushCalls2(props.pause ? state.pauseQueue : state.resumeQueue);
  } else if (state.paused) {
    props.pause = true;
  }
  const promises = (keys || Object.keys(ctrl.springs)).map(
    (key) => ctrl.springs[key].start(props)
  );
  const cancel = props.cancel === true || getDefaultProp2(props, "cancel") === true;
  if (asyncTo || cancel && state.asyncId) {
    promises.push(
      scheduleProps2(++ctrl["_lastAsyncId"], {
        props,
        state,
        actions: {
          pause: noop2,
          resume: noop2,
          start(props2, resolve) {
            if (cancel) {
              stopAsync2(state, ctrl["_lastAsyncId"]);
              resolve(getCancelledResult2(ctrl));
            } else {
              props2.onRest = onRest;
              resolve(
                runAsync2(
                  asyncTo,
                  props2,
                  state,
                  ctrl
                )
              );
            }
          }
        }
      })
    );
  }
  if (state.paused) {
    await new Promise((resume) => {
      state.resumeQueue.add(resume);
    });
  }
  const result = getCombinedResult2(ctrl, await Promise.all(promises));
  if (loop3 && result.finished && !(isLoop && result.noop)) {
    const nextProps = createLoopUpdate2(props, loop3, to22);
    if (nextProps) {
      prepareKeys2(ctrl, [nextProps]);
      return flushUpdate2(ctrl, nextProps, true);
    }
  }
  if (onResolve) {
    raf2.batchedUpdates(() => onResolve(result, ctrl, ctrl.item));
  }
  return result;
}
function getSprings2(ctrl, props) {
  const springs = { ...ctrl.springs };
  if (props) {
    each2(toArray2(props), (props2) => {
      if (is2.und(props2.keys)) {
        props2 = createUpdate2(props2);
      }
      if (!is2.obj(props2.to)) {
        props2 = { ...props2, to: void 0 };
      }
      prepareSprings2(springs, props2, (key) => {
        return createSpring2(key);
      });
    });
  }
  setSprings2(ctrl, springs);
  return springs;
}
function setSprings2(ctrl, springs) {
  eachProp2(springs, (spring, key) => {
    if (!ctrl.springs[key]) {
      ctrl.springs[key] = spring;
      addFluidObserver2(spring, ctrl);
    }
  });
}
function createSpring2(key, observer) {
  const spring = new SpringValue2();
  spring.key = key;
  if (observer) {
    addFluidObserver2(spring, observer);
  }
  return spring;
}
function prepareSprings2(springs, props, create) {
  if (props.keys) {
    each2(props.keys, (key) => {
      const spring = springs[key] || (springs[key] = create(key));
      spring["_prepareNode"](props);
    });
  }
}
function prepareKeys2(ctrl, queue) {
  each2(queue, (props) => {
    prepareSprings2(ctrl.springs, props, (key) => {
      return createSpring2(key, ctrl);
    });
  });
}
var SpringContext2 = React4.createContext({
  pause: false,
  immediate: false
});
var SpringRef2 = () => {
  const current = [];
  const SpringRef22 = function(props) {
    deprecateDirectCall2();
    const results = [];
    each2(current, (ctrl, i4) => {
      if (is2.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = _getProps(props, ctrl, i4);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.current = current;
  SpringRef22.add = function(ctrl) {
    if (!current.includes(ctrl)) {
      current.push(ctrl);
    }
  };
  SpringRef22.delete = function(ctrl) {
    const i4 = current.indexOf(ctrl);
    if (~i4) current.splice(i4, 1);
  };
  SpringRef22.pause = function() {
    each2(current, (ctrl) => ctrl.pause(...arguments));
    return this;
  };
  SpringRef22.resume = function() {
    each2(current, (ctrl) => ctrl.resume(...arguments));
    return this;
  };
  SpringRef22.set = function(values) {
    each2(current, (ctrl, i4) => {
      const update22 = is2.fun(values) ? values(i4, ctrl) : values;
      if (update22) {
        ctrl.set(update22);
      }
    });
  };
  SpringRef22.start = function(props) {
    const results = [];
    each2(current, (ctrl, i4) => {
      if (is2.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = this._getProps(props, ctrl, i4);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.stop = function() {
    each2(current, (ctrl) => ctrl.stop(...arguments));
    return this;
  };
  SpringRef22.update = function(props) {
    each2(current, (ctrl, i4) => ctrl.update(this._getProps(props, ctrl, i4)));
    return this;
  };
  const _getProps = function(arg, ctrl, index) {
    return is2.fun(arg) ? arg(index, ctrl) : arg;
  };
  SpringRef22._getProps = _getProps;
  return SpringRef22;
};
function useSprings2(length, props, deps) {
  const propsFn = is2.fun(props) && props;
  if (propsFn && !deps) deps = [];
  const ref = (0, import_react25.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef2() : void 0,
    []
  );
  const layoutId = (0, import_react25.useRef)(0);
  const forceUpdate = useForceUpdate2();
  const state = (0, import_react25.useMemo)(
    () => ({
      ctrls: [],
      queue: [],
      flush(ctrl, updates2) {
        const springs2 = getSprings2(ctrl, updates2);
        const canFlushSync = layoutId.current > 0 && !state.queue.length && !Object.keys(springs2).some((key) => !ctrl.springs[key]);
        return canFlushSync ? flushUpdateQueue2(ctrl, updates2) : new Promise((resolve) => {
          setSprings2(ctrl, springs2);
          state.queue.push(() => {
            resolve(flushUpdateQueue2(ctrl, updates2));
          });
          forceUpdate();
        });
      }
    }),
    []
  );
  const ctrls = (0, import_react25.useRef)([...state.ctrls]);
  const updates = (0, import_react25.useRef)([]);
  const prevLength = usePrev2(length) || 0;
  (0, import_react25.useMemo)(() => {
    each2(ctrls.current.slice(length, prevLength), (ctrl) => {
      detachRefs2(ctrl, ref);
      ctrl.stop(true);
    });
    ctrls.current.length = length;
    declareUpdates(prevLength, length);
  }, [length]);
  (0, import_react25.useMemo)(() => {
    declareUpdates(0, Math.min(prevLength, length));
  }, deps);
  function declareUpdates(startIndex, endIndex) {
    for (let i4 = startIndex; i4 < endIndex; i4++) {
      const ctrl = ctrls.current[i4] || (ctrls.current[i4] = new Controller2(null, state.flush));
      const update22 = propsFn ? propsFn(i4, ctrl) : props[i4];
      if (update22) {
        updates.current[i4] = declareUpdate2(update22);
      }
    }
  }
  const springs = ctrls.current.map(
    (ctrl, i4) => getSprings2(ctrl, updates.current[i4])
  );
  const context = (0, import_react25.useContext)(SpringContext2);
  const prevContext = usePrev2(context);
  const hasContext = context !== prevContext && hasProps2(context);
  useIsomorphicLayoutEffect2(() => {
    layoutId.current++;
    state.ctrls = ctrls.current;
    const { queue } = state;
    if (queue.length) {
      state.queue = [];
      each2(queue, (cb) => cb());
    }
    each2(ctrls.current, (ctrl, i4) => {
      ref == null ? void 0 : ref.add(ctrl);
      if (hasContext) {
        ctrl.start({ default: context });
      }
      const update22 = updates.current[i4];
      if (update22) {
        replaceRef2(ctrl, update22.ref);
        if (ctrl.ref) {
          ctrl.queue.push(update22);
        } else {
          ctrl.start(update22);
        }
      }
    });
  });
  useOnce2(() => () => {
    each2(state.ctrls, (ctrl) => ctrl.stop(true));
  });
  const values = springs.map((x2) => ({ ...x2 }));
  return ref ? [values, ref] : values;
}
function useSpring2(props, deps) {
  const isFn = is2.fun(props);
  const [[values], ref] = useSprings2(
    1,
    isFn ? props : [props],
    isFn ? deps || [] : deps
  );
  return isFn || arguments.length == 2 ? [values, ref] : values;
}
var Interpolation2 = class extends FrameValue2 {
  constructor(source, args) {
    super();
    this.source = source;
    this.idle = true;
    this._active = /* @__PURE__ */ new Set();
    this.calc = createInterpolator2(...args);
    const value = this._get();
    const nodeType = getAnimatedType2(value);
    setAnimated2(this, nodeType.create(value));
  }
  advance(_dt) {
    const value = this._get();
    const oldValue = this.get();
    if (!isEqual2(value, oldValue)) {
      getAnimated2(this).setValue(value);
      this._onChange(value, this.idle);
    }
    if (!this.idle && checkIdle2(this._active)) {
      becomeIdle2(this);
    }
  }
  _get() {
    const inputs = is2.arr(this.source) ? this.source.map(getFluidValue2) : toArray2(getFluidValue2(this.source));
    return this.calc(...inputs);
  }
  _start() {
    if (this.idle && !checkIdle2(this._active)) {
      this.idle = false;
      each2(getPayload2(this), (node) => {
        node.done = false;
      });
      if (globals_exports2.skipAnimation) {
        raf2.batchedUpdates(() => this.advance());
        becomeIdle2(this);
      } else {
        frameLoop2.start(this);
      }
    }
  }
  // Observe our sources only when we're observed.
  _attach() {
    let priority3 = 1;
    each2(toArray2(this.source), (source) => {
      if (hasFluidValue2(source)) {
        addFluidObserver2(source, this);
      }
      if (isFrameValue2(source)) {
        if (!source.idle) {
          this._active.add(source);
        }
        priority3 = Math.max(priority3, source.priority + 1);
      }
    });
    this.priority = priority3;
    this._start();
  }
  // Stop observing our sources once we have no observers.
  _detach() {
    each2(toArray2(this.source), (source) => {
      if (hasFluidValue2(source)) {
        removeFluidObserver2(source, this);
      }
    });
    this._active.clear();
    becomeIdle2(this);
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      if (event.idle) {
        this.advance();
      } else {
        this._active.add(event.parent);
        this._start();
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else if (event.type == "priority") {
      this.priority = toArray2(this.source).reduce(
        (highest, parent) => Math.max(highest, (isFrameValue2(parent) ? parent.priority : 0) + 1),
        0
      );
    }
  }
};
function isIdle2(source) {
  return source.idle !== false;
}
function checkIdle2(active) {
  return !active.size || Array.from(active).every(isIdle2);
}
function becomeIdle2(self) {
  if (!self.idle) {
    self.idle = true;
    each2(getPayload2(self), (node) => {
      node.done = true;
    });
    callFluidObservers2(self, {
      type: "idle",
      parent: self
    });
  }
}
globals_exports2.assign({
  createStringInterpolator: createStringInterpolator22,
  to: (source, args) => new Interpolation2(source, args)
});
var update4 = frameLoop2.advance;

// node_modules/@nivo/annotations/node_modules/@react-spring/web/dist/react-spring_web.modern.mjs
var import_react_dom2 = __toESM(require_react_dom(), 1);
var isCustomPropRE2 = /^--/;
function dangerousStyleValue2(name, value) {
  if (value == null || typeof value === "boolean" || value === "") return "";
  if (typeof value === "number" && value !== 0 && !isCustomPropRE2.test(name) && !(isUnitlessNumber2.hasOwnProperty(name) && isUnitlessNumber2[name]))
    return value + "px";
  return ("" + value).trim();
}
var attributeCache2 = {};
function applyAnimatedValues2(instance, props) {
  if (!instance.nodeType || !instance.setAttribute) {
    return false;
  }
  const isFilterElement = instance.nodeName === "filter" || instance.parentNode && instance.parentNode.nodeName === "filter";
  const {
    className,
    style,
    children,
    scrollTop,
    scrollLeft,
    viewBox,
    ...attributes
  } = props;
  const values = Object.values(attributes);
  const names = Object.keys(attributes).map(
    (name) => isFilterElement || instance.hasAttribute(name) ? name : attributeCache2[name] || (attributeCache2[name] = name.replace(
      /([A-Z])/g,
      // Attributes are written in dash case
      (n4) => "-" + n4.toLowerCase()
    ))
  );
  if (children !== void 0) {
    instance.textContent = children;
  }
  for (const name in style) {
    if (style.hasOwnProperty(name)) {
      const value = dangerousStyleValue2(name, style[name]);
      if (isCustomPropRE2.test(name)) {
        instance.style.setProperty(name, value);
      } else {
        instance.style[name] = value;
      }
    }
  }
  names.forEach((name, i4) => {
    instance.setAttribute(name, values[i4]);
  });
  if (className !== void 0) {
    instance.className = className;
  }
  if (scrollTop !== void 0) {
    instance.scrollTop = scrollTop;
  }
  if (scrollLeft !== void 0) {
    instance.scrollLeft = scrollLeft;
  }
  if (viewBox !== void 0) {
    instance.setAttribute("viewBox", viewBox);
  }
}
var isUnitlessNumber2 = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
var prefixKey2 = (prefix3, key) => prefix3 + key.charAt(0).toUpperCase() + key.substring(1);
var prefixes2 = ["Webkit", "Ms", "Moz", "O"];
isUnitlessNumber2 = Object.keys(isUnitlessNumber2).reduce((acc, prop) => {
  prefixes2.forEach((prefix3) => acc[prefixKey2(prefix3, prop)] = acc[prop]);
  return acc;
}, isUnitlessNumber2);
var domTransforms2 = /^(matrix|translate|scale|rotate|skew)/;
var pxTransforms2 = /^(translate)/;
var degTransforms2 = /^(rotate|skew)/;
var addUnit2 = (value, unit) => is2.num(value) && value !== 0 ? value + unit : value;
var isValueIdentity2 = (value, id) => is2.arr(value) ? value.every((v2) => isValueIdentity2(v2, id)) : is2.num(value) ? value === id : parseFloat(value) === id;
var AnimatedStyle2 = class extends AnimatedObject2 {
  constructor({ x: x2, y, z: z3, ...style }) {
    const inputs = [];
    const transforms = [];
    if (x2 || y || z3) {
      inputs.push([x2 || 0, y || 0, z3 || 0]);
      transforms.push((xyz) => [
        `translate3d(${xyz.map((v2) => addUnit2(v2, "px")).join(",")})`,
        // prettier-ignore
        isValueIdentity2(xyz, 0)
      ]);
    }
    eachProp2(style, (value, key) => {
      if (key === "transform") {
        inputs.push([value || ""]);
        transforms.push((transform) => [transform, transform === ""]);
      } else if (domTransforms2.test(key)) {
        delete style[key];
        if (is2.und(value)) return;
        const unit = pxTransforms2.test(key) ? "px" : degTransforms2.test(key) ? "deg" : "";
        inputs.push(toArray2(value));
        transforms.push(
          key === "rotate3d" ? ([x22, y2, z22, deg]) => [
            `rotate3d(${x22},${y2},${z22},${addUnit2(deg, unit)})`,
            isValueIdentity2(deg, 0)
          ] : (input) => [
            `${key}(${input.map((v2) => addUnit2(v2, unit)).join(",")})`,
            isValueIdentity2(input, key.startsWith("scale") ? 1 : 0)
          ]
        );
      }
    });
    if (inputs.length) {
      style.transform = new FluidTransform2(inputs, transforms);
    }
    super(style);
  }
};
var FluidTransform2 = class extends FluidValue2 {
  constructor(inputs, transforms) {
    super();
    this.inputs = inputs;
    this.transforms = transforms;
    this._value = null;
  }
  get() {
    return this._value || (this._value = this._get());
  }
  _get() {
    let transform = "";
    let identity = true;
    each2(this.inputs, (input, i4) => {
      const arg1 = getFluidValue2(input[0]);
      const [t4, id] = this.transforms[i4](
        is2.arr(arg1) ? arg1 : input.map(getFluidValue2)
      );
      transform += " " + t4;
      identity = identity && id;
    });
    return identity ? "none" : transform;
  }
  // Start observing our inputs once we have an observer.
  observerAdded(count) {
    if (count == 1)
      each2(
        this.inputs,
        (input) => each2(
          input,
          (value) => hasFluidValue2(value) && addFluidObserver2(value, this)
        )
      );
  }
  // Stop observing our inputs once we have no observers.
  observerRemoved(count) {
    if (count == 0)
      each2(
        this.inputs,
        (input) => each2(
          input,
          (value) => hasFluidValue2(value) && removeFluidObserver2(value, this)
        )
      );
  }
  eventObserved(event) {
    if (event.type == "change") {
      this._value = null;
    }
    callFluidObservers2(this, event);
  }
};
var primitives2 = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
globals_exports2.assign({
  batchedUpdates: import_react_dom2.unstable_batchedUpdates,
  createStringInterpolator: createStringInterpolator22,
  colors: colors22
});
var host2 = createHost2(primitives2, {
  applyAnimatedValues: applyAnimatedValues2,
  createAnimatedStyle: (style) => new AnimatedStyle2(style),
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  getComponentProps: ({ scrollTop, scrollLeft, ...props }) => props
});
var animated2 = host2.animated;

// node_modules/@nivo/annotations/dist/nivo-annotations.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
function g() {
  return g = Object.assign ? Object.assign.bind() : function(t4) {
    for (var n4 = 1; n4 < arguments.length; n4++) {
      var i4 = arguments[n4];
      for (var o2 in i4) ({}).hasOwnProperty.call(i4, o2) && (t4[o2] = i4[o2]);
    }
    return t4;
  }, g.apply(null, arguments);
}
var k = { dotSize: 4, noteWidth: 120, noteTextOffset: 8, animate: true };
var W3 = function(n4) {
  var i4 = typeof n4;
  return (0, import_react30.isValidElement)(n4) || "string" === i4 || "function" === i4 || "object" === i4;
};
var v = function(t4) {
  var n4 = typeof t4;
  return "string" === n4 || "function" === n4;
};
var b3 = function(t4) {
  return "circle" === t4.type;
};
var w3 = function(t4) {
  return "dot" === t4.type;
};
var z2 = function(t4) {
  return "rect" === t4.type;
};
var P2 = function(t4) {
  var n4 = t4.data, i4 = t4.annotations, e4 = t4.getPosition, r3 = t4.getDimensions;
  return i4.reduce(function(t5, i5) {
    var s = i5.offset || 0;
    return [].concat(t5, (0, import_filter.default)(n4, i5.match).map(function(t6) {
      var n5 = e4(t6), o2 = r3(t6);
      return (b3(i5) || z2(i5)) && (o2.size = o2.size + 2 * s, o2.width = o2.width + 2 * s, o2.height = o2.height + 2 * s), g({}, (0, import_omit.default)(i5, ["match", "offset"]), n5, o2, { size: i5.size || o2.size, datum: t6 });
    }));
  }, []);
};
var C2 = function(t4, n4, i4, o2) {
  var e4 = Math.atan2(o2 - n4, i4 - t4);
  return ht(ct(e4));
};
var S2 = function(t4) {
  var n4, i4, o2 = t4.x, a3 = t4.y, r3 = t4.noteX, s = t4.noteY, h = t4.noteWidth, d2 = void 0 === h ? k.noteWidth : h, c = t4.noteTextOffset, f = void 0 === c ? k.noteTextOffset : c;
  if ((0, import_isNumber.default)(r3)) n4 = o2 + r3;
  else {
    if (void 0 === r3.abs) throw new Error("noteX should be either a number or an object containing an 'abs' property");
    n4 = r3.abs;
  }
  if ((0, import_isNumber.default)(s)) i4 = a3 + s;
  else {
    if (void 0 === s.abs) throw new Error("noteY should be either a number or an object containing an 'abs' property");
    i4 = s.abs;
  }
  var y = o2, x2 = a3, m2 = C2(o2, a3, n4, i4);
  if (b3(t4)) {
    var p2 = ft(ut(m2), t4.size / 2);
    y += p2.x, x2 += p2.y;
  }
  if (z2(t4)) {
    var g2 = Math.round((m2 + 90) / 45) % 8;
    0 === g2 && (x2 -= t4.height / 2), 1 === g2 && (y += t4.width / 2, x2 -= t4.height / 2), 2 === g2 && (y += t4.width / 2), 3 === g2 && (y += t4.width / 2, x2 += t4.height / 2), 4 === g2 && (x2 += t4.height / 2), 5 === g2 && (y -= t4.width / 2, x2 += t4.height / 2), 6 === g2 && (y -= t4.width / 2), 7 === g2 && (y -= t4.width / 2, x2 -= t4.height / 2);
  }
  var W4 = n4, v2 = n4;
  return (m2 + 90) % 360 > 180 ? (W4 -= d2, v2 -= d2) : v2 += d2, { points: [[y, x2], [n4, i4], [v2, i4]], text: [W4, i4 - f], angle: m2 + 90 };
};
var O2 = function(t4) {
  var i4 = t4.data, o2 = t4.annotations, e4 = t4.getPosition, a3 = t4.getDimensions;
  return (0, import_react30.useMemo)(function() {
    return P2({ data: i4, annotations: o2, getPosition: e4, getDimensions: a3 });
  }, [i4, o2, e4, a3]);
};
var j2 = function(t4) {
  var i4 = t4.annotations;
  return (0, import_react30.useMemo)(function() {
    return i4.map(function(t5) {
      return g({}, t5, { computed: S2(g({}, t5)) });
    });
  }, [i4]);
};
var M2 = function(t4) {
  return (0, import_react30.useMemo)(function() {
    return S2(t4);
  }, [t4]);
};
var T2 = function(t4) {
  var n4 = t4.datum, o2 = t4.x, e4 = t4.y, r3 = t4.note, s = M(), l = Dr(), u = l.animate, d2 = l.config, k2 = useSpring2({ x: o2, y: e4, config: d2, immediate: !u });
  return "function" == typeof r3 ? (0, import_react30.createElement)(r3, { x: o2, y: e4, datum: n4 }) : (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [s.annotations.text.outlineWidth > 0 && (0, import_jsx_runtime2.jsx)(animated2.text, { x: k2.x, y: k2.y, style: g({}, s.annotations.text, { strokeLinejoin: "round", strokeWidth: 2 * s.annotations.text.outlineWidth, stroke: s.annotations.text.outlineColor }), children: r3 }), (0, import_jsx_runtime2.jsx)(animated2.text, { x: k2.x, y: k2.y, style: (0, import_omit.default)(s.annotations.text, ["outlineWidth", "outlineColor"]), children: r3 })] });
};
var E2 = function(t4) {
  var i4 = t4.points, o2 = t4.isOutline, e4 = void 0 !== o2 && o2, a3 = M(), r3 = (0, import_react30.useMemo)(function() {
    var t5 = i4[0];
    return i4.slice(1).reduce(function(t6, n4) {
      return t6 + " L" + n4[0] + "," + n4[1];
    }, "M" + t5[0] + "," + t5[1]);
  }, [i4]), s = It(r3);
  if (e4 && a3.annotations.link.outlineWidth <= 0) return null;
  var l = g({}, a3.annotations.link);
  return e4 && (l.strokeLinecap = "square", l.strokeWidth = a3.annotations.link.strokeWidth + 2 * a3.annotations.link.outlineWidth, l.stroke = a3.annotations.link.outlineColor, l.opacity = a3.annotations.link.outlineOpacity), (0, import_jsx_runtime2.jsx)(animated2.path, { fill: "none", d: s, style: l });
};
var I = function(t4) {
  var n4 = t4.x, i4 = t4.y, o2 = t4.size, e4 = M(), a3 = Dr(), r3 = a3.animate, s = a3.config, l = useSpring2({ x: n4, y: i4, radius: o2 / 2, config: s, immediate: !r3 });
  return (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [e4.annotations.outline.outlineWidth > 0 && (0, import_jsx_runtime2.jsx)(animated2.circle, { cx: l.x, cy: l.y, r: l.radius, style: g({}, e4.annotations.outline, { fill: "none", strokeWidth: e4.annotations.outline.strokeWidth + 2 * e4.annotations.outline.outlineWidth, stroke: e4.annotations.outline.outlineColor, opacity: e4.annotations.outline.outlineOpacity }) }), (0, import_jsx_runtime2.jsx)(animated2.circle, { cx: l.x, cy: l.y, r: l.radius, style: e4.annotations.outline })] });
};
var D2 = function(t4) {
  var n4 = t4.x, i4 = t4.y, o2 = t4.size, e4 = void 0 === o2 ? k.dotSize : o2, a3 = M(), r3 = Dr(), s = r3.animate, l = r3.config, u = useSpring2({ x: n4, y: i4, radius: e4 / 2, config: l, immediate: !s });
  return (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [a3.annotations.outline.outlineWidth > 0 && (0, import_jsx_runtime2.jsx)(animated2.circle, { cx: u.x, cy: u.y, r: u.radius, style: g({}, a3.annotations.outline, { fill: "none", strokeWidth: 2 * a3.annotations.outline.outlineWidth, stroke: a3.annotations.outline.outlineColor, opacity: a3.annotations.outline.outlineOpacity }) }), (0, import_jsx_runtime2.jsx)(animated2.circle, { cx: u.x, cy: u.y, r: u.radius, style: a3.annotations.symbol })] });
};
var L2 = function(t4) {
  var n4 = t4.x, i4 = t4.y, o2 = t4.width, e4 = t4.height, a3 = t4.borderRadius, r3 = void 0 === a3 ? 6 : a3, s = M(), l = Dr(), u = l.animate, d2 = l.config, k2 = useSpring2({ x: n4 - o2 / 2, y: i4 - e4 / 2, width: o2, height: e4, config: d2, immediate: !u });
  return (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [s.annotations.outline.outlineWidth > 0 && (0, import_jsx_runtime2.jsx)(animated2.rect, { x: k2.x, y: k2.y, rx: r3, ry: r3, width: k2.width, height: k2.height, style: g({}, s.annotations.outline, { fill: "none", strokeWidth: s.annotations.outline.strokeWidth + 2 * s.annotations.outline.outlineWidth, stroke: s.annotations.outline.outlineColor, opacity: s.annotations.outline.outlineOpacity }) }), (0, import_jsx_runtime2.jsx)(animated2.rect, { x: k2.x, y: k2.y, rx: r3, ry: r3, width: k2.width, height: k2.height, style: s.annotations.outline })] });
};
var R3 = function(t4) {
  var n4 = t4.datum, i4 = t4.x, o2 = t4.y, e4 = t4.note, a3 = M2(t4);
  if (!W3(e4)) throw new Error("note should be a valid react element");
  return (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [(0, import_jsx_runtime2.jsx)(E2, { points: a3.points, isOutline: true }), b3(t4) && (0, import_jsx_runtime2.jsx)(I, { x: i4, y: o2, size: t4.size }), w3(t4) && (0, import_jsx_runtime2.jsx)(D2, { x: i4, y: o2, size: t4.size }), z2(t4) && (0, import_jsx_runtime2.jsx)(L2, { x: i4, y: o2, width: t4.width, height: t4.height, borderRadius: t4.borderRadius }), (0, import_jsx_runtime2.jsx)(E2, { points: a3.points }), (0, import_jsx_runtime2.jsx)(T2, { datum: n4, x: a3.text[0], y: a3.text[1], note: e4 })] });
};
var q2 = function(t4, n4) {
  n4.forEach(function(n5, i4) {
    var o2 = n5[0], e4 = n5[1];
    0 === i4 ? t4.moveTo(o2, e4) : t4.lineTo(o2, e4);
  });
};
var J2 = function(t4, n4) {
  var i4 = n4.annotations, o2 = n4.theme;
  0 !== i4.length && (t4.save(), i4.forEach(function(n5) {
    if (!v(n5.note)) throw new Error("note is invalid for canvas implementation");
    o2.annotations.link.outlineWidth > 0 && (t4.lineCap = "square", t4.strokeStyle = o2.annotations.link.outlineColor, t4.lineWidth = o2.annotations.link.strokeWidth + 2 * o2.annotations.link.outlineWidth, t4.beginPath(), q2(t4, n5.computed.points), t4.stroke(), t4.lineCap = "butt"), b3(n5) && o2.annotations.outline.outlineWidth > 0 && (t4.strokeStyle = o2.annotations.outline.outlineColor, t4.lineWidth = o2.annotations.outline.strokeWidth + 2 * o2.annotations.outline.outlineWidth, t4.beginPath(), t4.arc(n5.x, n5.y, n5.size / 2, 0, 2 * Math.PI), t4.stroke()), w3(n5) && o2.annotations.symbol.outlineWidth > 0 && (t4.strokeStyle = o2.annotations.symbol.outlineColor, t4.lineWidth = 2 * o2.annotations.symbol.outlineWidth, t4.beginPath(), t4.arc(n5.x, n5.y, n5.size / 2, 0, 2 * Math.PI), t4.stroke()), z2(n5) && o2.annotations.outline.outlineWidth > 0 && (t4.strokeStyle = o2.annotations.outline.outlineColor, t4.lineWidth = o2.annotations.outline.strokeWidth + 2 * o2.annotations.outline.outlineWidth, t4.beginPath(), t4.rect(n5.x - n5.width / 2, n5.y - n5.height / 2, n5.width, n5.height), t4.stroke()), t4.strokeStyle = o2.annotations.link.stroke, t4.lineWidth = o2.annotations.link.strokeWidth, t4.beginPath(), q2(t4, n5.computed.points), t4.stroke(), b3(n5) && (t4.strokeStyle = o2.annotations.outline.stroke, t4.lineWidth = o2.annotations.outline.strokeWidth, t4.beginPath(), t4.arc(n5.x, n5.y, n5.size / 2, 0, 2 * Math.PI), t4.stroke()), w3(n5) && (t4.fillStyle = o2.annotations.symbol.fill, t4.beginPath(), t4.arc(n5.x, n5.y, n5.size / 2, 0, 2 * Math.PI), t4.fill()), z2(n5) && (t4.strokeStyle = o2.annotations.outline.stroke, t4.lineWidth = o2.annotations.outline.strokeWidth, t4.beginPath(), t4.rect(n5.x - n5.width / 2, n5.y - n5.height / 2, n5.width, n5.height), t4.stroke()), "function" == typeof n5.note ? n5.note(t4, { datum: n5.datum, x: n5.computed.text[0], y: n5.computed.text[1], theme: o2 }) : (t4.font = o2.annotations.text.fontSize + "px " + o2.annotations.text.fontFamily, t4.textAlign = "left", t4.textBaseline = "alphabetic", t4.fillStyle = o2.annotations.text.fill, t4.strokeStyle = o2.annotations.text.outlineColor, t4.lineWidth = 2 * o2.annotations.text.outlineWidth, o2.annotations.text.outlineWidth > 0 && (t4.lineJoin = "round", t4.strokeText(n5.note, n5.computed.text[0], n5.computed.text[1]), t4.lineJoin = "miter"), t4.fillText(n5.note, n5.computed.text[0], n5.computed.text[1]));
  }), t4.restore());
};

export {
  pn,
  hn,
  V,
  j,
  W2 as W,
  q,
  O2 as O,
  j2,
  R3 as R,
  J2 as J
};
//# sourceMappingURL=chunk-SBWSIULK.js.map
